# 📔 DIARY_REFERENCE.md — 日記鑑賞（第三軌·實驗中）

> ⚠️ **這一軌是可整個丟掉的實驗品。** 目的只有一個：回答「排程 → 命中 → 讀週記」這個迴圈**好不好玩**。
> 答案是否，就刪掉下面「刪除清單」那五處，鑑賞與 solo **一行都不會動到**。

## 🗑️ 刪除清單（不好玩時照這個刪）

1. `gas/Diary.gs`（全部後端）
2. `gas/Script_Diary.html`（全部前端）
3. `gas/Router_Action.gs` 的三行註冊（`enter_diary` / `diary_schedule` / `diary_week`）＋ `isDiaryCtx` 那兩處
4. `gas/Index.html` 的第三道門 ＋ `Script_Diary` 的 include
5. `gas/Script.html` `applyModeUI()` 裡的 `isDiary`（3 處）
6. 試算表分頁「日記眾生」
7. 本檔

## 🧱 為什麼分軌不融合

玩家問「先分一個模式測試看看好不好玩？還是直接融合？」——**分開做＝保留選項，融合＝先押注**。

- 先分開 → 再融合**可行**（兩邊都跑得起來，融合容易）
- 先融合 → 再分開**幾乎不可行**（得從纏在一起的系統裡拆回來）

而「好不好玩」正是還不知道答案的問題，不知道答案時不該押注。專案自己也有前例：`full`（九州全模擬）整個模式停用中，另外兩軌毫髮無傷。

## 🔒 資料分流（三方隔離）

| | pcId 前綴 | game_id 前綴 | 分頁 |
|---|---|---|---|
| solo | `PC_` | `g_` | 眾生 |
| 鑑賞 | `KPC_` | `k_` | 鑑賞眾生 |
| **日記** | **`DPC_`** | **`d_`** | **日記眾生** |

**你在鑑賞養到 95 的櫻，跟這裡的櫻是兩個人**，互不污染（CLAUDE.md「資料必須分流不污染」）。dispatcher 在 `Router_Action.gs` 依 pcId 前綴選分頁，三方各走各的。

帳號歸屬用本軌自己的 `DIARY_ACCT_TAG_`（`makeTextTag_('日記帳號')`）——鑑賞那邊是手拼 `"【帳號】"+name` 的裸字串且真正驗證走帳號表，本檔**不去動它**。

## 🔒 歸屬驗證走自己這一套（別再踩）

dispatcher 的 `verifyPcOwnership_` 只認兩種前綴：

```js
var col = id.indexOf("KPC_") === 0 ? COL.ACC.KPC : COL.ACC.PC;
```

`DPC_` 掉進 else，被拿去比對 **solo 的 PC 欄**，永遠對不上 → 整條路被回「查無御主」（玩家實測回報「讀不到行程表」就是這個）。

**修法刻意不是「去帳號表加一欄 DPC」**——那會動到共用 schema，這一軌就刪不乾淨了（且 `COL` 是位置索引）。改成：
1. 三個 action 進 `OWNERSHIP_CHECK_EXEMPT_`
2. 驗證責任落到 `Diary.gs` 的 `diaryVerify_()`，比對本軌自己的 `DIARY_ACCT_TAG_`

**不是少驗，是換個地方驗。** 已實測：換一個帳號拿同一個 `DPC_` id 呼叫 `diary_week`，回「查無御主。」。

⚠️ **之後在這一軌新增任何 action，兩件事都要做**：加進 `OWNERSHIP_CHECK_EXEMPT_`、handler 內呼叫 `diaryVerify_`。只做第一件＝開後門，只做第二件＝功能打不通。

## 🔁 核心迴圈

```
排程一週(7×3 格) → 命中判定(純比對) → 週記生成(1 次 AI) → 套用增量 → 下一週
```

**一個遊戲內的週，鑑賞要約 630 次 AI 呼叫（90 回合 × 7 天），這裡是 1 次。**

## 🗓️ 角色行程表 `DIARY_SCHEDULE_`

**這是整個模式的地基。** 排程玩法要求行程**可預測**，而鑑賞的 `kanshouRollDailyLocation_` 是 `Math.random()` 在需要時才擲、不存、擲完就忘——**你不可能對「你排完程之後才擲的骰子」做規劃**。

格式 `{ hero: 短名, week: [週一..週日][上午/下午/晚上] = 地點名 }`，地點沿用 `KANSHOU_LOCATIONS_` 的字面（唯讀共用，不另立地圖）。骨架期只放三位——驗迴圈不需要 25 位。**要加就往表加一列。**

## ⚖️ 好感由 GAS 給，AI 只微調

沿用全專案鐵則「GAS 掌數值、AI 只說書」：

- 主要增量 ＝ `命中次數 × DIARY_BOND_PER_HIT_`(2)，**GAS 算，不問 AI**
- AI 回的 `rel_changes` 只當情緒微調，夾在 ±`DIARY_AI_ADJUST_CAP_`(3)
- 之後照樣過 `kanshouSyncRelTier_`（**含好感棘輪**，唯讀共用鑑賞那套）

## 🧠 記憶一致性

`DIARY_SUMMARY_TAG_`（`【前情】`，上限 300 字）——AI 每週回傳 `summary` 把該週壓縮成 120 字內，下週當前情提要餵回去。**不丟全部歷史**，控 token 又維持連貫。

## 📝 提示詞形狀

跟鑑賞同一個原則：**只給事實，不給台詞**。
- 人設 ＝ 行為傾向（`性格/特徵/萌點(僅供內化)`，直接讀她那一列）
- 相遇 ＝ 時間地點人（既成事實清單，明文「不可增減人事時地」）
- 「沒列在清單上的人這週**沒有出現過**」——鏡射鑑賞的【在場驗證】

## 🚧 骨架期刻意不做

先驗迴圈好不好玩，這些等答案出來再說：

- 資源限制（精力／資金）——⚠ 這專案的經濟層是**刻意砍掉的**，加回來要先想清楚
- 結局線、旗標解鎖行程、撞期張力
- 排程 UI 還是 `prompt()` 選單（先驗迴圈，不雕 UI）

## ⚠️ 已知風險

**GAS 有 6 分鐘執行上限。** 一篇涵蓋 7 天、十幾次相遇的週記長度是鑑賞單回合（500 字）的數倍，單次生成撐不撐得住**要實跑才知道**。撐不住的話得拆成「每天一段串起來」，但那就退回多次呼叫了。

**這一軌沒有即時對話。** 玩家一週只讀一篇文章，從頭到尾不會跟她說到一句話——鑑賞那些（夜未眠／晨間餘韻／她主動）全部建立在「玩家打字、AI 當場回應」上，這裡一個都用不到。若玩過覺得「排程好玩但我還是想跟她講話」，那就是混合形狀：**排程 → 週記 → 從週記裡挑一個場景進去演**，那一段走鑑賞引擎。
