# 命運停駐之夜 — 設計鐵則

> 把九州武俠 GAS 遊戲改造成 Fate/stay night 聖杯戰爭（純按鍵單人）。
> 本檔記**設計意圖與鐵則**；全專案架構＋每個檔案在做什麼看 `HANDBOOK.md`；確切函數／常數／schema 看 `SOLO_REFERENCE.md` 與程式碼（常數會漂，這裡不複製）。

## 核心鐵則（最高優先）
- **GAS 掌控所有數值**：六圍、HP、魔力、戰鬥裁決、骰值、補魔結算、AP／時鐘、世界推進。**先算、寫表、再敘述。**
- **AI（LLM）只負責**：把 GAS 已裁定的結果**敘述**出來，並把玩家自由發揮**摘要成事實列**（因果/關係/外顯狀態）。
- **AI 永遠不決定勝負、不寫數字**：LLM 輸出契約裡硬數值（HP/MP/位置/陣營/貢獻/身世）一律被 GAS 忽略或夾值，天然鎖死「不能亂改」。
- **show-don't-tell（紅線）**：敘事禁止直述角色 願望／個性／萌點 字面（`servantCard_` 鐵則強制）。
- **有理有據**：每次敘述前，GAS 把「相關記憶＋世界規則＋當前數值」餵 LLM，避免矛盾亂編（對試算表做 RAG）。

## 雙軌設計（玩家定案，別偏離）
- **🎴 純淨 solo（主體）**：單人聖杯戰爭，只有「按鍵＋AI 敘述」，**SFW**。多帳號可玩、資料分流（game_id 實例化＋帳號綁定）。盡量貼近 Fate 原作。solo 全程無花錢入口，身世財力差異走起始禮裝（見下）。
- **🌹 慾海 kanshou（鑑賞後日談）**：不需先在 solo 打贏，直接從英靈殿召喚同伴進入後日談。**NSFW**，一張約會大地圖（無固定節點清單），只持久化 個性／特徵／關係／外顯／肉體＋歷史因果。**無戰鬥、無經濟、無房東房客世界觀**，其餘全靠 AI 即興。共用 `actionPlay` 引擎與 `nsfwBaseRules`（紅線①不可改）。
- **兩個唯讀視窗已砍**：原「📜 個人聖杯戰記」／「🏆 排行榜」2026-07 玩家定案全數移除（含 `showVictoryHistory`/`get_victory_history`/`actionLeaderboard`），單人專注、不做跨帳號回顧比拼。

## 已砍：經濟／生活層（全砍·兩軌皆無）
money／商城·店鋪／物品·背包／給銀兩／天命·任務／工房經濟／賭場／信件·飛書／生活技能／裝備欄——**solo 與 kanshou 都不要**，AI 需要時自己掰、不寫試算表。kanshou 亦無經濟、無房東房客框架（金錢/打工/房租/商店曾一度開放、後又全數移除）。身世財力差異改由**起始禮裝**體現（存 MEMORY，非裝備欄）。

## 三模式總開關
`solo`(FATE 單人戰爭·主體)／`full`(九州全模擬·停用中)／`kanshou`(鑑賞約會)，由 `applyModeUI()` 切換。

---

## 戰鬥系統（GAS 全裁決，引擎在 `Engine_Fate.gs`）
**一句話：D20 定命中、骰子定傷害、概念優先權與寶具規模定碾壓，魔力不足抽御主當電池。**
- **命中**：攻守各 `D20 + 階級隨機區間(rankBand_)`，高者命中；擲 20 暴擊、擲 1 大失敗。階級區間重疊 → 低階偶能爆冷。
- **傷害（D&D 風骰）**：武器骰 `rankTier d8` ＋ 階級底 ＋ 命中分差；寶具骰依階級 `E3d10→EX30d10`。暴擊多骰一輪。
- **🔱 概念優先權（Priority）**：高位階進攻概念碾穿低位階防禦（ea＞固有結界＞破魔/必中＞英靈防禦技能）；系統化「破魔無視神核」「神代凌駕對魔力」這類交互。
- **🏰 寶具規模相剋**：對人/對軍/對城/對界矩陣（對城打對人 ×2.5、對界碾壓常規防禦）。
- **🔋 出力電池制（御主＝唯一魔力池）**：從者**沒有自有魔力池**，全靠與御主**共用一池**(存御主MP，上限 `masterPoolMax_`=迴路×10＋從者魔力×2)。從者有「靈基出力檔位」旋鈕(20~100%，存【出力】，預設60)，持續抽御主MP維持；放寶具須100%全開。池見底→**被動燃血**(缺口÷2，全額由御主承擔·從者不扣血)。寶具 Prana 依階級(約 E40→EX300，A階≈耗盡滿池、EX須焚血)，故 EX/EA 極罕見、masterless 強者(無主金閃)補不了魔而自限。確切數值看 `Core_Settings.gs`/`SOLO_REFERENCE.md`。
- **🌟 寶具對轟**：玩家解放寶具、夠格的敵以寶具相迎，比火力、高者貫穿、勢均力敵則僵持。
- **⚡ 從者主動技**：依其 fx 簽名給一個本戰增益按鈕（魔力放出/氣息遮斷/怪力/狙準/鼓舞…，至少有「集中」），耗魔走電池。
- **🛡️ 十二試煉**：god_hand 自死亡歸來；但高位階寶具(對城/對界/ea)一擊可燒多條命，overkill 再加成。
- **令咒·絕對命令**：必中＋威力倍增（見下「令咒」）。
- 確切公式／fx 清單見 `SOLO_REFERENCE.md` 第 4 節與 `Engine_Fate.gs`。

## 從者能力結構（四層・機器可讀）
1. **六圍**：筋力/耐久/敏捷/魔力/幸運/寶具，階級字串 `E D C B A EX`（可帶 `+`，每 `+`＝+5）。
2. **職階技能**：對魔力、騎乘、單獨行動…（含階級）。
3. **保有技能**：直感、魔力放出、避矢加護…（含階級）。
4. **特性標籤**：神性、王、龍、人類、女神、神造兵器…（剋制旗標，不分級）。

技能帶**效果碼 fx** 供 GAS 計算（如 `nullify_magic`/`burst`/`gae_bolg`/`god_hand`/`ea`）。
**餵 AI 規則**：GAS 算全部數字；AI 戰報只收到「本回合**實際觸發**的標籤名＋一句白話」（如 `對魔力A(魔術無效)`、`神殺(剋神性)`），**不給公式/階級/整份字典**。

## 衍生數值（綁能力推導，別手調每隻）
- HP/MP 上限由 `fateMaxHpMp_`（御主）與召喚公式（從者＝耐久/魔力推導）算；確切係數看 `Core_Settings.gs`。
- **魔力收支（FATE 戰鬥機制，非經濟）**：靈脈／陣地／工房 供給（HUD `playerServantEconomy_`）；回魔三態：♻️自然(靈脈/休息·時回 `applyRegen_`)／💧補魔(`mana_supply`·燃迴路·**永久代價**：血上限↓/迴路↓)／🩸燃血(**被動**·池見底自動扣御主＋從者HP)。夜襲 `enemyAmbushOnServant_`、搜索撿魔 `scavenge`(零星·同地枯竭) 皆 GAS 結算。

## 從者好感／羈絆
- 能力（六圍/技能）一律照顯示（英靈眾所周知，不為保密而藏）。真名走一次性劇情揭露。
- 好感只用於：機制解鎖／配合度／個人劇情／戰鬥微加成。
- **solo 好感收歸 GAS**：只有羈絆互動／補魔／獻血／與盟友共處／結盟等**按鈕**能動好感；AI 自由敘事改不動。鑑賞那軌才由 AI 推進好感。

## 令咒（固定選單，純 GAS，貼近原作）
共 3 道；不開放自由文字解析（絕對命令權＝效果確定）。主要：絕對命令（必中＋威力倍增）、緊急脫離（撤退，因綁定同行故非召還）、靈基修復、強制補魔。強迫違背意志且好感不足 → 扣好感。

## 御主參戰
- **後方支援（預設）**：供給魔力（電池）、令咒、強化自家從者。
- **獵殺敵御主（戰略）**：御主既亡 → 其從者斷供消滅。斬首須護衛盡除或擲 20 大成功（否則遭護衛反噬 1.5×）。御主脆弱＝高風險高回報。
- 身世財力差異走**起始禮裝**（2026-06 全面**被動化**：持有即戰鬥自動加持我方從者，無主動發動/充能/迴路門檻；見 `Mystic_Code.gs` 的 `MC_COMBAT_`）。

## 時間／AP（單人）
- **每日 12 AP，1 AP ≈ 1 小時**（`AP_PER_DAY`）。移動/搜索/偵查/戰鬥/補魔各耗 AP。
- **睡覺＝休息**：推進到隔天、回 HP/MP、AP **補滿（非累加）**；危險地點睡覺有夜襲風險。
- **剎車＝戰爭期限＋世界自走**：拖時間則 NPC 互相廝殺、從者陸續陣亡，自動平衡。

## NPC 模擬迴圈（`worldTick_`，GAS 主導、AI 只點綴）
每次時間推進：NPC 領地化加權移動 → 同地碰撞 GAS 擲遭遇類型（戰鬥/對峙/結盟/迴避）→ 數值結算 → 只寫一條事實到戰記。玩家僅以「傳聞」得知（戰爭迷霧 SEEN）。NPC×玩家同地才觸發完整 AI 場景。

## 據點／工房（結界，mana 機制非經濟）
參戰者可設據點、加固結界、吃靈脈回魔；Caster（陣地作成）結界上限高、加固快。攻打＝侵蝕結界，歸 0 才闖入觸發防衛戰。

## 召喚（新角色唯三來源）
**開場自創御主 ＋ 召喚從者 ＋ 自動鋪設的敵方陣營**——沒有別的手動建角入口。
- 召喚：英靈殿有真名 → 直接召；無 → AI 依 schema 生成 → GAS 驗證夾值 → 寫入英靈殿（`source: ai_gen`）日後可重用。主鍵＝真名＋職階。

## 結局
- **死亡**：我方所有從者 HP 歸 0（或戰鬥致敗）→ 願望「假夢」(AI 依願望生成) → 老虎道場 → 清該場動態資料 → 回主畫面。
- **奪杯**：敵對從者全滅 → 莊嚴奪杯敘述（＋祝賀道場）→ `actionEndRun` 清理本局動態資料 → 回主畫面、可立刻開新局。**不再封存**——鑑賞改由主選單直接進入＋英靈殿召喚（`actionKanshouSummonHero`），不靠奪杯。

## 資料層（試算表）
- **靜態**：英靈殿（範本·唯讀，AI 生成新英靈才新增）、坤圖（地圖）、世界規則、戰鬥標籤種子。
- **動態**：眾生（一列＝一位參戰者，御主/從者各自一列，COL 位置索引固定不可刪欄）、關係(REL)、記憶(MEMORY 標記)、時鐘、戰記/歷史、鑑賞庫。
- 確切欄位與 COL 索引看 `SOLO_REFERENCE.md`。

---

## 技術架構
```
GAS Web App (HTML Service：聊天/地圖介面)
   └─ google.script.run → GAS 後端 (handleGameAction / ActionRouter)
        ├─ 數值引擎：戰鬥(Engine_Fate)/補魔/移動/世界推進（全在 GAS）
        ├─ Google Sheets（資料庫）
        └─ UrlFetchApp → OpenRouter（敘述用 LLM）
```
- API key 存 GAS **Script Properties**（不進程式碼）。
- 部署：commit→push→GitHub Action(clasp) 自動部署；管線單向 Git→GAS（`clasp push -f` 覆蓋）。

## 多人連線
v1 **只留入口（敬請期待）**，不實作共享世界。
