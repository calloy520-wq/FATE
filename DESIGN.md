# 命運停駐之夜 — GAS LLM 聖杯戰爭 設計文件

## 核心原則（鐵則）
- **GAS 掌控所有數值**：六維、HP、魔力、戰鬥/偵查/搜索判定、骰值、補魔結算、倒數計時。
- **AI（LLM）只負責**：敘述劇情、記錄重大事件、補魔的文字互動、把玩家自由發揮**摘要成事實列**。
- **玩家輸入皆為自由文字**：由 AI 判意圖 → GAS 判定。LLM 輸出契約裡**沒有數字結果欄位**，天然鎖死「不能亂改數值」。
- **有理有據**：每次敘述前，GAS 把「相關記憶 + 世界觀規則 + 當前數值」餵給 LLM，避免矛盾與亂編（對試算表做 RAG）。

## 技術架構
```
GAS Web App (HTML Service, 聊天/地圖介面)
   └─ google.script.run → GAS 後端 (doGet/doPost)
        ├─ 意圖判定：混合（關鍵字快篩 → fallback LLM）
        ├─ 數值引擎：戰鬥/偵查/搜索/補魔/移動（全在 GAS）
        ├─ Google Sheets（資料庫）
        └─ UrlFetchApp → OpenRouter（model: google/gemini-3.1-flash-lite）
```

## 已確認決策
| 項目 | 決定 |
|------|------|
| LLM 閘道 | OpenRouter（`Authorization: Bearer`） |
| API key | 存 GAS **Script Properties**（不進程式碼），由使用者自行填入 |
| 補魔尺度預設 | 成人向 fade（仍 fade-to-black；可在設定切換，上限受模型政策限制） |
| 單人模式 | **正史**（扮演既有御主，七組依正典）／**混亂**（御主×從者隨機配對，自訂御主） |
| 個性 | 極簡**個性錨點**（一人稱/語氣/性格詞/對御主態度）；官方角色短填、AI 生成必填 |
| 意圖判定 | 混合：GAS 關鍵字快篩，篩不出才呼叫 LLM |
| 多人連線 | v1 **只留入口（敬請期待）**，不實作共享世界 |
| 召喚 | 聖遺物（催媒）決定職階傾向，GAS 抽選，AI 演出 |
| 六維 | Fate 正典：筋力/耐久/敏捷/魔力/幸運/寶具（E~A、`+`、`EX`） |
| 英靈範圍 | 先建第四次、第五次聖杯戰爭英靈 |

## 戰鬥系統（D20 對抗 + 標籤）
- **分數 = 屬性基準值 + D20**。基準：`E10 D20 C30 B40 A50 EX60`，`+` 各 +5。
- **命中**：攻方(敏捷+D20) vs 守方(敏捷+D20[＋避矢加護等])，高者命中。
- **傷害**：`max(底, 攻方筋力 + 浮動 − 守方耐久/2)`，命中時計算。
- **階級重疊天然成立**：相鄰階差 10 < D20 擺幅 20 → 敏捷C 偶爾搶贏 B；差 2 階(20)幾近輾壓仍有萬一。
  呼應官方「階級只代表素質上限」。
- **標籤修正（機器可讀 fx）**：對魔力`nullify_magic`(魔術減免/無效)、神殺`bonus_vs_trait`(對神性×2)、
  避矢加護`evade_ranged`(遠程命中−X)、魔力放出`burst`(傷害+)、直感/心眼`first_strike`(先制)、狂化`mad`(筋力↑命中/防↓)。
- **令咒**：戰鬥中花 1 令咒 → 必中／全力一擊／緊急回避／真名解放／強制撤退。
- **流程**：回合制對抗，GAS 逐回合結算 → 產出「戰報重點」→ 交 AI 潤飾（detail 不拖累 prompt）。
- 原型 `prototype/index.html` 已可實跑（按「⚔ 攻擊／使用寶具」看真實 D20 判定與戰報）。

## 從者好感度（羈絆）系統
- **不為保密而隱藏**（英靈眾所周知）。能力（六維/職階技能/保有技能）一律照顯示。
- 好感度只用來：
  - **機制解鎖**：真名解放寶具、強化個人技能、補魔效率提升
  - **配合度**：低好感可能不完全聽令（需令咒強制）；高好感主動配合
  - **個人劇情**：解鎖心願線/個人事件
  - **戰鬥微加成**：士氣／暴擊小幅加成
- **真名**：走一次性劇情揭露（御主角色一開始不知，對話/共鬥後揭曉）。
- 階段：戒備(0-20)/容許(21-40)/信賴(41-60)/羈絆(61-80)/唯一(81-100)。
- 提升來源：補魔、共鬥、對話選擇、達成心願、贈禮。存於 `PlayerServants`（欄位 `bond`）。

## 從者能力結構（四層・機器可讀）
1. **六維**：階級 E~A/EX。
2. **職階技能**：對魔力、騎乘、單獨行動…（含階級）。
3. **保有技能**：直感、魔力放出、避矢加護…（含階級）。
4. **特性標籤**：神性、王、龍、人類…（傷害修正用）。

技能帶**效果碼**供 GAS 計算，例：
| 標籤 | 效果碼 | 說明 |
|------|--------|------|
| 對魔力 A | `nullify_magic` | 低於神代的魔術無效 |
| 神殺 | `bonus_vs_trait{trait:divinity, ×2}` | 對神性追加傷害 |
| 避矢加護 | `evade_ranged{+X}` | 對遠程攻擊閃避加成 |
| 神性 B | `trait:divinity` | 被神殺剋 |

## 戰鬥公式（草案）
- 階級基準：`E10 D20 C30 B40 A50 EX60`，`+` 各 +5。
- 實際值 = 基準 ± 浮動（幅度待定）。
- 對抗判定：物理 `攻方(筋力 roll) vs 守方(耐久 roll) → 差=傷害`，再套技能標籤修正。

## 補魔機制
- 觸發 → GAS 開倒數（預設 10 次對話），期間 AI 處理文字。
- 計數歸零 → GAS 結算魔力回復、寫入 EventLog。

## 時間／回合制（單人 v1）
- 「日 + 時段（晝/夜）」制，每時段固定**行動點數**；移動/搜索/戰鬥消耗點數，用完推進時段。
- 夜晚危險度↑。多人時間同步 **v1 不做**。

## 物品欄（輕量，不做數值膨脹）
- `Inventory`：消耗品（食物→少量回血/回魔 + 飽食度）、觸媒、任務道具。
- **不做裝備強化**。飽食度為可選風味（餓→行動點上限↓）。

## 回復與加成
- 休息回血；靈地依 `Locations.leyline`（靈脈值）回魔。
- 御主被動加成綁定建角時選的**魔術系統**（強化→近戰、召喚→使魔、寶石→魔力儲備…），不另設職業。
- **不做金錢系統**。

## 補魔內容尺度
- 機制（GAS）：魔力回復 + 好感度 + 倒數結算。
- 過程（AI）：文字處理，建議 fade-to-black；提供尺度設定（清水/曖昧/成人向 fade）。
  實際上限受模型內容政策限制。

## ⭐ 可擴充召喚系統
- 召喚模式：① 催媒（職階隨機）② 指定真名召喚 ③ 訂製召喚（玩家描述）。
- **DB-miss 流程**：查 `ServantTemplates` 無 → 呼叫 AI 依固定 schema 生成 → GAS 驗證+夾值 → 寫入 → 快取，日後直接調用。
- **主鍵 = 真名 + 職階**（同名不同職階各一筆，如 斯卡哈-Lancer / -Caster / -Assassin）。
- **平衡護欄**：AI 生成數值須夾在允許範圍（階級上限、寶具規則），GAS 驗證後才寫入。
- `source` 欄位：`official` / `ai_gen`，便於日後校稿。
- 預載策略：手工精修第四次/第五次當黃金樣本，其餘 on-demand 生成+快取（FAKE/FGO/Apoc 可日後批次匯入）。

## 模式與參戰流程
```
正史 → 選戰爭(第4次/第5次/FAKE…)
        ├ 扮演正史角色 → 挑該場既有御主，承接其從者
        └ 自創角色（額外參戰者，第 N+1 位）→ 建御主 → 詳細召喚
混亂 → 自訂御主 → 催媒隨機召喚
```
- 每場戰爭定義 `participants`（資料驅動）：4th/5th=7（自創=第8位）、FAKE 暫設 13（自創=第14位，可調）。
- FAKE 為不規則戰爭，含 False/True 從者與 Extra 職階（如 Watcher）；roster 先放部分樣本，其餘 AI 生成或日後補入。

## 詳細召喚（自創角色）
1. **選職階**：標準七階全開；Extra 職階（Ruler/Avenger/Foreigner/Watcher/MoonCancer/AlterEgo/Pretender）可展開。
2. **英靈殿名單**：顯示該職階資料庫現有英靈 → 點選召喚。
3. **真名召喚**：輸入真名；DB 有→直接召；DB 無→AI 依格式生成+夾值→寫入→日後可重用。
4. **自訂從者**：玩家描述 → AI 生成 → 寫入。
- 主鍵 `servant_id = 真名 + 職階`（支援同名不同職階）。

## 戰局七組（單人也需對手陣營）
- **正史**：七組照正典，扮演=選一位；自創=正典七組 + 玩家為額外席位。
- **混亂**：御主×從者隨機配對洗牌。
- 存於 `WarRoster`（per 存檔）：master/servant_id/is_player/alive。

## 個性錨點
- 欄位：`一人稱 / 語氣 / 性格關鍵詞 / 對御主態度`（極簡）。
- 知名官方角色短填即可（LLM 已有強錨點）；AI 生成角色**必填並鎖定**以保重用一致性。

## 時間與 AP 系統
- **每日 12 AP，1 AP ≈ 2 小時**（晝 6 / 夜 6）；夜晚危險度↑。
- 行動消耗：移動 1~2 AP（看距離）、搜索/偵查 1 AP、戰鬥結算後推進。
- **睡覺＝休息**：推進到下一時段/隔天，回滿 HP/MP 並把 AP **補滿（非累加）**，避免無限刷 AP。
  危險地點睡覺有夜襲風險。
- **剎車＝戰爭期限 + 世界自走**：拖時間則 NPC 互相廝殺、從者陸續陣亡，產生機會成本（自動平衡）。

## NPC 模擬迴圈（GAS 主導，AI 只點綴）
每次時間推進：
1. **移動（領地化加權，非純隨機）**：每位 NPC 有「據點/巡邏圈」自然分散；依加權往靈脈/目標靠近、避開明顯更強者。GAS 計算。
2. **碰撞解析**：
   - **NPC × NPC 同地點** → GAS 擲「遭遇類型」：戰鬥/對峙/結盟/迴避（依個性+實力差加權）。
     戰鬥→GAS 數值結算，敗方陣亡→更新戰場；只寫**一條簡短事件**到 EventLog（可選 1 句 AI 點綴）。
     玩家僅以「傳聞」得知（EventLog.玩家是否知情）。
   - **NPC × 玩家 同地點** → 觸發**完整 AI 場景**。
3. **遭遇判定**：同地點不必然相遇，再擲**遭遇機率**（受氣息遮斷/地點大小/晝夜修正），避免一直被打斷。
- 判斷「NPC 在這」：比對 `戰場.所在地 == 玩家所在地`（deterministic）+ 遭遇機率。
- **解決一直相撞**：靠「領地化分散 + 遭遇機率」根治，地圖放大為輔。

## 據點 / 工房（結界系統）
- 每位參戰者可有**據點**（御主的家／佔領地），帶 **結界值（0~上限）**。
- **NPC 基本上不會「移動到」據點**：隨機遊走不會自殺式闖入；只有**刻意攻打**才接觸。
- **攻打＝侵蝕**：敵每次攻打 → 結界值扣 X（依攻方破壞力 vs 結界防禦換算，保留保證最低削減防無限龜縮），
  **歸 0 才真正闖入** → 觸發防衛戰。
- **加固**：花 AP＋魔力（＋寶石道具）提升結界值與上限。
- **偵測**：結界被侵蝕時發告警事件，給玩家趕回防守的時間。
- **Caster（陣地作成）的重點**：結界上限高（工房/神殿）、加固快又便宜，並解鎖
  魔力回復↑／道具作成／來犯偵測／主場戰鬥加成；大結界每日耗魔力（Caster 減免）。
- 戰場分頁可加：`base_loc / barrier / barrier_max / base_tier`。
- 原型已實作：結界條、🛡加固（Caster 更強）、輸入「敵襲」模擬侵蝕→破關闖入防衛戰。

## 令咒（固定選單，純 GAS，貼近原作）
不開放自由文字解析——令咒是「絕對命令權」，效果確定。每招耗 1 令咒（共 3 道，可設特殊事件補充）。
| 指令 | 原作依據 | GAS 效果（確定值） |
|------|----------|------------------|
| 靈基修復 | 從者由魔力組成，灌注魔力＝治療 | HP／魔力回滿 |
| 絕對命令 | 凜的絕對服從／必中一擊 | 下一擊必中＋威力倍增 |
| 寶具強制解放 | 無視魔力不足/意願強制發動 | 強制發動寶具（高傷） |
| 緊急脫離 | 令咒干涉空間特權 | 御主＋從者脫離戰局/險境（綁定同行，故非「召還」而是撤退） |
| 強制補魔 | 緊急一道令咒灌滿魔力 | 魔力回滿＋好感跳升，跳過 10 回合倒數（fade） |

**原作校準規則**（依 4th/5th 各御主用例）：
- **御主與從者綁定同行** → 取消「瞬間召喚從者到我身邊」（多餘），改為**緊急脫離/撤退**。
- **強迫違背意志 → 好感下降**：絕對命令／寶具強制解放，若好感<信賴(60) 則扣好感（時臣命令吉爾→破裂、凜對Archer初期裂痕）；高好感則欣然從命不扣。
- **令咒疊加＝電池分級**（可選）：花 2~3 畫＝破格效果（越級寶具、強制狂暴化、壓過強意志；韋伯3畫純魔力、雁夜多畫狂暴、切嗣2畫破壞聖杯）。
- **命令越具體越強**：固定選單天然符合（每招皆具體動作＝滿強制力）；模糊命令不做。
- 破壞聖杯＝劇情級特殊令咒，留結局線。
- 原型已實作：動作列「❖ 令咒」固定選單，各招扣 1 令咒、GAS 結算；強迫扣好感、緊急脫離。

## 補魔與時間
- 補魔為**密封時段**：10 回合對話期間時間/NPC 凍結（不被打斷）。
- 結束時一次扣 **2 AP** 並讓 NPC 行動一次（世界仍合理前進）。
- 結算：魔力回滿 + 好感度上升（見好感度系統）。

## 事件日誌（EventLog）Schema — 單人/多人相容版
GAS 算完數值後寫入「純事實」（不花 token）；AI 說書時只撈相關最近 N 條餵 prompt。

| 欄位 | 型態 | 說明 |
|------|------|------|
| `event_id` | String | 唯一列 id（流水號/UUID） |
| `write_ts` | Number | **毫秒寫入時戳**（排序/去重/取最近 N，遊戲內時間會撞） |
| `game_id` | String | 區分戰局（單人各開各的 / 多人全服同一個） |
| `day_count` | Integer | 聖杯戰爭天數 |
| `time_hour` | String | 遊戲內時間（24h 制，如 `22:00`） |
| `location_id` | String | 發生地點（**對齊 Locations 表 id**，如 `emiya`/`shinto`） |
| `event_type` | String | 分類 enum：REST/MOVE/SEARCH/SCOUT/ENCOUNTER/AMBUSH/BATTLE/STANDOFF/ALLIANCE/MANA/NP_RELEASE/DEATH |
| `actor_id` | String | 發起者：**`slot_N`（NPC組）或 `ms_id`（人類玩家）**，前綴區分 |
| `target_id` | String | 目標：同上多型，無則留空 |
| `log_text` | String | GAS 寫入的結構化事實（如「slot_0 熟睡時遭 slot_3 突襲」） |
| `is_global` | Boolean | 是否公開情報（寶具解放=全城感知 / 暗處偷襲=FALSE） |
| `importance` | Integer | 0~2，narration 撈取時裁剪用（避免被 REST/MOVE 洗版） |

- **多人相容**：欄位不變；人類玩家用 `ms_id`、NPC 從者組仍用 `slot_N`，**兩者並存**於同一世界。
- **撈取規則**：`game_id 相符 且 (actor/target 含自己 OR is_global=TRUE OR (同 location 且 對方未氣息遮斷))`，依 `write_ts` 取最近 N 條 → 打包餵 OpenRouter。

## 試算表（資料庫）規劃 — 靜態 vs 動態
**核心觀念**：英靈殿＝唯讀定檔；開局時「抓基本資料 → 生成實例 → 寫入戰場」。
實例不複製整份能力，只存變動值 + `servant_id` 指回英靈殿，要用六維/技能時即時查。

### 靜態分頁（定檔・全玩家共用・只在 AI 生成新英靈時新增）
- `英靈殿`(ServantTemplates)：servant_id(真名+職階)/職階/真名/wars/六維/技能(JSON)/特性標籤/寶具/個性錨點/source(official|ai_gen)
- `地圖`(Locations)：id/名稱/x/y/類型/相鄰/危險度/靈脈
- `道具圖鑑`(ItemTemplates)：類型/效果碼/數值
- `世界規則`(WorldRules)：世界觀與規則（餵 AI 當底線）
- `戰爭範本`(Wars)：戰爭id/名稱/participants/正典roster(master+servant_id)

### 動態分頁（存檔・依帳號變動）
- `帳號`(Accounts)：**ms_id(毫秒ID,主鍵)** / 玩家名稱 / 建立時間 / 目前game_id / 道具(JSON) / 設定(尺度等)
- `戰場`(Battlefield)：**御主與從者合併，一列＝一位參戰者**（1:1 綁定）
  `game_id | slot(1~8) | is_player | 御主名/魔術系統/御主HP/MP/令咒/所在地 | servant_id(→英靈殿) | 從者HP/MP/好感度/真名揭露?/狀態 | alive | 據點(NPC巡邏中心)`
  - 開局依玩家 ms_id 一次寫入 8 列（玩家1 + 對手7）。**不另設御主分頁**。
- `記憶`(Memory)：EAV 事實表（捕捉玩家自由發揮）game_id/回合/對象/事實類型/內容/重要度
- `事件`(EventLog)：事件記錄，詳見下方「事件日誌 Schema」（多人相容版）
- `時鐘`(GameClock)：game_id/第幾日/時段(晝夜)/剩餘AP/補魔倒數/補魔鎖定中?

### 共享世界（v1 不做）
- `WarState`：存活從者數/陣營動向/全域事件

## 介面（已完成原型 `prototype/index.html`）
登入（無密碼）→ 主選單（單人／多人入口）→ 建立御主 → 召喚 → 主畫面（左：御主+從者狀態與標籤｜中：冬木市可點地圖｜右：敘述/聊天+動作按鈕+補魔倒數）。
可直接用瀏覽器開啟預覽；接 GAS 時把 `SERVER` 物件換成 `google.script.run` 封裝即可。
