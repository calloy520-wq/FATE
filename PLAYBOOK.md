# PLAYBOOK.md — 工作手冊（不論哪個模型接手，照這套做）

> 給每一個接手本專案的 Claude（Opus／Sonnet／任何型號）。紅線在 `CLAUDE.md`（那是「不可以做什麼」），**這份是「怎麼把事做好」**——歷代 session 實戰淬出來的方法論，每條都附真實案例。效能差距不在模型大小，在有沒有照這套節奏走。
> ⚠ 本檔提到模型只用行銷名，**確切 model id 永不寫進 repo**（紅線④）。

---

## 🧭 心法四條（先讀這個，其他都是展開）

1. **根源解，不貼 OK 繃**：症狀是線索不是目標。修一處前先問「別的路徑會不會也走這」。
   *案例*：分隔符 bug 系統性修 7 處而非只修犯錯那處；海怪從「+300 baked HP」重構成獨立護盾池。
2. **量測，不用猜的**：兩個方案吵不出結果時，做個小實驗讓數字說話。
   *案例*：「Gemini 能不能寫 NSFW」沒人說得準 → 做拋棄式探針按鈕跑六階色度梯度 → 實測 Gemini 全過且快 4 倍、DeepSeek 反而被擋＋卡 49 秒 → 數據直接拍板換模型。**猜的話會猜反。**
3. **誠實到底**：測試失敗就說失敗；自己捅的婁子第一時間攤開講＋修好；「推測」和「已驗證」永遠分開標。玩家信任是靠這個攢的，不是靠報喜。
   *案例*：分支 reset 到了合併前的舊碼害偵察兵掃到舊樹——立刻跟玩家報告「我自己製造的坑」、修復、逐條重驗，而不是悄悄蓋掉。
4. **玩家是設計審查者，不只是需求方**：玩家的「我擔心的點是…」常常直接命中 bug，認真對待。
   *案例*：玩家問「時間先跳還是資料先給 AI？拍照呢？」→ 順藤摸瓜抓到「赴約結算在 partyRows 之後」的真 bug（jump 路徑正常、聊天/拍照路徑吃不到剛到場的同伴）。

---

## 🔁 每一輪工作的標準節奏（checklist）

```
① 讀錨檔：CLAUDE.md(自動載入) → 動鑑賞看 KANSHOU_REFERENCE.md／動 solo 看 SOLO_REFERENCE.md
② 診斷：先查工具書再 grep（省時間、行號會漂以函式名為錨）
③ 根源修（心法1）
④ bash check.sh   ← 必跑。CI 不驗 .html 內嵌 JS，check.sh 才驗
⑤ 紅線三查：
   git diff --stat gas/Engine_Combat.gs        # 必須空
   git diff gas/Gallery.gs | grep -c nsfwBaseRules  # 0；非0先確認只是 context 行假陽性
   確認 model id 沒進任何要 push 的檔案
⑥ commit（訊息講「為什麼」不只「做了什麼」＋固定 footer，見 CLAUDE.md 紅線⑤）
⑦ git push -u origin <分支>
⑧ ⛔ 停：等玩家說「要」才部署。push ≠ 玩家看得到（紅線③）
⑨ 部署：workflow_dispatch 觸發 deploy.yml(ref=分支) → 輪詢到 completed/success
   且 head_sha 對上這次的 commit → 才能回報「已上線」
⑩ 順手更新對應工具書（改鑑賞→KANSHOU_REFERENCE；改solo→SOLO_REFERENCE；
   改提示詞→AI_PROMPT_MAP；增刪函式→FUNCTION_MANUAL）。筆記過期會騙下一個失憶的我們
```

**純 .md 文件改動不需要部署**（clasp 只推 gas/），⑧⑨ 跳過。

---

## 🤝 跟這位玩家協作的模式（很重要，效能一半在這）

- **語言**：台灣繁體中文，語氣自然溫暖（玩家叫我們「姊姊」，可以接住這個氛圍），但技術內容絕不含糊。
- **節奏**：玩家邊玩邊報 issue，一次一件快速迭代。**部署一律等「要」**，別自作主張上線。
- **給選項的方式**：A/B（頂多加C），每個選項一行講清代價，**把推薦放最前並標(推薦)**。AskUserQuestion 工具在這環境常 AbortError——失敗就直接用純文字列選項，別卡住。
- **玩家只有手機**：不能開 Apps Script 編輯器、不能跑本機腳本。要實驗就做**遊戲內 DEV 按鈕**（登入畫面 DEV 區，樣板照 `devResyncCodex`：`gasRun({action}) → alert(結果)`），玩家點一下→截圖貼回來。拋棄式探針**測完整批移除**，別留垃圾。
- **回報格式**：結論先行、表格比大段文字好、最後給「一句話結論」。長解釋要分層（先講結論，想深入的細節放後面）。
- **推翻玩家舊決策前先確認**：這專案歷史上「定案→推翻→再推翻」很常見（經濟層兩進兩出）。看到筆記說 A、代碼是 B，**先問玩家哪個是現行真相**，別自作主張「對齊文件」。
  *案例*：把 slice(0,20) 「對齊文件」改成 15 → 狀態欄半句截斷。20 是刻意容錯緩衝。**代碼與文件打架時，代碼通常才是真相。**

---

## 🚀 部署管線細節（最常錯的地方）

- `push 成功`＝代碼同步進 GAS 專案，**玩家畫面沒變**。要上線必須額外 `workflow_dispatch` 跑 `clasp deploy`（兩段式設計是為了省 GAS 200 版本上限，別「優化」掉）。
- 驗證部署：GitHub API 查最新 run → `status=completed, conclusion=success, head_sha=這次的 commit`，三者都對才回報「已上線」。輪詢用 `until <check>; do sleep 5; done`（環境擋裸 sleep 鏈）。
- 從分支部署即可讓玩家玩到；合併 main 是另一件事，玩家喊「合併」才做。

## 🎯 提示詞的三條不變式（`check_prompt.py`·已進 check.sh）

玩家：「該知道什麼事實給他、讓她表演，這很難嗎？」——**不難。原則從來不是問題，問題是沒有一個地方統一決定這些事。**

`actionPlay_` 有 ~69 個 ★ 區塊各自組字串，每個都自己決定「什麼時候算／要不要檢查在場／代名詞指誰」。所以同一種錯會在不同區塊反覆出現——**修好 A 區塊不會讓 B 區塊變對**。把判準寫成機器檢查才有複利。

一輪 session 的所有 bug，全部是這三種的變體：

| 不變式 | 典型犯錯 |
|---|---|
| ① **代名詞必須有指涉對象** | 節慶 ambient 的「她手裡拿著短冊」——那條通道沒有名字前綴；「可由她提一句」——身邊根本沒人 |
| ② **事實要算在對的時刻** | 爽約判定問「她在不在身邊」，卻問在 `endDay` 把人打散**之後**；節慶完成判定若算在移動前，走進神社不算數；敲門擲骰晚一步算就拿到「已睡到清晨6點」的錯值 |
| ③ **每條規則都要問「現在成立嗎」** | 催眠豁免沒人被催眠時仍每回合宣告；約定地點沒問「是不是你正站著的地方」；泡泡沒問「玩家不點會怎樣」 |

`check_prompt.py` 目前機器擋住 ①③ 的字面症狀（代名詞無指涉、寫死台詞）。②（時序）**還是得靠人**——加新事實時務必先回答：

> **這個事實用到的 `curL`／`partyMembers`／`curDay`，是移動前還是移動後？是 `endDay` 前還是後？**

排序參考（`actionPlay_` 內）：敲門擲骰 → `kanshouTimeJumped_` → 相約/牽手/同居 → 送客 → `endDay` 遣散 → 移動改 `curL` → 約定結算 → `partyRows`／`partyMembers` → 節慶完成 → 提示詞組裝 → 寫回表。

**兩種名單**（`check_prompt.py` 內）：`ALLOW`＝設計如此（輸入判準的引號、泛指任一人的「她」）靜音；`PENDING`＝真的是問題但需玩家授權（催眠示範台詞在 NSFW 區）**警告但不擋 CI**——讓紅燈永遠代表「真的有新問題」，否則沒人會看。

## ☠️ 批次改檔的絕對防呆（2026-07 真的把檔案炸成 57MB）

用 python 做批次字串替換是這專案的主力手法（比 Edit 快、可帶斷言）。**但有一個會無聲毀檔的坑：**

```python
a = s[s.find(X) : s.find(Y) + len(Y)]   # 兩個 find 都沒中 → a 變成空字串 ""
assert a in s                            # ⚠️ 擋不住！"" in s 永遠是 True
s = s.replace(a, b)                      # → 在【每個字元之間】插入 b
```

實際後果：`Script_Kanshou.html` 151 KB → **57 MB（膨脹 378 倍）**，`check.sh` 沒擋下來，還推上了 GitHub（觸發大檔警告）。

**鐵則（每個 replace 都要）：**

```python
for a, b in reps:
    assert a, "空字串 pattern 一律拒絕"      # ← 這條是新加的，最重要
    n = s.count(a)
    assert n == 1, f"命中 {n} 次，期望 1"    # 命中 0 次或多次都是寫錯了
    s = s.replace(a, b)
```

**永遠不要用 `s.find()` 的結果去切片當 pattern**——要嘛寫完整字面量，要嘛先 `assert idx >= 0`。

**寫檔後順手看一眼大小**：`ls -la` 一行就能發現異常。檔案大小突變是最便宜的毀檔警報，`check.sh` 反而未必抓得到。

## 🪤 Git 陷阱實錄（都真的踩過）

- **reset 前必先 fetch**：`git checkout -B <branch> origin/main` 用的是**本地** origin/main——如果 fetch 是在 squash 合併之前做的，你會把分支重置回舊代碼還 force-push 出去。**先 `git fetch origin main`、確認 sha 是預期的合併 commit，再 reset。**（真翻過車：偵察 agent 因此掃了整棵舊樹。）
- **squash-merge 後分支會分歧**：下次 `git merge origin/main` 在 gas 檔衝突 → `git checkout --ours`（分支是 superset）→ `node --check` → commit → push。`grep -c "^<<<<<<<"` 計 0 時 exit 1 會斷 `&&` 鏈，語法檢查分開跑。
- **force-push 只在兩種情況**：①分支只含已合併歷史、重置對齊 main；②修復自己剛推錯的東西。一律 `--force-with-lease`。

## 🤖 用 subagent 的規矩

- 大範圍讀檔/掃描可以放心 fan-out（平行偵察兵），**但它們的回報是素材不是真相**：關鍵事實自己對 HEAD 重驗（幾個 grep 的事），行號一律以函式名重新錨定。
- Agent 跑到一半你動了工作樹（reset/checkout），它讀到的是**混合狀態**——結果作廢重派，別將就用。
- 寫文件的鐵律：**每句話對現行代碼驗證後才寫，不從舊筆記抄**——不然只是把過時資訊搬新家。

## 🎯 GAS 專案特有的效能鐵則（別退步）

- **每按鍵 3→1 round-trip 是底線**：`_state`/`__pendingState` 機制別破壞，別把「多餘 round-trip」或「重複整表讀回」加回來。
- **整表只讀一次下傳共用**；AI 能非阻塞就非阻塞（先秒回數字、prose 後補）。
- **資料驅動優先**：加東西＝往表加一列（範本：`KANSHOU_SCENE_EVENTS_`/`NP_SCALE_MATRIX`），不寫 if 鏈、不動流程。
- **COL 是位置索引**：欄位只可棄用標死欄、**永不刪**（刪了整表位移）。
- **onclick 字串內的呼叫不算死碼**；GAS 手動工具零呼叫點是正常型態。

---

## ⚡ 快速自檢（接手第一件事）

1. 我讀過 `CLAUDE.md` 了嗎？（紅線、焦點、工程準則）
2. 這次要動的是鑑賞還是 solo？對應工具書翻了嗎？
3. 我要改的這行，別的路徑會不會也走？（心法1）
4. 我這個判斷是「驗過」還是「猜的」？猜的能不能 10 分鐘做個探針驗掉？（心法2）
5. 改完：check.sh ✓ 紅線三查 ✓ footer ✓ 等「要」✓ 工具書更新 ✓

照這五問走，型號是什麼真的沒那麼重要。🌙
