# KANSHOU_REFERENCE.md — 鑑賞（慾海後日談）唯一現況真相

> **前半是「現在是什麼」，後半是「怎麼走到這裡的」。** 想動鑑賞任何一塊，先讀到 §AI 管線為止就夠；再往下全是編年史，查特定一輪改動才進去。
> **行號會漂，一律以「函式名／常數名」為錨。**
> 🔴 動手前必讀 `CLAUDE.md` 紅線：`nsfwBaseRules` 不是禁區但是**演化核心**（改之前先量現況、改完跑全套探針）、`callGeminiAPI` 是兩軌共用基礎設施、model id 不進 repo。
>
> 🧹 **2026-09 大掃除**：好感／地點／作息三批整組砍除後，這份文件有 576 行還在用**現在式**講那些不存在的系統
> （關係五階·泡泡總表·橋段池·大區·老地方·時間流速·moveProposal·聚光燈·前端地圖…），已全數移除——
> 每一項都另有一份權威墓碑留著「為什麼砍」，砍掉的只是重複又過期的操作細節。
> 同輪把 `MEMORY 標記全表` 照代碼重寫（17 條 → **現在真的在讀寫的 12 條**），並修掉一個真的 bug：
> 選項 4→6 之後，前端打數字的捷徑還寫死 `[1-4]`，打 5/6 會被當成一般訊息送出去。

---

## 🌹 一句話定位

鑑賞＝**約會模式**後日談（NSFW）——**主選單直接進、從英靈殿直接召喚同伴，不需先打贏戰爭**（`actionEnterKanshou`＋`actionKanshouSummonHero`）。**一個更單純的世界**：無戰鬥、無經濟、無房東房客世界觀、**也沒有地點**，其餘全靠 AI 即興演出。建在 `nsfwBaseRules`＋`buildDefaultSystemPrompt` 上、與 solo 共用 `callGeminiAPI`（`actionPlay_` 是鑑賞自己的，兩軌不共用）。

### 世界觀鐵則（都是「已砍」的反向定義，別復活）
- **無戰鬥／無經濟**：`Router_Action.gs` dispatcher 明文擋 `KANSHOU_BLOCKED_ACTIONS_`。錢/打工/房租/商店/客房/肉償/欠租/宵禁全部**已刪**，grep 查無定義。AI 需要時自己掰、不寫試算表。
- **無地點·無移動**（2026-09 玩家「只要扯到移動都會很怪！直接都砍了吧」）：大區／地點／地圖分頁／住處／作息骰／移動泡泡整組砍除。留下來的只有**一格 AI 每回合自己寫的背景**（借 `COL.PC.LOC` 存，見下表）。
- **無奪杯封存**：`claim_grail`/排行榜/戰記**已刪**。
- **全程無花錢入口**（跟 solo 一樣）。`COL.PC.MONEY/UPKEEP_WEEK/ROOM` 是恆空死欄。

---

## 📊 資料層

### 分表隔離（與 solo 完全不污染）
- 鑑賞資料寫 **「鑑賞眾生」分頁**（`getKanshouPcSheet_`，lazy 建、schema 複製主「眾生」表頭、`COL.PC` 索引一致），與 solo「眾生」表隔離。
- **KPC_ 前綴路由**：dispatcher（`handleGameAction`）見 `pcId` 以 `KPC_` 開頭 → `sheets.pc` 指向鑑賞眾生。solo 是 `PC_`。
- **引擎硬擋**：`actionPlay` 開頭 `pcId` 非 `KPC_` 直接 return。
- **輸入清洗**（2026-07 稽核補）：主對話 `message` 欄位嵌入提示詞前補 `｜【】` 分隔符清洗，防玩家用這幾個符號偽造 MEMORY 標記或偽造「★【...】」格式的假系統指令段（遊戲本身的真實系統指令就長這樣，不濾會讓兩者無法區分）。
- **前綴白名單**：`KPC_`(御主 avatar)／`KHV_`(直接召喚同伴)／`DEAD_`；`KSV_` 是**舊奪杯封存邀請的遺留前綴**——封存管線已砍、不再產生新 `KSV_` 列，僅在 sync／`isKanshou` 判定保留向後相容識別（別當現行機制）。
- 🐛→✅ **2026-09 跨帳號污染：名字一旦離開陣列就不帶 game_id 了**。鑑賞眾生是**全帳號共用一張表**，而每個人都從同一座英靈殿召喚——**撞名是常態，不是巧合**。
  `actionPlay_` 的「第一次同床」蓋章：上游 `allEstablished` 有 `sameGame(r)` 過濾沒錯，但 `intimateNightNames`
  取出來的是**名字字串**，回頭 `pcData.forEach` 掃全表時只比名字。實測（`scratchpad/size/crossgame.js`）：
  甲乙兩帳號各召一個 SABER，甲按睡覺 → **乙那一列也被蓋上 `同床:1`**。補 `sameGame(r)` 修掉。
  ⚠ **這是一種形狀，不是一個 bug**：「先用 game_id 濾出一批列 → 取出名字 → 拿名字回頭掃全表」。
  全樹掃過只有兩處，另一處在 `actionRest` 的死分支裡（已連分支一起砍，見 `SOLO_REFERENCE.md`）。
  **之後要用名字回頭找列，一律先問「這個名字在別人那局會不會也有」。**
- 🐛→✅ **2026-09 長名同伴靠名字查無此人**：`sanitizeUserData_` 對 `targetName` 一律截到 `NAME_MAX=20`，但美遊／小黑／伊莉雅(Caster install) 三位不在 `KANSHOU_CASUAL_NAME_` 裡、列上存的是 23~30 字全名——釘選／刪除共同回憶、關係稱呼、專屬稱呼全部「找不到這位同伴」。根源修法＝**id 優先、名字備援**（`findPcRowIdx_` 本來就吃 `{id, name}`）：前端三個入口帶 `targetId`（關係面板開啟時記 `_krTargetId`；共同回憶按鈕帶 `c.id`），後端三支 handler 傳 `{ id: targetId, name: targetName }`。探針 `names.js`。
- 🐛→✅ **2026-09 世界帳本條目名被清成純中文**：`world` 的條目名原本借用 `name` 鍵，被 `sanitizeUserData_` 的 `CHINESE_NAME_FIELDS` 當成 solo 創角姓名——剝掉英數與「·」、截到 10 字；而 AI 寫進同一張帳本的名字卻允許英數＋20 字。改鍵名 `entryName`（含 rg_rename 的 `newName` 一起進 `STRICT_NAME_FIELDS`：剝 HTML／MEMORY 結構字元、上限 20），前後端八個呼叫點同步，`check_contract` 盯兩端。
- **歷史暫存**：solo/鑑賞**共用同一張「歷史暫存」表**，靠 `pcId` 前綴（`PC_` vs `KPC_`）隔離、非物理分表——架構唯一例外，記在案。

### COL.PC 鑑賞實際用到的欄位（定義 `Core_Settings.gs`）
| 欄 | 索引 | 鑑賞用途 |
|---|---|---|
| `MEMORY` | 12 | 所有中文方括號標記共用 cell（見下表），大量讀寫 |
| `LOC` | 6 | **只存玩家那一列**，裝的是 AI 每回合寫回的 `scene`（這一段演完人在哪·≤12字），下一回合當 `★【場景】` 餵回去。欄位名是歷史包袱——地點退休後這一格空著，就拿來當背景板 |
| `BOND` | 24 | ~~好感~~ 2026-09 整組砍除，鑑賞不讀不寫（solo 仍用） |
| `REL_TAG` | 25 | 關係稱呼。**AI 每回合自己維護**（`rel_changes[].rel_tag`）；玩家自己打過一次就鎖（【關係鎖】） |
| `REL_MEM` | 28 | 專屬稱呼＋兩把鎖（`KANSHOU_LOCKS_`：稱呼鎖／關係鎖）。組裝只走 `kanshouRelMemBuild_` 這一支 |
| `MEMOIR` | 27 | **共同回憶**（見專節）。原 `MAJOR_EVENT` 死欄復用 |
| `NAME/SEX/PREF/TRAIT/BACK/FACTION/GAME_ID/ID` | — | 組敘事卡片、sameGame 過濾、前綴判定 |

⚠ **`IS_PARTY`(26) 鑑賞刻意不讀不寫**；solo 仍用，兩軌並存。

### 🫂 同行制（2026-09 玩家定案）
玩家原話：「我可以指定 AI（類似牽手同行，他必須回應我）跟我一起，其他 AI 也可以出現，但就是很薄的背景板」。

**三層在場**（2026-09）：

| 層 | 誰決定 | 拿到什麼 |
|---|---|---|
| 🫂 **同行**（上限 `KANSHOU_PARTY_MAX_` = 3） | **玩家**按的，AI 動不了 | 完整人格卡＋這回合必定在場。AI 的 `cast.leave` 對他無效 |
| 👋 **臨時在場**（`KANSHOU_ONSTAGE_TAG_`） | **AI** 用 `cast.join`／`cast.leave` 拉進拉出 | 同樣拿完整卡，但隨時可能走 |
| 🌹 **待命** | 這一局召喚過、此刻不在場的人 | 只有名字進 `★【這座城裡還住著】`；玩家問起才由 AI 掰他在做什麼 |
| **常民** | 世界帳本「人物」類 | 名字＋一句話，可出現可開口，不追蹤關係 |
| **路人** | AI 隨手寫 | 卡片與帳本都沒提到的才算路人，不具名 |

**在場＝同行 ∪ 臨時在場**，總數封頂 `KANSHOU_ONSTAGE_MAX_` = 5。

- **唯一判準**：`KANSHOU_PARTY_TAG_`（`【同行】` 存**玩家列** MEMORY、逗號分隔 id）。`kanshouGetParty_` / `kanshouSetParty_` 成對；
  **只認 id**——「鑑賞眾生」是全帳號共用表，名字會撞。
- **AI 那一層的三道邊界**（都在 `actionPlay_`）：①同行者豁免——`cast.leave` 對他無效、也塞不進【在場】②`cast.join` 只認 `kanshouNameCandidates_` 查得到 id 的名字，查無此人整筆忽略（**騙不出新角色**）③在場總數封頂。
- **在場來由**（`pPresenceStr`）只剩一句：時間流轉後仍在身邊。一般回合不講——上一輪的敘事就在 `chatHistory` 裡、人也還在卡上，那句話沒有新資訊。
- **入口**：`kanshou_party`（`actionKanshouParty`，op = `add`/`drop`/`clear`）；前端在 👥 面板每張卡上的 `＋同行／−解散`，
  滿了就 disabled。召喚一個人**還有位子就直接站進來**（`actionKanshouSummonHero` 尾端）。
- **新局從空的開始**：`actionEnterKanshou` 建列時就寫一個空的 `【同行】`。2026-09 起連**起始住民也整組取消**了——
  開場世界上一個人都沒有，全靠玩家自己召喚（第一次進來有一段新手引導教怎麼召）。
- 探針 `party.js`（39 條）＋ `cast.js`（15 條·**兩層都有斷言**：`presentRows` 那個聯集自己也封頂、也無條件包含同行者，所以 cast 那兩道 guard 是第二層防線，只驗看得見的那層會全綠）。
⚠ **死欄`MONEY`(33)/`UPKEEP_WEEK`(34)/`ROOM`(35) 恆空**、鑑賞無讀寫端——**不可刪欄**（COL 是位置索引，刪了後面全錯位）。

### MEMORY 標記全表（工廠 `makeIntTag_`/`makeTextTag_` 在 `Core_Settings.gs`；多標記以全形 `｜` 分隔共用一 cell）

⚠ 這張表**只列鑑賞現在真的在讀寫的**。2026-09 好感／地點／作息三批整組砍除後，
舊表有九個標記的常數全樹已無宣告（【時間流速】【昨夜道別】【主動日】【橋段日】【好感底線】【冷卻日】【醒著陪同】【關係階】【住處】），一併移除。

| 標記 | 常數 | 存哪列 | 做什麼 |
|---|---|---|---|
| 【同行】 | `KANSHOU_PARTY_TAG_` | 玩家列 | **玩家**按的同行名單（逗號分隔 id·上限 `KANSHOU_PARTY_MAX_`=3）。`kanshouGetParty_`／`kanshouSetParty_` 成對。**只認 id**——鑑賞眾生是全帳號共用表，名字會撞 |
| 【在場】 | `KANSHOU_ONSTAGE_TAG_` | 玩家列 | **AI** 用 `cast.join`／`leave` 維護的臨時在場名單。同行者塞不進這一格（兩層刻意不重疊） |
| 【相處】 | `KANSHOU_MET_COUNT_TAG_`（Int） | 同伴列 | 同場回合累計。`kanshouKnownTier_` 依 `KANSHOU_FAMILIAR_TIERS_` 換算「我們之間此刻隔多遠」（四階**物理距離**，不是關係標籤） |
| 【眼中的你】 | `KANSHOU_NOTED_TAG_` | 同伴列 | AI `noticed` 落地（≤`KANSHOU_NOTED_CAP_`=3 條·各≤`KANSHOU_NOTED_LEN_`=14 字·分隔 `KANSHOU_NOTED_SEP_`＝`／`）。只記**會改變之後怎麼對玩家**的發現，`kanshouKnownOfYou_` 讀進在場卡 |
| 【晨間餘韻】 | `KANSHOU_MORNING_AFTER_TAG_` | 玩家列 | 同床隔天的引子，讀一次即清；**依「這回合她在不在場」過濾**，人不在就整條不送 |
| 【夜未眠】 | `KANSHOU_NIGHT_SCENE_TAG_`（Int·absDay） | 玩家列 | 兩段式就寢第一段（時間照流、不結束這一天），第二次按睡覺才真 endDay |
| 【設定已補】 | `KANSHOU_BACKFILL_DONE_TAG_`（Int） | 玩家列 | 創角 AI 擴寫只跑一次的閘 |
| 【英靈源】 | `KANSHOU_SRC_TAG_` | 同伴列 | 這一列是從英靈殿哪一筆種子召來的 |
| 【稱呼鎖】【關係鎖】 | `KANSHOU_LOCKS_` | 同伴列 | 玩家自己打過專屬稱呼／關係稱呼就上鎖，AI 從此不碰。組裝只走 `kanshouRelMemBuild_` 這一支（存 `REL_MEM`） |
| 【住所】 | — | 玩家列 | 家名（`getKanshouHomeName_`）。地點退休後只剩**稱呼**用途，不再是一個去得了的地方 |
| 【帳號】 | — | 同伴列 | 人工檢視辨識用（非驗證，歸屬走帳號表） |
| 【換裝】【小動作】【準則】 | — | 同伴列 | 鑑賞**只讀不寫**（`getOutfit_`／`getPersonaQuirks_`／`getPersonaLogic_`），寫入屬 solo/persona 生態。⚠【小動作】這個儲存鍵現在裝的是**怪癖兩格**（鍵名不改是為了不動既有存檔）；~~【口吻】~~ 已隨語癖退休 |

---

## ⚖️ 事實 vs 規則：提示詞的成分比

2026-09 玩家問「是不是我限制太多，AI 反而沒靈性／種子庫要不要減字」。量過之後：

| | 字數 | 資料（事實） | 規則（教它怎麼寫） |
|---|---|---|---|
| user prompt | 1,667 | 47% | 52% |
| system prompt | 1,700 | 0% | 97% |
| **合計** | **3,367** | **781（23%）** | **2,586（77%）** |

**種子庫不是肥的那一塊**：一位同伴在提示詞裡約佔 145 字（外貌 20＋打扮 8＋性格四格 25＋怪癖 20＋準則 20＋城裡身分 14＋關係與知情度）。
那正是唯一讓角色「是這個人而不是任何人」的部分。**要減字不該從這裡減。**（2026-09 六格改版後的數字；改版前約 110 字，增的是怪癖/準則/喜歡討厭三項，換到的是「AI 知道怎麼演、怎麼做選擇」。）

### 判準（之後加規則前先過這三關）

> 1. 這句告訴 AI **「世界上什麼是真的」** → 留
> 2. 告訴 AI **「你應該怎麼寫」** → 砍，除非它擋的是我們真的踩過、寫在 `CODE_NOTES.md` 裡的坑
> 3. 禁令**能被事實取代時，就用事實取代**——尤其別把要禁的句子本身寫進提示詞（模型會鏡射）

按這個篩，system 十條鐵律裡 7 條是事實或機制（承接玩家語氣、不重置歷史、裝扮是既定事實、JSON 格式…），砍了會壞；
純風格規定只有 2 條半，已拿掉：

- 鐵律 2：`女性角色柔美(柔嫩/雪白/婉約·出力柔中帶勁)；男性角色依其體格氣質寫` → **`每3~4句 <br><br> 分段。`**（分段是 `aiHtml_` 的機制，留）
- 鐵律 7：`女女＝純女女之愛·主導跟隨依個性·動作柔美；男女＝依器官自然互動…` → **`肢體互動依雙方【性別】欄自然呈現。`**
- solo `Router_Persona` 性別配對：`【禁】描寫插入式陽具動作(如「進入她」)，改以手指/舌尖/器物…` → **`★【性別】御主與「X」皆為女性。`**

⚠ **「純女女之愛」那條的根因已經消失**：它存在是因為 AI 會用男性視角寫女女，而那是因為**同伴卡上從來沒有性別欄**——
AI 不是被誤導，是根本沒被告知。卡片補上 `【性別:X】` 之後，同一件事已經講了兩次，技法規定是第三次。
（CODE_NOTES 記的原始意圖「直接算好給 AI、不需要它自己推理」本來就是對的，超出的是後來疊上去的技法清單。）
🔬 **這一條沒有實測背書**：`openrouter.ai` 被環境網路政策擋住，無法 A/B 真實模型。依據是「根因已修、內容冗餘」的推論，
不是量出來的等效。
✅ **2026-09 玩家結案：不追了。** 上線後玩家沒有回報筆觸問題，並明示取消追蹤——
**別再把這條當成「待驗證」重提**，也不要主動回滾。真的覺得不對時再說，退回去各是一行。

### 省下來的還給角色資料

同伴的身世一直存在 `COL.PC.BACK`（種子 `persona.dailyBack`，平均 12.8 字）卻**從來沒送進提示詞**——
凜那條「遠坂家現任當家，妹妹是間桐櫻，感情很好」是正典關係，AI 之前完全不知道。卡片補上 `| 經歷:`，
過濾比照 `servantCard_`（等於「職階・名字」或落在 `QUAD_EMPTY_` 的無資訊量 fallback 不送）。

淨效果：總量 3,413 → 3,367 字（**變少**），其中事實 739 → 781 字（**變多**）。

## ⚧ 性別：支援 男女／女女，不支援男男

`actionKanshouSummonHero` 明文擋掉 男玩家×男同伴（「僅支援 男女／女女 配對」）。
種子英靈 25 人裡 **12 男 12 女 1 無**（恩奇都是「無」，但在 `KANSHOU_SUMMON_BLOCKED_IDS_` 裡召不到）。
男性不會被 `actionEnterKanshou` 自動鋪墊進世界，但**玩家可以主動召喚**——所以 **女玩家×男同伴是完全可達的正常玩法**。

### 2026-09 稽核：整個提示詞層預設「御主是男、同伴是女」

實測 女玩家『薰』× 男同伴『庫·丘林』，送出的提示詞裡 **「她」×16、「他」×5，而那 5 個「他」全指向女性玩家**——兩邊同時指錯人，
最誇張的是同一句裡兩個代名詞各指錯一個人：

> ★【視角鎖定】：「我」＝玩家『薰』本人，只演**他**實際輸入的動作與五感……同伴外貌只取材**她**自己那份資料

**而且 solo 有同一個形狀、方向相反**：`Router_Persona`/`Router_Battle` 泛指御主一律寫「他」，但種子御主 15 人裡有 3 位女性（凜／伊莉雅／櫻），
玩家自己的御主性別也是創角時選的。所以這不是鑑賞的局部問題，是**全層的預設值錯誤**。

**根源不是措辭，是資料**：玩家卡有 `【性別:X】`，**同伴卡沒有這一欄**。AI 從來拿不到同伴性別，只能從規則裡那個預設的「她」去猜。

修法（由根到表，全域一次掃完 134 處）：
1. **同伴卡補上 `【性別:X】`** ← 真正的根源修正，AI 終於拿得到這格
2. **`pron_(sex)` 收進 `Core_Settings.gs`**（兩軌共用的單一真實來源，男→他／女→她／其餘→TA）。
   角色的 row 在手上就 `pron_(row[COL.PC.SEX])`，不在提示詞裡寫死
3. `nsfwBaseRules` 把「預設對方是女性」的措辭改成**標明適用對象**（鐵律 2「女性角色柔美…男性角色依其體格氣質」、鐵律 7「配對依雙方【性別】欄」）。
   **不隨在場者變動**，所以 system prompt 仍是穩定的可快取前綴（1739→1791 字，+52，一次性）
4. 泛指的「她／他」全部中性化（對方／那個人／要走的人）：視角鎖定、角色一致性、角色卡抬頭、收尾、獨處時光、在場來由 ×5、現況、
   ~~`KANSHOU_RAPPORT_TONE_`~~（已移除）全 11 條事實句、事件/橋段/撲空字串、三支日常化轉譯提示詞，以及 solo 的 `servantCard_`/`masterCard_`/`enemyMasterCard_`/戰報/盟誼
5. **`check_pronoun.py` 進 check.sh**（第七支）——這才是根源解，不然下次又會長回來

⚠ **在場來由那 5 句必須是中性字面，不能按人塞代名詞**——它們同時是「在場來由人人相同就抽成抬頭」的去重 key（`_presenceSeen_`），
一旦帶性別，混性別在場時去重就失效、每張卡又各自重複一遍。

驗收（兩個方向）：

| 情境 | user「她」 | user「他」 | system |
|---|---|---|---|
| 女玩家 × 男同伴 | 2（玩家本人·正確） | 0 | 0／0 |
| 男玩家 × 女同伴 | 0 | 2（玩家本人·正確） | 0／0 |

> 🧭 **泛指的「她」錯兩次，不只一次**：性別錯，而且**人數也錯**——同時有 2~3 位同伴在場時，單數的「她」本來就不對。
> 中性化是一次修掉兩個問題，所以這不是為了少數配對犧牲主線，主線本來也是錯的。
>
> 🐛 **我自己在這一輪踩的兩個坑**：①一開始是「探針抓到幾個就修幾個」（16→5→3→0），那是疊加不是根源解——
> 玩家要求整體掃描後才發現真實範圍是 134 處、而且 solo 也有。②批次替換時寫了 `sv[COL.PC.SEX]`，但那個作用域裡根本沒有 `sv`，
> **`check.sh` 只驗語法抓不到未定義變數**——批次改完一定要用探針真的把那條路徑跑起來。

## 🧠 AI 記得多久

分兩層，合起來才是「她記得你」：

| 層 | 內容 | 深度 | 重量 |
|---|---|---|---|
| `chatHistory` | 玩家輸入＋AI narration **全文** | `_histWindow_`＝6 則（3 輪）；剛換場景/剛跳時間砍到 2 則 | 約 2,300 字，佔整份 payload 四成 |
| ★【再往前的經過】 | 掉出窗口的較早回合，**只取玩家做過的事** | ~~`KANSHOU_DIGEST_ROUNDS_`~~（已移除）＝8 輪、每則 `KANSHOU_DIGEST_CAP_`＝22 字 | 約 145 字 |

~~那支摘要函式~~（2026-09 已移除）從同一張「歷史暫存」表往回多讀，扣掉已經進 chatHistory 的那幾則，
把更早的**玩家側輸入**串成一行。**不收 AI 的 narration**——那壓不了，也不該由 GAS 改寫別人寫的字。
兩者共用同一個 `_histWindow_`，不各算各的（各算各的就會重疊或漏接）。

⚠ 這一層補的是「純聊天的內容 3 輪後徹底蒸發」：MEMOIR 這個長期管道**要有事件發生才會寫**，
日常閒聊一個都不蓋戳。實測連打 8 回合日常，那四格全空。
## ⏰ 時間系統

- **每動作固定推進 10 分鐘**：~~`kanshouHourPerAction_()`~~（2026-09 已內聯進 `actionPlay_`）＝`KANSHOU_MIN_PER_TURN_/60`。（2026-09 玩家把流速旋鈕砍了——一個回合本來就不是一段固定的時間，交給玩家調只是把「說不準」變成一個要設定的東西。）
- **五時段** `KANSHOU_TIME_BANDS_`：清晨(5)／午後(11)／黃昏(17)／夜晚(20)／深夜(0)。
- **當日不跨日上限** `KANSHOU_DAY_LAST_HOUR_ = 23`；被動聊天每回合 `+1/6` 小時流動。
- **曆法**：Day1 = 12/20（`KANSHOU_CAL_START_MONTH_/DAY_`），前端顯示西曆年月日。
- **主動跳時間**：`kanshouNextStage`(下一時段·深夜則睡過夜)／`kanshouJumpBand`(跳指定時段)／`kanshouEndDay`(結束一天)。⚠ **跳時間不再重骰任何人的去向**——地點退休後沒有「去向」這種東西了，在場名單只由【同行】與【在場】決定，跳完時間人還是那些人。
- **`endDay` 語義＝「睡到即將到來的 6:00」**（2026-07 玩家實測跨2天根修）：`curHour >= 6` 才 `+1 day`；深夜 00:00~05:59 按結束一天＝睡到**當天** 6:00、不再多跳一天（夜→深夜本身已 +1 day，endDay 再 +1 就是雙倍扣）。前端 `_reHourAfter` 樂觀預測同步這套規則（endDay→6／jumpBand→時段起點／advanceHours→mod24）。
- 🗑️ **跨時段自然告辭（`kanshouBandCrossed_`／`kanshouNpcLeaveStr_`）2026-09 隨地點整組退休**。「誰會自己走掉」現在由 AI 的 `cast.leave` 決定，同行者豁免——⚠ 這剛好解掉當初砍 `npc_exit` 的理由（「AI 不問玩家就把人移出同行名單」）：現在 AI 動得了的只有【在場】那一層。
## 📖 共同回憶（memoir）

- **存 `COL.PC.MEMOIR`(27)**，每位同伴一份。
- ⚙ **2026-09 起 `processMemoir_` 只是一層包裝**：append/去重/上限的本體抽成頂層 `kanshouAppendUnique_(old, line, {sep, cap, maxLen, pin})`，與「她眼中的你」共用同一份（去重含 bigram 相似度比對，複製一份出去必然走樣）。
- **AI 每回合吐 `memory` 欄**（里程碑一句、≤30字、**2026-09 起同 narration 用第二人稱「你」稱玩家**），`processMemoir_` append：**cap 10**，去重（比對忽略★前綴），**★釘選永不驅逐但釘選本身另有上限8**（`actionKanshouMemoirOp` 已釘滿8條會拒絕新釘選，留2格給未來新回憶自然淘汰）。
- **玩家 UI 管理**：`actionKanshouMemoirOp`（pin/unpin/del，帳號綁定，後端只清 `｜【】[]★` 幾個分隔符、要求傳入文字與儲存原文逐字相符；2026-07 全面重構收尾：找目標同伴列改委派共用 `findPcRowIdx_`，取代原本手刻迴圈，行為不變）。前端 `kanshouOpenMemoir`/`kanshouMemoirOp`（`_kmBusy` 讀條保護擋連點）；同伴卡片 **💞回憶** 按鈕。⚠ **2026-07 稽核修**：前端組 onclick 參數時舊版用 `.replace(/'/g,'')` 把回憶原文的撇號整個剝掉才送出，跟後端「逐字相符」的比對規則衝突——含撇號的回憶（AI 自由生成的文字不保證不含）因此永遠釘選/刪除失敗；改成正確的 JS 字串跳脫（`\'`）而非刪字。同批也補上操作進行中點背景關閉會被 `finally` 重新打開視窗的遺漏 `_kmBusy` 判斷。
- 餵 AI 時★前綴會剝除。
## 🖥️ 同伴卡片按鈕（現況）

**兩個地方各有一套，別搞混：**

| 在哪 | 有哪幾顆 |
|---|---|
| 左欄**從者卡**（`Script.html`）——這一局的**全部住民**都有一張（`buildTagsPayload_`），同行的另標 `party` | `📜 狀態`（`openStatus`）｜`💞 關係`（`kanshouOpenBondHub`·分派面板，裡面才是關係稱呼／專屬稱呼／共同回憶）｜`🫂 同行／解散`（`kanshouPartyOp`） |
| 👥 **同伴面板**（`Script_Kanshou.html`） | `＋同行／−解散`｜`💞回憶`｜`🏷️關係`｜`🚪`(請她離開這座城·不可復原) |

`💞 關係` 刻意做成**有摘要的分派面板**而非把三頁硬合成一頁——互動型態差很多（選標籤／管清單），硬合只會變成更長的捲動頁；分派面板既省卡片空間，又能一眼看到目前的關係稱呼與專屬稱呼。

⚠ 從者卡**不露**血量／魔力／六圍／技能／寶具／動作鈕——那些是 solo 的東西。


## ⚠️ 種子改版必升 `CODEX_PERSONA_VER`（2026-07 複查抓到·差點整批白做）

`seedFateCodex_` 的升級閘是 **`props.codex_persona_ver !== CODEX_PERSONA_VER`**。線上 Script Property 已經是舊版號時，改再多種子 `upgradeCodexPersonas_` 都不會跑——**只有全新試算表吃得到**，既有存檔完全 no-op。

**改 `daily*`／`persona` 一定要順手把 `CODEX_PERSONA_VER` 升一版**（`gas/Seed_Codex.gs`）。忘了升版不會有任何錯誤訊息，只會靜靜地什麼都沒發生。

同批補上的第二個缺口：`resyncSummonedServants_` 的「鑑賞眾生」分支原本只刷 **BACK／TRAIT／MEMORY【口吻】** 三樣，漏掉 **PREF（`dailyWords`）與 ~~INTENT（`dailyMoe`）~~**（萌點 2026-09 整組退休，INTENT 欄恆空；【口吻】也已隨語癖退休——這段是 2026-07 的紀錄，現在 resync 刷的是 BACK／TRAIT／PREF）——但 PREF 一樣會被寫進在場卡（`formatPref` 的 [表象][內裡]）。結果是種子性格/萌點改版後，**已召喚的同伴永遠停在舊文字**，只有之後新召喚的才吃得到。已補齊。

`actionDevResyncCodex`（DEV 按鈕）可無視版本閘強制重刷，但那是救援手段，不是正常路徑。
## 🧭 帳本引擎正名成兩軌共用（2026-09）

帳本從一開始就是通用的（只認 `game_id`），solo 要用它記「這一局的因果」時，`kanshou*` 前綴就在說謊了。

- 分頁 `鑑賞世界` → **`世界帳本`**（`worldSheet_` 就地 `setName`，資料一列不動；另開新分頁會讓既有的鑑賞世界整個不見）。
- 函式 `kanshouWorld*` → `world*`（`worldSheet_`／`worldRow_`／`worldRead_`／`worldBust_`／`worldSame_`／
  `worldWrite_`／`worldSet_`／`worldDrop_`／`worldEvictees_`／`worldFeed_`／`worldPayload_`／`actionWorld`）。
  **仍然是鑑賞的**那幾支（~~`kanshouLocationsFor_`~~（已移除）／~~`kanshouRegionsFor_`~~（已移除）／`kanshouWorldRosterStr`）名字沒動。
- 規格搬進 `WORLD_SPEC_` **逐軌登記**：`sheet`／`kinds`／`cap`／`feedMax`／`atMax`／`writeMax`／`textMax`。
  判準是 `worldTrack_(gameId)`——`g_` 開頭是 solo，其餘是鑑賞。加一軌＝往表加一列，引擎自動吃。
- 前端不再手抄字數上限（`KC_WORLD_TEXT_MAX_` 已刪），改讀後端下傳的 `worldTextMax`。
## 🤖 AI 管線

### `actionPlay` 執行階段順序（**順序鐵則·勿亂動**）
0. **`actionPlay` 現在是一層薄包裝**（2026-07 稽核補）：真正的引擎邏輯搬進 `actionPlay_`（下面1~12步都在這支裡）。外層 `actionPlay` 只做「同 pcId 併發軟鎖」——見下方教訓區「play 故意豁免全域鎖」。
1. 入口守門（非 `KPC_` return）＋`driveOn` 旗標＋標籤化
2. 讀表（整表只讀一次）→ 定位 pc、curL/curDay/curHour
3. UI 按鈕意圖落地（同去提議/橋段 offer+accept）
5. **跳時間/advanceHours**（重骰全世界去向、換幕鐵律）
6.5 **「她主動」三型擲骰（visit/invite/want）** ← ⚠ **必須在第7步之前**（2026-07 矛盾掃描抓到）：`visit` 型會把她的 LOC 搬到你這裡，排在 partyRows 之後的話她沒有在場人物卡，AI 同時收到「她剛剛出現在這裡」與「只有【在場人物】可以開口」兩條打架的指令。這段需要的「誰此刻在你面前」是自己從 pcData 的 LOC 算的（`_hereRows`），不依賴 partyRows 這個變數。
7. **partyRows/partyMembers 組裝**（同行名單 → 詳情卡 `partyDetailsArr`；順手把同行者 LOC 同步成玩家的）
9. 提示詞組裝（USER prompt，見下）
10. `aiConfig` → 歷史餵入 → `callGeminiAPI`

### 系統提示詞 `buildDefaultSystemPrompt()`（**鑑賞專屬**，solo 走 `miniSystem`）
`nsfwBaseRules`（⚠ **Gallery 函式內自己那份·非 Engine_Combat.gs 紅線·可改**）＋`★【輸出範本】`(finalJson)。`specificRules` 現為**空字串**（舊「慾海律令」5+1 條已整併進 nsfwBaseRules，見下方「specificRules 改成空字串」；return 形狀不動）。
⚠ **2026-07 玩家「在場人物放最後面·歷史只是歷史」USER prompt 結構調整**：`PROMPT_PARTY_STABLE`(【目前在場人物命格詳情】·partyDetailsArr組裝)原本緊接在最開頭【敘事法旨】之後，改搬到整段USER prompt的**最尾端**(緊接在「現在演化玩家動作」之前)，並在前面補一句「★【歷史僅供參考·專注本回合】：對話歷史只是脈絡背景…別被歷史裡已經過期的設定/情緒/場景牽著走」——用「放在提示詞最後面」利用模型對鄰近生成點內容的權重(recency)，讓最關鍵的「現在到底誰在場/該依據什麼」在AI真正下筆前最後被讀到。中段既有的「★【在場驗證·最高優先】」行為規則沒動，這次調整的是**資料**(誰在場的實際名單)擺放位置，規則跟資料現在分離在提示詞前後兩端，彼此不衝突。（原本資料後面還補了一句「★【現場僅此名單】」，2026-07 玩家「提示詞會不會太多、有沒有重複」稽核時發現這句夾在【在場驗證·最高優先】canonical版跟真正收尾前的「★【動筆前最後確認】」之間，三次講同一件事、位置上兩邊都不是純屬贅字，已刪除，不影響任何護欄完整性。）
⚠ **2026-07 玩家「提示詞太大量」全面壓縮**（核心訴求「AI 只依資料扮演」）：System＋USER＋driveStr＋schema 逐欄全部壓成最短句，冗字/重複/客套/最高級通膨砍到見骨——**但機械護欄（在場驗證/親密尺度五階/移動鐵律/冠名格式/角色一致性/世界觀）一條不刪、只縮字**。實際送 AI 文字砍約 35~45%。加規則前先想「這是機械護欄還是冗字」，冗字不進提示詞。
⚠ **2026-07 玩家「我一直想要做減法」三次瘦身**（USER prompt **3069 → 2658 字，−13%**，規則一條沒少）。
先寫回歸網再動刀：`scratchpad/sim/dedup.js` 釘住 **24 條規則「必須還在講的那件事」**（用關鍵詞交集判定、容得下改寫）
＋**重複偵測**（同一件事出現次數的上限）。合併七處：
1. **好感天花板講了三次、而且互相矛盾** ← 最嚴重的一處。`driveStr`【主動掌握】①用的是「<40／高好感／80+」，
   跟【親密尺度五階】的 20/40/60/80 **對不上**；④⑤⑥ 又各複述一次「受天花板約束／未達依個性」。
   改成 driveStr 只講【怎麼寫】、門檻一律指回五階（那才是唯一真實來源）。順手修掉「冷傲冷傲地」錯字。
2. **【視角鎖定】→ 併進【視角鎖定·不替玩家腦補】**（同一份提示詞裡「我＝玩家」講了三次）。
3. **【歷史僅供參考】→ 併進【在場驗證】**。
4. **【移動鐵律】→ 併進【地點釘死】**，刻意併到**尾端**那條（緊貼「現在演化玩家動作」，recency 位置比中段強）。
   再壓成一句，並把「路程略過不演」改成**只在 `moveTarget` 為真的回合才送**——沒移動的回合根本沒有路程可演。
5. **【在場驗證】砍半**（玩家：「說太多了·只要專注在現在給AI的資料就好」）。⚠ 順手修掉一個**真矛盾**：
   舊版在這裡開了「例外①玩家引入第三人②系統豁免段」，但尾端的【動筆前最後確認】是**無例外的絕對句**
   （「其他名字即使歷史提過也不准出現」）——兩條打架，而位置靠後又點名在場者的那條一直在贏。例外既然沒生效就別再寫。
6. **「沒給的就沒有」散在三處 → 一條【沒寫的就不存在】**（世界觀的「來歷只能取材自資料」＋【不憑空生東西】＋路人條款）。
7. **【角色一致性】刪掉「禁止兩個人用同一套反應」**（玩家：「說了只有反效果」）——正面句「一律依她自己的個性演」
   已經把話講完，再補一句否定只是把「同一套反應」送進模型的注意力裡。
**第二波（同日·玩家「肯定還有可以刪減的地方」→ 2658 → 2484 字，累計 −19%）**：
8. **【在場驗證】vs【動筆前最後確認】真的是重複**（玩家：「這不是重複嗎...?」——我先前答「刻意的 recency 錨點」
   只對一半）。實際不重複的只有三樣：①路人條款 ②缺席者要交代去向 ③**尾端那份實際名單**。
   「只有在場人物能開口」這句確實講了兩次，而尾端那條**帶名字、位置又更好**，是嚴格更強的版本
   ——所以 canonical 移到尾端，前面那條只留它獨有的兩件事，改名【路人與缺席者】。
9. **【視角鎖定·不替玩家腦補】161→~120**：刪掉三個同義反覆的括號補述（「不誤寫成他人心境」被「我＝玩家」涵蓋、
   「禁挪用玩家特徵」被「只取材她自己那份資料」涵蓋、「旁白不得套用」被「只用在引號內台詞」涵蓋）。
10. **【命令/強迫】146→~120**、**時鐘 123→~95**（三句「以此刻為準」收成一句）、
    **【稱呼】77→~55**（「僅narration/台詞·JSON欄仍填真名」是 schema 自己的事；「非清單所有人都要出聲」語意不清，改寫成「名單上的人這一回合都要有戲」）。
11. **【角色一致性】163→~100**（玩家直接給了改法）：刪掉「(高傲咬牙不示弱/虔敬掙扎/活潑藏羞)」
    ——那正是同一批剛砍掉的**模板**，三個例子會把所有角色往同一個方向拉。
    ⚠ 但兩處**刻意留下**：①「生理反應可以有…不退化成發情機器」是 NSFW 的**雙向**平衡，只留「人格不崩」
    會被讀成「保持矜持」；②「既定事實、**不是演法**」的對比是上一批的核心，拿掉對比只剩半句。

12. **📍「現在演化玩家動作」移到整份提示詞的最後一行**（玩家：「這個組裝在最後面!」）。
    它原本被【稱呼】【收尾】【動筆前最後確認】三塊壓在後面——但**約束是背景、動作才是題目**，
    題目該貼著生成點。新順序：…資料 →【地點釘死】→【稱呼】→【收尾】→【動筆前最後確認】→ **玩家動作**。
    ⚠ 這也讓「在場名單」緊貼在動作前一行，recency 錨點的效果反而更好（先框住人，再給題目）。

13. **【主動掌握】改寫（玩家給的版本）146→127，同時是一次去重＋一次行為改動**：
    - 去重：「同伴主動推進至實際發生、拒絕空轉」跟尾端 🚨【收尾·主動掌握】的「大幅推進到位，該發生就發生，
      別在曖昧邊緣空轉」是**同一句話**，尾端那條位置更好 → 這裡不再重複。「禁霸道模板」也刪（正面句
      「依她自己的個性主動」已涵蓋）。
    - ⚠ **行為改動（不是壓縮）**：色度由「**跟隨**玩家」改成「**可以走在玩家前面**」，並加上 Dirty Talk／直白不迴避。
      仍受【親密尺度五階】天花板夾住（那是唯一的准不准線），且只在 `driveOn=true`（玩家自己按下主動掌握）
      才組進提示詞——平時矜持模式拿不到這一段。
    - 「不對玩家造成真實傷害」**刻意保留**：【命令/強迫】那條保護的是 NPC，這條是全提示詞裡唯一保護玩家角色的。
    🐛 這次順手抓到 `dedup.js` 一個**假通過**：「既定事實交棒句」原本只查「依她自己的個性」，
    而【主動掌握】剛好也有這幾個字 → 它一度是靠**錯的區塊**在通過。改成連「不是演法」這個對比一起查
    （那句只存在於 canonical 出處【角色一致性】）。**回歸網自己也會有覆蓋漏洞，這是第二次踩到同一種形狀。**

**第三波（同日·2481 → 2299 字，累計 3069 → 2299，−25%）**：
14. **【後日談模式·最高優先覆寫】72→37**：後半段「各自熟到什麼程度依相處基調演」已被【角色一致性】
    的交棒句涵蓋（那才是 canonical），刪。**前半段刻意留下**：模型對 Fate 這幾個名字有很強的先驗
    （她們在原作裡是打過聖杯戰爭的舊識），【沒寫的就不存在】這種泛用句壓不過**專有名詞的先驗**，
    得指名否認才擋得住。
15. **NSFW 記憶區 `nsfwMemories` 砍掉三種浪費**（玩家：「【在場人物】這是不是多餘?」）：
    - **裝扮講了兩次**：`[X 裝扮]` 一份、【角色背景資料】的「裝扮:」又一份，同一個值。刪前者。
      「五官/髮色/體態不隨換裝改變」原本跟著每個人重複 N+1 次 → 掛在名單抬頭講**一次**。
      玩家自己的裝扮同理（【玩家資料】那行本來就有；舊註解「不能只在【玩家命格】看得到」是排版
      還很長的時代留下的補償，現在那行就在提示詞第二行）。
    - **`[快照]：[羈絆]…` 整條刪**：REL_MEM 現在只裝【專屬稱呼】，而那個已經由 `pMemStr` 帶進
      【角色背景資料】。⚠ 更關鍵的是種子預設值「初次相遇，緣分才剛開始」會跟好感 70 的相處基調
      「她喜歡你卻說不出口」**直接打架**——這不只是冗字，是會讓 AI 演錯的**假事實**。
    - **`[X 肉體]:{"狀態":"如常"}` 預設值不送**（`_isPlainBody_`）：零資訊量卻每人一行。
16. **正名**（玩家：「改成角色背景資料?」）：【目前在場人物命格詳情】→**【角色背景資料】**、
    【玩家命格】→**【玩家資料】**。「命格」是九州武俠留下來的詞，跟「和平現代城鎮」的世界觀對不上。

17. **【命令/強迫】129→90**（玩家：「目前分段有做好·這段也是多餘的」）：五階本體已經逐階寫明
    「未達門檻會怎樣」，這裡只留兩件**推不出來**的事——①命令/強迫**不是繞過門檻的捷徑**
    ②禁演成真實傷害。⚠ 第②件刻意留：那是全提示詞裡唯一保護 **NPC 身體**的一句
    （【主動掌握】那句保護的是**玩家**，兩者不重複）。
18. **【在場來由】整條刪**（玩家：「不用再說明了吧?」）：每個人自己那一欄現在是**自我說明**的
    （「【你們從剛才就一直在這裡】相處著——她早已在場，這一刻是延續，不是重新登場」），
    這個 ★ 只是在說「請照那一欄演」＝用一句話介紹一句話。
    🧭 **新判準：欄位本身就講清楚的事，不必再開一條規則去介紹它。**
    （MIN_STAR_BLOCKS 124→123；改 ★ 標題時同步改了 `check_prompt.py` 的 ALLOW key，那個坑今天踩過一次。）
**累計：3069 → 2244 字（−27%）。**

**第五波·清醒度（玩家：「我不是要省字數·我是要 AI 更清醒！不做無聊的禁止——他本來就不太會 OXOX，一說他就有可能會 OXOX」）2244 → 2015 字**
判準從「短」換成 **①只講事實 ②negative 只在模型真的會犯時才留、能用正面句就用正面句 ③同一件事只講一次**。
- 🐛 **抓到真矛盾**：時鐘寫「本回合只寫這**十分鐘**」、收尾卻寫「時間刻度不超出這**一分鐘**」——差十倍，
  兩條都是硬指令。統一成十分鐘。
- 🐛 **五階裡藏著演法模板**：`<20` 那格寫「(閃避/擋手/喝止/還手依個性)」——四個具體動作等於所有角色
  用同一套反應，正是同一天砍掉的東西。全部改成只講「到哪裡為止」，怎麼擋交給個性。
- **刪掉「他本來就不會做」的禁令**（講了反而把概念放進去）：【主動掌握】的「不對玩家造成真實傷害」
  （她主動示好時傷害玩家不是模型的失敗模式；真實傷害統一由【強迫】那條講一次、涵蓋雙方）。
- **負面改正面**：「不退化成發情機器」→「她說話做事仍然照她的性格」；「不自創第三種叫法」→「就這兩種叫法」；
  「禁大段內心戲/禁替他決定」→ 刪（「只演他實際輸入的動作與五感」已經涵蓋）；「別裝作還在」→「用一句話交代去向」。
- **刪掉「教她怎麼用」的話，只留事實**（玩家：「★【今日天氣】：雪後轉晴 這樣就好」）：
  天氣後面的「自然滲入場景不必每句提」、時鐘的「歷史停在哪個時段都不算數」、世界觀的「調性不限悠閒」、
  篇幅的「不必精確別落差太大／低好感別寫大段內心戲」（字數本來就依好感自動調 250/400/500）。
- **刪掉第三次講的東西**：【敘事法旨】（「我＝玩家」第三次）、五階標題的「(人格不崩)」、
  【演出而非說明】的「數值只輸出 rel_changes」（schema 自己每回合都在講）。
- ⚠ **世界觀那句刻意留七個字**（玩家版是「和平現代城鎮，大家都是生活在這冬木市」）：
  改寫後仍保留「沒有魔術與從者」——因為這句剛好**指名了冬木市**，那是聖杯戰爭的舞台，
  光講地名會**加強** Fate 先驗而不是壓下去。這是「世界事實」不是「禁令」。
- 🐛 我改寫時引進兩個**沒有指涉對象的「她」**（那兩塊講的是任一在場者），`check_prompt.py` 當場抓到並擋住 push。
  **這正是那支掃描器存在的理由。**

**第六波（同日·2015 → 1860 字，累計 3069 → 1860，−39%）**——玩家一句話講完了整個方向：**「寫再多它會歪」**。
- **💕【後日談】整條刪**（玩家：「這個不能砍嗎」——可以，而且是**今天自己的改動讓它變多餘的**）：
  ①同一份提示詞現在寫著「和平的現代冬木市…**沒有魔術與從者**」＝聖杯戰爭沒發生過，「沒有戰前舊識」是它的推論；
  ②**相處基調**（今天新增）逐人講明「你們到底多熟」（好感 5＝「你對她而言是陌生人」），比一句全體通用的
  否定精準得多——那一句本來就是基調表還不存在時的 1D 替代品；③空場那一支也重複（`PROMPT_PARTY_STABLE`
  已寫「這個地點沒有其他人」，【動筆前最後確認】再講一次）。
- **★★【強迫也照上面那張表判】整條刪**（玩家：「理論上好感可以控制了」）：五階現在寫的是
  「**超過的那一步不會發生**」，那是對【任何嘗試】講的，命令/強迫本來就包含在內——**指名它反而
  把那些字送進模型的注意力**。
  ⚠ **有意識的取捨**：唯一沒被涵蓋的是「不寫成真實傷害(流血/骨折/撕裂傷)」，全提示詞現在沒有別處在講。
  日後若敘事真的出現受傷描寫，**補回來的是那一句、不是整段**。

**第七波·系統提示詞（每回合不變的那半份）2379 → 2097 字**（玩家：「那每次給系統不變動的…那邊是不是也很多可以砍?」）
🚨 **先確認可動範圍**：系統提示詞＝`nsfwBaseRules`(623 字·**紅線①·一個字都不可改**)＋【慾海律令】(481·可改)
＋★【輸出範本】schema(1275·可改·最大塊)。本波只動後兩者，`git diff` 確認紅線常數本體 0 改動。
- **律令7 砍掉跟 schema 重複的前半**（兩處都在講「有變化才填、否則留空」），只留它獨有的
  「玩家指定的裝扮＝既定事實」。
- **律令6（options 出題規則）搬進 schema 的 options 欄位本身**——規則貼著欄位，不必兩處對照。
- **三個 `_note` key 全部拿掉**：它們長得就像欄位，模型很可能照抄回來——那是**輸出 token**（比輸入貴），
  而我們一個字都不讀。內容併進欄位自己的說明字串。
- **`fav_change` 拿掉「單回合上限+5」**：`actionPlay_` 早就 `Math.max(-5, Math.min(5, …))` 硬夾。
  照「GAS 掌數值、AI 只說書」——告訴它一條違反也沒用的規則，只是多給它一個可以鑽的縫。
- 🐛 **我自己做壞又當場修掉的兩個**：①刪掉律令 6 之後**編號跳號**(1,2,3,4,5,7)，小模型會以為漏了一條；
  ②options 的說明我一度塞成陣列的**第 5 個元素**——`sanitizeAiData_` 是 `slice(0,6)`，五條會照單全收，
  **前端真的會長出第 5 顆按鈕**。說明併回第 1 條字串裡，陣列維持剛好 4 個。
- ⚠️ **`attitude` 是「沒用卻不能刪」的唯一一處**：今天已證明它是自我強化迴圈、拿掉了落地與餵回，
  grep 全檔沒有任何地方讀它——但**紅線 `nsfwBaseRules` 第 5 條指名了這個欄位**，刪欄位會讓紅線文字
  指向不存在的東西。欄位與律令 5 只能留著空轉（約 60 字）。**要處理只有一條路：玩家明確授權動紅線那一句。**

## ⚠️ 教訓區（防回歸警語·只留警告不留故事）

- **COL 是位置索引**：`MEMOIR`(27)/`MONEY`(33)/`UPKEEP_WEEK`(34)/`ROOM`(35) 就算恆空也**不刪欄**——刪掉後面全部欄位錯位、砸爛既有試算表每列對齊。要棄用就標「死欄」，不刪。
- **`grep -c nsfwBaseRules` 假陽性**：顯示非0常是 `return nsfwBaseRules + ...` 那行被帶進 diff context，`+`/`-` 兩側都沒該字樣＝本體未動。仍須自己判斷是否碰到紅線①保護的機制範疇。
- **`physical_state` slice(0,20)** 是刻意的容錯緩衝，**別對齊文件改回15**（會半句截斷「臉頰帶著因尷尬而生的」）。
- **敘事終極警告**：被玩家搭話的 NPC **必須先給出她此刻的回應**才停筆，別只寫完玩家動作就收尾（「她不理我」的根因）。
- **好感突破改「約會路徑」**：送禮突破已廢；橋段/約定提示詞尾會 append「好感已由系統上調·敘事勿再另計」防 AI 重複計。
- **查洩漏查兩份鏡像**：同伴側 vs 玩家自己側、AI生成內容 vs AI失敗的降級保底值，兩層都要查。
- **平行世界原則**：`persona.back`/`look`/`words` 的**戰時值別照搬進鑑賞**（後日談無聖杯戰爭）；`quirks`/`logic` 是刻意寫成兩軌通用的日常向，兩邊共用同一欄。
- **onclick 字串內的呼叫不算死碼**；`removeAllTriggers()` 零呼叫是 GAS 工具正常型態、非死碼。
- **HTML 刪除先手算 div 開合平衡**再刪，避免刪頭忘刪尾崩整頁。
- **泡泡(必點UI)別跟可關閉的東西同住一個容器**：「AI選項開關」曾整個 `options-container` display:none，所有泡泡(前往/邀約/同居/敲門/橋段/結識)陪葬（那些泡泡多數已在 2026-09 大瘦身砍掉，教訓仍成立）——關掉選項的玩家**從沒見過任何泡泡**。現制：【命運的抉擇】包在 `#ai-options-grid` 小盒、開關只藏它；容器本身恆 flex(載入時強制恢復)。新泡泡一律放容器直下、別放進 grid。
- **同一格欄位有兩條寫入路徑時，消毒要抽成一支**：REL_MEM 的 `[專屬稱呼]` 有「玩家手動」與「AI `mutual_nicknames`」兩條路，舊版只有手動那條消毒——AI 回 `"小可愛| [稱呼鎖]是"` 就能偽造出稱呼鎖。現在兩條都只走 `sanitizeNickname_`。
- **前端渲染的順序＝資料就位的順序**：`updateClock` 讀 `kcClock`(判深夜) 與 `window._lastTags.nightScene`(判夜未眠) 決定那顆最常按的鈕寫哪個字；`send()` 與 `applyClientState` 兩處都曾把它排在這兩份資料**之前**，按鈕永遠慢一回合——按下🌙睡覺、夜未眠成立了鈕上卻沒變，玩家再按一次就真的睡掉。**任何讀 tags 的渲染，tags 必須先就位。**
- **永遠不會成功的按鈕就不要長出來**：私人房間(`isRoom`)在後端是 region `'room'`、相約/邀同去一律不受理，前端卻照樣給 👋。比照地圖上鎖住的地點：不給、或給灰底🔒＋說明，別讓玩家按了才被拒。
- **前端手抄的後端表會走鐘**：前端所有 `KC_*` 鏡射常數全是手動同步。表一走鐘 UI 就會說謊（例：關係面板讓好感30點『戀人』）。scratchpad 有 `mirror.js` 逐項比對，改任一邊記得跑。
- **群體動作要點名全部人**：👋 邀同去按下「同意」時 `moveWithCompanion` 帶走**同地全部人**，舊版提示詞只點名第一位——其餘的人是無聲跟來的。點名誰就走誰（裁定取好感最低的那位）。
- **別依賴小模型「自發」填選填欄位**：Gemini-lite 從不自發填自由選填欄位(舊 move_proposal 就是活教訓，2026-07 乾脆整欄砍掉)——玩家發起的機制動作一律走「明確 payload → pre-AI 記提議 → 確定性管線」(👋proposeMove/相約同款)，別再指望 AI 自己決定「要不要」。
- **🙂 `physical_state`（狀態欄）2026-07-28 改標點收尾**：schema 要 ≤15 字、後端截 20 當緩衝，但 AI 把整句敘事寫進來時照樣硬剪出斷尾（玩家實測「狀態：原本專注看書的動作停下，抬頭望向風音，眼」——正好 20 字）。改成**在預算內的最後一個標點處收尾**（沒有標點才退回硬剪），並把欄位定義從「顏面神情」改寫成「只寫臉上看得到的(眼神/臉色/表情)，不寫動作與劇情」。探針 `scratchpad/sim/pstate.js`。
- 🗑️ **玩家提議（相約/同去）＝GAS 依好感擲、AI 只演**（2026-07）：整套已隨好感與地點退休移除（~~`kanshouProposalAccepts_`~~／~~`kanshouPromiseOffer_`~~／~~`kanshouCohabitOffer_`~~／`proposal_accept` schema 欄）。**留著的教訓在上一條**：玩家發起的機制動作走確定性管線，別指望小模型自發填選填欄位。
- **通用錯誤文案是查案毒藥**：send()/saveFate 的 catch 已帶出 e.message(【原因】行)；後端 success:false 的 message 會演進故事流。別再寫吞掉真因的 alert。
- **獨立事件別共用單一回饋槽**：赴約/爽約「結算」與相約/牽手/同去「提議結果」是兩條獨立事件流，舊版共用 `proposalResult` 單槽＝並發時後寫的吞掉先寫的(爽約通知無聲消失2.0)。現制：結算走 `promiseSettle`、提議走 `proposalResult`，前端各自出通知條。新回饋事件進來時先問「跟既有槽是同一條事件流嗎」，不是就開新欄位。
- **「下方/上方」方位詞會過期**：prompt 注入段引用其他規則時**用【規則名】不用方位**——注入點會搬家，方位詞跟著說謊(告辭豁免句曾指「下方」而鐵律實在上方)。
  - **⚠ 同類 bug 第二例（2026-07 玩家實測「紫水晶眼眸是我的ㄟ，AI又拿去用」）**：玩家御主卡「氣質」特徵是「紫水晶眼眸」，跟美狄亞聊天時 AI 卻把「紫水晶般的眼眸」直接套在美狄亞身上——查證美狄亞自己的種子資料只有「紫色長髮」，從沒設定過紫水晶眼睛。跟螢幕那個 bug 同一種病根：【玩家命格】跟【在場人物】兩段的「特徵」在提示詞裡位置相近，小模型把剛讀到的玩家外貌詞彙錯誤沿用到同伴身上。同一句★【不替玩家腦補】結尾再加一句處理：「同伴的外貌只能取材自她自己的資料，禁止把玩家自己命格的外貌/氣質特徵挪用套在同伴身上」。
- **假容量上限別憑空捏造**：2026-07 稽核抓到 `Script.html` 同伴卡片列表曾用寫死的「3」補畫「（空位）」佔位框，暗示「此地最多3位同伴」——上限只在後端（`KANSHOU_WORLD_MAX_`），這個「3」是介面上憑空捏造、跟後端規則脫鉤的假限制，已整段移除。加任何「剩餘X個」「上限N」這類顯示前，先確認後端真的有對應的數量約束，沒有就別顯示。
- **審查攔截保底文字別讓它悄悄變成歷史**：`callGeminiAPI`(Engine_Combat.gs) 全部重試/審查攔截皆失敗時，回傳的是一組跟真正生成成功長相一模一樣的「保底文字」JSON——2026-07 抓到 `actionPlay`(Gallery.gs)/`narrateWithState_`(Router_Narrative.gs)舊版都會把這句「什麼都沒發生」的保底措辭原封不動存進 `saveGameHistoryBatch`，下次呼叫又把它當成上一輪的既定事實餵回AI，可能接續出跟實際劇情矛盾的敘事。已加 `_genFailed` 旗標讓兩處呼叫端辨識、失敗時完全跳過寫歷史。**任何新的敘事呼叫端要接 `callGeminiAPI` 並自己存歷史，記得先檢查這個旗標。**
- **提示詞講的規則，代碼要真的照做**（2026-07 全面稽核抓到）：`rel_changes`/`intimacy_feedback.npcs[]` 寫入好感/外顯前，提示詞明講「只有【目前在場人物】才准變動」，但兩處寫入邏輯從沒真的檢查 `pcData[idx][COL.PC.LOC]` 是否等於 `curL`——AI 若因對話歷史殘留或幻覺提到不在場的人名，好感值一樣被悄悄寫入。同批也發現 `rel_changes` 的「單回合漲跌上限±5」只寫在提示詞裡，代碼只有 `sanitizeAiData_` 的 `[-100,100]` 粗夾，從沒真的把±5夾進去。兩處都已補上對應的程式碼檢查/夾值。**提示詞裡承諾的每一條護欄，都要回頭確認代碼是不是真的照做了，不能只靠告訴AI「請遵守」。**
- **回饋通道只開一槽，同回合兩筆就吞一筆**（2026-07 稽核抓到）：~~`kanshouPromiseSettle_`~~（已移除）沿用了 `proposalResult` 那次改版學到的「獨立通道」教訓，卻自己還是單槽——玩家若同時有兩位同伴的約定在同一回合結算(如A赴約成功+B同時爽約)，`forEach` 跑兩輪，後跑的無條件覆寫前一筆，前一筆的通知條就消失(底層BOND/MEMORY寫入正常，只有UI通知被吞)。改成陣列，前端逐筆渲染。**同一件事「可能同時發生不只一次」時，回饋通道要用陣列，不要嫌麻煩用單一物件卡死自己。**
- **字串前綴比對記得連 `indexOf` 的意義都要核對，不要只挑「有沒有寫」**（2026-07 稽核抓到）：`Router_Action.gs` 的 `_state` 隨動作回應夾帶機制原本只判斷 `String(pcId||"").indexOf("PC_")===0`——但 `"KPC_xxx".indexOf("PC_")` 結果是 `1` 不是 `0`，鑑賞被整個排除在外，即使 `update_fate`/`update_rel_tag` 兩個handler明明就是特地做給鑑賞共用、也確實有交棒`STATE_PRE_DATA_`，改命/改稱呼存檔後鑑賞玩家還是得白跑一趟整表`sync`。已補上 `isKanshouCtx` 條件放行(這兩個action是`isKanshouCtx`為真時唯一能走到這個判斷點的，其餘solo專屬action都在更早被`KANSHOU_BLOCKED_ACTIONS_`擋掉，改動安全)。
- **給小模型的篇幅指示，沒數字的那端會被無視**（2026-07 玩家實測「好感高了字數還是100~200」）：`★【篇幅隨關係濃淡】`原本只有低好感端給了具體數字(200~300字)，高好感端只寫「越深越濃才放長寫細」這種沒有錨點的模糊話——`AI_MODEL`(gemini-3.5-flash-lite)這類小模型對沒有具體數字的指示執行力很弱，結果好感再高篇幅也沒跟著拉長。已補上熟識(350~450字)/親近以上(450~600字)的具體區間。**任何要求「隨程度增減」的提示詞，每一端都要給具體數字，不能一端有數字一端純形容詞。**
  **⚠ 2026-07 再修（玩家「字數一下很長一下很短」）**：即使兩端都給了數字區間，還是留了兩個變因給 AI 自己判斷：①要把好感數字bucket進哪個區間本身對小模型就不穩、②多人在場、各自好感不同時不知道該以誰為準——兩者疊加就是忽長忽短的根因。改成 GAS 直接算好一個**具體目標字數**（`_kanshouTargetWords_`，取在場好感最高者、沒人在場用最低檔：<40→250字／40~59→400字／60+→500字）直接指定，規則名也從`★【篇幅隨關係濃淡】`改成`★【篇幅指定】`（`driveStr`/schema 的`narration`欄描述同步改引用新名字）。AI 不必再自己做「好感→區間」的判斷，只需要照給定的數字寫，同名多人不同好感也有唯一答案。
- **持久欄位的「欄名」跟「範例」都會窄化 AI 的理解，兩處都要顧**（2026-07 玩家實測「幫她戴貓耳朵，過幾輪就忘記」）：`appearance_extras`(原名`outfit_change`)本身是每回合都重新餵回的持久欄位(存`【換裝】`)，理論上不該被對話歷史的6則滑動視窗限制住——但舊欄名字面就是「換裝」、範例又只給「絲綢襯衫」這種正經換裝，AI 容易把玩家臨時加的配飾/道具(貓耳朵之類)當成那句台詞的趣味描述、覺得不算「持久狀態」而不寫進來；沒寫進持久欄，這個設定就只活在對話歷史裡，滑出視窗後就真的看不到了。已改名`appearance_extras`(外觀附加物，不再暗示只認衣服)＋範例加「貓耳頭飾」，兩處一起下手。**排查「AI 忘記某個設定」時，先確認有沒有對應的持久欄位在接住這類事實——通常有，缺的只是欄名/範例沒涵蓋到這種情境、AI 沒把它跟該欄位對上號，不是機制本身漏接。**
  - **🔄 2026-07 曾把配飾讓給小道具機制**（玩家「小道具已經有專門機制了，外觀服裝也幫我專注在外觀服裝吧」）：當時範例拿掉「貓耳頭飾」、只專注服裝本身，避免兩套機制搶著記同一件事。**⚠ 2026-09 小道具整套移除後已回滾**：欄位改回「穿著與配飾」、範例補回「貓耳髮箍」——那個承接配飾的專門機制沒了，這一欄要是還把配飾排除在外，配飾就會變成沒有任何機制記得住的東西（正是這條教訓當初要根治的問題）。
- **前端 maxlength/長度檢查不能取代 backend 上限**（2026-07 玩家「全面防呆、限制輸入的字號前後端都要」全面複查抓到）：一次性稽核揪出好幾個「只有前端擋、backend 從沒設上限」的欄位——`actionEnterKanshou`首次建檔的 pcName(只靠前端`maxlength=16`)/appearance・persona(只靠前端`maxlength=60`)、`actionBackfillKanshouAi`餵進 AI 提示詞的同三欄。也抓到反向缺口：`changeKanshouName()`前端連檢查都沒做(對照組~~`kanshouRenameHome()`~~（已移除）有做)，白白多一趟round-trip才被backend擋下。**兩條都要顧，且順序是backend優先**：backend 的上限才是真正擋得住的那一道(前端能被繞過)，前端檢查只是省一趟round-trip、體驗更好，兩者不能只做一邊。另抓到`setKanshouHomeName_`只裁長度、沒清`｜`等標籤分隔字元(小道具的`name`欄之前也犯過同款錯)——凡是要塞進 MEMORY 單值`【tag】`格式的自由輸入，一律要過`kanshouSanitizeTagValue_`，不能只信賴呼叫端自己記得清。**新增任何吃自由輸入的欄位，checklist：①backend 長度上限(不是只靠前端) ②若寫入的是 MEMORY tag，字元淨化不能省 ③前端也做同款檢查省一趟round-trip，但不能只做前端。**
- **「強度三階」不該是同一根拉桿把所有軸一起拉**（2026-07 催眠三階二度重寫）：第一版三階套同一句「更強烈」→小模型分不出來；第二版改成弧線式具體指令、但把「意識/行為/常識」三條軸綁在一起越拉越滿，最高階變成「判若兩人」——結果**最高階反而是最壞的一階**：人格被關掉（＝紅線②的反面），而且對方沒反應就沒有互動性，NPC 從對手降級成道具。定案版把三階改成**作用點不同**（意識永遠清醒／只有「能不能拒絕」隨階數升／最高階動的是「這算不算異常」的常識判斷，人格不動）。**任何做成「輕/中/重」的機制，先問清楚每一階實際上在動哪一條軸；如果最高階的做法是「把某個既有系統整個關掉」，那多半是設計錯了，不是強度大。**
- **改一階的措辭，要同步掃全部替它開的例外**（同上，2026-07）：催眠三階當初為了讓「判若兩人」不跟其他規則打架，在 `driveStr`①、~~`PROMPT_REL`~~（已移除）【角色一致性】、【親密尺度五階】三處各開了一道 `_kanshouHypnosisActive_` 例外。三階重寫後，【角色一致性】那道「★催眠暗示生效中例外，可判若兩人」直接變成新設計的反面，不改就是兩處自相矛盾。**`grep _kanshouHypnosisActive_`（或任何為某機制開的旗標）是改這類機制的必跑第一步——例外散在別的區塊裡，只改主體會漏。**
- **不變式掃描器自己也會有覆蓋漏洞，而且是靜默的**（2026-07 小道具★行）：`check_prompt.py` 的 ★ 區塊標題上限寫死 `{2,20}`，任何標題較長的區塊直接不進 `blocks`，兩道不變式對它形同不存在——放寬到 40 之後掃描數 80→83，等於先前有 3 個區塊從來沒被檢查過、卻一直顯示「✅ 全部通過」。**加了新的提示詞區塊之後，順手確認掃描數有跟著增加**；數字沒動就代表它沒被看見。
  **⚠ 同一天內就再犯一次**：放寬到 40 之後，合併多件道具的那條 ★ 標題又超過 40、再次靜靜掉出掃描（82→本該 84）。根源是「上限」這件事本身沒道理——`[^】]` 跨不過收尾的 `】`，不會失控。已改成 120 純防呆，並在 `check_wiring.py` 加了 `MIN_STAR_BLOCKS` 下限：覆蓋數只要無聲變少就擋下來。放寬後現形的兩個區塊裡，立刻有一個被抓到代名詞無指涉（深夜訪客·別有用心，已補上訪客名字）——**它從寫出來那天就沒被檢查過**。
- **重複踩過的漏洞形狀，能寫成檢查就別只寫進筆記**（2026-07 玩家「感覺好多小漏洞」）：筆記會過期、人會失憶，檢查不會。目前 `check.sh` 除語法外跑三支：`check_prompt.py`（★ 區塊不變式）、`check_mirror.js`（前後端常數鏡射，**自動發現**所有 `KC_*`，對不上又沒登記就擋）、`check_wiring.py`（門檻有無前端出口／查表覆蓋與各階相異／★ 覆蓋數下限／死路由）。三支的共同設計原則：**不消滅差異，而是逼每個差異都有人簽名**——「忘了接」跟「不用接」從外面看長得一樣，只有白名單＋理由能區分。所以新增常數/門檻/★ 區塊時你會被強迫做一次有意識的選擇。

---

## 📎 主要常數速查（都在 `gas/Gallery.gs`）


---


---

<!-- CURRENT_STATE_END：這條線【以上】是現況，check_docs.py 會逐個常數/函式名對照代碼，寫錯就叫。
     以下全是編年史（怎麼走到這裡的），裡面本來就滿是已經砍掉的東西的名字，不掃。
     新增現況說明請寫在線上面；記錄某一輪改動請寫在線下面。 -->

# 📜 編年史（以下只記「怎麼走到這裡」，不是現況）

## ✂️ 提示詞瘦身·鑑賞輪（2026-07）

玩家問「鑑賞提示詞是否有太多贅詞」＋「這模組塞太多給他他也會忘記，精簡的時候表現更好」。實測後的結論：

| 區塊 | 量 | 判斷 |
|---|---|---|
| 🔴 `nsfwBaseRules` | **266 字 / 6 條** | **已是全場最精簡**（`·` 串接、零解釋句）——玩家授權可動，實測後判定**不需動**，砍它風險高回報極小。已逐字驗證未改。 |
| `specificRules` 慾海律令 | 314 字 / 7 條 | 其餘 6 條合計 179、**律令7 一條就 131**（含純解釋句「那是你腦補的修正…」）→ 已壓到 79。 |
| USER prompt 常駐 ★ 指令 | 1702 → **1577 字** | 肥肉所在，但多數在前五輪已壓過。 |

**實際省下 177 字，其中 134 字來自兩處**：①今天剛加的「在場來由」(161→79，我自己寫得囉唆) ②慾海律令7(131→79)。其餘 19 段每段只擠得出 0~5 字——**代表舊有指令確實早就壓乾了**，不是還有大片贅詞沒砍。

**刻意不動的兩條**：`【親密尺度五階】`、`【天花板也管命令/強迫/暴力】`。這兩條的歷史紀錄顯示是靠「講得更明確」才修好玩家實測抓到的繞過漏洞（見上方親密尺度章節），壓縮它們風險不對稱。

沿用第五輪判準：**拿掉後 AI 會少知道一件事 → 保留、只換短詞；只是解釋「為什麼」給維護者看 → 整句砍。** 全程零規則流失。

### 🚫→🎯 禁令改成範圍（2026-07·玩家「一提禁止他又只記得關鍵字，沒把禁止記住」）

**問題**：否定句在小模型裡編碼很弱，字串卻編碼很強。`禁「那一刻/那一夜/自此/從此」` 等於把那四個現成的收尾詞遞到它眼前——它記住字、丟掉禁。**列舉反例＝發誘餌。**

**判準（三分法）**：

| 類型 | 例 | 處置 |
|---|---|---|
| 禁 ＋ 可抄字串 | `禁「鏡中我的表情」`／`禁沿用舊時段(「這麼晚了」)` | **改寫**成正向界定範圍 |
| 禁 ＋ 抽象行為 | `禁憑空開口`／`禁大段內心戲` | 留（沒東西可抄） |
| 禁 ＋ 它本來就不會犯 | `不輸出戰鬥/生命值`（schema 根本沒這欄） | **刪** |

**改寫公式：「禁 X」→「只能是 Y」**——界定範圍後 X 自動落在範圍外，而且沒有誘餌。

| 舊（禁令＋誘餌） | 新（正向界定） |
|---|---|
| 禁「那一刻/那一夜/自此/從此」等翻頁式收尾 | 最後一句必須落在正在發生的動作或剛說出口的話上，時間刻度不超出這一分鐘 |
| 禁沿用舊時段(「這麼晚了」)／禁寫「過了好幾個鐘頭/一段時間」 | 所有光線/氣溫/作息的感受一律依此刻重寫；本回合敘事跨度上限【十分鐘】 |
| 禁提聖杯戰爭/從者/御主/令咒/寶具/英靈/召喚 | 在場每個人就是這座城裡的普通市民，來歷只能取材自下方她自己的在場資料 |
| 禁讓玩家自動「準備好禮物」「掏錢包」「變道具」 | 玩家手上只有他自己說出口的東西，其餘一切物品與金錢在這個世界不存在 |
| 禁「鏡中我的表情」這類外部視角 | 『我』的描寫只能來自他自己的感官——他看不見自己的神情 |
| 禁問「你怎麼會在這裡」 | 她把你在場視為理所當然 |
| 禁介面詞(泡泡/按鈕/地圖)入敘事 | **不提地圖就不必禁地圖**——移動鐵律改寫成「一律由系統裁定」，介面詞整條消失 |

**刪掉的純贅詞**：`不輸出戰鬥/生命值`——鑑賞 JSON schema 根本沒有生命值欄位，而且同一件事在世界觀那行已用「血量」禁過一次，等於每回合把「血量」送到它眼前兩遍。

⚠️ **刻意保留列舉的例外**：`【天花板也管命令/強迫/暴力】` 裡的「命令她跪下」「強姦」「壓制」——那不是「禁止 AI 這樣寫」，而是**辨識玩家輸入的判準**（玩家真打這些字時要擋下）。功能是輸入分類，不是輸出禁令，字串必須在。

### 🎯 更有效的一招：收緊條件式區塊的觸發

壓字只省 177；**真正的浪費是「名義上條件式、實際上每回合都成立」的區塊**——它們整段（含 200+ 字的指令本體）在絕大多數用不到的回合照送。已收緊兩處：

| 區塊 | 原觸發 | 現觸發 | 一次省 |
|---|---|---|---|
| `kanshouWorldRosterStr`（世界概況·你認識這些人） | 只要有人不在場（≒永遠） | 玩家這句話**真的可能問到不在場的人**：句中出現名單上任一人的名字（含大小寫變體）或有詢問關鍵詞（認識/聽過/見過/在哪…） | **~355 字** |

**典型回合省 300~400 字，遠超過整輪壓字的 177。** 兩者都是 GAS 端的關鍵詞/狀態判斷，不靠 AI 判斷，符合「GAS 掌事實」。

**判準**：這個區塊是「AI 每回合演出都要用的規則」還是「查得到就好的參考資料」？後者一律加觸發條件。其餘條件式（質變/獨處）本來就稀有，維持原樣。

### 🧬 在場者命格詳情：儲存不動、只砍「送出去的」

玩家：「性格四格／特徵四格分這麼細，AI 也沒辦法演出來，是不是可以再刪減一些？」

**結論：分格本身不佔字，佔字的是內容；而 4 格是儲存 schema**（逆天改命編輯 UI／工房／AI 生成／solo 共用同一組 `COL.PC.PREF`/`TRAIT`），動它是全面重構，風險與收益不成比例。**故儲存維持 4 格，只精簡送給 AI 的呈現**：

| 欄 | 原本送出 | 現在送出 |
|---|---|---|
| 特徵 | `[外貌本相][氣質舉止][台詞自稱(僅其本人引號內用，非旁白視角)][卸下心防的私密一面]` | `[外貌氣質]`（前兩格本來就是同一幅畫面，合併）＋`[台詞自稱]`；**`[私下一面]` 只在真的獨處時才送**　⚠ **2026-09 起 `[台詞自稱]` 已整格不存在**（自稱併進【口吻】、特徵收成 3 格，見文末 §「自稱退休」） |
| 性格 | `[表象][內裡][喜歡][討厭]` | `[表象][內裡]`（反差是核心、必須分開）＋`[喜惡]`（聊到才用的話題燃料，併一格） |

**一位在場者的【特徵】欄：99 → 53 字**（非獨處回合）。三人同場＝一回合省 138 字。

**再一輪（玩家「只要給超級少資料就可以了，因為之前玩起來沒有特別的差異」）**——選擇 A：砍掉的欄位，鑑賞側的編輯 UI 也一併處理，不留「填了沒效果」的假欄位。

| 欄 | 鑑賞現況 | 對應的 UI 處理 |
|---|---|---|
| 性格 `[喜歡][討厭]` | **不再送** —「聊到才用」的話題燃料 | 鑑賞的逆天改命**只顯示前兩格**；後兩格改 hidden input 原值帶回（**資料不清空**，solo 仍吃完整四格） |
| 同伴的 `身世` | ~~不再送~~ → **2026-09 改回送**（在場卡 `\| 經歷:`，過濾比照 `servantCard_`，見 §省下來的還給角色資料） | 舊的「鑑賞敘事不會用到」加註是假話（2026-09 稽核抓到），已改成標籤「固定事實·AI 讀得到但不會改寫」 |
| 玩家自己的 `經歷` | **仍有送**（見【玩家命格】那行） | 不變 |

性格＋身世：**55 → 21 字/人**。

⚠️ **創角流程完全不必改**：這些欄位不是鑑賞專用——solo 的 `servantCard_`／`masterCard_` 仍吃完整四格、前端「📜詳細狀態」仍顯示、逆天改命仍在編輯同一份。變的只有「鑑賞這一軌要不要塞進提示詞」。

另外把「台詞自稱僅其本人引號內用、非旁白視角」這條**全域規則**從欄位標題移到常駐規則區只講一次——它原本被當成每個人的欄位標題，三人同場就重印三遍（同 `performanceNote_` 那次的「跑時重複」修法）。
⚠ **2026-09 這條規則本身已不存在**：旁白改第二人稱「你」之後，旁白與角色台詞不再搶同一個「我」字，這條註記沒有存在的理由了。

## 🗜️ 提示詞大精簡（2026-09·玩家「我只要給 AI 當下情況就好！能砍都通通砍掉！」）

先量：穩態一回合（3 位同行、歷史滿、帳本有東西）輸入 **4781 字**、輸出 460 字 → **10.4 : 1**。
玩家要 3 : 1。兩刀砍完 **2986 字 → 6.5 : 1**（−38%）。

| | 前 | 後 |
|---|---|---|
| SYSTEM | 1700 | 902 |
| USER | 1662 | 1138 |
| chatHistory | 1419（6 筆） | 946（4 筆） |

**砍掉的（都是筆法指導，不是事實也不是格式）**
- `inner_monologue` 整欄（126 字·範本最長的一條）：叫模型先寫 50 字自省再下筆。
- 鐵律 11 → 5：`drive`／`continuity`／肢體互動依性別／裝扮＝既定事實／`immersion` 五條，
  `agency` 收下 `enact` 那半句。
- `pov`（視角鎖定）／`feel`（你也是這座城裡的一個人）兩格。
- ★【稍早做過的事】整條，連同 `kanshouRecentDigest_`——它講的正是 chatHistory 已經在講的事，
  只是壓過的版本。**兩個真實來源，差的那個每回合多花 168 字。**
- 🛑【誰說了算】：`【成立】` 全樹只出現在這條指令自己身上（提議／約定機制早砍光），**指向空氣**。
- 範本不再 pretty-print：縮排與換行每回合都在付字。

**留著的**：三張在場人物卡、玩家卡、地點、時間、世界帳本、在場來由＝**當下情況**；
`voice`／`agency`／`dialogue`／`length`／`ending`＋JSON 範本＝**格式**；
`lewd` 是玩家自己的旋鈕，也是這一軌存在的理由。

⚠ **3 : 1 靠砍輸入到不了**：光是三張人物卡（454）＋歷史（946）就快 1400，而那是角色的區別度與對話的連貫性。
比例的另一個把手是**輸出**——篇幅檔位（☰⚙ 說書人設定）拉到 900 字，2986 : 950 ≈ 3.1 : 1。

`style.js` 加了一組「已砍的段落真的不在提示詞裡了」的斷言（9 條），共 57 條。

### 🗣️ 第三刀：卡片改寫成人話（玩家「有需要這麼生硬嗎？」）

`[表象]`／`[內裡]`／`[外貌氣質]`／`名字:`／`| 欄位:` 那些是**我們自己的分欄符號，不是資料**。
而在場人物卡是模型用來【變成那個人】的依據——遞一張資料庫欄位過去，回來的就是資料庫腔調。

```
前 【在場人物】名字:SABER【性別:女】 | 裝扮:藏青連身洋裝 | 性格:[表象]一絲不苟 [內裡]對平凡日常滿是好奇 | …
後 【在場人物】SABER，女。穿著藏青連身洋裝。表面一絲不苟，骨子裡對平凡日常滿是好奇。…
```

字數還少了（USER 1111 → 929），因為分欄符號本身就佔字。`【在場人物】` 留著當區塊界線。
玩家卡抬頭也從「【玩家資料·旁白用】」改成「【你自己】」。

⚠ **這一刀我量不到成效**：探針把 `callGeminiAPI` 換成罐頭字串，所以驗得了大小與接線，
**看不到文字變好還是變壞**。語氣這件事只有真的玩才知道，隨時可以退回去。

⚠ 連帶修一個當場長出來的 bug：去重在場來由的正則還在找舊的 `｜__PRESENCE__` 前綴，
改格式後配不到，於是 `__PRESENCE__` 標記原樣漏進提示詞、而且每張卡都重複一次在場來由。

三支探針（`style.js`／`kspot.js`／`party.js`）改成**認那個人真正的資料內容**而不是欄位標籤——
比原本紮實：標籤還在但內容空了，舊寫法看不出來。

### 🔎 第四刀：全篇語意稽核（玩家「有沒有這種類似、但語意差很遠的？」）

把整份提示詞逐句讀過一遍，找跟「表面」同一類的——字面看得懂，意思卻是錯的：

| 原本 | 問題 | 改成 |
|---|---|---|
| `"name": "真名"` ×2 | **鑑賞刻意全用日常稱呼**（SABER／櫻／凜），`KANSHOU_CASUAL_NAME_` 整張表就是為此存在。叫它填「真名」會拿到正式全名，而欄位比對要求逐字相符——**悄悄失敗、整條被跳過**（CODE_NOTES 早記過這個形狀） | 照卡上的名字寫 |
| `跨回合認同一個人` | 「認同」讀起來是【贊同某人】；本意是「認定是同一個人」 | 同一個人跨回合都用同一個名字 |
| `表面X，骨子裡Y` | 「表面」暗示是**裝出來的**，但 SABER 是真的一絲不苟。而且套不上另外三個（「表面大姊頭般罩著大家」） | 兩格直接並列——資料自己就帶轉折（其實怕寂寞／越被道謝越兇） |
| `在她眼中你還是初識` | 初識是狀態不是身分，不成話 | 一階一句：`你們還只是初識`／`你們已經混熟`／`你們是老交情了`（`KANSHOU_FAMILIAR_TIERS_` 加 `say` 欄） |
| `禁上帝視角` | 否定式（玩家自己的規則要正面指示），而且不精確 | 旁白只寫「你」看得到聽得到感覺得到的 |
| 範本裡的 `"同上"` | 多一層指代要模型自己解 | 直接把那句寫出來 |
| `你自己的家「風音的家」的私人房間` | 巢狀重複 | 「風音的家」裡你自己的房間 |
| 一般回合的 `【你們從剛才就一直在這裡】` | 上一輪敘事就在歷史裡、人也在卡上，**沒有新資訊** | 不講。只保留「剛結伴走到」與「時間跳過之後」——那兩種 AI 猜錯會演壞 |

`party.js` 加三條釘住「在場來由只在 AI 猜不到的時候才講」。



## 🚨 2026-07：紅線① 由玩家明確授權開啟過一次（`nsfwBaseRules` 整合）
**這是全專案唯一一次動紅線常數本體，逐項授權如下，沒有第二次的預設許可。**
1. **「女子體態柔美(…)」只拿掉「體態」兩個字，其餘原封不動** ← ⚠ **這一條是我當天最大的教訓，請讀完再動它。**
   我先判斷它是「統一體態模板」要收窄，玩家說明了它的真正用途（「不要讓AI敘述女性時候有甚麼薄繭…
   出力也不能用甚麼鋼鐵夾住這種奇怪的形容才會增加這一句的」）——**它是對的補丁**：這些角色在原作是戰士，
   模型會自然寫出薄繭/傷疤/鋼鐵般的力道。然後我改了四版，**每一版都引進一個新的失敗**：
   ①給詞彙範例「力道用絞緊/包裹/吸附」→ 玩家實測「我每次都被毒蛇絞緊…他會換方式搞我」（給詞＝指定方向）
   ②加「溫熱」→ 把**體溫**也綁死（玩家：「有些人是冰冷的阿」）
   ③寫「無繭無疤」→ 否定句被當成要提的事（玩家：「他就會一直強調 無繭無疤」）
   ④只留「膚質細緻柔嫩」→ 玩家：「先用原本的 體態可以拿掉」。
   **最終落點＝原句減兩個字。** 🧭 判準：**改一個活著的句子之前，先問它為什麼活著**；
   玩家實際玩過、活下來的措辭，比我推理出來的「更乾淨的版本」可靠。
2. **`nsfwBaseRules` 與【慾海律令】整合成一份 10 條**（玩家：「妳看看能不能整合吧」）。兩份實測 5 處重複：
   律令1(inner_monologue)、律令4(mutual_nicknames·schema 欄位自己講就好)、律令5(attitude)、
   律令6(裝扮)、鐵律5/6 與 schema 的 options。`specificRules` 改成空字串（不動 return 形狀）。
3. **`attitude` 欄位整個移除**：全檔已無任何程式讀它，它活著的唯一理由就是紅線第 5 條指名了它。
   紅線那句一走，欄位跟著走——AI 每回合填一格然後被丟掉，那是白付的**輸出** token。
   （三處解釋「為什麼刻意留著」的註解已同步改掉，不然會騙下一個失憶的我。）
**系統提示詞 2379 → 1899 字。**
✅ **CLAUDE.md 的紅線① 已由玩家決定取消**（「nsfwBaseRules 紅線設定直接取消吧」）：改成「不再是禁區，
但它仍是演化核心——改之前先用模擬器量現況、改完必跑 `dedup.js` 與全套探針」。
**編號刻意保留為 ①**：②③④⑤ 在全專案的註解與文件裡被大量引用，重編會讓那些引用全部指錯。

🚩 **給下一個失憶的我：為什麼前幾次瘦身都只砍得動一點點**（玩家問「為什麼之前找妳削減 妳都砍一點點而已...」）
- **根因是上面那句「機械護欄一條不刪、只縮字」**——它聽起來很負責，實際上是**自己給自己設的天花板**。
  句子能擠的水有限，擠完就沒了。諷刺的是那句點名保護的六條護欄裡，**移動鐵律**與**在場驗證**
  今天都被合併掉了，規則一個字都沒少：它們從來不是六條護欄，是**四條護欄講了六次**。
- **我把「這條規則存在」當前提，只問「怎麼寫得更短」。** 該問的是「**這條需要存在嗎／別處是不是已經在講**」。
  兩個問題的收穫差一個量級。
- **每條規則背後都有一道看得到的疤**（註解寫著它修好哪個 bug），刪掉像把 bug 放回去，於是一律選「留著縮短」。
  那是拿「看得見的成本（被抓到回歸）」換「看不見的成本（提示詞被稀釋）」——而後者才是玩家一直在抱怨的。
- **沒有量測就不敢動。** 今天第一件事是寫 `scratchpad/sim/dedup.js`（24 條規則的「那件事」＋重複偵測）。
  **一旦能證明沒東西掉，刪除就變便宜了**；在那之前每一刀都是憑感覺，所以只敢下小刀。
- ✅ **下次的開工順序**：① 先寫回歸網（釘事實、不釘措辭）② 逐塊問「這條需要存在嗎」③ 才輪到縮字。
  順序反過來就會再交出「一點點」。

🧭 判準：**負面指令能用正面句涵蓋就刪**；**只在需要的回合才送的句子不要每回合送**；**同一條規則只能有一個 canonical 出處**
（recency 錨點是例外，但要在註解裡寫明是刻意的）。
⚠ 刪 ★ 區塊要同步把 `check_wiring.py` 的 `MIN_STAR_BLOCKS` 調下來（128→125），這是刻意設計成「必須有意識地動一次數字」。
⚠ 改 ★ 標題時記得看 `check_prompt.py` 的 `ALLOW` ——它的 key 是**標題文字**，改了標題豁免就失效（這次踩到一次）。

⚠ **2026-07 玩家「提示詞會不會太多、有沒有重複」二次瘦身**：抓到 3 處意外重複（非刻意的 recency 設計）：①schema 欄位`appearance_extras`描述與`specificRules`第7條把「禁寫成場景/姿勢(如「在水下」「泡在浴池」)」這句完整範例講了兩次——第7條改成指回schema說明，不再重複舉例；②【此地唯一真實】跟【地點釘死·此刻只有這裡】幾乎同義(都在講「歷史提過的其他地點是過去式」)，且不是recency設計、單純位置相鄰——刪除前者，後者已涵蓋全部內容；③【現場僅此名單】見上一則。三處都只刪重複陳述，親密尺度五階/移動鐵律/冠名格式/角色一致性/世界觀等機械護欄本體字句一個未動。
⚠ **2026-07-28 玩家實測·收尾第二輪：所有按鈕補上【結果·GAS 已裁定】**（玩家：「交換聯絡 不是100%成功嗎 幹嘛還要這樣演 到底GAS給AI甚麼資料」「玩家不能是最後說話的 因為這樣玩家很難接!! 請依照這個方向 去檢查所有按鈕 並補上」）

上一輪把 🚨【收尾】的錨點翻到對方身上了，但玩家又撞到同一件事：按「結識」（100% 成立），
AI 演成「我鼓起勇氣開口問……忐忑地觀察她的反應」然後停筆，RIDER 一句話都沒回。

**★ 區塊其實寫對了**（`★【正式結識】：你與『X』交換了聯絡方式，這段緣分正式接上了…`），
壞在**提示詞的最後一行**：`現在演化玩家動作：『【玩家意圖】：鼓起勇氣向『X』提出想繼續深交…』`
——那是離生成點最近的一句話，而它只寫「他開口提了什麼」，等於叫 AI 演一個**還沒有答案的請求**。
★事實寫在二十行以前，**recency 贏了**。這跟 check_wiring ⑥ 對 solo 釘的鐵則是同一條
（「意圖→GAS 結果→AI 只演，缺一樣 AI 就會自己補那一半」），只是鑑賞這邊兩半被拆在提示詞的頭尾。

修法：新增 `_settledVerdict`（跟 `_pendingProposal` 同處宣告），各分支填一句人話，
prompt 組裝前收成 `_settledTail_` 接在 `finalUserMsg` 後面：

> 【結果·GAS 已裁定，不可改寫】『櫻』答應了這個約定——本回合演到這件事發生為止。

**「拋一個玩家接得住的話題」搬進 🚨【收尾】了**（玩家：「這可以用在敘述裡吧? 不然有時候常常沒話題接」）——那不是按鈕專屬需求，**每一回合**都該留鉤子，所以放在每回合都吃的收尾鐵律才是單一真實來源；收束句只留它獨有的那件事（「這件事已經有答案了，演到它發生為止」），不再重複講收尾。
現行收尾全文：`把最後一句留給被搭話的那個人——用對方的答話或神情收尾，並讓她拋出一個玩家接得住的話題(一個問句、一句邀約、一件她此刻在意的事，都行)，停在等玩家回應的那一刻。玩家只說他自己輸入的那一句，結尾不替他再開口、再發問。沒有別人在場時才收在「我」的動作上。`
裁定句一律由名字開頭（玩家原話：「『RIDER』同意交換聯絡方式」），這樣收尾鐵律裡那個「她」才有貼身的指涉對象。

相約／同去兩型的成敗都在 `_pendingProposal.accepted`，**一處集中轉成人話**（新增第四型會自動吃到）；

順手抓到一個真 bug：promise 型的 `_pendingProposal` 只有 `idx` 沒有 `name`，直接讀 `.name` 會印出空的『』
——比照 post-AI 落地那段的既有寫法，拿不到就回列上讀。

**脈絡量過了**（玩家問「按鈕有吃到全部的脈絡嗎? 歷史資料那些??」·探針 `scratchpad/sim/ctx.js`）：按鈕回合與純聊天回合走的是**同一條 `actionPlay_`**，拿到的脈絡完全一樣——歷史 6 筆(3 輪)、同一份 system(約 1810 字)、角色卡／五階／時鐘天氣地點／收尾鐵律一塊不缺；按鈕只是多帶一段 ★事實與收束句（user prompt 1498→約 2100 字）。唯一會變少的是**換幕縮窗**（移動／跳時段／結束一天只餵 2 筆＝1 輪），那是刻意的，防止被舊場景帶著跑。

回歸探針 `scratchpad/sim/tail.js`（13 個情境·跑真的 `actionPlay_`·比對提示詞最後一行）：

⚠ **2026-07-28 玩家實測抓到的兩個 bug（同一批修）**：

**① 敘事的 `<br><br>` 變成畫面上看得見的四個字。** 2026-07-24 補 self-XSS 那次（`fa56630`）把 AI 敘事本文整段 `escapeHtml`，但提示詞（`nsfwBaseRules` 第 2 條與 solo `miniSystem`）明寫「換行一律用 `<br><br>` 分段」——escape 之後分段標籤就成了字面文字，**兩軌同時中招整整四天**。根源解：`Script.html` 新增共用的 `aiHtml_(text)`（escape 全部 → 只把 `<br>` 放回來 → 再轉真換行），`narrate()`／鑑賞 `send()`／老虎道場三處統一走它。**放行清單就只有 `<br>`**，`<br onload=…>` 這種帶屬性的假標籤不放行。**這條已改成機器擋**：`check_render.js`（`check.sh` 第四支掃描）把 `aiHtml_` 真的跑起來對答案——排版（`<br>` 要活）與安全（其餘標籤要死）兩個方向同時釘死，另查「印【說書人】的地方有沒有繞過 `aiHtml_`」與「提示詞還要不要 `<br>` 分段」。實測把當初那個 bug 重新種回去，三條同時紅燈。

**② 敘事收在玩家自己的台詞上，玩家沒東西可接。** 舊的 🚨【收尾】寫的是「停在『我』當下進行式…最後一句落在正在發生的動作、**或剛說出口的話**上」——那句話字面上就在叫 AI 收在玩家的台詞。實測結果：AI 替玩家多問了一句「你今天……怎麼會在這裡？」然後停筆，沒有任何人回答，玩家只能對著自己被代寫的問題發呆（玩家原話：「玩家不能最後說話 超級難繼續對話」）。改成把結尾錨點翻到對方身上：

> 🚨【收尾】：把最後一句留給被搭話的那個人——用對方的答話或神情收尾，停在等玩家接話的那一刻。玩家只說他自己輸入的那一句，結尾不替他再開口、再發問。沒有別人在場時才收在「我」的動作上。

`dedup.js` 的釘子同步從「當下進行式」換成「被搭話的那個人／不替他再開口」——舊釘子釘的正是這個 bug 本身。

⚠ **2026-07 玩家「認真看看有沒有可以合併的」三次瘦身**：逐段細讀整個 system+USER prompt 找「同一件事講兩次」，這輪抓到 1 處可合併：★【不替玩家腦補·結尾停外部】(USER prompt中段)跟🚨【收尾】(USER prompt真正結尾、緊接動筆前最後確認)都在講「narration 停在當下未完成的動作、把下一步留給玩家、不要用總結式收尾」——差別只在措辭，不是刻意的早晚雙重reinforcement(不像在場驗證/移動鐵律那兩對有明確記錄的 recency 設計理由)。已把中段那句唯一與收尾重複的子句(「禁把段落收在玩家的期待/渴望上·結尾停外部當下…把下一步想怎樣留給玩家」)拿掉、改指回【收尾】，中段只留它獨有的兩條規則(不寫自己的臉/同伴外貌不可挪用玩家命格特徵)；把「禁把段落收在玩家的期待/渴望上」這句唯一的新資訊補進真正收尾那句的括號裡，不遺漏任何約束。**同時稽核並確認排除以下兩對不合併**（都是有意圖的「canonical版早段+recency版尾段」設計，不是意外重複，貿然合併會抵銷 recency 增強效果，且【地點釘死】/【移動鐵律】那對正是這幾個月「同伴消失又亂入」bug系列反覆修的核心防線）：①【在場驗證·最高優先】(早·完整版含例外)＋【動筆前最後確認】(真正尾端·recency)；②★★【移動鐵律】(早·機制面「AI不能自己決定換地方」)＋★【地點釘死】(近尾端·敘事面「歷史地點是過去式」)。
⚠ **2026-07 玩家追問「移動鐵律？？」逼出的第四輪瘦身——同一條規則「自己」內部的重複**：上面兩輪都在找「跨區塊」的重複，玩家這次點名要求再檢查【移動鐵律】本身，結果發現這條規則**自己內部**把「你現在在curL」講了兩次：開頭「你和玩家【此刻在「curL」】」跟結尾「沒有系統明講換地方，就是還在curL」是同一件事講兩次，不是跟【地點釘死】的跨區塊重複（那兩對早已確認保留）。同一個毛病也長在【地點釘死】自己身上：開頭「你們現在人在curL，就是這裡，不是別的地方」跟後面「同伴此刻的言行、場景描寫只能圍繞curL，不可以講得好像人還在別處」也是同一件事講兩次。兩條各拿掉了純屬贅述的開頭句，只留一次陳述＋各自獨有的機制/敘事細節，內容一個字沒少。**教訓**：稽核「有沒有重複」不能只查「不同規則之間」，同一條規則自己起手式重申標題含意、結尾又重申一次，也是同一種浪費，下次抓重複要連單一規則內部也一起看。
⚠ **2026-07 玩家「我覺得可以再精簡」第五輪——不找重複、改找純措辭壓縮**：前四輪都在抓「同一件事講兩次」，這輪換一種性質：找**沒有重複、但單條規則本身寫得比必要囉唆**的地方，壓縮措辭但不動任何判準/例子(牢記二輪改版的教訓——拿掉具體例子小模型會抓不住重點，這輪只砍轉折詞/重複框架用語，例子/門檻數字/行為選項一個沒刪)。改了3處：①`specificRules`第6條(options建議越界)砍掉「只能是…真能做到的動作」「那是玩家當下不可能知道、也跟眼前這回合無關的設定資料」這類重複框架語，221字→180字；②★【天花板管命令】跟★★【強迫/暴力】兩條相鄰且高度相關(都在講「未達門檻的舉動不會得逞」)，合併成一條★★【強迫/暴力/傷害類…】，順手把命令規則裡「命令不因動口沒動手就豁免」跟「不會單純因為玩家用命令的口吻講就順從照做」這句自我重複的第二句也砍掉，577字→521字。兩處合計省約100字，親密尺度五階的門檻數字/反擊行為/例子(命令跪下/命令過來/命令脫/幫她解決OO困擾/提點家電困擾)全部保留，只是句式更緊。
⚠ **2026-07 玩家「再再再精簡！！！！！！！」第六輪——確認已到安全底線**：再逐段複查一次找純措辭空間，只再找到1處可再砍(【親密尺度五階】標題括號裡「見上方佩戴道具說明」這句純交叉引用、後面已經接著講清楚例外內容，屬多餘的meta備註，直接刪)，其餘如`driveStr`/`dialogueFormatRule_`/appearance_extras schema 描述都已經過前面幾輪或更早的「Gemini改寫再壓縮」處理過，內含的具體例子清單(喘息/輕吟/悶哼等口腔聲響、絲綢襯衫/牛仔褲/浴巾等穿著範例)都是先前實測證實小模型需要的錨點，繼續砍字只會讓提示詞變輕但判斷力變差——**已達成本輪能做的安全精簡上限**，再要更短就得拿掉具體例子，那是拿「省字數」換「AI理解力下降」，划不來，故在此停手，除非之後又抓到新的實際行為異常、需要用具體例子去對治。
⚠ **2026-07 玩家「保留範例、其他都可以砍！砍到現在的10~15%！我去測試，真的不行再還原」第七輪——大膽壓縮實驗**：玩家明確授權更激進的實驗性壓縮，並自備還原計畫，因此這輪把整段 system+USER prompt(不含`nsfwBaseRules`本體——紅線保護，一字未動)重寫成電報式短句：`dialogueFormatRule_`(4條格式規則)、`specificRules`第6條、schema欄位描述(inner_monologue/appearance_extras/intimacy_feedback._note/memory/master_note)、`driveStr`(6條主動掌握規則)、以及 USER prompt 固定規則鏈(視角鎖定/角色一致性/在場驗證/焦點禮讓/世界觀/親密尺度五階標題/天花板管命令與強迫暴力合併/篇幅指定/演出而非說明/移動鐵律/不憑空生東西/不替玩家腦補/歷史僅供參考/地點釘死/收尾/動筆前最後確認)全部大幅壓縮連接詞與說明句，只留判準/門檻數字/具體例子(命令跪下/過來/脫、強姦/壓制/無視掙扎、絲綢襯衫/浴巾/在水下/泡在浴池、鏡子顯現我的表情、那一刻/那一夜/自此/從此、泡泡/按鈕/地圖等全部保留)。**⚠ 過程中一度誤觸紅線**：第一次改寫時手滑把`nsfwBaseRules`常數本體(那6條鐵律)也一起砍了，被`git diff | grep nsfwBaseRules`當場抓到，立刻逐字還原回原文後才繼續——之後全程繞開這個常數。**實測結果誠實回報**：這輪(不含schema部分因較早幾輪已動過)實際只壓到約 81.5%(省 18.5%)，遠遠達不到玩家要求的「10~15%」(=省85~90%)——因為「保留所有具體例子/門檻數字」這個前提本身就決定了壓縮的物理上限，例子/數字本身占了原文很大比例，能砍的只剩連接詞跟說明句，那些早在前幾輪已經砍得差不多了。若要真的砍到10~15%，勢必得犧牲部分例子或把判準本身簡化，那就違背「保留範例」的前提了。**還原點**：這輪改動前的乾淨版本是 commit `94c3858`，玩家實測若發現narration品質下降(如忘記在場驗證/親密尺度天花板失守/角色一致性跑掉)，可以 `git revert` 這輪commit或直接checkout回`94c3858`重新部署。
⚠ **2026-07 後續二輪·玩家拿 Gemini 改寫再壓縮回精簡風格**：`nsfwBaseRules`(規則1)/`driveStr`/`🛑角色一致性`/`dialogueFormatRule_`①②③ 這幾段文字重新順過措辭，同步拿掉幾個「具體例子/原因說明」（如規則1的「？？不可演成從容挑釁」範例、driveStr的「該親就親/該進一步/該索求」具體推進動詞、角色一致性的「冷酷」型例子、對話格式②冠名規則的「為何要冠名(櫻認錯自己台詞)」解釋、③的錯例/正例對照）——**這些多半是「示範用具體例子」而非機械護欄本體，砍了會讓提示詞更省字，但也可能讓模型在對應的邊角情況下少一點依循，若之後實測某個對應行為(如認錯自己台詞/停在曖昧不推進)又回歸，優先考慮把對應的具體例子/原因說明補回去，而不是整句重寫**。**曾考慮把 driveStr(僅driveOn=true才出現的USER prompt動態片段)直接合併進恆常存在的System prompt(nsfwBaseRules/慾海律令)裡——玩家決定不做**：driveStr關掉時是0字(GAS動態組裝不塞這段)，合併進恆常規則反而讓「開關關掉」的多數回合每回合都要多付這段字數，且拿掉了「矜持/主動掌握」兩種節奏可切換的實際功能，省字帳算下來不划算。
⚠ **2026-09 玩家「我想要拉高 AI 回覆的字數」第十輪——先找天花板再調**：

**天花板不在提示詞，在 `max_tokens`。** 它管的是【整份 JSON】不是只有 narration，而 narration 以外的
欄位開銷很可觀：

```
JSON 固定開銷（三人在場）：典型 757 字 ／ 欄位全滿 1,057 字
舊 max_tokens 1500 → narration 撐到約 670 字就會被截斷
截斷 ＝ JSON.parse 失敗 ＝ 重試 ＝ 玩家看到「🌫️ 因果紊亂」
```

也就是舊的 500 字目標**已經貼著上限在跑**，再往上加提示詞也沒用——會直接壞掉。

三件事必須一起改（只改一件都無效）：
| # | 改動 |
|---|---|
| ① 目標字數 | `250/400/500` → `380~500 / 550~700 / 700~900`，並改成**資料驅動查表** `KANSHOU_WORDS_` |
| ② 指示寫法 | 「約 X 字」→「寫 A~B 字，**不可少於下限**」。小模型看到「約」一律往下取（舊教訓已記在本檔）。再補一句「寫不滿就往互動裡加：動作細節、觸感／氣味／聲音、多給一次真實反應。別靠拉長環境描寫充數」——給它**填充的方向**，否則它會用環境描寫灌水 |
| ③ `max_tokens` | `1500` → `2400`（時間轉場那支 600 → 700）。餘裕實算：narration 900 字＋欄位全滿 ≈ 2,054 tokens，即使 AI 超寫到 1,000 字也還在 2,400 內 |

`check_wiring.py` 的 `TIER_TABLES` 新增一組盯著 `KANSHOU_WORDS_` 每階都要有相異的 `range`
（已用故意弄壞一階驗證抓得到）。

⚠ 排序不受影響：改完重量可快取前綴仍是 **92%**（第九輪的成果沒被打壞）。

⚠ **2026-09 玩家「輸入那麼多、AI 只回一點點，是不是虧大了」第九輪——排序，不是再砍**：先量投入產出比。

```
穩態每回合：system 1,655 ＋ user 1,812 ＋ history 1,536 ＝ 約 5,000 字輸入
            AI 產出 662 字（narration 500 ＋ 內心 50 ＋ 4 選項 ＋ 經歷）
            比例 7.6 : 1
```

**結論：不是「給太多」，是「同樣的字每回合重付一次」。** 相鄰兩回合逐行比對，
**93% 的內容一字不差**（system 53 行只有 5 行變、user 26 行只有 2 行變）。

⚠ **這一輪是在「假設 prompt cache 存在」的前提下做的**——玩家指示「先用預設有快取做修正」。
實際有沒有快取**尚未證實**：payload 裡沒有任何快取參數、回應也沒讀 `usage`，而此開發環境的網路
政策擋掉 `openrouter.ai` 無法查證。**若最後證實沒有快取，本輪三項改動對成本沒有幫助**（但也沒有
壞處——排序本身不影響字數），屆時要省只能真的送更少字（history 6→4 筆、JSON 範本再壓）。

**排序原則（新增·寫進 `actionPlay_` 的 prompt 上方註解）**：cache 是**逐 token 比對前綴**，
一個會變的東西插在中間，它後面全部作廢。所以【穩定的放前面、每回合會變的放後面】。
唯二例外是 🚨【收尾】與 ★【在場名單】——它們雖然穩定，但 recency 對它們特別重要
（實測過「事實寫在 20 行以前就會被 AI 當成沒發生」），故仍壓在最尾端，不為快取讓路。

三項改動與量測（探針 `scratchpad/size/prefix.js`，逐字元算可快取前綴）：
| | 可快取前綴 |
|---|---|
| 改前 | 47~59%（天氣/時間卡在第 5 行，把後面全部作廢） |
| ① user prompt 重排（世界觀/規則/視角 → 玩家資料 → 角色卡 → 尺度/篇幅/地點 → 情境 → 天氣時間 → 收尾/名單 → 玩家動作） | 48% ↔ 92% 交替 |
| ② `master_note` 節流改在落地端做，schema 一律保留該欄 | 62% |
| ③ 【玩家資料】的「(可透過 master_note.經歷 滾動增補)」改成固定文字 | **每回合穩定 92%** |

②的取捨：原本非側寫回合把 `master_note` 從 schema 刪掉，省 77 字輸入，卻讓 system prompt 每 3 回合
變一次形狀、1,655 字前綴整個作廢。改成 schema 固定、只在落地端用 `_doSideWrite` 決定要不要寫回表
——代價是那兩回合 AI 多吐約 40 字（丟棄不用）。

⚠ **2026-09 玩家「我給太多、它注意力跑掉」第八輪——先量再砍**：這輪不憑感覺挑段落，先把真正送進 `callGeminiAPI` 的 system／user／history **整串攔下來秤**（探針 harness 直接驅動真實 `handleGameAction`，不是估算）。量出來的事實推翻了原本的假設：

```
鑑賞 play（3 人在場、不含歷史）：4,143 字
  試算表來的資料（3 張角色卡 895 ＋ 玩家資料 98）＝ 993 字，只佔 24%
  其餘 3,150 字全是我們自己寫的規則與 JSON 範本
```

**所以問題從來不是「寫太多進試算表」**——角色卡一張 286 字已經很精實。真正在稀釋注意力的是**指令數量**：一次呼叫 24 個 ★🚨🛑 記號＋10 條編號鐵律，而且同一條規則被重複講：「不替玩家做決定」6 次、「篇幅字數」5 次、「用真名別用暱稱」5 次、「在場只有這些人」5 次。

本輪動作與結果（`4,143 → 3,539`，**−15%**；指令記號 24 → 18）：
1. **JSON 範本瘦身**（1,154 → 約 700）：每欄只留 schema 級約束（字數/真名/留空沿用），演法一律退回鐵律講一次。
2. **重複指令合併**：路人許可＋缺席者交代合成一條；焦點禮讓＋演出而非說明＋沒寫的就不存在合成一條；★【稱呼】＋★【動筆前最後確認】合成結尾的★【在場名單】。繁體要求在 system 首行與 `callGeminiAPI` 的【語言鐵律】已各講一次，user 端第三次刪除。
3. **親密尺度改資料驅動**：門檻併進既有 `KANSHOU_REL_TIER_` 的 `ceiling` 欄（同一組 20/40/60/80 原本存兩處），`kanshouIntimacyLines_` 只吐在場者實際落在的那幾階——三人全在 60~79 就只送一行，其餘四行是雜訊；沒人在場整塊不送。
4. **在場來由去重**：三張卡逐字重複同一句「你們從剛才就一直在這裡」，相同就抽成抬頭講一次。
5. **玩家自己那張卡改走 `formatPref`/`formatTrait`**：原本是裸資料直接倒，提示詞裡真的印著 `性格:沉穩寡言·認真、、、` 和 `特徵:…、、我、無`。
6. **`QUAD_EMPTY_` 補齊佔位字**（見 `FUNCTION_MANUAL.md`）：一張敵從者卡曾同時夾帶「珍視之物」「厭惡之事」「Archer 職階英靈」三格純噪音；從者卡的「自稱」也講了兩次。

**教訓（下次想精簡時先讀這條）**：憑感覺找肥肉會找錯地方。**先把真的送出去的字攔下來秤、再數同一條規則被講了幾次**——體重計跟重複計數器合起來才指得出該砍哪裡。探針做法見 `scratchpad/size/`（`sheet.js` 假試算表＋`probe.js` 攔 `callGeminiAPI`），容器重開會消失，重建成本約十分鐘。

⚠ **2026-07「色色部分搬去給點火」實驗（進行中·結果待玩家實測回報）**：慾海律令原 0(色度跟隨)＋4(情慾場生理特寫)兩條**整段搬進 `driveStr`**（新編號⑤⑥，只有 `driveOn=true` 才組進提示詞），慾海律令本體剩 5 條(重新編號1~5)。**這兩條原本是「怎麼寫得好」的常駐風格指導、不是「准不准寫」的開關**——准不准寫全程由【親密尺度五階】的好感天花板決定、跟 driveOn 無關，天花板不變。搬走後的實際影響：矜持模式(driveOn=false)不再拿到這兩條的具體露骨寫作指引，即使好感已達戀人階(80+，天花板本身仍允許無上限)，措辭可能反而變保守含糊；主動掌握模式因為同時吃到 driveStr 的「推進到真的發生」指令＋這兩條的露骨寫作指引，兩者疊加可能更猛。**若實測發現矜持模式下的高好感場景意外變乾癟/含糊，這就是根因，把這兩條原樣搬回 specificRules 即可還原。**
- **finalJson 欄位**（2026-09 現況）：`narration`(字數照【篇幅】區塊·**第二人稱「你」＝玩家**)／`options`(**6 條**〔2026-09 玩家「只有4種 是可以擴大到6種 分支寫得更加明顯?」；後端 sanitize 上限本就是 `slice(0,6)`，數字只活在 schema 那一行〕·各≤20字·**分歧寫在【後續】上**「六條分別通往六種不同的後續」——⚠ 刻意【不】給固定分類（開口/靠近/觀察…），那會讓六格每回合長成同一套模板，就是「背脊永遠打得筆直」「收尾固化」同一個形狀·可由 optionsOn 整欄刪)／`intimacy_feedback`{player:{physical_state,appearance_extras}, npcs[]:{name,physical_state,appearance_extras,mutual_nicknames,memory,**noticed**(≤14字·她眼中的你)}}／`world_note`[]{kind,name,text,sex}(世界帳本)／`scene`(這一段演完人在哪·≤12字·存玩家列 `COL.PC.LOC`)／`cast`{join[],leave[]}(AI 動【臨時在場】那一層·同行者豁免·查無 id 整筆忽略)。~~inner_monologue~~／~~npc_exit~~／~~rel_changes~~／~~attitude~~／~~master_note~~ 都已拿掉（`rel_changes` 隨好感整組砍除；`npc_exit` 換成 `cast`）。
  **⚠ 2026-07 四度改版·`dynamic_skills`(雙修技巧)整個拔掉**（玩家「雙修技巧還有在用？UI拿掉玩家也看不到了」）：稽核發現這欄早就沒有任何玩家UI顯示，也沒有任何規則告訴AI該怎麼運用讀回的技巧清單(純粹讀進去擺著)，形同每回合白吃AI注意力換不到實質效果——連帶`kanshouSkillTagStr_`/`processSkills`/`setSkillTag_`三個輔助函式與`【身體記憶】`/`【快照】[技巧]`兩段USER prompt注入全數刪除，`[雙修技巧]`MEMORY標記不再讀寫(舊存檔殘留值不影響任何邏輯，純孤兒資料)。順手修正一個連帶發現的off-by-one：`mutual_nicknames`/`attitude`schema文字原本寫「見律令5」「見律令6」，實際慾海律令只有5條，拔掉dynamic_skills後律令4=mutual_nicknames、律令5=attitude，schema引用已同步改對。
  **⚠ 2026-07 三度改版·`promise_proposal`／`cohabit_proposal`／`proposal_accept` 三欄全部拔掉**（玩家「proposal_accept可以拿掉…promise_proposal也可以拿掉，讓GAS好感超過90…詢問玩家她是否可以與玩家同居…想要當好感卡39之類的時候GAS主動發出邀約」）：AI 不再有任何欄位能自己決定「要不要開口約/邀同居」，這兩件事改由 GAS 依好感數值直接判定觸發（見上方「約會地點好感分級」與「她也能主動邀約/邀同居」段落），`proposal_accept` 本就早已停用、一併真正刪除不再保留相容佔位。
  **⚠ 2026-07 拔掉 `move_proposal`＋`location` 欄（玩家實測「AI一直提議移動、頭痛」）**：舊版讓 AI 自己決定要不要提議換地方、換去哪，結果反覆出現「同地點原地邀約」「跟歷史地點串戲」等 bug，且體感一直被打斷。現在 AI **完全沒有任何欄位能提議或宣告換地點**，換地方只剩兩條 GAS 決定的路：①玩家自己用地圖走(`moveTarget`)；②玩家在地圖對在場同伴提議「一起去」（`proposeMove` 機制標記，見下），GAS 依好感直接裁定接不接受，AI 只演她答應/婉拒的反應。因為①②都不讀 AI 輸出，也就不需要「攔截 AI 硬吐 location」這層保險了，直接整段刪除。
  **🐛→✅ 2026-07「options 建議越界」修法**（玩家「4個選項…有時候會出現移動、或是呼喚/尋找人物，但這些都做不到、點了AI直接當機」）：`options`(命運的抉擇)schema/rule 原本只約束格式(4條·每條≤20字·分類標籤)，完全沒有內容約束——AI 會生出「去咖啡廳走走」「找Saber聊聊」這類選項，但選項一旦被點擊會**原樣當成玩家輸入送回**(`Script_Kanshou.html` `send()` 的 `currentOptions[optIndex]`)，而移動/呼喚不在場者本來就不是 AI 演得出來的事(受【移動鐵律】/【在場驗證】約束)，AI 被迫在明知不可行的前提下硬掰，容易生出前後矛盾/離題的敘事，玩家觀感上讀成「當機」。**修法**：`specificRules`(慾海律令，可改)補第6條——「options 只能是玩家對『此刻在場人物』在『當下這個場景』真能做到的動作，禁止建議移動地點、呼喚/尋找不在場角色」，從產生選項的源頭擋掉，不必等到玩家點下去才在敘事端硬圓。
  **🐛→✅ 同日再抓一種「options 越界」**（玩家實測：場景明明是凜在便利商店對著一款冷飲的成分皺眉，選項卻冒出「3. 注意到她對家電的困擾，提點兩句」——查 `Seed_Codex.gs` 凜的 `dailyMoe`「操作家電永遠一竅不通，出糗次數多到數不清」，證實 AI 是把她的**萌點設定表**字面掏出來塞進選項，跟本回合場景(冷飲)完全無關，玩家角色也不可能知道這件事）：跟上面「建議做不到的動作」是不同失敗模式——那次是動作辦不到，這次是**選項另外調用了角色設定表**，等於繞過 narration 直接把不該說出口的萌點字面端上桌，同時違反 show-don't-tell(紅線②)與「只能演本回合」。同一條第6條規則追加後半句：「只能基於『這回合 narration 實際寫出的內容』出題……禁止另外調用她的萌點/個性/背景設定表字面塞進選項……那是玩家當下不可能知道、也跟眼前這回合無關的設定資料」。
  **🚶👋 玩家提議同去**（~~當時唯一存活的換地點提議路徑~~·2026-09 隨地點整組退休）：地圖 👋 鈕帶 `proposeMove` 機制標記 → pre-AI 記 `_pendingProposal{type:'move'}`＋依好感用 `kanshouProposalAccepts_` 直接裁定成不成，注入★【提議·同去·GAS已裁定】鐵律讓 AI 只演反應 → 接受＝post-AI 回填 `moveProposal` 出既有「前往」泡泡＋`proposalResult` 通知條，玩家按同意才真的移動（moveWithCompanion 帶同地眾人）。多人在場＝一起邀（以第一位個性判定）。**裁定基準 2026-07-28 由「最生疏那位」改成「在場平均」**——提議對象是【同地全部人】，取最低等於把否決權交給剛好路過的陌生人：跟好感 80 的她在咖啡廳，來一個好感 5 的路人，成功率直接從 97% 掉回 58%（玩家：「提議移動的成功機率是多少 太低了吧」）。平均值仍保有「人越多越難成行」的意思，但一個生面孔不再一票否決。
  **移動規則二條版**（2026-07 三度改版拔掉 `promise_proposal` 後，★【地點清單】整條刪除——那條規則存在的唯一理由就是替 AI 的 `promise_proposal.loc` 圈合法地名，AI 現在連這個欄位都沒有了，自然也不再需要看完整地點清單）：★★【移動鐵律——換場景完全不是你能決定的事】(合併版·含地圖移動例外與換幕一致)／★【地點釘死·此刻只有這裡】(想去別地方頂多嘴上聊聊、不會真的發生，註明自然告辭豁免)。
- **對話格式** `dialogueFormatRule_()`：**鑑賞專用**，唯一呼叫點＝ Gallery 版 `nsfwBaseRules` 第3條（⚠ 非 Engine_Combat.gs 紅線那支·紅線不呼叫它·改它安全）。規則三條：①口/喉發出的聲音(話語＋喘息/輕吟/吸吮)一律進單層「」，禁（輕哼）括號描述　②每句角色台詞在「」前冠說話者名字（硬格式·2026-07 玩家「櫻不知道是自己說過的話」→ 台詞浮在敘述裡沒名牌·輕量模型跨回合認錯人），例 `櫻「…」`；玩家台詞免冠名，且**語氣照原句寫、禁改寫成「於是我問了一句」這類轉述句**（2026-07 玩家實測：打「Saber啊你們不知道？」，AI 寫成「於是我又追問了一句」——台詞被轉述掉、沒有真的說出口。文意上併進 `nsfwBaseRules` 第1條更順，但那是常數本體，改在這裡效果相同）　③動作/撞擊/水聲走敘事、單層「」禁巢狀。
  ⚠ **2026-09 更正：solo 的 `miniSystem` 並【不】呼叫這支**——全樹只有一個呼叫點，文件四處長期寫成「兩軌共用／全遊戲單一真實來源」都是錯的。而且**不該改成共用**：這支的具體例子是 NSFW 的（喘息／吸吮／舔啜／啪啪／交合處水聲），灌進鎖 SFW 的 solo 軌是污染。solo 第2條是刻意的精簡 SFW 版（冠名＋單層「」＋動作走敘事）；少掉的「玩家台詞照原句寫」在 solo 也用不到——**solo 是純按鍵，玩家根本不打字**。兩條規則刻意分家，不是漏同步。

### ~~🤝 相處基調~~（2026-07 新增·2026-09 隨好感整組砍除）

`KANSHOU_RAPPORT_TONE_` 的 11 條事實句已全數移除。它是拿好感×相處次數換一句「事實：對方喜歡你，但你們認識還太短」餵 AI——好感沒了，這一層跟著沒了。**現在同一個位置站的是 `KANSHOU_FAMILIAR_TIERS_`（四階物理距離，只看【相處】次數）。**

### 📝 她眼中的你（知情度 ＋ 她偷偷做的筆記）— 2026-09 新增

**玩家原話**：「跟外面 ai 不同，這裡的 ai 明確知道所有設定，所以**第一次遇到玩家就把玩家看透了**，
經歷也是亂掰；現在有開店功能，但我創角色就幫我說有開店，我其實還沒開店」／
「應該要**用關係去掌握角色對玩家角色的熟悉度**，外面的聊天 ai 也會這樣子**偷偷做筆記**紀錄玩家的特點吧？」

**根源**：玩家卡（原【玩家資料】）一直被**每個在場角色無條件全知**——包括 `性格:[內裡]`、`萌點(僅供內化)`、
`經歷`。那份資料其實有**兩種用途被混在同一行**：①寫玩家自己的內心與感受（★【你也是這座城裡的一個人】要用）
→ **該全知**；②在場角色對玩家的認識 → **不該**。
`grep "還不認識\|不會一眼看穿\|知情" gas/Gallery.gs` 改之前是 **0 筆**。

**⚠ 第一版寫壞了，玩家當場擋下來**：我留著洩漏、另外寫一個 225 字的★★區塊（含初識/混熟/老交情
三段定義）叫 AI「你讀得到但請假裝不知道」。玩家：「**我是要妳幫我讓 ai 更自然，不是一直追加設定！
妳必須要去看增加這個是否對 ai 有用處，如果沒有就不要增加雜音**」「**核心越簡單越好，所有的設定都只留
必要的提示**」。**初識／混熟／老交情 是模型天生就懂的詞，不需要我寫三段定義去教它**——留機制、砍說明。

**現況＝兩層，各由不同的東西保證：**

| 層 | 誰算 | 提示詞成本 |
|---|---|---|
| **她對你多熟** | **GAS**：`KANSHOU_MET_COUNT_TAG_` 查 `KANSHOU_FAMILIAR_TIERS_` | 卡上一句物理距離，約 10 字 |
| **她記下你哪些特點** | AI 吐 `noticed` → `kanshouAppendUnique_` | schema 一行 ＋ 卡上最多 3 條 |

擋住「一眼看穿」的是**第一層**，GAS 自己算得出來，**AI 吐不吐都擋得住**。

- **界線只有一句，掛在玩家卡自己那一行**（不另開★區塊）：
  `【玩家資料·旁白用】(只給旁白寫「你」的內心用·在場的人沒讀過這張卡)`。
- **`kanshouKnownTier_` / `kanshouKnownOfYou_`**：共用 `KANSHOU_FAMILIAR_TIERS_` 這條軸，**不另立門檻**。
  卡上印 `你在○眼中:<熟悉段>`，有筆記才接 `·<筆記>`；**沒筆記就只印熟悉段，不多說一句廢話**。
- **`【眼中的你】`**（`KANSHOU_NOTED_TAG_`，存**她自己那列**的 MEMORY）：分隔符 **`／`**——
  **不可用 `｜`/`【】`**，`makeTextTag_` 會把結構字元從值裡剝掉。cap 3、單條 14 字。
  **存在她列上（不是玩家列）是關鍵**：每個人各記各的，同一個玩家在不同人眼中才會不一樣。
- **schema `noticed`**：「≤14字·只記【會改變之後怎麼對玩家】的發現·**喜好習慣瑣事與卡上已有的一律不記**·
  多數回合填「無」」。⚠ 第一版寫成「真的看出來的一件事」，結果記的全是「咖啡只喝黑的」這種
  **卡上性格四格早就有的東西**（玩家：「不是記那些沒用或是重複的東西，像是玩家的喜好之類的」）。
- **AI 生成的 `background` 不准先幫玩家寫好「現在擁有什麼」**（`KANSHOU_MASTER_GEN_SYS`）：
  只寫【來到冬木**以前**】的來歷；開店／店面／在哪上班／與誰同住／交往對象／養了什麼／已經有哪些朋友
  **全禁**——那些由系統逐項記錄，創角就寫好＝玩家一開局讀到一段沒發生過的人生。

**探針** `scratchpad/size/known.js`（30 條斷言）＋**退化測試**：把 `noticed` 寫入改 `if (false && …)`
→ **7 條 ❌**，還原 → 0。
⚠ **測資必須彼此夠不像**：bigram 去重是真的會擋（這正是它的工作），第一版用「第N件同樣的話」
被自己的防線吃掉、害我誤判上限壞了——**探針說「沒作用」時先確認是不是探針自己的問題**（本專案第五次）。

**目前刻意不做**：面板不顯示這些筆記（玩家要的是「**偷偷**做筆記」）。

### 🧹 2026-09 提示詞去雜音（玩家「核心越簡單越好」）

上一節那 225 字被玩家擋下來，順勢把整份鑑賞提示詞重量一次：**指示段落 32 → 27 段、
全域最高子句重疊 0.26 → 0.19、總字數 3738 → 3681**（知情度＋筆記兩個新功能含在裡面，**淨減 57 字**）。
每一刀都先回答同一個問題：**這句改變 AI 寫出來的東西嗎？**

| 刀 | 砍掉什麼 | 為什麼它是雜音 |
|---|---|---|
| ① | ★★【對方對你的認識】整塊 225 字 | 見上節：教模型它本來就懂的詞 |
| ② | 鐵律 8 後半「關係標籤由玩家定，不可改動」 | **死規則**——`rel_changes.tag` 2026-07 就從 schema 移除了，AI 根本沒有管道寫它，這句是在對著不存在的欄位下禁令 |
| ③ | 鐵律 5 整條（85 字） | 與 schema 的 `inner_monologue` 欄位說明講同一件事（**全域最高重疊 0.26 就是這一對**）。併進欄位說明裡（那裡更具體，且就在欄位旁邊），鐵律編號 5~11 → 5~10 |
| ④ | 鐵律 4 尾巴「肢體親密照【親密尺度五階】」 | ★【親密尺度·**最高優先**】自己就是最高優先，不需要鐵律再指一次路 |
| ⑤ | ★【視角鎖定】「旁白一律用第二人稱」 | SYSTEM 第一行已經寫著「第二人稱「你」＝玩家·禁上帝視角」 |
| ⑥ | world_note 的兩段（207→177） | 兩段互相解釋同一件事，合成一段 |
| ⑦ | ★【你也是這座城裡的一個人】的「動作與台詞仍只有玩家能決定(鐵律1)」 | 對鐵律 1 的**回頭指路**，而鐵律 1 就在同一份提示詞的最上面 |
| ⑧ | ★【篇幅】的「加什麼」清單 | 「動作細節、觸感/氣味/聲音、感受與身體反應」與上一條的感受清單逐項重複 |

⚠ **砍過頭一次、被探針抓回來**：⑦ 同時把「卻感覺得到臉發燙、喉嚨發緊」也刪了，`mecard.js` 立刻叫。
那 12 字是**這條規則唯一能抓的具體錨**（玩家當初要的就是「用這個角色的角度去感受」），已還原。
**規則可以砍，讓規則可執行的那個例子不能砍。**

### 💗 ~~告白~~（2026-07 新增·2026-09 大瘦身整組砍除）

曾是戀人階唯一的入口（`KANSHOU_CONFESS_BOND_` 60 開得了口、`kanshouConfessAccepts_` 擲骰定成敗、成立蓋 `KANSHOU_LOVER_TAG_`）。**砍它的理由值得記著**：玩家主動說出口的意圖，被一顆看不見也影響不了的骰子否決——那是把玩家的決定交給運氣。現在關係稱呼由 AI 每回合自己維護（`rel_tag`），玩家自己打過一次就鎖。

### 🧠 記憶全景（AI 每回合看得到什麼·寫回什麼·多久一次）— 2026-07 整理·2026-09 稽核修正過期處
**AI 每回合看得到（組進 prompt）**：
- **近期對話**：`getGameHistoryBatchRaw(pcId, 6)` 滑動窗（6筆＝3輪，更早的靠下面的持久欄接力）。
- **玩家**：性格(PREF)／特徵(TRAIT)／裝扮／**經歷(BACK·固定事實·AI 只讀不寫·≤80字)**／位置（地點活動表已隨預寫池砍除）／肉體(PHYSICAL)。⚠ 萌點(INTENT) 2026-09 整組退休、兩邊都不再有這個欄位；性格創角時 `actionBackfillKanshouAi` 寫一次定案，遊戲中只有玩家自己改命能動，AI 完全不碰。**2026-07 四度改版拔掉雙修技巧(身體記憶)**：沒UI也沒使用規則的孤兒欄位，見上方finalJson欄位說明。
- **每位在場 NPC**（`partyDetailsArr` 一行一人）：身世(BACK)／裝扮／性格／特徵／日常風味／共同回憶(MEMOIR)／與玩家的約定／關係 tag＋好感＋相處記憶＋階調＋「你在她眼中」(知情度＋noticed)；NSFW 區另帶 肉體＋專屬稱呼(REL_MEM)。（聊天天花板、REL_MEM 態度欄、當前活動欄都已移除）

**AI 寫回（GAS 落地）**：
- **每回合**：`physical_state`/`appearance_extras`(原outfit_change)→PHYSICAL·【換裝】；`mutual_nicknames`→REL_MEM；`memory` 里程碑→MEMOIR(cap10·★釘選不驅逐)；`noticed`→MEMORY【眼中的你】；`world_note`→世界帳本；`rel_changes`→BOND；`npc_exit`→LOC。（`attitude`、proposals 泡泡都已不存在）
- ~~每 3 回合 `master_note` 經歷滾動~~：2026-09 整組移除（經歷改回固定事實，見 §經歷改回固定）。

**🩺 AI 負擔瘦身（2026-07 玩家診斷「滾動式+衣服外觀神情太要他老命」·小模型注意力有限，能省則省）**：
- **狀態差分**：`physical_state`/`appearance_extras`(原outfit_change)/`attitude` 沒實質變化留空＝系統沿用舊值（GAS 空值本就跳過寫入；attitude 配套修掉「空值洗白態度」舊 bug——空→從 oldRMem 撈回舊態度；**「無/同上/不變/沿用/維持原樣/如前」等敷衍值也視同空**，否則差分模式下 AI 真的會把「同上」二字寫進態度欄）。有變化（脫/穿/沐浴/情事/神情轉變）必須更新，NSFW 場景照記。
  - **🐛→✅ 2026-07 玩家實測「用👕換裝手動設定的裝扮，下一回合就默默變回別的」**：schema `_note` 裡「沒實質變化就留空」只是 JSON 欄位描述文字，對這個模型的約束力不夠強——AI 會自己判斷「這身打扮好像跟這幕場景/氣氛不搭」就主動「合理化」改寫成別的描述，即使敘事裡根本沒有任何穿脫/更衣動作發生，等於自行推翻玩家手動鎖定的裝扮。已在**慾海律令**(`specificRules`，非紅線)新增第7條，把「appearance_extras只在劇情真有穿脫/更衣/入浴動作時才填新值、不准因『不搭』自行合理化改寫」提升成跟第4/5條(mutual_nicknames/attitude)同等級的明文規則(schema _note是弱信號、慾海律令明文規則對這個模型才是強信號，同一套教訓)。
  - **🐛→✅ 同批再抓一個變種**（玩家回報「在溫泉泡湯→移動到商店街，服裝狀態卻顯示『在水下』」）：AI 把**所在環境/姿勢描述**(「在水下」)誤寫進這個**只該記衣物本身**的欄位，離開泡湯地點後這句話語意不通、也永久卡住(因為上面第7條規則已讓AI不再主動改寫，這格就會一直卡著錯的舊值不放)。schema描述與慾海律令第7條都補上明文：**這格只能寫「身上穿/沒穿什麼」，禁止寫成所在環境或姿勢**(如「在水下」「泡在浴池」)——那是敘事場景，該讓narration自然演出，不該塞進持久裝扮欄。**已卡住的存檔需要玩家自己用👕換裝手動蓋掉**(輸入正確裝扮或留空清除)，這次修正只防未來不再發生，不會回溯修好已寫壞的舊值。
- **options 連動開關**：前端帶 `optionsOn`（玩家關【命運的抉擇】＝false）→ `buildDefaultSystemPrompt` 唯一參數 `includeOptions=false` 把 options 欄整個 delete——沒人看的東西不叫 AI 生。連 Gallery 版 `nsfwBaseRules` 第2條的「options固定4個」字樣也隨開關拿掉（schema 刪了、規則文字還催繳＝AI 精神分裂）。
- **🎬 換幕縮窗**：移動/跳時段/跳節慶/推進時間/結束一天的回合，歷史窗 6筆→2筆（1輪）——舊場景對話物理上不進 AI 眼睛，根治「換地點/時段被舊場景帶著跑」（「此地是唯一真實」「此刻時段是唯一真實」兩條鐵律是文字輔助線，縮窗才是確定性主力）。
- 原則：**AI 只管演戲，記帳全給 GAS**——別再往每回合 schema 加欄位，要加先想「能不能差分/節流/事件驅動」。

**⚠「今日情景」查證結論（2026-07·勿重複造輪）**：曾考慮加「今日情景」滾動摘要接住 3 輪窗外的當日細節——查證後**不做**：**經歷(BACK) 的滾動摘要實質已涵蓋今日進展**（實測會寫入「正在逛街、計畫一同前往咖啡廳」等當日動態），另設欄位＝跟經歷重複。若長場景實測出現「忘記前段」，優先調經歷的提示詞（讓它多保留今日細節）而非加新欄。舊 `log_summary` 是因果表的主/被動方向記錄、非情景摘要，已隨因果表一起砍除。

### 模型配置（`Core_Settings.gs`）— 🚀2026-09 玩家定案「只要這兩顆」
```
AI_MODEL       = google/gemini-3.5-flash-lite  (屬性 MODEL)           ← 兩軌共用主力
FALLBACK_MODEL = x-ai/grok-4.20                (屬性 FALLBACK_MODEL)  ← 被擋才自動換的後援
```
- **後援是全域行為、不是呼叫端特例**：`callGeminiAPI` 裡 `config.fallbackModel || FALLBACK_MODEL`，任何路徑被擋死都有救。`actionPlay` 的 `aiConfig` 只寫 `model: AI_MODEL`＋`retries = 1`（Gemini 試一次、真被擋才交棒，不浪費柔化重試）。
- ⚠ `SOLO_MODEL`／`UNLOCKED_MODEL` 已整組刪除，同名指令碼屬性從此無效；換模型只改 `MODEL`／`FALLBACK_MODEL` 兩個屬性（或 `Core_Settings.gs` 的預設字面）。
- **`driveOn`(點火/主動掌握) 只控敘事推進幅度的 `driveStr`、不再切模型**。⚠ **2026-07 玩家「一直步步逼近都不做」重寫**：`driveStr`＋敘事終極警告的點火分支原本框架是「把玩家逼向毫無招架餘地／堵退路／想跑也跑不掉」＋「不必每回合寫到終點」——這等於授權 AI 永遠停在「快要、就差一步」空轉。改成**「主動且明確地推進到真的發生」**取向（該親就親、該進一步就進一步、嚴禁在曖昧邊緣反覆空轉），保留全部好感天花板/角色一致性/不真正傷害護欄，只把「逼近但不做」的空轉框架換掉。點火≠壓迫鋪陳，點火＝她主導、實際往前推到位。
- 採樣：`temperature 1.08, top_p 0.97, top_k 60, repetition_penalty 1.12, presence/frequency_penalty 0.25, max_tokens 2400（時間轉場且無人在場 700）`。⚠ 後四顆旋鈕 gemini-lite 被 OpenRouter 靜默忽略（原壓重複用）——若 Gemini 跳針/套路化，需另想防重複提示詞手段。
- 歷史：`getGameHistoryBatchRaw(pcId, 6)`（6筆＝3輪）。

### `callGeminiAPI` 失敗行為（`Engine_Combat.gs`·兩軌共用·只讀不改）
被 NSFW 擋（`PROHIBITED_CONTENT/SAFETY`/空 content）→ 同模型內首次觸發加 `softenSuffix` 降階柔化重試 → 整組失敗且有 `fallbackModel` 才換模型再跑一輪 → 最終被擋回「🌸結界觸發」、一般錯誤回「🌫️因果紊亂」。

## §「鬼打牆」的根因：歷史那條規則被砍掉了（2026-09）

**玩家原話**：「超級怪!! 我的話也擴寫太多了吧，櫻話很少沒錯，但我跟鬼打牆一樣!!!!!」
＋「而且我沒有跟她同行，我是去那個地點找她，劇情又變成一起」
＋「是不是要『現在沒有同行人／目前地點有誰誰誰』這樣呢?」

### 把四段實測文字疊起來比對

```
米白色針織衫              ×4
甜得發沉的氣質            ×4
胸口起伏／浪濤聲          ×3
雖然方向感完全沒轍        ×3
深紫長髮滑落耳側          ×3
「如果塔上的草莓太甜…量體重很討厭」  第2回合說過，第3回合【一字不差再講一遍】
```

玩家一句「輕聲問她喜歡什麼形狀的貝殼」被擴寫成 400 字，其中八成在重播上一回合。

### 根因：鑑賞整份 system 沒有任何一條說「歷史是已經發生過的事」

那條原本住在 `continuity` 模組裡，**2026-09 大精簡時整格砍掉**（當時歸類成「筆法指導」）。
但它其實是**事實陳述**不是筆法——少了它，模型看到自己上一輪寫的 500 字就順著同一個調子
把同一個場景再描述一次。solo 的 `miniSystem` 第 5 條一直都有這句，只有鑑賞這一軌沒有。

補回來成固定模組 `history`：
> 上面的對話歷史是已經結束的事，只供語氣與細節連貫；這一回合要寫的，是玩家這一步【接下來】發生的那一段。

同一批補的兩條，都是為了止住「每回合重述同一批細節」：
- `perform` 加一句：★卡上寫的是這個人【一直以來】的樣子，不是這一回合新發生的事。
- 台詞擴寫加界線：★擴寫的範圍就是這一句話。（原本只說「可以擴寫」，結果一句被擴成整幕。）

### 「我是去找她，劇情變成一起來」——在場改判準時漏掉的配套

`pPresenceStr` 在 `moveTarget` 的回合一律講「【與你結伴一起來到】這裡」，但在場改成同地點之後，
**那句話只對同行者成立**——本來就站在那裡的人是你找過來遇到的。現在分兩種：

| | |
|---|---|
| 同行者 ＋ 移動 | 【與你結伴一起來到】這裡(一路同行) |
| 非同行 ＋ 移動 | 你走進來的時候，【本來就在這裡】(在這裡做自己的事，是你找過來的) |

### 同行與在場講明白（玩家提案）

【他們此刻】那一段的抬頭改成玩家提的寫法：
- 有同行：`同行中：凜（一路跟著你走）。這個地方此刻還有：櫻（本來就在這裡）。`
- 沒同行：`現在沒有人跟你同行。這個地方此刻有：櫻、SABER（他們本來就在這裡）。`
- 都沒有：`現在沒有人跟你同行，這個地方也沒有別人，你是一個人。`

⚠ 刻意留在 **user**：同行名單隨時會變，放 system 會把整段快取前綴拖下水。

## §「實測回報第二輪」（2026-09）

玩家貼了兩段真的玩出來的文字，六件事一次打開。

### `SABER 女「…」` — 名字跟性別黏在一起

卡片寫的是 `【在場人物】SABER，女。`，模型把「SABER 女」整組當成**冠名**寫進對話。
改成 `【在場人物】SABER（女）。`——括號讓性別明確是屬性而不是名字的一部分，
`perform` 那格與對話格式的冠名規則同步改口。

### 「劇情不需要解說角色為什麼這麼做」

實測文字裡出現「我心裡暗自覺得有趣，開始思考下一個問題要怎麼問才能讓她多露出不為人知的那一面」
「我總是選擇讓對方感到自在」——**AI 把卡上的準則與性格原樣當成內心獨白背出來**。
這正是紅線②（show-don't-tell），而大精簡之後鑑賞這一軌把它砍掉了，只剩 solo 的 `servantCard_` 還有。

補回來，但寫成**事實**而不是演法（玩家：「告訴她意思、事實，不要教他該怎麼做」）：
> ★卡上這些句子只給你看，在場的人並不知道自己被這樣寫著——讓它們從舉動、語氣與選擇裡透出來。

### 篇幅多一檔「隨意」

玩家：「還是我字數設太高，他只能拿設定掰了？能夠讓他自由決定字數嗎？平淡就平淡？」
——指定 500 字，平淡的一幕就只能拿設定來湊滿。新增 `free` 檔：**★【篇幅】那一行整個不送**，
長短交給 AI 依這一幕自己決定。（`auto` 維持原行為：依這回合有沒有大事調。）

### 地點上看不到有誰

玩家：「開局 4 位直接不見…不對，是地點上沒有顯示這裡有人！！」
在場＝同地點之後，地點旁邊不標人，玩家就看不出要去哪找誰。
`get_tags` 補下傳 `myPeople`（跟 `myPlaces` 同一趟，零額外 round-trip），
地圖每一列後面標 `👥 凜、櫻`。

### 同行鈕搬到左邊的從者卡上

玩家：「應該要把同行放到左邊的詳細狀態跟關係那邊吧…不然我還要打開誰在哪這個畫面」。
`servants[]` 補一個 `party` 旗標，卡上那一排變成 `📜 詳細狀態｜💞 關係｜🫂 同行/解散`。

### 邀請清單還看得到已經加入的人

`_kcHeroesAvailable` 的去重比的是**名字**，但已入駐的人用日常短名（凜／櫻／SABER）、
英靈殿是全名（遠坂凜…），**永遠對不上**。改比 `srcId`（【英靈源】戳記＝來源種子 id，
撞名守門本來就靠它）；舊存檔沒戳記的才退回比名字。

### 開場白跟世界前提打架

`【日常】聖杯戰爭結束後的冬木。` ——而這個世界的前提是**從來沒有發生過聖杯戰爭**
（`world` 那格寫著「和平的現代冬木市」）。玩家第一眼看到的就是矛盾的設定。改成「和平的現代冬木市。」

### 外貌被砍字

`TRAIT_SEG_HINT_`（講給 AI 聽）11 ／ `TRAIT_SEG_MAX_`（真的截）18——**兩個數字貼太近**，
AI 多寫兩個字就被切在句子中間，玩家看到半截話。改成 14 / 22，留 8 字緩衝：
小幅超出照樣完整，真的離譜才截。

## §「在場＝同地點、第一人稱、尺度跟著玩家走」（2026-09）

**玩家原話**：「左邊應該要顯示這世界上有誰、在哪裡才對。我剛剛進去玩，全部人在客廳，但是只有
大河在跟我說話；我想找凜，他說他在凜的家，整個很錯亂」＋「我只是要日常聊天，結果進去每個姐姐
都要推倒我色色」＋「想敘述是第一人稱的玩家視角」＋「NPC 叫玩家的 妳/你 要確實依照性別」。

### 三個症狀，一個根因

`Gallery.gs` 起始住民那行寫著「起始住民就落在玩家開局的地方（位置模擬拆掉後不再骰去向）」——
四位起始住民**全部落在同一格**。而【在場人物】只收同行名單，所以另外三個明明在同一間房卻不存在
於敘事；世界概況又只給名字不給地點，AI 只好自己編一個「凜的家」出來。
2026-09 拆掉位置模擬之後，**地點變成了一個存在於資料裡、卻沒有人維護也沒有送給 AI 的半殘欄位**。

### 現在的規則

| 概念 | 意思 |
|---|---|
| **在場** | 站在**同一個地點**的人。走到哪就遇到誰 |
| **同行** | 她會**跟著你走**（上限 `KANSHOU_PARTY_MAX_`＝3） |
| **解散** | 離開同行，**人留在你解散她的那個地方**（玩家原話：「我 A 解散他，他會一直在 A」） |
| **🚶去找** | 你走過去（走既有的 `play + moveTarget`，刻意不另開 action） |
| **🙋叫來** | 她走過來（`kanshou_party` 的 `op:'bring'`，**不動同行名單**） |
| **🚪離開** | 請她離開這座城，空出一個世界位子（`op:'evict'`·不可逆·前端問兩次） |
| **世界人數** | `KANSHOU_WORLD_MAX_`＝8（起始 4 ＋ 自己邀 4） |

⚠ **刻意不另設在場上限**：在場＝同地點，而人數本來就被世界上限封頂，少一個要維護的數字。
⚠ `npc_exit`（AI 說某人告辭了）現在連 **LOC 一起改到別的地方**——只移出同行名單不夠，
人還站在這裡就還在場，「嘴上說走卻還在場」會原封不動長回來。

面板入口從「👥 邀請」改名「👥 誰在哪」——玩家找不到同行/解散，是因為那個名字讓人以為只能邀新人。

### 第一人稱玩家視角

2026-09 稍早才從第一人稱改成第二人稱（玩家「我覺得不能第一人稱，ai 根本分不清楚要扮演誰」），
現在改回來。當時的根因是「角色的自稱『我』跟旁白的『我』是同一個字」，而那兩個前提都不在了：
**自稱欄（`firstP`）已整組退休**、**對話一律冠說話者名**、模型也從 gemini 換成 grok。

翻面清單：`voice`／`narration` 與 `memory` 的 schema／【我自己】／【在我身邊的人】／
我們一起走過／我們還只是初識／她是我的○○／我在她眼中／這座城裡還有誰。
`kanshouLocNameForAI_` 的「我的房間→你的房間」翻譯**整個拿掉**——第一人稱之後資料鍵本來就對得上。

**NPC 當面叫玩家的字**：中文的「你／妳」也分性別，新增 `pronYou_(sex)`（`PRONOUN_YOU_`），
跟 `pron_` 一樣從資料算、不在提示詞裡寫死；【我自己】那張卡帶一句「在場的人當面叫我是『妳』」。

### 尺度跟著玩家走

舊的 `lewd` 預設是「情慾場面放到最色、寫滿寫透」——**沒有任何條件**，模型把「情慾場面」讀成了
要生產的東西。改成「玩家在聊天就好好聊天…玩家真的伸出手了，才順著往下走」，
真進到情慾場面時的那半字字保留（玩家：「現在尺度超色是很棒」）。

同一批修掉的還有**對話格式的例句**：「話語/喘息/悶哼/笑聲/吸吮/吞嚥」——四個例子裡三個是性的，
每回合都在幫模型定調。換成「話語/笑聲/嘆息/悶哼」。照玩家自己的規矩：「說越多它會越想歪」。

### 演法那一格：兩次修正，同一個方向

我原本寫的是「怎麼演」（外貌拆成鏡頭、性格兩句互相拉扯、台詞守前半行動走後半）。玩家兩次打回：

1. 「讓它們互相拉扯…? 這不用提示吧，他會一直拉扯，很怪…**因為這是每次都會給 AI 所以要避免固化**」
2. 「**告訴她意思、事實，不要教他該怎麼做**」

所以現在那一格**只下定義**：「在場那幾張卡，每個人的句子依序是：名字與性別、平常看得到的性格、
熟了才看得到的那一面、喜歡的、討厭的、外貌與氣質、怪癖、做選擇的方式、在這座城裡的身分。」
怎麼用、什麼時候用交給模型——這也正是這一軌「全靠 AI 即興」的前提。

同一批抓到 ★【誰在場】的「每一位都要有反應」：五個人同場就變成每回合點名輪流。
改成「被搭話、被牽涉到的人給出反應，其餘的人在場、做自己的事就好」。

已補第 20 道掃描器 `check_standing.py`（見 CLAUDE.md）。⚠ 它上線後第一件抓到的，
就是我為了第一人稱剛寫的「在場的人**一律**用名字稱呼」——連我自己都會再犯一次。

### 玩家的台詞可以擴寫

舊的寫法是「★玩家的台詞免冠名、**照原句一字不動**」。玩家打回：
「這是可以依照玩家角色的卡片進行擴寫＋動作的！」「**但不能偏移語意太多！**」

改成：「★我的台詞免冠名，可以依【我自己】那張卡擴寫成完整的一句、補上動作與神態，
**語意跟原句一樣**。」`agency` 那條也跟著鬆綁——它要守的是「這一步做什麼由玩家的輸入決定」，
不是「一個字都不能動」。

## §「人物卡搬進 system 吃快取」（2026-09）

**玩家原話**：「鑑賞人物卡搬進去快取吧，要確保玩家無法修改其他角色的資料！！！」
＋「變動的資料、能新增的資料都拉到 USER 區，以後也是這樣？」

### 規矩（以後都照這個）

| 放哪 | 收什麼 | 為什麼 |
|---|---|---|
| **system** | 整局**不會變**的：鐵律、演法、輸出範本、在場那幾位的**六格人設** | 提示詞快取是逐 token 比對前綴，只有每回合逐字相同的東西吃得到 0.25 倍折扣 |
| **user** | 每回合**會動**的：穿著、在場來由、相處階、關係稱呼、共同回憶、她眼中的你、地點、時間、世界帳本、篇幅、收尾 | 它們本來就每回合不同，放 system 只會把整段快取拖下水 |

⚠ **一個會變的東西插在中間，它後面全部作廢**——這條 2250 行那段註解早就寫著，現在它管的範圍
從 user 擴到 system。

### 量測

| | 搬之前 | 搬之後 |
|---|---|---|
| system | 1095 | **1498**（＋六格人設 403） |
| user | 1039 | **692** |
| chatHistory | 946 | 946 |
| 輸入合計 | 2992 | 3136 |
| **快取命中時的等效輸入** | **2171** | **2009** |

⚠ **總 token 沒有變多**（3136 vs 2992 的差是卡片格式微調）。變的是【計費】：
system 那 1498 字在命中時算 0.25 倍。第一回合沒有折扣，但 **Grok 的快取寫入免費**，
所以第一回合跟搬之前一樣貴，不會更貴。

### 換人會怎樣（探針 `kspot.js` 逐條釘住）

同行名單是**插入順序**（`add` 是 push 到尾巴、`drop` 是 filter 保序），所以：

| 動作 | 快取 |
|---|---|
| 不換人 | ✅ 完全命中（探針：連打四回合 system 一個字都沒變） |
| **加一個人** | ✅ 接在尾巴 → **前面幾張卡的前綴原封不動，照樣命中**，只有新增那段沒折扣 |
| 移除最後一位 | ✅ 前綴仍相同 |
| 移除第一/中間那位 | ❌ 從那個點之後作廢，**那一回合**重建（寫入免費，下一回合又開始命中） |

### 安全：AI 只寫得到「同一局 ＋ 是同伴 ＋ 此刻在場」的人

搬進 system 是**唯讀的提示詞組裝**，沒有新增任何寫入路徑。既有的三道閘原封不動：

- `kanshouIsAlly_(row, myGameId)`：`FACTION === '從者'`（**玩家自己那列永遠寫不到**）
  ＋ `GAME_ID === myGameId`（**別人的局寫不到**）＋ 不是 `DEAD_`
- 再加一道 `LOC === curL`（**不在場的人寫不到**）
- `npc_exit` 只在 `partyRows`（已過 gid 過濾）裡找，且只改玩家自己那列的【同行】標記

讀取端同理：`kanshouPartyCards_` 組卡時走的是同一支 `kanshouIsAlly_(row, myGameId)`，
所以**別局的同伴、別的玩家本人，都不可能出現在我的 system 裡**。
鑑賞眾生是全帳號共用一張表，撞名是常態不是巧合（實測兩個帳號各召一個 SABER），
`kspot.js` 第 ⑤ 段就是在釘這件事。

### 聚光燈退休

原本「這一步沒被點名的人只送精簡卡」（省 34%）已移除——system 必須逐字相同，沒辦法逐回合修剪。
而且它反而更貴：全給是 0.25 倍，修剪過的短卡留在 user 是 1.0 倍。
沒命中的那一回合兩者打平，所以**新做法從來不會更差**。

## §「鑑賞清場：砍死碼、拆 actionPlay_」（2026-09）

**玩家原話**：「我在想…是不是整個鑑賞要重新建構比較好，一堆遺留的東西根本用不到，超級亂的」

### 先量再答：「一堆遺留」不成立，亂的是一個 705 行的函式

| 量的東西 | 結果 |
|---|---|
| Gallery.gs 的 85 個函式 | 真的沒人叫的只有 **2 個** |
| 46 個常數 | 沒人讀的只有 **3 個** |
| 相容舊存檔／遷移的碼 | 總共 **54 行** |
| `actionPlay_` | **705 行＝整檔的 27%** ← 真正的問題 |

**不重寫的理由**：36 支探針 ＋ 18 道掃描器守的不變式要全部重建，而它們守的正是踩過的坑
（分隔符、戰時口吻洩漏、階段表蓋掉角色底色、種子沒人吃、提示詞否定式、扣了 AP 戳記沒落地、
敘事排版壞四天沒人發現）。重寫最可能的結果是那些坑一個一個長回來，而且一樣零錯誤訊息。

### 「亂」的真正形狀是孤兒註解

最誇張的一塊：連續七行註解在描述**七個已經不存在的東西**（她主動的調校依據、只有她走來找你
要看時鐘、側寫節流、醒著陪同標記、日常落點保底池、池子空掉的保底落點），卡在一個完全無關的
`kanshouSanitizeTagValue_` 上面。下一個失憶的我讀到會直接被誤導。
另外 `actionPlay_` 自己內部也長死碼：`kanshouSceneLoc_`／`_reHourAfter`／`kanshouReBand_`
三個算完就丟，全樹只有定義那一行。

### 拆成四段（行為零改動，探針全程當安全網）

```
actionPlay_ 705 → 442 行
  ├─ kanshouAdvanceClock_            101 行  結束一天／兩段式就寢／時段跳躍／每回合流動
  ├─ kanshouPartyCards_               67 行  聚光燈／在場來由／六格人設
  ├─ kanshouApplyIntimacyFeedback_   117 行  AI 回報的狀態落盤（七種回寫）
  └─ relMemMemoryStr_（提到檔案層）
```

### ⚠ 拆的時候踩到的坑：`ctx` 漏傳一個鍵是【靜默】的

把回寫那段搬出去時漏傳了 `curL`，於是每位在場者的地點比對都拿 `undefined` 去比，
**整段 NPC 回寫靜靜失效**——而它住在 `actionPlay_` 的 try/catch 裡，連例外都不會冒出來。
`check.sh` 全綠、部署會成功，玩家只會覺得「AI 寫的關係稱呼怎麼都沒存到」。
是 `kbody.js`／`reltag.js` 兩支探針抓到的。

已補第 19 道掃描器 **`check_ctx.py`**：函式體裡出現 `ctx.X` 就把 X 記下來，
再找所有 `函式名({ … })` 的字面呼叫端逐一比對鍵。附自我退化測試，
並且已反向驗證——真的把 `curL` 拔掉它會叫。

## §「點火退休 ＋ 模型分軌 ＋ 快取量測」（2026-09）

**玩家原話**：「SOLO 繼續 GEMINI，補魔換 GROK，鑑賞直接 GROK 了？點火拿掉吧？」
＋「我有辦法用快取嗎？」

### 點火（`driveOn`／`driveStr`）整組退休

它最後只剩一句話：「🔥【主動掌握】：尺度一律以【親密尺度五階】為準…」——
而**親密尺度五階 2026-09 已經隨好感整組砍除**。等於每次點火都在叫 AI 以一個不存在的東西為準，
零錯誤訊息。而露骨程度現在由玩家自己的 `lewd` 旋鈕管（「放到最色、寫滿寫透」，恆開），
點火那句是多的。連帶移除：前端 `#drive-mode-toggle` 開關、`play` 的 `drive` 欄、
`_kanshouBigBeat_` 的 driveOn 分支（現在只看 `kanshouNightSceneStr`）。

### 模型分軌

| 軌 | 模型 | 常數 |
|---|---|---|
| solo 敘事 | Gemini | `AI_MODEL`（預設 `google/gemini-3.5-flash`） |
| 鑑賞 | Grok | `LEWD_MODEL` |
| 補魔三支 | Grok | `LEWD_MODEL` |
| 一般後援 | Grok | `FALLBACK_MODEL`（必須跟 `AI_MODEL` 不同顆） |
| NSFW 後援 | **預設不設** | `LEWD_FALLBACK_MODEL`（空＝被擋就不打第二輪） |

⚠ 型號 id 全部走指令碼屬性，程式碼裡只是預設值。**改 id 之前先確認那顆真的存在**——
打錯的話每一次 AI 呼叫都會失敗，而玩家看到的只會是「命運的絲線忽地紊亂」。

### 降階重試整組拿掉

`softenSuffix`（「改用更含蓄典雅的筆法…避免直白器官名稱與動作描寫」）已移除。三個理由：
① 它跟 `lewd` 旋鈕正面衝突——玩家要「寫滿寫透」，降階卻在同一次請求裡改成「意境留白」，而且零提示；
② 它是**否定式寫法**，正是 `check_prompt` 在擋的形狀，躲在共用層所以一直沒被抓到；
③ 被審查擋下後還原地重試 5 次、每次退避兩秒——**玩家要乾等 10 秒**，
而程式碼自己的註解就寫著「原樣重送對攔截無意義」。
現在：攔截 → 立刻 `break`，交給後援；NSFW 軌的後援預設是空的，所以直接給失敗提示，不再白等。
（連線錯誤仍照舊退避重試、走一般後援——那是真的可能換一顆就成功。）

### 快取量測

`payload` 加了 `usage: { include: true }` 與 `session_id`：

- **`usage`** 把 `prompt_tokens_details.cached_tokens` / `cache_write_tokens` 帶回來，
  `logCacheUsage_` 印一行 `[cache] 模型 prompt=N cached=N write=N hit=N%`。
- **`session_id`**（鑑賞 `k_<gameId>`／solo `s_<pcId>`）：OpenRouter 的黏著路由靠它把同一局的
  連續請求釘在同一個端點。⚠ **沒有它的話「要等偵測到快取命中才啟動」**——每局開頭那幾回合都在賭。
  順帶讓 OpenRouter 的 Logs 把同一局歸成一組。

**探針量到的事實**（`scratchpad/size/cache.js`、`cachesplit.js`）：

| 區塊 | 字數 | 能不能快取 |
|---|---|---|
| system | 1095 | ✅ 五回合逐字相同 |
| chatHistory | 936 | ❌ 固定 4 筆滑動視窗，第 3 回合起每次都換掉最前面那筆 |
| user | 961 | ❌ 每回合不同 |

只有 system 進快取、讀取算 0.25 倍 → 輸入等效 2992 → 2171 字（省 28%）。

### ⛔ 結案（2026-09）：xAI 沒有給我們前綴快取

玩家連打四回合、間隔 23~44 秒，四行完全一致：

```
@xAI prompt=1855 cached=128 hit=7% sys=1369字/a12c3ba7
@xAI prompt=2588 cached=128 hit=5% sys=1369字/a12c3ba7
@xAI prompt=3254 cached=128 hit=4% sys=1369字/a12c3ba7
@xAI prompt=3178 cached=128 hit=4% sys=1369字/a12c3ba7
```

三個變因全部排除：**不是 TTL**（間隔夠短）、**不是我們的前綴在變**（雜湊逐次相同）、
**不是換上游**（一直 @xAI）。prompt 從 1855 到 3254，`cached` 永遠是 128——那是底噪不是命中。

**結論：不要再為快取調架構。** system／user 的切分留著（那個切分本身是對的：穩定的跟會動的
分開，讀起來也清楚），量測那幾行也留著（零成本，哪天他們開了會自己顯示）。

**以下兩件原本要做的，取消**：

**還沒做的兩件（等量到命中率再決定）**：
1. **把六格人設搬進 system**：實測那部分整局永遠不變，3 人同場共 341 字。搬完 system 1436／user 620，
   再省 12%。⚠ 代價是 `_spotlight_` 聚光燈（沒被點名的人只送精簡卡、省 34%）會失效——
   system 必須每回合一樣，所以只能一律送完整卡。**有命中才划算，沒命中是純虧。**
2. **歷史視窗改成「長到 8 筆才砍回 4 筆」**：那樣 4 回合有 3 回合的 system＋history 是同一個前綴，
   可快取段 37% → 69%。代價是平均歷史長度 4→6 筆。

## §「六格人設」（2026-09）

**玩家原話**：「自稱和語癖 這可以砍了 這不該是我們要求的? 我們是要設定角色 讓AI演活他 不要太多設定!!」
→「妳覺得 人物如何? 鮮活嗎?」→「鑑賞需要這些經歷嗎?」→「看看有甚麼是適合我們用的」

### 現在一位角色只有六格

| 格 | 欄位 | 判準 |
|---|---|---|
| 外貌 | `dailyLook`（2 段） | 外貌本相（不含衣服）、氣質意象。⚠ 舊的第 3 段「日常口氣」已退休，`DAILY_LOOK_SLOTS_` 3→2 |
| 打扮 | `dailyOutfit` | 一句日常穿搭 |
| 性格 | `dailyWords`（4 格） | 平常／熟了之後／喜歡／討厭。**喜歡與討厭 2026-09 回到卡上**（見下） |
| 怪癖 | `quirks`（2 格） | 一個看得見的習慣動作 ＋ 一個應付不來的領域。由舊的 `tic` 升級 |
| 準則 | `logic` | **做選擇的方式**：把兩件都想要的東西擺在一起，說出最後放掉哪一個 |
| 城裡身分 | `dailyBack` | 身分／在哪／跟誰有關係。**不是傳說經歷** |

已退休：`firstP`（自稱）、`speech`（語癖）。語癖跟性格欄講的是同一件事——凜的口吻「毒舌卻藏著關心」
＝性格②「刀子嘴豆腐心」，同一個事實存兩處。

### 為什麼「經歷」要改成「在這座城裡是誰」

鑑賞世界的前提是**從未發生過聖杯戰爭**，所以「十二道試煉都活了下來」這種傳說經歷放進來，
等於一邊說沒有戰爭、一邊要 AI 演一個被戰爭壓著的人。

改版前逐筆檢查 22 筆 `dailyBack`：**8 筆有用、9 筆只是把性格再抄一遍、5 筆是空的**。
有用的那 8 筆長得一模一樣——身分＋在哪＋跟誰有關係（「遠坂家現任當家，妹妹是間桐櫻」
「在深山町經營小工房」）。失效的那 9 筆全是形容詞（「正直守序，生活規律自持」）。

這一格還有第二個作用：**它是世界帳本的起點**。鑑賞起手地圖只有 5 個地方，其餘靠 AI 邊玩邊長；
一個「在商店街尾開裁縫鋪」的人，AI 馬上有地方可去、有事可發生，一個「沉靜寡言」的人給不出任何東西。
角色之間的連線（阿爾托莉雅→士郎、恩奇都→吉爾伽美什、凜→櫻）也是靠這一格長出來的，不必另設一欄。

### 演法（`perform` 風格模組）

卡片是**自然語言、不帶欄位名**（2026-09 玩家「有需要這麼生硬嗎」之後改的），所以
一張「欄位定義表」會定義到 AI 根本看不見的東西。真正該加的是**演法**——拿到卡上那幾句沒有名字的話
要怎麼演。這一條寫進鐵律（`KANSHOU_STYLE_MODULES_` 的 `perform`，固定段、約 126 字）：

> 卡上那幾句是給你取景與拿捏分寸用的：外貌拆成鏡頭裡的細節；性格開頭兩句同時在場，一句在表面
> 一句在底下，讓它們互相拉扯；喜歡與討厭是這個人的開關，話題碰到就給出明顯的情緒；
> 做選擇的那一句，台詞守前半、行動走後半；怪癖在情緒起伏或想掩飾的時候浮出來。

⚠ **玩家提案裡被否決的三件事**，寫在這裡免得下次又加回來：
1. **`archetype`（傲嬌／病嬌／大和撫子）** — 同一個事實在一張卡裡講四遍（archetype ＝ 性格② ＝ 準則），
   單一真實來源直接破掉；而且標籤會讓模型去演「傲嬌這個類型」而不是演凜，區別度就是這樣消失的。
2. **「社交面具／內在核心／精神防線」那組標籤** — 已經驗證過會壞，見 `formatPref` 上面那段註解：
   「表面／面具」語意是【裝出來的】，但 SABER 是真的一絲不苟，那個詞會把她演成在演戲；
   套上去讀起來也不成話（「個性完美的優等生」「表面大姊頭般罩著大家」）。22 位裡赫拉克勒斯的
   「沉默寡言」、恩奇都的「平和無垢」都不是面具。**「喜歡／討厭」則反過來可以貼**——那兩個詞是
   中文本來就有的講法、不是欄位名，貼上去仍讀得成一句話。
3. **「每 1-2 回合必須在括號內融入怪癖」** — 定頻率＝強迫 AI 每回合表演一個動作，正是
   `check_seed` 第 ⑪ 道擋的那個形狀（玩家原話「這就是強迫 AI 這樣扮演吧」）。改成「情緒起伏或
   想掩飾的時候浮出來」，讓它自己挑時機。

### 喜歡／討厭回到卡上

2026-07 砍掉是因為「分這麼細，AI 也沒辦法演出來」——那個判斷在當時是對的，因為那兩格只是被列出來、
AI 不知道要拿它們幹嘛。現在演法那條給了它們工作（「話題碰到就給出明顯的情緒」），才值得付那 33 字
（同行上限 3 人）。

### 狂化改讀技能資料

`servantIsMad_(row)` 讀同一列 `COL.PC.TAGS` 的技能 `fx === 'mad'`，不再比對口吻字串
（語癖退休後那個字串來源沒了）。⚠ **附帶效果：所有 Berserker 都會命中**——職階自動附贈狂化 C，
不再只有種子裡的赫拉克勒斯與蘭斯洛特兩位。會說話的狂戰士（開膛手傑克那型）在這個遊戲裡不存在。

### 機器擋（`check_seed.py` 第 ⑬ 道）

- 準則沒有取捨語氣詞（卻/但/還是/最後/反而/寧可/只/先/選/讓/放/挑/比）就叫——「重視朋友」那種
  單向形容詞接不住沒寫過的情境，而這一欄存在的理由就是要接住它們。
- 怪癖不是剛好兩格就叫（一格會退化回舊的 `tic`）。
- 「在這座城裡是誰」沒有身分/地點/關係實詞就叫（全形容詞＝把性格再抄一遍）。

### 量測

| | 改版前 | 改版後 |
|---|---|---|
| SYSTEM | 968 | 1095（＋演法 126） |
| USER | 902 | 1039（3 張卡各 ＋45） |
| chatHistory | 946 | 946 |
| **輸入合計** | **2816** | **3080** |
| 比例 | 6.1 : 1 | 6.7 : 1 |

⚠ 這次是**刻意往回加字**。砍掉的（自稱/語癖/日常口氣）約省 30 字，加上的（怪癖/準則/喜歡討厭/演法）
約 290 字。換到的是「AI 知道每一格要怎麼演，以及角色遇到沒寫過的情境往哪邊走」。

## §「旁白改第二人稱『你』」＋「自稱退休」（2026-09）

**玩家原話**：「我覺得不能第一人稱 ai根本分不清楚 要扮演誰？？？」／「是不是 取消自稱這個設定....感覺太細 很亂」

### 旁白人稱翻面

`nsfwBaseRules` 開頭與 ★【視角鎖定】原本都寫「第一人稱「我」」，但**場上每個角色引號內的台詞自稱也是「我」**
——同一個字在同一份提示詞裡指兩個不同的人。歷來的補法全是加註記去繞（同伴卡 `[台詞自稱]` 加括號限定範圍、
`inner_monologue` 特地要求第三人稱、常駐規則區再重申一次「旁白的我永遠是玩家」），沒有一個是解掉根因。
旁白改「你」後衝突不存在，那些註記一併拆掉。

鑑賞這一軌動到 8 處：`nsfwBaseRules` 開頭、schema 的 `narration`／`memory`、`photo_caption` 規則、
★【視角鎖定】、🚨【收尾】的「收在「我」的動作上」、★【再往前的經過】、以及爽約寫進 MEMOIR 的那句系統文案。

⚠ **紅線① 程序**：改前量了現況（system 1700 字），改後跑了全套鑑賞探針
（`k` / `k_male` / `k_sex2`（女玩家×男同伴、男玩家×女同伴兩組配對都確認沒被改壞）/ `k_verify` / `k_cd` / `k_flow` / `k_hist`）
＋等效重複子句檢查（原 `dedup.js` 已不在 repo，改用臨時腳本：74 條子句、完全重複 0、近似重複全是同場多人的同款狀態句）。
system 1700 → 1724 字。

⚠ **近期摘要餵回去的是玩家自己打的字**（天然「我替她倒了杯茶」），已在該行補一句括號說明，
免得模型把它當旁白範例鏡射。

### 自稱退休（特徵 4 格 → 3 格）

鑑賞這邊的自稱**本來就重複兩次**：`heroToKanshouRow_` 把 `dailyLook` 第3段抽進 MEMORY 的【口吻】，
同一段又原封不動當成特徵第3格 `[台詞自稱]`（`formatTrait`）。`pFlavorStr` 甚至有一段
`_traitSpeech` 判斷「兩者相同就不印口吻」——那個 workaround 現在整段拆掉了。

現況：
- **特徵 2 格** = 外貌本相／氣質舉止。⚠ 原第三格「卸下心防的私密一面」2026-09 整組退休（`TRAIT_SLOTS_` 已收成 2），`formatTrait` 只送 `[外貌氣質]`。
- **口吻**永遠印（來源：MEMORY【口吻】→`dailySpeechByName_` 退回英靈殿 `DAILY_LOOK` 第3段）。
- **`dailyLook` 仍是 4 段**（外貌／氣質／**日常口吻**／私密面）——第3段是口吻的唯一來源，`parts.length>=4` 三處硬依賴不動。
  種子庫 17 個零資訊量的「自稱我・」前綴刪掉，6 個有特色的（俺／拙者／吾／余／我們／本小姐）保留在口吻裡。
- `translateLookToDaily_`（工房/自訂角色轉日常版）第3段的指示從固定格式「自稱「X」，…」改成
  「日常口氣（自稱不是尋常的「我」才寫進這句）」。
- 舊局相容：`traitParts_`（GAS）／`traitSegs_`（`Script.html`）讀到 4 段就地剝掉 index 2，**不改存進表裡的值**。


---

## §「全面出戲稽核」（2026-09）

玩家：「全體 慢慢檢查一遍 看哪裡怪怪的，不可以出戲！重點的檢查鑑賞模式」

拜訪別人家／同居邀約）真正送出去的 system+user 全部收下來，再拿一份違和詞表掃全文。
掃出來的東西**沒有一個是靠讀原始碼會發現的**——都得把提示詞真的組出來才看得見。

### A. 世界觀自打嘴巴

第一行寫「★世界觀＝…**沒有魔術與從者**」，往下三行就冒出聖杯戰爭詞彙：

| 洩漏點 | 原本送出去的字 | 現在 |
|---|---|---|
| `kanshouLocContextForAI_` ×3 分支 | 「**御主**自己的家…的私人房間」 | 「你自己的家…」 |
| schema `name` 欄 | 「不填暱稱/**職階**/台詞/地名」 | 「不填暱稱/稱號/台詞/地名」 |
| 鑑賞御主創角提示詞 | 「與身邊**英靈**共度…的「**御主**」本人形象」 | 「與身邊夥伴共度…的主角本人形象」 |
| `kanshouRoomDisplayName_` fallback | `"御主" + '的房間'` | `"自己" + '的房間'` |

### B. 「GAS」四個字母直接寫在提示詞標題裡

`★【提議·相約·GAS已裁定】`／`同去`／`牽手`／`【結果·GAS 已裁定】`。
既是三個拉丁字母（跟「全程繁體中文」的語言鐵律互斥），又是我們後端的名字——抄進敘事就是最徹底的出戲。
同一句後半本來就寫「系統已依好感…裁定」，統一成「系統」。solo 的示好提示詞也有一處，一併改掉。

### C. 「TA」

`關係:TA是你的○○` 每位在場者印一次。同樣是拉丁字母＋非台灣用語。
**資料裡本來就有【性別】欄**——改成 `pron_(sex)` 直接印「她/他」，既不是外來寫法、也更精確。

### D. 地名「我的房間」是第一人稱

資料鍵叫「我的房間」，而旁白剛改成第二人稱——等於一邊說「旁白不可用『我』」、一邊遞給它一個叫
「我的房間」的地方。新增 `kanshouLocNameForAI_〔已移除〕（)`：**只改餵 AI 的字面**（→「你的房間」），　※（2026-09 地點整組退休·已移除，見 KANSHOU_REFERENCE.md）
存表／比對／前端一律仍用原鍵（改鍵會動到存檔、住處骰點、前端分頁）。
四個把 `curL` 寫進提示詞的點都走它。

### E. 時鐘顯示「1年12月20日」

`kanshouAbsDayToDate_` 的 `year = 絕對天數/365 + 1`，從來沒有起算年份——
開局玩家的時鐘與送給 AI 的日期都在講一個不存在的年份。新增 `KANSHOU_CAL_START_YEAR_ = 2005`。
⚠ 週年是用 absDay 差算的，不吃 `.year`，所以改基準年只動顯示字串。

### F. 鑑賞漏了兩道 solo 早就有的濾網

1. **`stripLeakedScaffold_`**：AI 偶爾把 `★【…】` 指令或 `〈演出依據〉` 卡原樣抄進敘事，
   solo 走 `narrateWithState_` 會濾掉，鑑賞這條路徑從來沒濾過——玩家直接看到提示詞。
   補在 `sanitizeAiData_`（單一咽喉）。⚠ 得先把真實換行轉成 `<br>` 再濾：濾網是掃到下一個「<」為止，
   沒有 `<br>` 的話會把整段吃光。
2. 順手修了濾網本身的舊坑：剝掉指令會留下它前後各一組 `<br>`，疊成四連斷行＝玩家看到一塊莫名空白。
   現在收斂回 `<br><br>`，並改成只壓縮水平空白（原本 `\s{2,}` 會把真實換行也吃掉）。兩軌同時受惠。

### G. 選項的鷹架原樣變成按鈕

schema 教 AI 寫 `"1. [主動]…"`，而 `data.options` 是**原樣**長成按鈕文字的——
玩家看到的是「1. [主動]伸手替她理瀏海」，按下去那整串又原樣回灌成【玩家意圖】、再進近期摘要。
分類本身有用（逼出四種不同走向），但那是給 AI 的。`sanitizeAiData_` 落地前剝掉編號與標籤，
半形全形、任一順序都剝（迴圈剝到不再變動）。

### H. 缺一條全域「不報幕」鐵律

提示詞裡滿是 `好感:75`、`【親密尺度五階】`、`系統`、`回合`、`narration`，
而慾海律令十條**沒有一條**說「這些不准寫進敘事」——只有「關係質變」「週年」兩個區塊各自補了一句
「不可報幕」。同一個漏洞形狀補兩次，就該收成一條鐵律。新增律令 10（原 10 順延為 11）。

### I. 兩條規則打架

- 【只演給的資料】：「玩家專一對著一個人時，其他在場者維持**背景輕描**」
- 【在場名單】：「名單上每個人這回合**都要有戲**」

改成「都要真實存在（沒被搭話的人有個動作或反應即可，**不必平分戲份**）」。

### J. 跑時重複（第三次遇到同一形狀）

每張在場卡的 `現況:…賴床…` 後面都掛著同一句 30 字的演法規定，三人同場印三遍。
**事實留在卡上、規則抽成抬頭講一次**——跟當年 `在場來由` 與 `performanceNote_` 同一個修法。

### K. 其餘措辭

`名號:` → `名字:`（九州武俠殘留，現代冬木日常線不合）／
`現在演化玩家動作：` → `接著往下演，玩家這一步是：`（「演化」是機器語感，而且這是整份提示詞的最後一句、recency 最強）／
`inner_monologue` 的「對方」在三人同場時指誰不明 → 點名成「被搭話的那個人」／
`更早的回合裡` → `稍早`（新律令 10 剛把「回合」列為不准寫進敘事的字，不該在前一行就遞給它）。

### ⚠ 紅線① 程序

`nsfwBaseRules` 動了兩處（開頭人稱那次之外，這次加律令 10、律令 8 措辭）。
改前量：system 1725 字。改後：1822 字（增量全在快取前綴裡，等於不花錢）。
全套鑑賞探針（k／k_flow／k_hist／k_male／k_sex2／k_verify／k_cd／kscan 10 情境）＋
等效重複子句檢查（76 條子句、完全重複 0）。
**「原本正常的配對沒被改壞」**：k_sex2 兩組配對（女玩家×男同伴、男玩家×女同伴）輸出逐句比對過。


---

## §「篇幅改成事件驅動」＋「近期摘要砍包裝」（2026-09）

玩家：「1 沒有更好的方式嗎.....ai自己決定字數？」「3 我要簡單 有力 不要太多多餘的！！盡可能砍吧！」

### 篇幅：AI 自己決定？——這條路已經走過了

`KANSHOU_WORDS_` 當年就是因為「約 X 字」不管用才改成【下限~上限】的：
**小模型對「約」一律往下取**（實測過，見上方篇幅段）。完全放手讓 AI 自己拿捏，
只會是同一個結果的極端版——它永遠寫最短的那個量。**所以不是 AI 決定，是換一個更準的裁定依據。**

舊版的毛病不是「有裁定」，是**裁定依據只有好感**：
場上只要有人好感 ≥60，連一句「早安」都得寫到 700~900 字——那不是細膩，是逼 AI 灌水。

新版：**好感給底盤、這一回合真的發生了大事才拉到上限**。

| 好感 | 平常 | 有事發生 |
|---|---|---|
| ≥60 | 480~620 | 700~900 |
| ≥40 | 400~520 | 600~750 |
| 其餘 | 300~400 | 450~580 |

「大事」不靠猜也不必多花一次呼叫——**那些區塊本回合有沒有組出字串，GAS 自己最清楚**：
`kanshouConfessStr`（告白）／`kanshouTierCrossStr`（關係質變）／`kanshouFirstsAnnivStr`（週年）／
`kanshouNightSceneStr`（夜已深）／`kanshouKnockRaidStr`（深夜訪客）／`kanshouCohabitStr`／
`kanshouCohabitEndStr`／`kanshouPromiseMetStr`（赴約）／`driveOn`（主動掌握）。（節慶刻意不在串裡：它是日子不是戲，舊的「就是此刻」regex 是死旗標、已拿掉。）
**加新橋段時往那串旗標加一個即可**，不動查表邏輯。

⚠ 上限 900 沒動 → `max_tokens` 的截斷風險不變。

### 近期摘要：框比內容還大

原本 145 字的框只裝得下 100 字內容，而且**每句開頭的「我」還得另外寫一句話去解釋**
（旁白已改第二人稱，摘要卻是玩家自己打的字、天然自稱「我」）。

改法是**把禁令換成事實**——句首的「我」直接剝掉，變成純動作，那句解釋就沒有存在的理由了：

```
舊 ★【再往前的經過】：更早的回合裡，你依序做過這些事——我替她倒了杯熱茶→…→我承認自己不太會做菜。
   （這幾句是玩家當時自己打的字、所以自稱「我」；旁白一律仍寫「你」。）這些都已經發生過了，
   需要時自然呼應、別當沒發生過，也不要重演一次。

新 ★【稍早做過的事】：替她倒了杯熱茶→問她昨晚睡得好不好→…→承認自己不太會做菜
   ——都已發生過，需要時自然呼應，別重演。
```

包裝 145 → 28 字，內容一字未減。順手剝掉句尾標點（原本會出現「她在哪？。」這種雙標點）。

### 沒做的一項

鑑賞介面上還有三十幾處「御主／從者／召喚英靈」，跟「這裡從來沒發生過聖杯戰爭」的世界觀不合。
**玩家定案：「應該沒差」——不動。** 送進 AI 的提示詞已經清乾淨，UI 維持原樣。


---

# 🌍 §「世界帳本」——鑑賞從選單變成沙盒（2026-09）

玩家：「我鑑賞覺得不夠自由不夠好玩／我的想法是我應該是自由的！但ai有試算表可以記錄我的歷程！
應該要這樣才對...不是像現在全都有設定過」

## 問題出在一句話

```
★【只演給的資料】：系統給的資料就是這個世界的全部，沒寫到的人/物/過往都不存在
```

量出來的事實：鑑賞的「世界」是 **22 張寫死的表**（28 個地點、固定住處表，
連「她此刻心裡想要什麼」都是 6 選 1）。而 **AI 能寫回試算表的東西，全部是「已經在表上那些人」的屬性**
（好感／外顯／暱稱／共同回憶／玩家經歷）——它不能新增一個地方、一個人、一件設定。
所以它只能在玩家畫好的盒子裡排列組合。玩家感覺到的「全都有設定過」，就是這件事。

## 反轉

> 試算表從【AI 讀的選單】變成【AI 寫的帳本】。

那句禁令當初有它的理由（AI 憑空捏造角色和地點、玩家當成設定、它下回合就忘了）。
但正確的修法不是禁止發明，**是讓它發明完要負責記下來**。
發明會出問題，只是因為沒落盤；落了盤，發明就不是雜訊，是在蓋世界。

## 資料層：分頁「鑑賞世界」

`遊戲ID | 類別 | 名稱 | 內容 | 性別 | 建立日 | 最後提及日 | 提及次數 | 釘選`

類別＝`地點`／`人物`／`設定`。**刻意用一張表加一個類別欄，不開三張表**——
一條寫入路徑、一套淘汰政策、一份快取；以後要加類別是加一個值，不是加一張表。
（性別只有「人物」用得到，但升格成正式同伴時它是必要的，所以存在表上而不是事後猜。）

| helper | 做什麼 |
|---|---|
| `worldRead_(gid)` | 讀這一局的帳本，走 CacheService（每回合都要讀） |
| `worldWrite_(gid, entries, day)` | 唯一寫入點：清洗 → 去重 → 更新/新增 → 淘汰 → 整批寫回 → **快取換成新內容**（不作廢，否則同一趟執行裡後面那支 read 又要整表讀一次） |
| `worldEvictees_(d, gid, added, day)` | 純函式，只回答「該砍哪幾列」：超過各類上限就砍「最久沒被提到、提及次數也最少」的；★釘選永不驅逐 |
| `worldRow_(a, rowNum)` | 一列 → 一個條目；讀與回填快取共用（欄位長相的單一真實來源） |
| `worldFeed_(rows, loc, names, msg, day)` | **不是全餵**：算相關性分數排序取前 6 |
| `worldSame_(a, b)` | bigram 近義比對，**只用在近期迴聲**（見下方⚠） |

**相關性分數**：釘選 +100／此刻地點提到它 +40／在場者名字出現在內容裡 +30／
玩家這句話提到它 +50／最近 3 天剛提過 +20／提及次數（上限 +5）。

⚠ **`worldSame_` 只比對最近兩天寫的，不掃全表。** 第一版掃全表，實測把
「她喜歡在便利商店買關東煮」和「…買茶葉蛋」merge 成同一條——句型相近但語意不同的事實太常見，
拿去掃全表會把世界愈合併愈空。門檻也從 0.6 提到 0.7。
**mechanical 去重抓不到真正的語意複述**（實測「她討厭下雨」的兩種說法 bigram 只重疊 0.36），
那一層靠提示詞（「已經在名單上的不必重寫」）＋淘汰處理，不硬調門檻換誤殺。

## AI 的新出口

```json
"world_note": [{ "kind": "地點|人物|設定", "name": "...", "text": "≤40字·之後要當真的事實",
                 "sex": "僅 kind=人物 時填 男/女/異" }]
```

一回合最多 2 筆。`sanitizeAiData_` 擋結構（合法類別、筆數），
逐欄的字元清洗與長度在 `worldWrite_` 做（唯一寫入點）。

籠子那句改成：
> ★【可以發明，但發明完要記下來】：這是一座活的城，資料沒寫到的店家、路人、往事、習慣，
> 你可以當場創造——但凡是【之後要當真】的東西，都必須同時寫進 world_note，否則下回合它就不存在了。

## 四條放開的線

### 🗺️ 地點：世界會長大
- `kanshouLocationsFor_〔已移除〕（gid)` ＝ **內建地圖 ∪ 這一局自己走出來的地方**；`kanshouFindLoc_` 是唯一查法。　※（2026-09 地點整組退休·已移除，見 KANSHOU_REFERENCE.md）
  ⚠ 原本 16 處直接 `.find(KANSHOU_LOCATIONS_)` 已全數改走它——漏一處，自己走出來的地方就會查無、被當成非法目的地。
- 前端「🧭 去別的地方…」自由輸入 → `newPlace` → 後端查無就**當場把它加進這一局的世界**、走過去，
  並給 AI 一句「你第一次來，由你決定它長什麼樣，並寫進 world_note」。
- 地圖多一區 `mine`＝「走出來的地方」。**靜態鏡射長不出這些**，所以後端 `buildTagsPayload_` 下傳 `myPlaces`
  逐項清單（同 `unlockedResidences` 的做法：後端算好結果，不下傳門檻）。

### 🧑 人：不限那 25 人
- 帳本的「人物」＝這座城的常民（名字＋樣貌＋性別），**可以出現、可以開口，只是不追蹤好感**。
- 【在場名單】鐵律改寫清楚：它管的是**正式同伴**（好感/關係只跟這些人算），不管常民。
- ~~**升格**~~：曾由 `inviteResident` 兼任，已隨結識一併移除。
  刻意不叫 AI 補一整份設定（那又變回「全都有設定過」），比照玩家自己的御主走「留白＋滾動成長」。
  ⚠ ID 沿用 `KHV_` 前綴：三處白名單都認它，另開新前綴要同步三個地方。
- AI 這回合造了新的人 → 曾回傳 `folkOffer` 跳泡泡，2026-09 已砍。

### 🎭 事件：砍掉預寫橋段池
整批移除 **7 張表**（22 → 16 張）：

| 砍掉的 | 它原本在做什麼 |
|---|---|
| `KANSHOU_SCENE_EVENTS_` | 情境橋段的 ambient 句庫 |
| `KANSHOU_LOCATION_EVENTS_` | 地點×時段 → 寫死的情境 |
| `KANSHOU_COHABIT_EVENTS_` | 同居日常 時段→事件 |
| `KANSHOU_EVENT_SEEDS_` | 三類各七句、20% 機率抽一句當「氛圍靈感」 |
| `KANSHOU_INIT_WANTS_` | 「她此刻想要什麼」6 選 1 |
| `KANSHOU_LOCATION_ACTIVITY_` | 14 個地點各兩句「她正在做什麼」 |
| `KANSHOU_FESTIVAL_EVENTS_` | 每個節慶「老規矩是做什麼」＋達成追蹤 |

依既有判準（①事實留 ②寫法砍）：**節慶的「日子」留著**（`KANSHOU_FESTIVALS_` 是世界事實，
「跳到節慶」按鈕也吃它），砍掉的是「這一天該做什麼、做了沒」——那是把節日變成待辦清單。

連帶清掉的孤兒：`kanshouRollEvent_`、`kanshouLocActivity_`、`KANSHOU_FESTIVAL_DONE_TAG_`、
`kanshouPlayerHomeLocs_`、前端 `KC_LOCATION_EVENTS_` 的五條橋段徽章
（那張表兼做「🌙她熟睡中」提示，那部分是 `pSleepStr` 的顯示層、留著，改名 `KC_SLEEP_HINTS_` 並登記為純前端）。

## 🌍 面板：☰ →「這個世界」

玩家：「世界帳本我能看到嗎？有地方看嗎？還是後台紀錄」——問得好，**原本只有後台**：
地點那一類會出現在地圖的「走出來的地方」，人物與設定在遊戲裡完全看不到。
而且帳本有 `釘選` 欄、淘汰時會保護它，**卻沒有任何入口能設定**——做了一半。

`actionWorld`（action `world`，兩軌共用，op = `list`／`pin`／`unpin`／`del`；抬頭/說明/分組由 `WORLD_SPEC_[軌].panel` 下傳）
把「看得到」與「管得動」一起補上，分工比照 `actionKanshouMemoirOp`：

- 三類分組列出（地點／人物／設定），各自標 `現有數 / 上限`
- 📌 **釘住 → 永遠不被淘汰**（這是 `KANSHOU_WORLD_CAP_` 那套政策唯一的人為出口）
- 🗑 **刪掉 AI 亂記的**——刪了 AI 就真的不記得了
- 排序**跟提示詞不同**：面板是「釘選優先、其次最後被提到由新到舊」（給人看的）；
  提示詞是相關性分數（給 AI 的）。兩者刻意不共用。

空的時候不顯示空表格，而是講清楚它會怎麼長出來——新開的世界一定是空的，
直接給一張空表會讓玩家以為壞掉。

## ⚠ 已知代價

**帳本會無限長大**——這是這整套機制唯一的真風險（提示詞爆炸、快取失效）。
所以從第一天就有淘汰（各類上限 地點40／人物24／設定30）＋餵回上限（一回合最多 6 條）。
實測：41 條灌到上限後淘汰回 31（30 未釘選＋1 釘選），餵回去 125~180 字。
**加新的「類別」時記得同步 `KANSHOU_WORLD_CAP_`。**

~~隨機 NPC 日常落點（`kanshouRollDailyLocation_` 等）仍只用內建地點池~~（2026-09 地點退休後整組不存在），
沒有把「玩家走出來的地方」納入——那些是公共城市地標，隨機把人丟進玩家自己發現的地方
會有一堆邊界狀況。要不要開放是之後的事。

## 驗證

`world.js`（發明→落盤→餵回→去重）、`world2.js`（淘汰/釘選/相關性單元測）、
`place.js`（自訂地名→世界多一格→走得回去→前端拿得到）、`folk.js`（造人→記住→升格→不重複跳框）。
紅線①：system 1822 → 1977 字（增量全在快取前綴）；全套鑑賞探針；
等效重複子句 85 條、完全重複 0；k_sex2 兩組配對逐句比對沒改壞。


---

## 🎨 說書人風格交給玩家（2026-09）

玩家：「其實我應該直接在這裡打造一個類似酒館就好？！」→「好 稽核跑完就往這個方向做 符合自由 玩家自己決定增減！」

**邊界（量出來的）**：鑑賞一回合 prompt 約 3600 字＝事實·GAS 裁定 ~1280（不交）／技術契約 ~1200（不交）／資料 ~283／**風格 ~965 字·15 段（其中尺度與篇幅兩格交給玩家）**。
交的只有「怎麼寫」的口味；「發生了什麼」（好感、在場、地點、時間）與「怎麼回」（JSON、分段、在場驗證）一律不交。

**模組表** `KANSHOU_STYLE_MODULES_`（Gallery.gs·資料驅動，加一段＝往表加一列）：

| key | 名稱 | slot | 預設＝原本寫死的那句（**只在 .gs 裡，不下傳前端**；玩家看到的是 `hint`） |
|---|---|---|---|
| voice | 筆觸 | sys | 「後日談敘事核心·輕小說筆觸…」（nsfwBaseRules 開頭） |
| agency | 玩家主權 | sys | 鐵律 1 動作與台詞只有玩家能決定 |
| enact | 演玩家這一步 | sys | 鐵律 2（2026-09 新增）：narration 從玩家這一步演起、撐成完整的一拍，但只擴寫他真的寫的那一步 |
| dialogue | 對話格式 | sys | `dialogueFormatRule_()`（預設是函式，所以走 `kanshouStyleDefault_`） |
| drive | 推演 | sys | 鐵律 4 |
| continuity | 情緒連貫 | sys | 鐵律 5 |
| ~~moe~~ | ~~語癖與稱呼~~ | — | 2026-09 整段退場（「自然滲入、偶爾點到即可」整句都是寫作指導）|
| immersion | 不出戲 | sys | 鐵律 9 |
| world | 世界觀 | user | ★世界觀＝和平的現代冬木市… |
| pov | 視角 | user | ★【視角鎖定】（`{玩家}` 佔位） |
| feel | 你的感受 | user | ★【你也是這座城裡的一個人】（`{玩家}`／`{代名詞}`） |
| length | 篇幅 | user | ★【篇幅】（`{篇幅}`＝這回合查表算出的區間） |
| ending | 收尾 | user | 🚨【收尾】（`{主動掌握}`／`{推進}` 隨 driveOn） |

留死不交：鐵律 2 `<br><br>`、6 性別欄、8 裝扮既定、10 JSON、整個 finalJson、親密尺度、地點釘死、這個世界有誰、world_note 規則、此刻、在場來由、角色一致性。

- **🖋️ 玩家打的字要被演出來**（2026-09 玩家「我想要使用者輸入的文字也會被擴寫到劇情」）：兩件事一起修才成立。
  ① **標籤說實話**：玩家自己打的字原本跟按鍵一樣被冠上 `【玩家意圖】：`——那是 GAS 寫的意圖摘要專用的標籤，
  貼在玩家原話上等於告訴模型「這只是他想做什麼」，於是 AI 把它當前提跳過、直接寫對方的反應。
  改成 `【玩家原話】：`（按鍵路徑仍是 `【玩家意圖】`，因為那真的是 GAS 的摘要）。
  ② **`enact` 鐵律**：narration 從玩家這一步演起，撐成完整的一拍（動作、距離、觸感、開口的語氣與視線），
  但只擴寫他真的寫的那一步——不補沒做的動作、沒說的話。跟 `agency`（不替玩家決定）是同一件事的正反兩面，故緊鄰排在鐵律 2。
  ⚠ 台詞「照原句抄、禁轉述」**不在這條裡重複講**——`dialogue`（對話格式②）已經寫了，同一件事只留一個出處。
  ⚠ 這條是 **sys** 模組，拿不到 `{代名詞}`（那組變數只餵 user 段），所以句子必須中性——`check_pronoun` 第一版就抓到寫死的「他」。
  ③ **輸入框的提示字**：原本是「輸入行動...」——等於告訴玩家這裡只打動作，跟「你打的台詞會照原句演出來」正面打架（UI 說謊的同一個形狀）。改成「說點什麼，或做點什麼…」。
  ⚠ **刻意不做「玩家要學的語法」**（`*動作*`／`"台詞"`）：沒打標記的人會默默拿到比較差的結果、又沒有任何訊號，那是沉默的陷阱；中文本來就有現成的「」可用。也刻意**不讓 GAS 去猜**哪段是說、哪段是做——句子一長就錯，錯了沒人發現。界線本身已經分在兩條規則裡（`dialogue` ②照原句抄／`enact` 擴寫成畫面），不再寫第三句重複講。

- **資料層**：新分頁 `鑑賞風格` `[遊戲ID, 模組, 文字, 開關]`（`KS_`）。缺列＝預設；文字空＝預設；開關 `0`＝整段不送。表上只放真的改過的列（存空文字＋開啟＝刪列）。快取 `KS_<gid>` 120 秒，比照世界帳本 `KW_`；歸零重來一併清掉。
- **讀取**：`kanshouStyleRead_(gid)` → `{key:{text,on}}`；組 prompt 唯一讀口 `kanshouStyle_(styles, key, vars)`：玩家版 → 關閉＝'' → 預設，並代入 `{玩家}{代名詞}{篇幅}{主動掌握}{推進}`。
- **接線**：`buildDefaultSystemPrompt(includeOptions, styles)` 的 `nsfwBaseRules` 改成**陣列組裝＋動態編號**（關掉一段其餘自動補號）；`actionPlay` 的 USER prompt 五段改讀區域閉包 `_sty_`（＝`kanshouStyle_(_styles_, key, _styleVars_)`）。**預設一格不改時，SYSTEM＋USER 逐字等於改版前**（`scratchpad/style/baseline_prompt.txt` diff 為空）。
- **動作**：`kanshou_get_style`（回 `modules[]{key,name,slot,hint,text,on,custom}`＋`max`，**只含沒有 `fixed` 的那兩段**——**`def` 刻意不下傳**，見下）／`kanshou_set_style`（`{key,styleText,on}`／`{key,reset}`／`{resetAll}`）。文字上限 `KANSHOU_STYLE_TEXT_MAX_`=300；**不剝 ｜【】**（這不是 MEMORY，它自己一張表；`sanitizeUserData_` 只擋控制字元與公式前導）。
- **UI（2026-09 改三態）**：☰ →「⚙ 說書人設定」（`openKanshouStyle`·Script_Kanshou.html）：每段 名稱／狀態（預設·自訂·已關閉）／**一句 `hint`「這段管什麼」**／三顆鈕 **預設｜自訂｜關閉**；選「自訂」才長出文字框（中性 placeholder ＋ 字數計 ＋ 儲存），底部全部還原。
  - **🙈 預設句本體不顯示給玩家**（玩家：「把我的設定目前就是預設值 但是不要顯示內容給玩家看到」）：預設值就是調好的提示詞，貼在畫面上玩家會照著看、照著改，等於把敘事的骨架攤在桌上出戲。所以 `actionKanshouGetStyle` **改回 `hint` 不回 `def`**——連網路封包裡都沒有那串字；`KANSHOU_STYLE_MODULES_` 每列多一欄 `hint`（一句話講這段管什麼，不透露句子怎麼寫）。
  - **三態＝原本兩個控制項的合併**：舊版是「文字框（灰字＝預設）＋啟用勾選」，玩家要從灰字/黑字/勾沒勾去推自己現在是哪一種；灰字一拿掉那個介面就無法讀了。現在狀態是明講的一顆亮著的鈕：`ksMode(m)` 是唯一判讀口（關閉 → 自訂 → 預設）。
  - **按「自訂」不打後端**：只是本地 `_ksEdit_[key]=true` 把輸入框長出來，按【儲存】才送（守住每按鍵最少 round-trip）。留白存檔＝刪列＝回到預設，跟後端原本的規則同一條，沒有新特例。
    ⚠ 分頁會**把狀態藏起來**，所以每個分頁右上角有角標＝那一類有幾段不是預設；`style.js` 釘住「每段都歸在存在的分頁」「沒有空分頁」。
  - 🔒 機器擋：`check_ui.js` 的 `ksRender_ 三態＋分頁` 會餵一個**帶 `def` 的誘餌模組**進去渲染，只要那串字出現在畫面 HTML 就當場叫（退化測試確認過會叫，改版後再測一次），並驗分頁列畫得出來、切頁真的換內容、別頁的東西不會漏到這一頁；`style.js` 另有「get_style 不下傳預設句本體」「每個模組都有 hint」兩條。
- **紅線①流程**：改前 `_audit.js` 存基線 → 改後 diff 零差異 → `style.js` 32 條（預設零差異／改一段只動那段／關一段整段消失且鐵律重新編號／單格與全部還原／兩帳號互不污染／注入 `=SUM ｜【同居】1 <b>` 不切壞 MEMORY／300 字上限／不存在的模組被拒／歸零清風格）→ `known.js`／`k_sex2.js`／`k_cd.js`／`audit_all.js` 零退化。
- ⚠ **佔位符刻意叫 `{代名詞}` 不叫 `{他她}`**：`check_pronoun.py` 會把字面「他／她」當寫死代名詞抓出來，而它抓得對——預設句裡不該出現任何一個。
- ⚠ **預設值留在 `.gs`、不搬進試算表**：十三支掃描器只看 `.gs`，搬走等於讓 `check_prompt`／`check_pronoun` 對這 12 段失明。
## 🩸 肉體狀態不跨夜（2026-09）

玩家要求查「有沒有新的變動資料跟原廠設定衝突」。量出來的第一個是**變動資料自己卡住不退**：
`PHYSICAL`（肉體）由 AI 的 `intimacy_feedback[].physical_state` 寫入，但
**AI 沒吐那個欄位的回合不會覆寫它**（`if (nCleanState)` 才寫）——於是「腿還在發軟」會一路跟著那個人
走過好幾天、好幾個地點、甚至跨過關係質變，跟後來的一切互相打架。

修法比照既有的「🤝 睡覺自然放手：牽手不跨夜」：【一天結束】時 `kanshouRestBody_` 把玩家與所有正式同伴
的肉體狀態設回 `{"狀態":"如常"}`（`_isPlainBody_` 認得這個值，回到如常就不再送進提示詞）。
**同一天內刻意不清**——那一欄寫的就是此刻的身體，當天還在是對的。
⚠ 探針 `kbody.js` 釘住三件事：寫得進去／同一天內留著／跨夜歸位。
⚠ 寫探針時踩到**兩段式就寢**：身邊有羈絆 ≥60 的人時，第一次按睡覺只會進「夜未眠」，
第二次才真的結束一天——只按一次會讓人以為修沒生效。

## §「後日談歸零重來」（2026-09）

玩家：「我用風音這個帳號進去 可以有個按鈕把鑑賞資料歸0重新開始」

**兩個入口，一支核心**（`kanshouResetCore_(kpcId, acct, reloadAfter)`）：

| 入口 | 何時顯示 | 清完 |
|---|---|---|
| 🏠 **主選單**（登入後、🌹進入鑑賞 底下） | `account_login` 回傳的 `kpcId` 非空才長出來——沒東西可清就不長那顆鈕 | 收起按鈕、旗標打掉，**不重載**（`pc` 根本還沒載入） |
| ⚙️ 遊戲內系統設定 | 只在 `pc.mode === 'kanshou'` 時顯示 | **必須整頁重載**——`pc` 指著已經被刪掉的那一列 |

`reloadAfter` 是兩者唯一的差別。solo 有自己的「⚜️奪得聖杯」在收尾，不在同一個地方混兩套入口。

| | |
|---|---|
| **不會動** | 英靈殿（含工房鑄的原創英靈——兩軌共用的資產）／solo 那一局的任何東西／**別的帳號的鑑賞** |

清完解除 `COL.ACC.KPC` 連結，下次 `actionEnterKanshou` 查無連結就從頭建一個全新世界。

**順序刻意是「先清資料、最後才解連結」**：中間任何一步炸掉，連結還在，玩家至少回得去原本的世界。

🔒 **授權比照 `actionEndRun` 那次稽核的修法**：`pcId` 是可預測的時間戳，【不可】裸 find——
一律驗「這個帳號登記的鑑賞角色是不是就是它」(`getAccountKanshouPcId_`)，
否則任何人都能猜 id 清掉別人的存檔。前端再加一道「照抄帳號名」的打字確認（不可逆的東西不給按一下就走）。

`kanshouPurgeByGame_(sh, gidCol, gid, idCol)` 是共用的「依 game_id 刪整批列」helper
（由下往上刪避免索引位移），三張表共用一支。

⏳ **不可逆的操作要看得到它在跑**：玩家實測「輸入名字後就消失了，不知道有沒有在執行」。
輸入框一關、設定視窗還開著，後面整段清除完全沒有畫面回饋。現在先收掉設定視窗、壓一張
**不可點背景關閉**的讀條（跑到一半被關掉，玩家會以為沒事而重按一次），跑完換成結果、再重載。

⚡ **但等待畫面只是症狀，慢才是病**：原本逐列 `deleteRow`，在 GAS 裡那是**一列一次 API 呼叫**。
改成「留下來的整批寫回、尾巴一次砍掉」（同 `actionPurgeOrphans` 的寫法）。
實測 400 筆歷史＋5 位同伴的整局清除：**`deleteRow` 0 次、總共只打 4 次試算表**（原本要 405 次）。
`purgeHistoryForPcIds_` 也一起批次化了——solo 的 `end_run` 走同一支，兩軌同時受惠。

**探針 `reset.js`** 開兩個帳號各一局，驗四件事：別的帳號清不動／沒帶帳號名擋下／


---

## §「經歷改回固定」（2026-09）

玩家：「我的經歷怪怪的.....好像是滾動式的／改回固定吧....？」

### 病在「反覆重新摘要」

`master_note.經歷` 每 3 回合（`KANSHOU_SIDEWRITE_EVERY_`）叫 AI
「承接舊經歷·只增補本回合有意義的新事·**滾動摘要≤50字**」——
**壓過的東西再壓一次**。開局寫的身世撐不過幾輪就被最近幾回合洗掉，
內容往「剛剛發生的事」漂、細節一路掉光。這是 lossy re-summarization 的典型衰變，
不是提示詞措辭能修的。

而且「一路上發生了什麼」現在是**世界帳本**的工作，它是**追加＋淘汰**、不是反覆重寫——
留著這條等於用一個更差的機制做同一件事。

### 現況

**經歷＝固定事實**：創角時 `actionBackfillKanshouAi` 生成一次，之後只有玩家能透過
逆天改命改（限 80 字）。AI 全程只讀不寫。

這是 2026-07「性格四格/萌點不再交給 AI」那次的最後一塊——
**鑑賞的角色欄位現在全部只有兩條寫入路徑：創角時生成一次、玩家改命。AI 一欄都碰不到。**
（AI 能寫的只剩「發生了什麼」那一類：好感、外顯、暱稱、共同回憶、世界帳本。）

### 連帶移除

`master_note` schema／`_doSideWrite`／`KANSHOU_SIDEWRITE_EVERY_`／
`kanshouGetSideWriteCount_`／`kanshouSetSideWriteCount_`／MEMORY 的【側寫計數】標記／
`buildDefaultSystemPrompt` 的 `includeMasterNote` 參數（簽名變成 `buildDefaultSystemPrompt(includeOptions)`）。
舊存檔殘留的【側寫計數】是純孤兒資料，不影響任何邏輯。

**副作用（好的）**：system prompt 不再每 3 回合變一次形狀，可快取前綴恆定。

探針 `exp.js`：連打 10 回合、AI 每回合硬吐 schema 外的 `master_note` → 經歷一字未動；
玩家逆天改命仍改得動。

---

## 🔍 2026-09 整體稽核（玩家：「檢查整體吧！！！！詳細 慢慢看 不要有任何遺漏」）

抓到四件事，都已修。四件裡有三件是**綠燈、零錯誤訊息、玩家不會收到任何提示**的那種壞法。


事件自由那輪砍掉預寫橋段池時，`kanshouLocActivity_`／`kanshouReFest_` 連同表一起刪了，

修法：節慶旗標改用 `curDateObj_` 現場算（`KANSHOU_FESTIVALS_` 還在）；活動欄留空
（**欄位保留不刪**——COL 是位置索引，刪欄會位移全表）。

根源修法是新的掃描器 **`check_undef.py`**：`node --check` 只看語法，看不到「叫一個不存在的東西」，
而 GAS 這種 try/catch 包起來的落地路徑正是最容易這樣壞的地方。探針 `land.js` 盯著落地結果。

### ② `world_note` 沒擋公式引導字元

`world_note` 是 **AI 產的、不經過 `sanitizeUserData_`**，而帳本的名稱/內容會直接寫進儲存格——
開頭是 `=`／`+`／`-`／`@` 會被 Google Sheet 當公式執行（`=IMPORTXML(...)` 這類）。
探針 `sec2.js`。

### ③ 每按鍵整表讀 6 → 9 次（效能退化）

世界帳本第一版三支各自讀表：餵回讀一次、寫入讀一次、淘汰再讀一次。
CLAUDE.md 寫著「別把多餘 round-trip 或重複整表讀回加回來」——修法見
`CODE_NOTES.md` 的 `worldWrite_` 條。現在 **7 次**（多出的 1 次是帳本寫入，無法省）。
探針 `perf2.js` 直接數 `getDataRange`/`setValues`。

### ④ 文件裡有 17 條幽靈條目

`kanshouRollEvent_`／`kanshouLocActivity_`／`stanceLine_`／`summonTab`／`checkName`… 
功能砍掉了、索引沒跟著砍。CLAUDE.md 說 `FUNCTION_MANUAL.md` 零容錯，正是因為
「照著文件去 grep、查無此函式」比沒有這份文件還誤導人。全數修正，並加掃描器 **`check_docs.py`**
（文件裡用 `` `名字(` `` 點名的函式，代碼裡必須真的還在）。

### 順手修

`actionPrepMeal`（solo）沒從者時仍講「御主與從者」——沒附卡卻點名從者＝邀 AI 憑空生一個
（同 `check_cards.py` 在擋的形狀）。改成條件式兩分支。


---

## ⏳ 等待畫面（2026-09，玩家第二次提同一件事）

玩家：「如果需要玩家等待的地方 都加上一個等待畫面 **不要只是背景執行**！」
（第一次是歸零重來那顆：「輸入名字後 就消失了 不知道有沒有在執行」。）

**鑑賞這邊補上等待畫面的入口**：同伴面板（👥，**兩趟 round-trip**，原本按下去到面板彈出中間完全沒反應）、
`withProcessing_('…中…', () => gasRun(...))`。

**刻意不擋畫面的**：`backfillKanshouAi`（進場後補完御主設定）。它非阻塞是對的——玩家已經可以開始玩，
擋他 20 秒是把好事做成壞事；但「不擋」不等於「不講」，改用 `bgHint_()` 的小提示條。

**維持原樣的**：對話主線 `send()` 本來就有 `loaderCaptions` 行內讀條；
約定變動後的背景刷新（已抽成具名的 `kcRefreshAfterPromise_`）玩家沒在等它。

機器擋：`check_wait.py`——每個 `gasRun()` 呼叫點往上找不到等待指示就叫，
樂觀更新/背景同步要逐支登記並寫理由（**登記過的才算例外，沒登記的就是漏了**）。
另有 runtime 探針 `scratchpad/size/wait.js`：把三個 `Script*.html` 串進同一個作用域配最小 DOM，
真的把 `withProcessing_`／`bgHint_` 跑起來對答案（巢狀不可提早收、例外一定收、小提示計數歸零才消失）——
CI 不驗 .html JS，只有這裡驗得到。


---

## 🎭 你也是這座城裡的一個人（2026-09·紅線① 改動）

玩家：「鑑賞我想要的是 AI 扮演玩家的角色，用這個角色的角度去感受這個世界。」

### 改了什麼

**`nsfwBaseRules`（紅線①·演化核心）兩條**
- 鐵律 1：「不擴寫**不代玩家加戲**」→「**玩家的動作與台詞只有玩家能決定**：不替他新增動作、
  不替他開口、不改寫成轉述（玩家角色的**感受不在此限**）」。原句把「感受」跟「加戲」綁在一起，
  一刀切掉了整個內心。
- 鐵律 5：`inner_monologue` 從「只判**對方**的真實反應」→「判對方，**也判玩家這個人此刻真正的感受**
  （依其性格/經歷，不是表現出來的那個）」。感受要有依據，否則每回合即興、跨回合會飄。

**★【視角鎖定】拆成兩塊**
- 視角鎖定只留「第二人稱、旁白不可用『我』」。
- 拿掉「**只演你實際輸入的動作與五感——你看不見自己的神情**」（這就是把玩家關成攝影機的那句）。
- 新增 **★【你也是這座城裡的一個人】**：用這個人的角度感受世界（觸感/冷熱/氣味/聲音、**真正**的情緒、
  性格帶來的反應底色、沉默也要有理由）；但**做了什麼說了什麼只有玩家決定**；
  **寫感覺得到的，不寫看不到的外觀**（臉看不見，臉發燙感覺得到）。
- 代名詞走 `_mePron_ = pron_(pc[COL.PC.SEX])`——御主性別是資料，而且隨時可切換。

**玩家角色卡補厚（4 格 → 6~7 格）**
補印 `萌點(僅供內化)`、`口吻`、`招牌小動作`，全部**複用同伴那一行的同一組 helper**
（~~`getPersonaSpeech_`~~／~~`getPersonaTic_`~~／~~`traitPrivateOf_`~~·已移除／`COL.PC.INTENT`）。
⚠ 萌點**本來就有存**（`actionBackfillKanshouAi` 一直在生成），只是從來沒印出來。
口吻/小動作是新生成的：schema 加 `speech`／`tic` 兩欄，落地走召喚同伴那支 `stampPersonaFlavor_`。

**篇幅**：寫不滿時「你自己此刻的感受與身體反應」也是正當的填法（本來只列在場者的動作與感官）。

### 順手修掉的兩個舊漏洞

- **`driveStr` 寫死「她」**（`🔥【主動掌握】①她依自己的個性主動出擊`）——同伴性別是資料，
  種子 25 人裡 12 位男性，男同伴的主動橋段會被指成女性。改「對方」。
  這條**藏在多行樣板字串裡，升級前的 `check_pronoun.py` 看不到**（見下）。
- **`actionBackfillKanshouAi` 會洗掉玩過的角色**——原本無條件覆寫，改成「第一次蓋【設定已補】章、
  之後只填還空著的格子」。舊角色（玩家自己就是）靠這個才拿得到新的口吻/小動作欄位。

### 紅線① 程序（全跑完）

| 步驟 | 結果 |
|---|---|
| 改前量現況 | system 1900 字／user 1732 字 |
| 改後 | system 2000 字／user 2420 字（合計 4038→4420，**+382 字/回合**） |
| 全套探針 | 50 支跑完，無結構性退化（`dump2.js` 改前改後都壞，是本來就失效的暫存探針） |
| 等效重複子句檢查 | 12 條規則兩兩比對，最高重疊 0.15（門檻 0.50）✅ |
| 配對沒被改壞（`k_sex2`） | 女玩家×男同伴 她×10／他×3；男玩家×女同伴 她×1／他×12 ✅ 兩邊代名詞各自跟著自己的資料走 |
| 新探針 `mecard.js` | 20 條全過（落地／不覆寫／卡片厚度／感受解禁但動作仍鎖） |

### ⚠ `check_pronoun.py` 本來一直在說謊

舊版是**逐行**用正規表達式找字串字面，所以**只看得到單行字串**——而這個專案幾乎所有提示詞本體
都住在跨數十行的樣板字串裡，那裡面寫死「她/他」它一個都抓不到。
實測：把「★【退化測試】她一定會這樣做。」塞進 `nsfwBaseRules` 附近，它照樣回報 ✅。

已改成走一次真正的 tokenizer（逐字元分辨 程式碼／註解／三種字串／正規表達式字面），
跨不跨行都看得到，並且**報命中那一行**而不是字串開頭那一行。
⚠ 正規表達式字面必須單獨處理——repo 裡真的有 `/[<>&"'` + "`" + `｜【】…]/` 這種內含引號與反引號的樣式，
不認得它的 tokenizer 會從那裡開始把整份檔案的引號配對全部算錯。
升級後**當場抓到兩個藏了很久的真漏洞**（`driveStr` 的「她」、以及我自己剛寫進去的寫死代名詞）。


---

## 🪶 大道至簡（2026-09·紅線① 改動）

玩家：「看看鑑賞有什麼是多餘的、或太細緻對 AI 來說是累贅的，可以刪除或整合成一塊！
還有角色關係跟世界帳本的效果要分清楚，世界帳本應該是要紀錄更大事件的？剛剛連咖啡廳都紀錄。
另外世界帳本的新增角色功能真的能做到嗎？如果不行就不要有。」

### ① 新增角色：能做到，整條路是通的（探針 `folk2.js` 端到端驗過）

AI 當場造人 → 寫進帳本 → `folkOffer` 泡泡 → 下回合 AI 還記得他 → 玩家點「往來下去」
→ 建 `KHV_` 列 → 正式在場卡、關係追蹤得到。（~~`kanshouFolkToRow_`~~ 已移除；現在的升格路徑是 `actionKanshouSummonHero`，召喚時會清掉同名的帳本「人物」條目。）

**🐛→✅ 但抓到一個真 bug**：升格成正式同伴之後，帳本裡那條常民【沒有清掉】——
同一個人變成**兩份真相**，同時出現在【在場人物】卡與【這個世界已經確立的事】名單裡，
而帳本那句是升格當下的舊描述、之後永遠不會更新。
已加共用的 `worldDrop_(gid, kind, name)`，升格時清掉；面板的「刪掉」也改走同一支。

### ② 地點歸地圖：不是兩個系統，是同一筆資料被展示兩次

玩家：「我本來就有地圖阿....是否地圖系統大改進」——**這句話是對的，但不用大改。**
量出來：地圖有 6 個分區，最後一區「走出來的地方」的資料來源**就是世界帳本的「地點」類**
（`kanshouLocationsFor_` ＝ 內建 29 個地點 ∪ 帳本地點）。咖啡廳被記下來時，
它其實**已經是地圖上的一格**了；問題是它同時又出現在世界帳本面板裡，長得像一條回憶。

修法：**展示收成一處**。帳本面板不再逐條列地點，改成一行指路
（「你走出來的地方有 N 個——在地圖的『走出來的地方』那一區」）。
資料層完全不動（地點仍存在帳本表，地圖照樣長）。

### ③ 帳本 vs 角色關係：分工從來沒寫給 AI 看過

這才是「連咖啡廳都紀錄」的真正成因。`world_note` 與 `memory`(共同回憶) 兩個欄位並存，
**但提示詞裡沒有任何一句話說它們誰管什麼**，AI 只好自己猜，於是把「她說過她討厭下雨」
（某個人的事）寫進了世界帳本。已在提示詞明講：

> world_note 記【這座城有什麼】；某個人的喜好習慣、你們之間發生的事記進那個人的
> memory【你們之間發生過什麼】——兩邊不要互相寫。

並把門檻拉高成「只記之後真的還會用到的」。
**「大事件」本來就有地方記**——每個人各自的共同回憶(MEMOIR)，那是 `memory` 欄的工作。

### ④ 提示詞瘦身：4420 → 4066 字（−354，−8%）

玩家選「連細節規則一起砍」。量出來最大的一團是**「誰算在場」散在 8 段、855 字**，
其中三段在講同一件事：

| 原本 | 字 |
|---|---|
| ★【路人與缺席者】 | 96 |
| ★【可以發明，但發明完要記下來】 | 161 |
| ★【在場名單】 | 151 |

合併成 ★【這個世界有誰】（正式同伴/常民/路人 三層一次講完）＋
★【要它之後還在就寫進 world_note】。順手把兩句寄生句歸位——
「萌點只演不直述」回鐵律 8、「專一時其他人輕描」進在場那段。

其餘：我上一輪造成的重複（動作台詞講兩次、收尾撞一次）、換裝講兩次、
親密散在 5 段、對話格式 ④ 併進 ③、在場來由與現況併一條、時間與天氣併一行、
options 四條各一行併成一行。

⚠ **不動**：角色卡（那是資料不是指示）、親密五階、篇幅、對話格式①②
（①②是踩過坑才加的：AI 曾把喘息寫進敘事、台詞認錯人）。

改後 `nsfwBaseRules` 12 條 539 字，等效重複子句最高 0.15（門檻 0.50）。

### 紅線① 程序

| 步驟 | 結果 |
|---|---|
| 改前量現況 | 合計 4420 字 |
| 改後 | 合計 4066 字（−354） |
| 等效重複子句 | 最高 0.15 ✅ |
| 配對沒被改壞 | 女玩家×男同伴 她×5/他×3；男玩家×女同伴 她×1/他×7 ✅ |
| 全套探針 | 無結構性退化（`solo_all` 的 ❌ 數本來就在 7~13 間跳，是從者死活的骰運） |
| 新探針 | `folk2.js`（升格全鏈）、`census.js`（逐段字數）、`overlap.js`（段落重疊） |

⚠ **探針的坑（第三次踩）**：提示詞裡的★規則現在會**提到**區塊名（「下方【在場人物】的卡」），
所以探針抓卡片一律要錨行首 `/^【在場人物】/m`，裸 match 會抓到那條規則、看起來像卡片消失了。
`legacy2.js`／`mecard.js`／`legacy3.js` 都已改。
## 🎲 GAS 擲骰的地方剩下哪些（2026-09 盤點＋清理）

玩家：「有哪些是用 GAS 去算機率的？你覺得必要嗎 還是都給 AI 發揮較好??」→「改成必定成功」

### 分界線

**要寫進試算表、之後要當真的 → GAS**（AI 沒辦法「決定」誰在哪、關係有沒有成立）。
**怎麼發生、什麼反應、什麼表情 → AI**。

### 已經整組退休的擲骰

| 原本 | 為什麼拿掉 |
|---|---|
| `kanshouConfessAccepts_`（告白成敗·好感×相處次數） | 玩家主動說出口的意圖，被看不見也影響不了的骰子否決 |
| `kanshouProposalAccepts_`（相約/同去/牽手·依好感） | 同上；而且好感高時本來就幾乎必成，骰子多半只在低好感時擋路 |
| `kanshouRelChatCeiling_`（聊天加好感封頂） | 節奏本來就被每回合上限管著，這是第二層，還會叫 AI 踩煞車 |

連帶刪掉的：3 天告白冷卻、被拒扣 3 點、【告白日】標記、`confessWait` 下傳欄位、
三個提議的「婉拒」分支與 `_ppNo` 字串、提示詞裡的「系統已裁定…成敗由系統定」措辭
（改成直接陳述事實：★【約定成立】／★【同行成立】／★【牽起來了】／【這件事已經發生，不可改寫】）。

⚠ 前端兩句確認框也跟著改——它們原本寫「她也可能婉拒／被拒後要隔幾天」，
現在是謊話（同居是**門檻**不是擲骰、告白必定成功）。

### 保留的擲骰（都是「必須寫進試算表」那一類）

| 還在擲 | 為什麼必須是 GAS |
|---|---|
| 每個同伴今天在哪（`kanshouRollDailyLocation_`） | 要寫 LOC 欄。AI 搬不動人 |
| 出門走走 70% 遇到誰（`kanshouRollEncounter_`） | 同上 |
| 深夜訪客 / 夜襲 / 她主動邀約 | 「她主動來找你」——AI 敘述得了但搬不動她的位置；這正是「世界是活的」的來源 |
| 泛用住處分配、ID 尾碼 | 建檔一次性，不是玩法 |

### 門檻（不是擲骰，是資格）——全部保留

告白 ≥60 開得了口／同居 ≥90／登門拜訪 ≥40／親密橋段 ≥40（`KANSHOU_SCENE_MIN_BOND_`）／
告白牆（好感 79 封頂直到真的告白）。這些是**有沒有資格**，不是**運氣**，玩家看得見也控制得了。
## 🔍 2026-09 鑑賞全面稽核

玩家：「全面檢查鑑賞！！」——這一輪動了很多（帳本、角色內心、瘦身、大區/店、好感瓶頸、
擲骰、時間流速），所以重點放在**跨功能交互作用**與**大量移除留下的殘骸**。

### 結果：沒有找到功能性缺陷

| 查什麼 | 怎麼查 | 結果 |
|---|---|---|
| 十一支既有掃描器 | `bash check.sh` | ✅ 全綠 |
| 孤兒宣告（移除留下的） | 150 個 `KANSHOU_*`／`kanshou*` 宣告逐一數使用次數 | ✅ 0 個孤兒 |
| MEMORY 標記寫了沒人讀／讀了沒人寫 | 37 個標記的 get/set 計數 | ✅ 4 個「讀了沒人寫」都是 solo 創角用字面寫的，不是 bug |
| 跨功能交互 | 新探針 `cross.js` 14 條 | ✅ 全過 |
| 提示詞殘句 | 新探針 `stale.js`（10 個已移除的東西 ＋ 5 個該在的） | ✅ 全乾淨 |
| 前端 runtime | 新探針 `ui.js` 25 條 | ✅ 全過 |
| 文件宣稱的數字 | 12 個常數 ＋ 內建地點數 ＋ 角色出沒點數 對照代碼 | ✅ 全一致 |
| 全套探針 | 75 支 | ✅ 零 runtime error |

### 交互作用實測（`cross.js`）

暫停 × 約定（時間凍住約定不會消失、AI 仍看得到）／暫停 × 睡眠現況／
自訂大區 × 同伴行程（**同伴真的會走進你開的店**，實測 4.5%）／
刪掉腳下的地點（不炸，AI 仍知道你在哪）／收掉大區時人在那區裡（地點回到「走出來的地方」）／
告白必成 × 同居／帳本常民升格 × 行程。

⚠ 一個記錄下來的行為（不是 bug）：**刪掉腳下的地點後你還站在那裡**，
但地圖上沒有它了——移動一次就自然解決。

### 瘦身的成果（量出來的）

| | 之前 | 現在 |
|---|---|---|
| 提示詞總量 | 3781 字 | **3403 字** |
| 「誰算在場」散落 | 8 段 855 字 | **3 段 254 字** |
| 「萌點別直述」 | 2 段 202 字 | **1 段 41 字** |
| 指示段落兩兩重疊 | 最高 0.31 | **沒有任何一對 ≥0.14** |

### 這次稽核最大的結構性發現

**`.html` 的 runtime 完全沒有自動防線**——CI 不驗 .html JS，`check.sh` 的語法檢查也只看得到
「這段 JS 合不合法」，而 `wait.js`／`ui.js` 這些 VM 探針住在 scratchpad，只有想到才會跑。
這正是這專案被燒過的形狀（2026-07 敘事排版壞了四天：綠燈、部署成功、零錯誤訊息）。

已補上第十二支掃描器 **`check_ui.js`** 並掛進 `check.sh`：把三個 `Script*.html` 串進 VM、
配最小 DOM，真的把 19 個玩家入口與 4 個面板叫起來。兩種注入退化都確認會叫
（入口被改名 → 「叫不到 openKanshouTime()」；面板裡叫不存在的東西 → 「拋例外」）。


---

## 🔎 玩家看到的 vs 後台（2026-09 對帳）

玩家：「檢查玩家看到的資訊和後台是否一致」。UI 說謊有三種形狀，逐一查：

### ① 下傳契約（前端讀的欄位後端有沒有送）

`buildTagsPayload_` 24 個欄位逐一比對前端讀取點 → **每個前端讀的欄位後端都有送**。
反向抓到一個白送的：`servantSlots: servants.length` —— 沒人讀，而且是 `servants` 的重複
（同一個數存兩處，違反單一真實來源）。已移除。

### ② UI 文案宣稱的規則 vs 後端實際的規則 🐛→✅

**抓到兩處說謊**（都是這一輪改動的殘留）：

| 位置 | UI 說 | 後端其實 |
|---|---|---|
| 告白結果 `_pr.blocked` 分支 | 「前幾天才被拒絕過，還要 N 天才說得出口」 | 冷卻整組退休，**這個分支永遠不會被回傳** |
| 告白被拒訊息 | 「沒有答應——**好感略降**，要隔 N 天你才說得出第二次」 | 不扣好感、沒有冷卻；唯一的失敗原因是好感未達 60 |

改成一句真話：「話說不出口——你們之間還遠不到那個程度(好感要到 60)。這次沒有任何變動。」

（先前那一輪還修過兩句：同居確認框的「她也可能婉拒」實際是**門檻**不是擲骰、
告白確認框的「被拒後要隔幾天」。同一個形狀，這次補完。）

### ③ 寫死的數字前後端各寫一份 🐛→✅

**共同回憶的兩個上限**原本是**寫死在三處的魔術數字**：後端總量 `processMemoir_(..., 10)`、
後端釘選 `>= 8`、前端說明文字「最多10條…上限8」。改一個地方另外兩個不會跟著動，
而 `check_mirror.js` 只盯得到 `KC_*` 常數，**看不到寫死的數字**。

已抽成 `KANSHOU_MEMOIR_CAP_`(10) / `KANSHOU_MEMOIR_PIN_CAP_`(8) 並鏡射成
`KC_MEMOIR_CAP_` / `KC_MEMOIR_PIN_CAP_`，前端文字改成讀常數。
鏡射從 14 組變 16 組，退化測試確認會叫（後端改 10→12 → 「KC_MEMOIR_CAP_ ↔ KANSHOU_MEMOIR_CAP_ 走鐘了」）。
⚠ 釘選上限刻意比總量少 2：釘滿會讓新回憶永遠擠不進來。

### ④ 值一致性（試算表 ↔ 下傳 ↔ 畫面）

新探針 `truth.js` 11 條：好感／關係階／時鐘時刻／日期／時間流速／走出來的地方清單／
帳本條目數／帳本上限／大區上限，全部三處一致；並驗「後端拒絕時畫面不會留下假象」。

⚠ 記一筆給下一個人：**時鐘不在 `get_tags` 裡**——它隨每回合 `play` 回應下傳
（`data.kanshouClock` → 前端 `kcClock`）。在 tags 裡找時鐘會找不到，那不是 bug。

## 🧹 提示詞垃圾字（2026-09 全面稽核）

**形狀**：送進 AI 的那份文字裡混進 `undefined`／`NaN`／`[object Object]`／空標籤／佔位字。
玩家看不到它，AI 卻會當真——solo 那邊就抓到「這一局輸在：**undefined** 日時限耗盡」
（`DOJO_CAUSE_` 在檔案載入當下求值了別的檔的常數，已改成機器擋 `check_loadorder.py`）
與「25 位從者的卡上都寫著**討厭的事物：之事**」（佔位字被剝疊字剝成殘骸、逃過濾網）。

**怎麼查**：靜態 grep 看不到這種，樣板字串要**真的組起來**才知道長什麼樣。
鑑賞這側的探針是 `scratchpad/size/k_garbage.js`（9 種情境：開局第一句／三人在場高好感／同居中／
有約定／深夜／走去新地方／空白訊息／超長訊息／危險字元），逐回合掃真的送出去的 user＋system。
solo 那側掛在 `solo_all.js` 尾巴（24 顆按鍵跑完一起掃）。現況兩邊皆乾淨。

⚠ **「：」後面接換行是正常的**——提示詞常寫「鐵律：↵1. …」。第一版規則寫成「冒號後面沒東西就叫」，
九條情境全紅，那是掃描器自己的問題。只抓「：｜」與整段結尾。愛叫的掃描器會被直接無視，比沒有更糟。


## 🔰 第一次進來看到的那段（新手引導）

> **2026-09 砍到只剩一句**（玩家「新手教學也砍光，就留一個先邀請人就好」）：
> 「👥 先邀一個人（時鐘左邊）——選好誰跟你走，再開口。」一片空白的開局，第一件事只有一件。
> 舊版那七段裡有三段已經在說謊（可點的時鐘、「同一個地方才見得到面」、📜詳細狀態都拆掉了）。
> `check_ui` 對這段的斷言仍在：點名的圖示必須是真的存在的控制項（現在只剩 👥 一顆）。

鑑賞**沒有** ❓教學 入口——那顆在 `top-navbar`，`applyModeUI` 在鑑賞把整條藏了，而且內容全是聖杯戰爭的事。
所以「這裡怎麼玩」只有一個地方講得到：`enterKanshou()` 裡「歷史是空的」那個分支（`Script_Kanshou.html`）。

七段：怎麼玩／👥 邀請／🗺️ 地圖／⏰ 時鐘／👤 標籤（📜💞📅🤝）／＋ 更多（📚🌍⚙）／🔥 主動掌握。

2026-09 整段重寫（玩家：「整個也很 ai 人機感覺 整體修正 不用太詳細的介紹 也不要說什麼日子還長 簡單的介紹就好」）：
- **修掉一句假話**：舊版寫「這座城一開始只有你」，但 `KANSHOU_STARTER_IDS_` 進場就種了四個人（大河／凜／黑櫻／Saber）。
  現在寫「城裡本來就住著幾個人。想見的人不在，從這裡請進來」。
- **砍掉語氣句**：開場的「你只是住在這座城裡的一個人，想做什麼都可以」與結尾整段「好感只會從相處裡長出來…日子還長」全刪；
  每段只留「這顆鈕是幹嘛的」。字數 從 ~330 收到 ~215。

⚠ **它點名的每一顆按鈕都必須真的存在**——寫一顆不存在的鈕比不寫更誤導人。
2026-09 起這條由 `check_ui.js` 機器擋：抓出那段裡的控制項**圖示**，逐一確認介面別處真的有對應的
`onclick`／抽屜項／`title`。⚠ 比對的是【圖示】不是整串字：圖示穩定，後面幾個字常為了語氣微調
純段落圖示（✍️）登記在 `TUT_NOT_A_BUTTON`。退化確認：把 🗺️ 改成不存在的 🛸，它確實會叫。
⚠ **切段的收尾用結構不用文案**：第一版切到「日子還長」為止——那是一句語氣話，2026-09 重寫時就被刪了，
切點跟著失效、整段只會剩開頭幾個字，掃描器安靜地少看好幾顆鈕。現在切到模板字串真正的結尾 `</div>`。

## 🚫 萌點整組退休（2026-09）

玩家：「萌點不該由我們定義⋯我們應該只要給性格 萌不萌 是玩家的事情」。**鑑賞這邊受影響的地方**：

- 【在場人物】那一行不再有 `萌點(僅供內化)`，【玩家資料·旁白用】那一行也不再有。
  每人每回合少 ~14 字，三人在場約省 45 字。
- 創角提示詞（`KANSHOU_MASTER_GEN_SYS`）拔掉 `npc_intent` 欄與整段「★【萌點怎麼寫】」，
  後者改寫成一句通用的「★【設定怎麼用】以下設定是給你內化的素材，禁複述字面」。
- `translateMoeToDaily_`（戰時萌點→日常萌點的 AI 轉譯）整支刪除；`COL.HERO.DAILY_MOE` 棄用。
- 說書人風格模組 `moe` 改名「**語癖與稱呼**」，只管口癖/專屬稱呼。（2026-09 已整段退場。）
  ⚠ **key 仍叫 `moe`**：那是玩家設定存在試算表上的對位欄，改 key ＝玩家改過的那一格對不回來。
- 同伴卡的改命鈕本來就沒有萌點那顆（2026-07 起隱藏），現在連後端路由 `fateType==='intent'` 也拔了。

完整清單與機器防線見 `SOLO_REFERENCE.md` §萌點整組退休。

## 🌸 氣質格＝第一眼的氛圍，不是舞台指示（2026-09·v79）

v77 把 21 筆「氣質舉止」從純形容詞（厭世冷峻／睥睨的威壓）改寫成「看得到的畫面」，
方向對了一半、落地錯了另一半——寫出來的是**常態舞台指示**：

```
❌ 背脊永遠打得筆直        ❌ 下巴總是微抬半分        ❌ 袖子總是捲到手肘
```

玩家一眼看穿：「這就是強迫 AI 這樣扮演吧」。**「永遠／總是」＋動作＝每回合都得演一次的指令**，
而這一格在卡上的位置是 `特徵:[外貌氣質]`——那是**第一眼的印象**，不是行為守則。

v79 全 25 筆改寫成**可感的氛圍意象**（玩家：「可以用 雨後百合 類似這種的嗎? 帶點微微氣味?」）：

```
阿爾托莉雅  雨後百合的清冽          遠坂凜    剛剝開的柑橘，亮而酸
EMIYA      曬過的舊木頭味，帶點鐵鏽   間桐櫻    開過頭的花，甜得發沉
美狄亞      乾燥花草的微苦          吉爾伽美什 金屬與薰香，冷而貴
```

**寫法規格收成單一真實來源 `AURA_SPEC_`**（`Router_Creation.gs`），五處生成這一格的提示詞
全部接上它：`translateLookToDaily_`（戰時→日常）／鑑賞御主創角／solo 御主創角／
工房 flavor 補完／AI 從者創角。少接一處，那條路生出來的角色就會走回常態動作。

⚠ **機器擋**：`check_seed.py` 第⑪條兩件事一起驗——①種子的氣質格不可出現
`永遠／總是／老是／一律／每次／從不／不停` ②每條叫 AI 寫氣質格的提示詞都必須帶上 `AURA_SPEC_`。
退化測試注入「背脊永遠打得筆直」確認會叫。
⚠ **氣質格裡不可以出現頓號「、」**：`dailyLook` 是三段頓號字串，段內再用頓號會切成四段，
下游 `[kparts[0], kparts[1]]` 就取錯格（這次寫「海風、日曬與一點鹽」當場被 `creation.js` 探針抓到）。

## 🗣️ 提示詞一律寫成「要做什麼」（2026-09）

玩家兩句話定調：

> 「【禁】寫就不要再給他例句了，只要告訴 AI 該怎麼寫就好！說越多它會越想歪」
> 「他本質還是 LLM，會有重點還有語意問題，有時候只會記憶重點、忘記前面的禁——
> 　所以我們專注在要他做什麼，盡可能不要做禁止什麼」

兩個獨立的失效機制：**①否定句後面附例句＝把那個寫法示範給模型看**（`AURA_SPEC_` 第一版就是這樣，
一邊說別寫「背脊永遠打得筆直」，一邊把這句話送進去）；**②長提示詞裡的否定容易被丟掉**，
模型記住了內容、丟掉了那個「不」。

作法：把 33 處硬禁令**全部**改寫成正面指示，剩 0 處。幾個代表：

```
【不可】替玩家決定要去或不去        → 停在等你回答的那一刻，去或不去留給玩家決定
【不可】報幕式宣告階級或數字        → 只讓它從語氣、距離與能不能碰觸裡看出來
【嚴禁】其此後繼續出手/存在於場上    → 其此後只存在於其他人的反應裡
【禁寫「現在擁有什麼」】：開店／…    → 【只寫來到冬木以前的來歷】：…全部留給玩家自己做出來
旁白【不可】用「我」                → 旁白一律用「你」稱呼玩家，「我」留給角色引號內的台詞
```

順手拔掉的例句：`AURA_SPEC_` 的四個示範、`BUST_NOTE_` 點名的那個詞、show-don't-tell 的
「羈絆加深了」「更信任了」、語言鐵律的大陸詞舉例、格式鐵律裡跟「只用頓號」重複的那半句。

⚠ **機器擋**：`check_prompt.py` 第③條——ⓐ硬禁令標記 0（例外登記 `HARD_BAN_ALLOW` 寫理由）
ⓑ否定句後面零例句。⚠ 插值 `${…}` 不算例句（第一版把 `「${svName}」` 當成示範，整排誤報）。
退化測試注入一行同時犯兩條的提示詞，確認兩條都會叫。

**第二輪（同日，玩家「掃 先修改 不要做的事情 不要告訴他」）**：軟性否定詞 136 處也全部掃完，剩 0。
代表：

```
★【鐵律】御主不參與物理交鋒：不可寫御主揮拳/持械/格擋/以身相代
  → ★【鐵律】御主留在戰圈之外、毫髮無傷：能動用的只有【指令、魔力、令咒】，交手全部交給從者
★輸出合法 JSON、禁 Markdown        → ★輸出合法 JSON（純文字，無 Markdown）
不要用一句話帶過或摘要關鍵過程      → 篇幅全部留給實際發生的細節
禁止替御主做決定、禁止詢問玩家…     → 下一步由玩家自己決定：收在等他行動的那一刻
道別完…之後任何回合一律禁止再讓這些人開口 → 之後的回合裡他們只活在其他人的談話中
```

⚠ `nsfwBaseRules` **本體 0 行改動**（紅線①，`git diff` 逐行核對過）；唯一動到的 NSFW 句是
性別配對那行「禁插入式陽具動作，以手指/舌頭/器物替代」→「以手指/舌頭/器物進行」，語意等價。
改完 29 支探針全過。

⚠ 有三支探針原本是在 grep 舊的**措辭**（「不參與物理交鋒」「不可捏造招式」「不可捏造人名地點」），
跟著改成新句子——順手把其中一條從「以身相代只准出現在禁令裡」改成「這個字整個不該出現」，
因為禁令連同它的例句一起被拔掉了。

## ✂️ 鑑賞提示詞瘦身（2026-09·跟 solo 同一輪）

玩家看完 solo 那批後：「鑑賞呢? 檢查看看」。同樣的毛病、同樣的判準（留事實、砍演出指導）。

一次 `actionPlay` 實測：**system 2018→1728 字、user 1934→1805 字，合計 3952→3533（−11%）**。

砍掉的：
- 鐵律 1 四句併兩句；鐵律 2 拿掉分鏡清單「(具體的動作、距離、觸感、開口時的語氣與視線)」。
- 鐵律 4 的兩個例句（`(例：「唔……啾，好甜」)`、`(例：櫻「風音學姐……」)`）。
- 鐵律 5 的選項菜單「(順從/猶豫/半推半就/婉拒皆可)」。
- 風格模組 `feel` 的整串分鏡：「此刻的觸感/冷熱/氣味/聲音、{代名詞}【真正】的情緒…
  卻感覺得到臉發燙、喉嚨發緊」→「旁白從『{玩家}』的感官與【真正】的情緒寫起…
  {代名詞}感覺得到自己的體溫與心跳，看不見自己的臉」。
- 輸出範本：`physical_state` / `appearance_extras` / `noticed` / `name` 各自的否定補充與例字全拔。
- `world_note` 那段兩句重複的規則併成一句。

⚠ `nsfwBaseRules` **本體 0 行改動**（紅線①，`git diff` 逐行核對）；改的是鐵律陣列
（`KANSHOU_STYLE_MODULES_` 的 `def` 與 `miniSystem` 那幾條）與輸出範本。29 支探針全過。

⚠ 順手修掉一個我自己造成的重複字：`view` 模組讓提示詞印出「·旁白**旁白**一律用「你」…」。

### 🔍 掃描器的兩個盲點（同輪修掉，都會讓它安靜地少看）

1. **只掃有 `★`／`【` 的行** → `nsfwBaseRules` 那種「一條一行、沒有標記」的鐵律整批看不到。
2. 改成抽【字串字面量】之後又冒出第二個：**幾乎所有提示詞本體都住在跨數十行的樣板字串裡**，
   單行引號比對一樣看不到（`check_pronoun` 2026-09 踩過同一個坑）。現在先掃一遍反引號奇偶、
   標出哪些行在樣板字串裡面，裡面的整行都算提示詞文字。
   ⚠ 反引號要**只數不在單/雙引號裡的**——引號裡的反引號是資料不是語法，數進去奇偶會歪，
   整段程式碼會被誤判成提示詞（第一版就這樣誤報 4 處）。
   ⚠ `message:` 的判斷要放在【抽字面量之前】，否則那個鍵早就被抽掉了，玩家看得到的錯誤訊息會被誤報。
   ⚠ 結構佔位範例（`「(外貌)、(氣質)」`）示範的是斷句形狀、不是內容，不算例句。

## ⚖️ 誰說了算：GAS 判定 vs AI 即興（2026-09）

玩家看到鐵律 5「答不答應由[個性]×[好感]決定」就問：「沒有 GAS 嗎?」——問得對，但實情是**兩層**：

**GAS 判定（按鍵，結果直接寫進提示詞，AI 只負責演）**

| 動作 | 判準 | 送進提示詞的字樣 |
|---|---|---|
| 告白 | `KANSHOU_CONFESS_BOND_`(60) | ★【告白·成立】／★【告白·被拒】 |
| 同居 | `KANSHOU_COHABIT_BOND_`(90) | ★【同居開始】／★【同居·婉拒】 |
| 相約 | 好感 ＋ 地點/時段合法性 | ★【約定成立】／★【約不成·地點不合適】 |
| 現在一起去 | 地點合法 ＋ 對方在場 | ★【同行成立】 |
| 牽手 | GAS | ★【牽起來了】／★【放手】 |
| 結識 | GAS | ★【正式結識】／結識未成 |
| 赴約 | 時間/地點比對 | ★【依約相會】／★【遲到赴約】 |
| 親密尺度 | 好感分級天花板 | ★【親密尺度·最高優先】…超過的那一步不會發生 |
| 關係基調 | `KANSHOU_RAPPORT_TONE_` | 「事實：對方喜歡你，但你們認識還太短，不會承認。」 |

**AI 即興**：只剩**沒有按鍵、也不改任何狀態**的自由輸入（「陪我去拿個東西」「借我靠一下」）。
GAS 讀不到自由文字要的是什麼，硬給它擲骰只會在玩家只是打招呼時擲出「拒絕」——
所以這一塊按 CLAUDE.md「其餘全靠 AI 即興」留給 AI，這是設計不是缺口。

**真正的問題是那句話沒有分層**：鐵律 5 寫成無條件的「答不答應由個性×好感決定」，
等於給了 AI 一張可以覆蓋 ★【告白·成立】的許可證——**兩個真實來源打架**，
跟 `TOMASTER_BAN_`（對御主態度寫成劇情弧）是同一種形狀。

作法（單一真實來源，不在兩處各寫一次）：
- 新增 `🛑【誰說了算】：標「事實：」與標【成立】【被拒】【婉拒】【開始】的，都是系統已經判好的結果，
  照著演；怎麼表現才依那個人的個性。`（跟在【角色一致性】後面）
- 鐵律 5 改成只管剩下的那一塊：`系統沒有判定的那些小要求，由對方的[個性]×[好感]決定。`
- `drive` 模組的 hint 同步（面板上給玩家看的字也要說實話）。

## 🐛 兩個玩家實測抓到的洞（2026-09）

### ① 同伴被骰進玩家家裡　<sub>`kanshouRollDailyLocation_`</sub>

玩家：「我重開後~ 其他角色直接到我家客廳了... 這樣正確嗎= =」

保底池的過濾寫成 `l.region !== 'room' && l.region !== 'visit'`——排掉了玩家**臥室**與別人的住處，
**卻沒排掉玩家自己家的其餘空間**：`客廳`／`浴室`／`和室` 三個都是 `region:'home'`，一直留在池子裡。
實測 4000 次：**15.4% 的日常落點直接骰進玩家家**（浴室 5.4%／和室 5.2%／客廳 4.9%）。
四位起手同伴同時在場時，等於每次重骰有將近一半機率至少一個人在你家。

上面那行註解本來就寫著「不同行的英靈不該隨機骰進玩家臥室或別人家裡」——**意圖是對的，範圍漏了一塊**。

修法：排除分區收成單一真實來源 `KANSHOU_ROLL_EXCLUDE_REGIONS_ = ['room','home','visit']`。
同居者走的是上面自己的分支（深夜/清晨→`和室`、夜→`home` 池），不受影響（探針驗過 505/600）。

### ② `world_note` 把地方寫成人 → 「你認識了『風音的家』」

玩家：「你認識了「風音的家」。要繼續往來，讓對方成為記得你的人嗎？????? 啥阿= =」

`folkOffer` 直接吃 `aiData.world_note` 裡 `kind==='人物'` 的條目，**沒有任何驗證**——
AI 把玩家自己的住所寫成 `kind:"人物"`，GAS 就拿地名去問玩家要不要結識一個人。

兩層都有責任：
- **提示詞**：`world_note` 那段原本寫著「地點＝多一個去得了的地方｜人物＝這個人還會再出現｜
  設定＝這座城的規矩或風景」，**2026-09 瘦身時被我一起剪掉了**——那是【定義】不是演出指導，剪錯了。已恢復。
- **GAS**（真正的防線，輸入當不可信）：新增 `kanshouNameIsPlace_()` ＋ `kanshouFixWorldKinds_()`，
  比對內建地點、玩家自己開的地方、大區名、玩家住所，以及地名尾巴（`的家/的店/宅邸/屋/館/寺/社/…`）。
  命中就**就地改判成 `地點`** 並清掉 `sex`——改判而不是丟掉，那個名字通常是有意義的新地方，丟了等於白發明一次。
  接在 `folkOffer` 與 `worldWrite_` **兩個消費端之前**，只改一次（單一真實來源）。

探針 `wkind.js`（16 條）：7 個地名要被改判、5 個真人要留著、落點 0/3600 進玩家家、同居者仍回得了和室。

## 🧹 砍掉「AI 發明的人 → 要不要深交」那顆泡泡（2026-09）

玩家：「是不是不要偶遇了.... 太複雜了?? 真的要 就手動加?」→「照這個砍 巧遇留吧」。

把三件被混在一起的事分開之後，該砍的只有一件：

| | 誰決定的 | 處置 |
|---|---|---|
| **巧遇**（`encounterOffer`） | GAS `kanshouRollEncounter_` 骰正典英靈路過 | **留** |
| **folkOffer** | **AI 在 `world_note` 隨手寫了一個人**，GAS 就跳泡泡問要不要升格 | **砍** |
| 同伴日常落點 | GAS 骰 | 留（另修，見上一節） |

`folkOffer` 是**唯一一顆源頭是 AI 自由文字的泡泡**（其餘 `moveProposal`／`nightGuest`／
`promiseWait`／`cohabitOffer`／`encounterOffer` 全是 GAS 判的），所以它也是唯一會問出
「你認識了『風音的家』」這種話的。AI 發明的人本來就會留在【常民】名單上、之後還會出現、
只是不追蹤好感——升格這件事玩家從來沒要求過，卻每回合被問一次。

連帶清掉：`inviteResident` 的常民升格分支（沒有 UI 入口了）＋ `kanshouFolkToRow_` 整支。
**新同伴一律由玩家自己加：👥 邀請（英靈殿）＋ 巧遇結識。**

## 🔍 順著同一種形狀做的整體稽核（2026-09）

問的是同一句話：**「GAS 有沒有別的地方照單全收 AI 的輸出、或照單全收一個沒驗過的地點？」**

**A. 同一個地點過濾寫了四份，其中兩份漏掉玩家家**（跟上一節同源）：

| 位置 | 誰挑的地點 | 結果 |
|---|---|---|
| `kanshouRollDailyLocation_` 保底池 | 系統 | ❌ 已修 |
| `npc_exit` 離場後的保底去處 | 系統 | ❌ **同一個洞，一併修** |
| 「對方開口約你」的約會候選 | **AI/系統** | ❌ **「明天在你家浴室見」，一併修** |
| `promiseMeet`／`proposeMove` 的地點合法性 | **玩家自己指定** | ✅ 不動 |

判準（寫下來免得下次又漏）：**玩家自己指定的地方可以是自己家（那是邀請）；系統或 AI
隨機挑的不行。** 四處統一走 `KANSHOU_ROLL_EXCLUDE_REGIONS_ = ['room','home','visit']`。

**B. AI 輸出 → 持久狀態，逐欄查過**（結論：其餘都有防線，不必動）：

- `rel_changes[].fav_change` → `clampInt(-100,100)` ＋ 梯度天花板（純聊天推不過當前階）。✅
- `npc_exit[]` → 必須比對得到在場的正式同伴列，比不到就跳過。✅
- `intimacy_feedback.npcs[]` → 名字要比對得到、同一局、非 DEAD_、**而且人要在同一個地點**；
  `physical_state`／`appearance_extras`／`mutual_nicknames` 各有 sanitizer。✅
- `photo_caption`／`narration` → 清洗過（公式引導字元、`stripLeakedScaffold_`）。✅
- `world_note` → 結構白名單 ＋ 筆數上限 ＋ 逐欄清洗，**再加這次新增的 kind 改判**。✅

**C. kind 改判補上反向**：把**正式同伴**寫成 `kind:'地點'`，世界上就會多出一個以她為名的
地方。刻意只認**逐字完全相同**的名字——「凜的房間」是真的地名，模糊比對會把它一起吃掉。

## 🔬 鑑賞整體稽核（2026-09·不是跟著症狀打地鼠那一輪）

玩家：「你有確實的看鑑賞整體嗎... 怎感覺都是東修西補的」——說得對，前幾輪確實是跟著回報的症狀修。
這一輪改成照層次讀：**資料層 → 門檻層 → 動作層 → AI 契約 → 前端出口**。

### ① 鑑賞沒有死亡，那個條件卻被抄了 34 遍

`DEAD_` 前綴的**寫入端一支都不在鑑賞**：`Router_Battle`(6)／`Router_Movement`(2)／`Time_World`(2) 都是
solo 的戰鬥與日結算，`Account.gs` 的殘列清理只讀「眾生」。而「鑑賞眾生」這張分頁只有
`Gallery.gs`／`Seed_Codex.gs` 碰得到，兩支都不寫 `DEAD_`。**那 34 處判斷一次都不會成立**，
而且我還在稽核回覆裡把它當成一道「防線」講給玩家聽過。

收成一支 `kanshouIsAlly_(row, gameId, loc)`：`Gallery.gs` 裡 `DEAD_` 前綴的比對 35 → 5
（剩下的是這支自己、御主列查找、自我排除、ID 前綴白名單——都是別的概念）。
之後要加條件（例如「封存中的同伴」）改一處，不是 34 處。

### ② 好感門檻有三處寫死數字

`KANSHOU_REL_TIER_` 是階級表（80 戀人／60 親近的人／40 熟識／20 普通朋友），但
`kanshouAsleepOutcomeStr_` 寫死 `bond >= 80`／`>= 60`、同床判定寫死 `>= 80`——
**同一個數存兩處**，階級表一改就靜靜對不上。

改成從階級表推：`KANSHOU_LOVER_BOND_ = KANSHOU_REL_TIER_[0].min`／
`KANSHOU_CLOSE_BOND_ = [1].min`，`KANSHOU_CONFESS_BOND_`／`KANSHOU_KNOCK_MIN_BOND_` 也改成引用它們。
⚠ 這一改讓 `check_mirror` 整排解析失敗（後端值從字面量變成運算式），所以順手教它**跟一層別名**
（`const A_ = B_;` 與 `B_[0].min`，最多四跳、只認同檔常數）。退化確認：把階級表 60 改成 55，
`KC_CONFESS_BOND_` 立刻報走鐘。
⚠ 兩個新的具名門檻是階級表的別名、不是新的玩家關卡，登記進 `check_wiring` 的 `NO_UI_NEEDED` 並寫明理由。

### ③「結識」是唯一沒有回饋條的動作

相約／牽手／同行／同居／告白五種成敗都會下傳 `kanshouProposalResult_`，前端印一條系統通知；
**只有結識沒有**，玩家得從敘述裡自己猜成不成功。而前端的 icon 對照表**早就備好 `'🤝'` 那一格在等它**
——是後端漏接，不是設計。已補上成功／早就認識／緣分已散三種回饋。

探針 `wkind.js` 加一組契約斷言：後端會下傳的每一種 `proposalResult.type`，前端都必須有對應分支。
退化確認：把前端的 `invite` 改掉，它確實會叫。

### ④ 查過但沒有問題的

- **資料層**：逐欄比對「鑑賞這條路上誰寫誰讀」，沒碰過的欄位都是 solo/經濟層的遺留
  （MONEY／UPKEEP_WEEK／ROOM／MAX_MP／AP…）且多數已登記在 `DEAD_COL_ALLOW`；
  `IS_PARTY` 鑑賞從不寫是已知的（改命走 `k_` 世界豁免）。
- **AI 契約**：`rel_changes`／`npc_exit`／`intimacy_feedback`／`photo_caption`／`world_note` 逐欄有防線（見上一節）。
- **前端出口**：`check_wiring` 的門檻→UI 對照全綠。

## 📏 篇幅檔位＋分頁重排（2026-09）

玩家「字數太多，他有點不知道該寫什麼」「說書人設定可以把字數獨立出來嗎，順便把上限也一起 300/500/700/900 這樣分類」。

**篇幅本來玩家碰不到**：`KANSHOU_WORDS_` 依好感給底盤、這回合有大事再拉上限（300~400 ～ 700~900），
`length` 那格只是**句子**，數字是 `{篇幅}` 代進去的。所以新增的是一個**選數字的控制**，不是再寫一段文字：

- `KANSHOU_LEN_TIERS_`（自動／300／500／700／900）——**字數區間與 `max_tokens` 綁在同一列**。
  ⚠ 只改字數不改 token 會被截斷成壞 JSON（JSON 欄位全滿的固定開銷約 1057 字）。
- 新模組 `lenTier`，`kind: 'pick'`、`slot: 'none'`——面板畫成一排按鈕，且**不進任何提示詞**
  （組裝一律照 key 點名，沒有人依 slot 迴圈，所以 `'none'` 是安全的）。選了就蓋掉自動表，`auto` 維持原行為。
- 原本的 `length` 改名「篇幅的說法」——一格管數字、一格管講法，名字分開才不會兩個「篇幅」打架。

⚠ **`_styles_` 的讀口要排在篇幅計算之前**：第一版把檔位寫在 `KANSHOU_WORDS_` 旁邊，
但 `_styles_` 是在下面才宣告的，當場 `ReferenceError`（`check_undef` 看不到這種「宣告在後面」的時序問題，探針才抓到）。

### 說書人系統稽核（2026-09）

玩家「檢查看看說書人系統有沒有問題」。逐格塞哨兵字串跑了一次全面稽核（不是重跑既有探針）：

**驗過沒問題的**：15 段的 `slot` 都合法；
**14 個文字模組全部真的進到自己宣告的那一段**（sys 9 段／user 5 段，沒有「玩家改了卻沒人讀」的死模組）；
檔位型 `lenTier` 選 300 真的讓提示詞寫 260~340；玩家文字上限 300 字是後端在夾（塞 500 只存下 300）；
**全部關掉之後提示詞不會垮**——鐵律仍有 4 條寫死的、`【在場人物】` 那類事實也還在。

🐛 **抓到一個洞**：檔位欄收表外的值（`styleText:'999'`）會**存得進去、回 `success`、
面板卻沒有任何一顆亮著，實際又靜靜當成 auto**——零錯誤訊息的那種壞法。
補上 `kind: 'pick'` 的專屬分支：只收 `KANSHOU_LEN_TIERS_` 有的 key，選回 `auto` 就刪列不留殘料，
`on` 固定 true（檔位沒有「關閉」這個狀態）。探針 `style.js` 補兩條釘住。
## 🔞 尺度加重（2026-09）

玩家「鑑賞的尺度也一起加重」。動的是 `nsfwBaseRules`（紅線①的演化核心），照規矩量了前後：

**改之前量到的現況**：system 1737 字／user 1861 字／合計 3598；篇幅 480~620 字；
鐵律 11 條；模型 `AI_MODEL`（gemini-3.5-flash-lite）。
**最關鍵的發現是兩個缺口**：① 13 段風格模組裡**根本沒有管尺度的那一格**——鑑賞的「開放」一直只是
「沒人叫它收斂」的副作用，沒有任何一條真的叫它寫開；② 鑑賞跟 solo 的一般敘事**共用 flash-lite**，
補魔那三支 2026-09 已經換 grok 了，鑑賞還留在原地。

**改了兩處**：
1. 新增第 14 段模組 `lewd`（名稱「尺度」，`slot: 'sys'`，玩家可改可關）。
   預設值跟 solo 的 `LEWD_EXPLICIT_` 同一個語域（兩軌各自一份，**刻意不共用**——solo/kanshou 機制徹底隔離）。
   接在鐵律「肢體互動依雙方【性別】欄自然呈現」之後：那條講誰對誰，這條講寫多開。
2. `actionPlay` 的 `aiConfig.model` 由 `AI_MODEL` 換成 `LEWD_MODEL`（x-ai/grok-4.20）。
   temperature 1.08／top_p 0.97／top_k 60 都沒動——那組是既有調校，這次只換模型。

**改完量的**：system 1737→**1861**（+124）、user 1861 **不變**、合計 3722。
鐵律 1~7 **逐字不變**，新的排第 8，舊的 8~11 順移成 9~12——原本正常的配對沒被改壞。
臨時腳本做的重複子句檢查：84 條子句、去重後 84 條，**零重複**；
關鍵詞各出現一次（器官 x1／體液 x1／肢體互動 x1），跟既有的對話格式那條（喘息/吸吮）沒有打架。

探針 `style.js` 從 39 條擴到 43：模組數 13→14、預設鐵律 11→12 條、關一段後 10→11 條、
補號斷言 10→11，另加一組「尺度那段」自己的（預設在第 8 條／關掉整段消失／換成玩家版）。

### 掃描器加寬（2026-09，跟 solo 補魔整修同一輪）

`check_prompt` 的 SOFT_BAN 補了 `不必／不能／別演`（`能不能` 是疑問，`(?<![要能])` 排掉），當場抓到 21 處，鑑賞佔 14：
「不必解釋機制」「不必替玩家找補」「不必強行慶祝或報幕」「【不必】報幕式地宣告年份數字」「別演成翻臉決裂」這類，
全部改成句號停或正面寫法（「慶祝多大、提不提年份數字，都依對方的性格」「兩人仍有往來」）。
`check_pronoun` 不再放行 `她/他`——創角提示詞的「一句她/他今天的日常穿搭」改成「一句今天的日常穿搭」。

## 🗣️ UI 文案口語化（2026-09）

玩家：「檢查鑑賞 減少AI感 盡可能的UI口語化！」

把鑑賞**所有玩家看得到的字**撈出來讀了一遍（確認框／泡泡／回饋條／面板說明／tooltip／按鈕／
後端 `message:`），178 條改寫。「AI 感」來自四種形狀，往後寫文案照這四條擋：

| 形狀 | 舊 | 新 |
|---|---|---|
| **括號裡解釋機制** | 邀「X」搬來同住？(關係夠深就會答應；她本人要在場才問得出口) | 問「X」要不要搬來一起住？ |
| **系統詞漏到玩家眼前**（AI／系統模板／擴寫／分佈／淘汰／解鎖／上限／參數） | AI 會依你填的內容一次幫你擴寫成完整形象（想自己設定…） | 外貌和個性可以先不填，之後都能改。 |
| **公告腔**（`<b>成功！</b>`＋機制後果） | 💗 **她答應了！**你和「X」從這一刻起是戀人。好感的上限就此打開，關係稱呼與專屬稱呼也一併解鎖。 | 💗 她答應了。你和「X」在一起了。 |
| **技術錯誤訊息** | 這局沒有可辨識的世界編號，為安全起見不執行重置。／參數不完整。 | 這局的資料不完整，先不歸零。／少了東西，再試一次。 |

幾個決定：
- **「好感」只留在它是量表的地方**（同伴卡的「好感 75」、🔒 好感到 60 才說得出口）。
  句子裡的「好感↑／好感下降／好感的上限」一律拿掉——第一版改成「她記得」「她有點在意」，
  第二輪連這也砍了（見下：事實講完就停）。
- **玩家面前不出現「AI」這個字**——世界裡那個叫說書人；創角頁「留空由 AI 幫你決定」→「可以不填」。
- **「大區」→「地區」**（玩家面前）；程式識別字與後端 `KANSHOU_REGION_*` 不動。
- 按鈕改成回答句：同意／拒絕 → 一起走／這次不了；邀她同住／現在還不用 → 問她／先不要；
  結識她 → 跟她要聯絡方式；召喚 → 請她來；⚧ 切換性別 → 換性別。
  ⚠ 回饋條裡提到按鈕名的句子要跟著改（「點下方的「同意」」→「點下面的「一起走」」）。
- 深夜訪客兩顆鈕的副標（「夜還很長·想待多久都行」「送她回自己家睡·今晚到此為止」）整個拿掉——
  那是在替玩家預告畫面。

### 第二輪：事實講完就停（2026-09）

玩家從 solo 教學挑出「那是代價，不是挨打」「休越久，外面的世界也走越多」：「這 2 句都是多餘的 AI 感，
說結論就好」。第五種形狀——**事實講完之後掛的那一截評語／提醒／安慰／建議**。鑑賞這邊跟著掃了一輪：

| 尾巴 | 舊 | 新 |
|---|---|---|
| 提醒 | 約好了：明天黃昏在「X」見「Y」。別遲到。 | 約好了：明天黃昏在「X」見「Y」。 |
| 她的反應 | 你赴了跟「Y」的約。她記得。／約過了，你沒去。她有點在意。想見就再約一次。 | 你赴了跟「Y」在「X」的約。／跟「Y」在「X」的約過了，你沒去。 |
| 預告 | 「Y」搬來了。以後晚上她多半在家，睡和室。／牽起來了。「Y」會跟著你走，直到你放手。 | 「Y」搬來了，晚上會在家。／牽起來了。「Y」會跟著你走。 |
| 建議 | 她沒跟。想自己去「X」就點地圖。／你們還沒熟到能直接上門。先在外面多見幾次面吧。 | 她沒跟。／你們還沒熟到能直接上門。 |
| 安慰 | 沒填的部分會替你補上，之後都能改。／把「X」忘掉？之後就當沒這回事。 | 沒填的部分會替你補上。／把「X」忘掉？ |
| 定性 | 現在睡就是放她鴿子。／之後只能算遲到。／沒去不算放鴿子。 | 只留「⚠ 今天還有約：…」那個事實 |

「她記得／她有點在意」那類本來是拿來取代「好感↑」的，但它同樣是我們替玩家下的註腳——
她記不記得、在不在意，該由說書人在下一段裡演出來，回饋條只講「你去了／你沒去」。
寫法同 solo：**GAS 判定出來的事實，句號，停。**

⚠ 探針 `misc.js` 原本 grep 訊息裡的「上限」兩字，改成 `/最多|上限/`；`propose.js` grep 的是
後端提示詞裡的 ★【約定成立】，那不是 UI、沒動。

## ✂️ 2026-09 大瘦身：說書人只剩尺度與篇幅

玩家定案「鑑賞只留【記憶・角色・好感・地圖】」，說書人面板收成兩格。

- **做法不是刪提示詞**：13 段的文字一字不動，只是在模組表上標 `fixed: true`。提示詞組裝照舊讀
  `kanshouStyleDefault_`，所以輸出逐字不變；變的只是「玩家改不到了」。三道一起關：
  面板不列（`actionKanshouGetStyle` 過濾）／路由拒收（`actionKanshouSetStyle` 與 `kanshouStyleWrite_`）／
  **表上的舊列一律忽略**（`kanshouStyleRead_`）——最後這道最重要：上一版玩家改過的字若還留在表上，
  它會繼續生效而面板上看不到，那是「改不掉的隱形設定」。
- 分頁表 `KANSHOU_STYLE_CATS_` 整張砍掉：兩格不需要分頁。
- 🐛 **順手修掉一個從上線就壞的 bug**：前端有兩支函式都叫 `ksPick_`（篇幅檔位一支、尺度三態一支），
  函式宣告後者蓋前者，於是**按篇幅檔位其實走進三態那支、被當成「關閉」送出去**——畫面照常變亮、
  零錯誤訊息，玩家設的字數從來沒生效過。檔位那支改名 `ksTier_`。
  `check_ui` 補一條：攔下 `gasRun` 比對**真的送出去的 payload**（只驗畫面看不出這種錯），已注入退化確認會叫。

## 🗣️ 2026-09 玩家定案：提示詞只給格式，不教該寫什麼

玩家原話：「鑑賞 不要教 ai 該寫什麼 只要用我的格式寫的就好／只有嘴巴會發出的聲音寫在台詞中／
人物的台詞與互動至少佔整體劇情 70%」。

**判準**：一句話如果拿掉之後 AI 仍然知道「這一局的事實是什麼、輸出要長什麼樣」，那它就是在教寫作，砍。
留下的只有三種——①**格式**（引號怎麼用、誰冠名、幾句分段、JSON 欄位）②**事實**（GAS 裁定的結果、
在場是誰、好感幾分、此刻幾點）③**判準**（沒判定的小要求由個性×好感決定、親密尺度天花板）。

砍掉／改寫的（前後都用模擬器逐字量過，紅線①：重複子句 0）：

| 原本 | 現在 | 為什麼 |
|---|---|---|
| 對話格式③「背景描述精簡，篇幅留給互動」 | ④「台詞與人物互動【佔整段 narration 七成以上】」 | 「精簡」是形容詞，七成是可以照做的數字 |
| 對話格式①「口/喉聲音…都寫進單層「」」 | 「單層「」裡【只放嘴巴發得出的聲音】」 | 同一件事改成正面指示，且講清楚引號的邊界 |
| 演這一步「把那一步撐成完整的一拍」 | 只留「從玩家這一步演起，再往下接對方的反應」 | 「一拍」是節奏教學 |
| 推演「確實推演往下走·不停滯敷衍」 | 只留「答不答應由個性×好感決定」 | 前半是在催稿，後半才是判準 |
| 語癖那整條鐵律 | 整條退場（模組一併刪除，13→12 段） | 「自然滲入、偶爾點到即可」整句都是寫作指導 |
| 不出戲「都用神情、語氣與彼此的距離去表現」 | 「系統數值留在系統裡」 | 前半指定了表現手法 |
| 主權「優先接反轉/否定/突發情緒」 | 「被搭話的人本回合都給出反應」 | 前者在排劇情優先序 |
| 你的感受「沉默也要有理由」 | 拿掉；改補「看得到自己的手，看不到自己的臉」 | 後者是事實（第二人稱的物理邊界） |
| 收尾「拋出一個玩家接得住的話題(問句/邀約/此刻在意的事)」 | 只留「停在等玩家回應的那一刻」 | 括號裡那三個就是菜單 |
| 主動掌握②「大量生理特寫(絞緊/吸吮/痙攣/蜜液/水聲/啪啪)+斷續喘息破碎台詞」 | 只留「在尺度範圍內對方主動出擊，色度可以走在玩家前面」 | 那是分鏡表 |
| 尺度（玩家可改那格）「衣服怎麼被解開、手先碰到哪裡、姿勢怎麼換、進出的節奏怎麼變…鏡頭貼著身體走」 | 只留「放到最色、寫滿寫透：器官用本名，體液/聲音/氣味/溫度照實寫，身體的反應寫具體」 | 露骨程度是玩家的旋鈕（留），分鏡不是（砍）。要拿回來：⚙ 說書人設定 →【尺度】→ 自訂 |
| 篇幅「字數靠互動與真實反應撐起來」 | 只留字數 | 七成那條已經把它講完了 |
| 此刻「★光線/氣溫/作息一律依此刻的時段寫」 | 只留時刻本身 | 時段是事實，怎麼用是 AI 的事 |
| 世界有誰「玩家專一對著一個人時其他人背景輕描」 | 拿掉 | 同上 |
| 角色一致性「劇情推進不軟化誰」 | 拿掉 | 同上 |

量到的結果：系統提示詞 1913→1705 字、USER prompt 1610→1497 字，鐵律 12→11 條。
`{主動掌握}`／`{推進}` 兩個佔位隨收尾改寫失效，`_styleVars_` 一併清掉。

⚠ 這輪的形狀值得記住：**這些句子沒有一句是錯的，它們是「說越多模型越想歪」的那種多**。
玩家 2026-09 已經講過同一件事兩次（「LLM 有重點還有語意問題…專注在要他做什麼」、
「【禁】寫就不要再給他例句了」），這次是第三次，對象換成【正面指示裡的形容詞與例句】。
`check_prompt.py` 管得住否定式，管不住這種——**加提示詞之前先問：拿掉之後 AI 還知道事實跟格式嗎？**

## 💔 2026-09：好感度整組砍除

玩家：「還需要好感度嗎…？」查完現況給的判斷是——它其實在做**兩件不一樣的事**：
**當天花板**（超過的那一步不會發生）是限制，**當溫度計**（那句基調事實）是事實。
玩家選了全砍。

砍掉的（鑑賞側；`COL.PC.BOND` 欄位本身留著，COL 是位置索引，solo 仍在用）：

| 砍掉 | 原本在做什麼 |
|---|---|
| `KANSHOU_REL_TIER_`＋`ceiling`＋`kanshouIntimacyLines_` | ★【親密尺度·最高優先】五階天花板 |
| `kanshouSyncRelTier_`／`kanshouBondFloorOf_`／`KANSHOU_BOND_FLOOR_TAG_` | 棘輪地板＋自動改關係標籤 |
| `KANSHOU_RAPPORT_*`／`kanshouRapportTone_` | 好感×熟悉度的 2D 基調句 |
| `KANSHOU_REL_RANK_TAG_`／`kanshouRelRank_`／`kanshouRelTierLabel_` | 關係階質變 ★【關係質變·就在此刻】 |
| `KANSHOU_CHILL_*` | 🧊 冷落趨勢（`pChillStr`）|
| `KANSHOU_SCENE_BOND_`/`_MIN_BOND_`/`_DAY_TAG_`＋`kanshouAloneBondStr` | 獨處時光 +3 |
| `KANSHOU_VISIT_BOND_` | 登門拜訪門檻 40 |
| `KANSHOU_CUSTOM_TAG_BOND_`／`KANSHOU_CLOSE_BOND_` | 自訂稱呼 80／夜未眠 60 |
| schema 的 `rel_changes` 欄與整段落地 | AI 每回合 ±1~5 好感 |
| `set_bond` 的鑑賞分支＋🎚️ 調整好感鈕 | 玩家自己拉數字 |

**改判準的地方**（原本靠數字，現在靠事實）：

- **夜未眠**：身邊有人在就切這一段（原本要 ≥60）。
- **誰留下過夜**：此刻誰真的在你身邊（原本要 ≥80）。提示詞從「★【入夜·好感達門檻】…可自然發展到同床」
  改成 `★【今晚留下的人】：…這一夜怎麼過，依各人的個性與你們之間的歷史決定`。
- **登門拜訪**：屋主入駐就進得去。人家願不願意讓你進門，是 AI 當場演的事。
- **在場卡與世界概況的排序**：改讀**相處次數**（現在唯一還在累積的關係軸）。
- **篇幅 auto**：不再有「關係越深寫越長」，`KANSHOU_WORDS_` 從三階表收成一組數字（400~520／大事 600~750）。
- **關係稱呼**：只剩玩家自己打的字，沒有預設五階、沒有門檻。
  （同一輪稍後改成交給 AI 維護，見下一節——`kanshouFreshRelTag_` 這個過渡用的過濾器因此退場。）

**solo 完全沒動**：`Router_Bond.gs` 的羈絆、`BOND_MILESTONES_` 照舊。
自訂稱呼的 80 門檻在 solo 仍然成立（那段文字會被字面塞進提示詞），常數改名 `CUSTOM_TAG_BOND_`
搬進 `Router_Bond.gs`——名字裡不該再有 `KANSHOU_`。

**掃描器跟著收**：`check_wiring` 的 `TIER_TABLES` 與 `FACT_TABLES` 兩張表現在是空的，
刻意留著空陣列並寫明理由——**日後再有「一張表自己就是各階」或「餵給 AI 的事實查表」，往裡面加一個名字就重新生效**。
`check_mirror` 拿掉 `KC_REL_TIERS_` 鏡射。

量到：系統提示詞 1705→1593 字、USER prompt 1497→1279 字。紅線① 重複子句 SYS 0／USER 0。

## 🏷️ 關係稱呼交給 AI（2026-09·玩家「有些東西開放給 ai 自己修改呢？」）

查現況查出一件事：**專屬稱呼早就是 AI 在寫的**（schema 的 `mutual_nicknames`，`processTags` 去重/消毒/留 3 個，
玩家手打過就蓋【稱呼鎖】），只有**關係稱呼**從來沒開過。當初擋它的具體理由是：

> 玩家實測抓到：把 REL_TAG 改成露骨自訂稱呼後，即使好感才 39/59 也能讓 AI 無視親密尺度天花板直接演到底

**那道天花板已經隨好感一起砍掉了**，所以擋它的牆沒了、它守的門也沒了。玩家定案：兩格都開，打過就鎖。

**做法＝照抄稱呼那套，抽成兩把鎖共用一組 helper**：
`KANSHOU_LOCKS_`＋`kanshouRelLocked_`＋`kanshouRelMemBuild_`，兩把鎖都住 REL_MEM 這一格。

⚠ **`kanshouRelMemBuild_` 是 REL_MEM 的唯一組裝口，少接一把鎖那把鎖就會被洗掉。**
舊版那行是 `pcData[...][REL_MEM] = nickPart;`——直接【整格取代】。如果照抄那個寫法再把關係鎖塞進同一格，
玩家蓋的鎖會在下一回合 AI 寫入時靜靜消失（零錯誤訊息，玩家只會覺得「我明明設過」）。

**餵自己的風險是真的，所以收緊兩件事**：
關係稱呼會被原樣塞進提示詞當既定事實（「她是你的○○」），AI 寫完下一回合又讀回去，**這是會自己滾的**。
① 只收 AI 真的吐了值的回合（跟上回合一樣就填「無」）；② 走跟專屬稱呼同一個消毒口 `sanitizeNickname_`
（剝 `|｜[]`、限 8~20 字）。玩家覺得滾歪了，在 🏷️ 關係稱呼打一次就永遠歸自己。

**順手發現的死碼**：`actionSetNickname` 還在保留 `[態度]` 段——那個欄位 2026-07 就不再落地、全檔無人讀，
是 lossy 重組時留下的孤兒。跟著這次換成共用組裝口一併清掉。

### 第三輪對讀：重複／打架／跨區塊指路

把整份提示詞＋22 筆種子貼給另一個模型做第三輪對讀。它挑出來的**不是字寫錯，是結構**——
這類東西每回合都送、零錯誤訊息，自己讀一百遍也看不出來（每一句分開看都是對的）。三處都改了：

| 位置 | 原本 | 現在 | 為什麼 |
|---|---|---|---|
| `voice` 開頭 | 「…；在場的人各自用自己的名字稱呼。」 | 整半句移除 | 跟 `dialogueFormatRule_` 講同一件事，而那條才說死了「用卡片開頭那個名字、跨回合都用同一個」 |
| ★【誰在場】 | 「這個世界裡已經有名字的人可出現可開口，其餘路人不具名。」 | 「其餘路人不具名。」 | 🐛 跟 ★【這座城裡還有誰】的「不在這一幕的畫面裡」**互相打架**，模型只會挑一句聽。那半句是我前一天自己改出來的 |
| 【在我身邊的人】抬頭 | 「(以下是他們是誰；此刻穿什麼、和我走到哪一步，見下方【他們此刻】)」 | 「(以下是他們是誰)」 | 跨區塊指路：【他們此刻】在 user slot、這句在 system 快取段，每回合白付字數去指另一份文件 |

種子側同批修掉三格**串味**（`CODEX_PERSONA_VER` v82→v83）：百貌的 dailyLook 寫了「深色裝扮」
（衣著，而且跟 dailyOutfit 重複）與「神秘身影」（評語不是外觀）；蘭斯洛特的 dailyLook 寫了「沉穩」
（性格詞放在外貌格）；櫻的 logic 寫了「站著等到對方讓出來」（姿勢——會逼她在大家都坐著時站著）。

**沒採納的三條**：咒腕的「先走」（玩家選擇先觀察 `npc_exit` 移除後的實況）、
斯卡哈的「事後卻私下把人撿回來」（臆測，原本的取捨形狀是完整的）、
規則 6「把日常寫得有滋味」改成臨床寫法（會把玩家明講要的那份溫度洗掉）。

`style.js` 103 條（+4，注入退化四條全叫）。⚠ 同一批抓到探針自己在說謊，見 `CODE_NOTES.md`。

### 資訊邊界：兩人以上同場時的「先知」

第二意見的診斷（行話：全知視角汙染）。三條建議裡**兩條我們早就做到了**，逐條對過代碼：

| 它建議的 | 我們的現況 |
|---|---|
| 物理隔離：不在同一畫面就不進 prompt | ✅ 在場＝同 `COL.PC.LOC`，不同格的人卡片根本不組進 prompt |
| 檢查回饋迴路有沒有變成全域變數 | ✅ 且更嚴：`memory`→`MEMOIR` 欄、`noticed`→`KANSHOU_NOTED_TAG_`，**都寫在那個人自己那一列**，寫回去前還有一道地點比對 |
| SYSTEM 補資訊隔離鐵則 | ❌ **這條是真的洞** |

`perform` 原本只講了一半：「在場的人並不知道自己被這樣寫著」——防了 NPC 讀**自己的**卡，
沒防 NPC 讀**隊友的**卡。補完另一半：

> 「…每張卡上的事是我跟那個人之間的事，其他人手上有的，僅限於自己在場時看得到聽得到的那些。」

⚠ 第二意見給的原句是「嚴格禁止…絕對無從得知…不可展現」，三個否定式會被 `check_prompt` 擋，
而且正犯玩家自己立的規矩。**診斷採納、措辭重寫**——這已經是第二次了。

同批收窄 ★【這座城裡還有誰】：「也可以說出對方此刻在哪」→「我主動問起時，依彼此交情與
日常作息回應對方的去向。」名單上的**地點本身留著**（系統真值，玩家真的能走過去，
當初開這口子就是為了修「AI 自己編一個地方出來」），改的只是怎麼說出口。

`style.js` 105 條（+2，注入退化兩條都叫）。
## 🗑️ 地點整組退休（2026-09·玩家定案）

玩家：「**只要扯到移動都會很怪**！直接都砍了吧」「**地點就是可以一個亂掰的背景**」
「現在地點全部拿掉~~~~」。

**壓垮它的那一局**：劇情已經走到咖啡廳了，下面泡泡才問「要不要去咖啡廳」，玩家按了，
於是**又走了一次**——原本跟他同地的凜被重新搬了一遍，場景倒帶成跟櫻在一起。
那正是上線時列出來要盯的「AI 搶跑」：**敘事跑在資料前面**。加上泡泡只是把它變得看得見。

### 在場改由同行決定

`presentRows` 從「同 `COL.PC.LOC`」改成**就是同行清單**。這是 2026-09 稍早那一刀
（在場改成同地點）的回退，但前提換了：那次是為了修「全部人在客廳但只有大河跟我說話」，
而那個錯亂的來源正是**系統在記位置**這件事本身。

| | 之前 | 現在 |
|---|---|---|
| 誰在這一幕 | 站在同一格的人 | **同行清單**（上限 3） |
| 解散一個人 | 他留在原地、還在場 | 他就離開這一幕 |
| 🙋叫來／🚶去找 | 把人搬過來／走過去 | **整組移除**（路由 op `bring` 也砍了） |
| 她此刻在哪 | 面板顯示、提示詞也給 | 不存在這個概念 |

### 整組砍掉的東西

**後端**：`moveTarget`／`newPlace`／`newPlaceRegion`／`newPlaceOwn`／`moveWith`／`movePrompt`／
schema `move_to`／`at`／`region`、★【地點】、★【這座城裡有哪些地方】、★【這座城裡還有誰】、
★【你的店今天開張】、★【第一次來到這裡】、`KANSHOU_LOCATIONS_`／`KANSHOU_STARTER_PLACES_`／
`KANSHOU_REGIONS_`／`KANSHOU_REGION_KIND_`／`KANSHOU_REGION_CAP_`／`KANSHOU_MAP_SEED_TAG_`、
地圖種子、大區/地點的七個管理 op、世界帳本的「有根條目」(`at`)、時鐘 HUD 的 📍。

**前端**：🗺️地圖分頁（鑑賞底下整頁隱藏）、分區切換、地點按鈕、新地方輸入、大區增刪改、
🚶去找、🙋叫來、一起過去泡泡、同伴列的「她在哪」。**共 264 行**。

⚠ **`COL.PC.LOC` 欄不刪**——solo 還在用它一百多處（`Router_Movement`／`Router_Battle`／
`Time_World`…）。鑑賞這一軌單純不再讀寫（CLAUDE.md：COL 是位置索引，寧棄用不刪欄）。

### 世界帳本的「地點」還在，但只是 lore

`worldFeed_` 原本明確排除 `kind:'地點'`（理由是「脈絡由 ★【地點】那行給」），那行沒了，
所以**地點現在跟人物／設定同級**：AI 隨口編的「巷口那間舊書店」記得住、之後也餵得回來，
只是**走不進去**。這正是玩家要的「可以一個亂掰的背景」。

### 順手抓到的真 bug

`worldFeed_` 的簽名少了一個參數，而 **solo 的戰記** (`Router_Narrative.gs`) 還用舊簽名呼叫
——地點字串被當成在場名單，整段因果會靜靜餵不回來。`saga.js` 當場叫，才沒有漏掉。

### 同一批的其他改動

- **歷史窗口 2 → 4**（玩家：「劇情拉長到 4 筆 2 次對話」）。單位是**筆**，4 筆＝2 輪。
- **【他們此刻】的抬頭字砍掉**：「同行中 X（一路跟著我走）／這個地方此刻還有 Y」那兩句，
  在「在場＝同行」之後只是把同一份名單用兩種講法再講一次。
- **在場來由只剩「時間流轉之後」一種**——「剛結伴走到」不再是會發生的事。
- 過夜那句從「今晚就在**這個房間**裡過夜」改成「今晚跟我一起過夜」（沒有房間這回事了）。

**量出來**：user prompt 約 700 → **441 字**，system 1470 字上下不動（卡片為主）。
`check.sh` 全綠、37 支探針全過（刪掉 `world_at.js`／`movebub.js`／`locdesc.js`／`bubattr.js` 四支，
它們驗的東西整個不存在了）。


### 🎬 留下來的那一格：背景場景

玩家：「**只要留一個 給 AI 隨時變動的背景版地點就好**」。

它**不是**地點系統的縮小版——沒有名單、沒有驗證、走不進去、也帶不走人。就是**一句話的布景**：

```
★【場景】：上一段演完，我們在「巷口的關東煮攤」。
```

⚠ **措辭刻意是「這一段演完，人最後在哪」而不是「這一幕的設定」**（玩家原話：
「劇情說到哪裡，應該就要到哪裡」）。第一版寫成「換了地方就換掉它，**沒換就照原樣寫回來**」
——那是在教模型抄自己，正是 `rel_tag`／`physical_state`／`noticed` 三次固化的同一個形狀。
改成**每回合都問一次「演完之後人在哪」**，它就沒有「維持現狀」這個選項可抄。

| | 做法 |
|---|---|
| 誰決定 | **AI**。schema 多一格 `scene`，它想換隨時換 |
| 存在哪 | 玩家那一列的 `COL.PC.LOC`（那一格在鑑賞已經空著）。⚠ 欄位名是歷史包袱，它現在的語意是「這一幕的背景」；solo 的 LOC 仍是真的地點，兩軌互不相干 |
| 開場 | 「我的房間」——給一句話當起點，AI 第一回合就有畫面可寫 |
| 邊界 | 限 12 字、剝結構字元（跟玩家自由輸入同規格）；AI 這回合沒寫就沿用上一個 |
| 跟「誰在場」的關係 | **零**。在場永遠只看同行名單，換場景不會動到任何人 |

⚠ 這是一個**自我回饋欄**（AI 寫 → 存 → 下回合餵回去），跟踩過三次的那個形狀同型
（`rel_tag`／`physical_state`／`noticed`）。這裡是**刻意**的——布景本來就該持續到被換掉。
但如果實玩發現它卡在同一句不動，那就是同一個坑的第四次，回頭看那三次怎麼修的。

探針 `scene.js`（11 條），注入退化三條都叫（不限長／不消毒／不餵回去）。

### 同一批的面板修正

- 世界帳本面板多一組 **🗺️ 這座城裡的地方**——地點現在是普通條目，該跟人物／設定並列。
  舊的 `locNote`（「地點歸地圖，這裡只提醒一句它們在哪一區」）連同地圖一起移除，兩軌都沒有了。
- 同伴面板的抬頭本來就是「👥 這個世界裡的人」，**不再分在不在同一地**（那兩欄已隨地點退休）。
- 首頁那句「出門走走會遇見人」改成「帶著誰一起，故事就從哪裡開始」。


### 🗑️ 起始住民整組取消（2026-09）

玩家：「**取消內建！**」。開場不再自動入駐大河／凜／櫻／SABER 那 4 位——
新局是**真的一片空白**，誰住進來由玩家 🌟 召喚決定。

理由：地點退休、在場改成同行之後，那 4 位躺在面板裡什麼也不做，
卻先吃掉一半的世界位子（`KANSHOU_WORLD_MAX_` 8 格）。現在 8 格全是玩家自己邀的人。

⚠ **只影響新開的局**——既有存檔那幾位照舊在（種子只在 `enter_kanshou` 那一刻跑）。
⚠ 召喚是免費的、沒有門檻，只擋世界人數上限，所以「取消」的代價只有多點一次。

⚠ **探針全批受影響**：八支靠「開局就有人」的探針要自己召喚，
所以 `probe.js` 多了一支共用的召喚 helper（`summonN`，住在探針裡、不是 gas 代碼）。
⚠ 寫的時候踩到一個：**召喚會順手把人加進同行**，所以要驗「開局誰都不在」的探針
得先 `op:'clear'`，不然它驗到的是召喚的副作用。


### 🔁 ★【這座城裡還住著】救回來（我砍過頭了）

玩家：「如果我召喚 4 個人都沒有用同行呢，AI 能知道他們在哪裡而已?」
實測答案是 **一個字都沒有**：

```
這座城裡住著：SABER、無名（EMIYA）、庫·丘林、RIDER（4 位，沒有人同行）
  SABER：system 沒有　user 沒有
  （四位皆然）
  現在沒有人跟你同行，你是一個人。
```

原本那一段有**兩半**：
- 「她此刻在哪」——地點系統的補丁，**該砍**
- 「這座城裡有誰」——**跟地點無關**，而且它是唯一讓「召喚」有意義的東西

我把整段一起砍了，於是召喚只剩「加進一個你挑得到的清單」，**這座城從 AI 的角度是空的**。

現在只給名字：

```
★【這座城裡還住著】：SABER、無名（EMIYA）、庫·丘林、RIDER。
我們都認識他們，他們此刻不在這一幕裡；我問起就照認識的樣子回答。
```

⚠ **同行的那位會從名單裡拿掉**（她已經有卡片了，不重複出現）。
⚠ 舊版還會附大小寫別名（`EMIYA/Emiya/emiya 也是同一人`），那是為了讓 `at` 逐字比對得上；
`at` 已隨地點退休，附別名純粹是雜訊——實測會生出「無名（emiya）也是同一人」這種沒意義的附註。
⚠ 成本：4 個名字約 40 字（user 430 字裡的 9%）。上限沿用 `KANSHOU_WORLD_ROSTER_CAP_`。

`party.js` 補 ⑩ 一段（6 條），注入退化確認會叫。

**教訓**：一段提示詞有兩個不相干的職責時，砍掉其中一個的理由**不會自動適用於另一個**。
分開評估，別因為「這一段是為了地點才長出來的」就整段丟。

#### 「他現在在做什麼」——刻意不存，讓 AI 掰

玩家：「**我不要真的近況，給 AI 掰就可以呢? AI 知道有誰，依照時間可能性隨便掰?**」

這是整段裡最划算的一刀。原本我提的方案是**每人一格近況**（AI 寫、存起來、下回合餵回去），
玩家直接把它換成**零儲存**：AI 手上已經有名單 ＋ ★【此刻】的時段，掰得出來。

```
★【這座城裡還住著】：凜、櫻、大河。我們都認識他們，他們此刻不在這一幕裡；
我問起誰，就依此刻的時段說說那個人這時候大概在做什麼。
```

**不存就不會固化**——今天已經踩過三次自我回饋欄卡住不動（`rel_tag`／`physical_state`／`noticed`），
每人一格近況就是再開 4~8 個同形的坑。這一行是那個方案的等價物，成本 0。

⚠ `check_pronoun` 當場擋下第一版的「說說**他**這時候在做什麼」——名單是男女混的，
泛指要用中性寫法（改成「那個人」）。


## 🎭 三層在場（2026-09·玩家定案）

玩家：「左邊是同地人物（包括同行）。如果玩家點同行就不會分開；如果沒點同行，那個角色可以
隨時離開到一個類似**等待召喚的地點**。如果玩家有扯到、或是劇情有安排到，就可以讓它們移動到
玩家當前地點進行互動。」

| 層 | 誰決定 | AI 動得了嗎 | 面板 |
|---|---|---|---|
| **同行** | 玩家（＋同行） | ❌ **動不了** | 🫂 同行中·不會自己走 |
| **臨時在場** | AI（`cast.join`） | ✅ 也能讓他走（`cast.leave`） | 👋 此刻在這一幕裡 |
| **待命** | — | 被提到／劇情需要就進場 | 🌹（★【這座城裡還住著】） |

**在場 ＝ 同行 ∪ 臨時在場**（`KANSHOU_ONSTAGE_TAG_`，存玩家列）。
**待命 ＝ 這一局的人 − 在場**，就是那條 ★【這座城裡還住著】。

⚠ 這個設計**剛好解掉當初砍 `npc_exit` 的理由**（「AI 不問玩家就把人移出同行名單」）：
現在是**兩個不同擁有者的清單**，AI 只動得了自己那一層。

### 三道邊界（全在 `actionPlay_`）

1. **同行者豁免**：`cast.leave` 對同行者無效，也塞不進臨時在場那一格（兩層不重疊）
2. **只認這一局的人**：名字走 `kanshouNameCandidates_`，查無此人一律忽略
3. **在場總數封頂** `KANSHOU_ONSTAGE_MAX_ = 5`（同行 3 ＋ 臨時 2）

⚠ **哪一層才是真的擋住**：`presentRows` 那個聯集自己也封頂、也無條件包含同行者，
所以 cast 那兩道是**第二層防線**。寫探針時第一版驗不到它們（拿掉 guard 照樣全綠）——
改成直接驗【存下來的那一格】才測得到。**兩層都要有斷言，別只驗看得見的那一層。**

### 記憶不受進出影響

玩家問：「這樣一直進出地點，還會有連續的記憶嗎?」——**會**。
共同回憶／她眼中的你／相處次數／專屬稱呼全部存在**她自己那一列**，不在對話歷史裡。
她走了三天再回來，卡片逐字重建成原樣（`cast.js` 有一條專門驗這件事）。
⚠ 唯一會變慢的是**相處次數**：只在她在場的回合 +1，所以常進出的人距離四階推得慢——
那是對的，你們相處的時間真的比較少。

探針 `cast.js`（14 條）＋ `uiparty.js` ⑤（4 條），注入退化都確認會叫。


### 🎭 不召喚、直接哄 AI 掰一個 SABER 出來會怎樣

玩家實測提問。三個結果，其中一個是沒想到的 bug：

| | 結果 |
|---|---|
| `cast.join` 騙得了嗎 | ✅ **騙不了**——鑑賞眾生沒有那一列就沒有 id，`_idOf` 回空字串，整筆忽略 |
| 寫成【常民】(`world_note kind:'人物'`) 行不行 | ✅ **行**：她會被記住、會出現、會開口，只是沒有卡、不追蹤關係 |
| 之後才真的召喚她 | 🐛 **同一個名字兩份**——一張在場卡 ＋ 一條常民條目 |

第三條是真 bug：AI 會同時看到「SABER（完整人格卡）」與「SABER（金髮碧眼的少女，住在附近）」。

**兩道都修**（一道處理未來、一道處理既有髒資料）：
1. **召喚＝升格**：`actionKanshouSummonHero` 清掉同名的 `人物` 條目——她現在有完整的卡了，
   那條一句話的 lore 就該讓位。
2. **餵回去時過濾**：`worldFeed_` 多收一個 `allyNames`，跟正式同伴同名的 `人物` 條目一律不餵。
   既有存檔裡已經重複的，清不到也不會再一起出現。

⚠ 這其實回答了更早那個問題「**能用世界書直接呈現角色嗎**」——**已經可以了**：
AI 掰出來的人就住在世界帳本裡，記得住、會出現，只是不追蹤關係。
要追蹤才需要召喚，而召喚會自動把那條 lore 升格掉。

探針 `fake.js`（10 條），注入退化兩道都確認會叫。

### 世界會不會一直長人

玩家：「這些居民……我停留在一個地方就會一直長?」——**不會**，四道煞車：

| | |
|---|---|
| AI 一回合最多寫 | 3 筆（`writeMax`） |
| 人物上限 | 40 條（地點 60、設定 50） |
| 超過就淘汰 | 最久沒被提到的先掉；📌 釘住的不掉 |
| 每回合餵回去 | 最多 6 條，不是全部 |

同名**不會新增一列**，只更新「最後被提到」，所以你常講的人永遠排在前面。
⚠ 同批把 `world_note` 的指示收緊：原本只說「新出現的就寫」，**沒說值不值得記**——
改成「挑之後還會再遇到、再提起的寫」。

⚠ 已知的死條件：`worldEvictees_` 還豁免 `AT`／`OWN` 不淘汰，但那兩格隨地點退休後永遠是空的。
無害（純 no-op），沒有一起清是因為那支是兩軌共用的，動它沒有好處。


### 🐛 「其餘路人不具名」把常民也吃掉了（同一個形狀第二次）

玩家追問：「所以不召喚一樣可以讓 AI 把它當成背景人物互動?」——**可以**，但把兩句話
放在一起看，它們在打架：

| | |
|---|---|
| ★【誰在場】 | 「**其餘**路人不具名。」 |
| 世界帳本那條 | 「常民……**開口**、被寫進場景都可以」 |

`其餘` 讀起來把帳本裡的常民一起包進去了，模型會挑一句聽——跟這一天稍早修掉的
★【誰在場】↔★【這座城裡還有誰】**完全同形**（一句放行、一句收回）。

收窄成 **「卡片與帳本都沒提到的路人不具名」**：路人＝真的沒有名字的那些，
常民有名字、在帳本上，不在此列。

⚠ **同一個坑第二次**：`其餘`／`可出現可開口` 這類**靠讀者自己界定範圍**的詞，
在一份由好幾塊拼起來的提示詞裡特別危險——另一塊隨時會長出新的成員進來。
寫範圍就把範圍寫死（「卡片與帳本都沒提到的」），別用相對詞。
`style.js` 兩條墓碑釘著。
