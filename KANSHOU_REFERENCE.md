# KANSHOU_REFERENCE.md — 鑑賞（慾海後日談）唯一現況真相

> **這份只寫「現在是什麼」，不寫演變過程。** 想動鑑賞任何一塊、開機失憶要重建脈絡，看這一本就夠，不必去 `SOLO_REFERENCE.md` 考古編年史。
> **行號會漂，一律以「函式名／常數名」為錨。** 每條都以現行代碼為準（2026-07 對 main `1b4d98c` 核實）。
> 🔴 動手前必讀 `CLAUDE.md` 紅線：`Gallery.gs` 的 `nsfwBaseRules`＋NSFW 機制不可改（玩家授權才碰）、`Engine_Combat.gs` 的 `callGeminiAPI` 是兩軌共用基礎設施、model id 不進 repo。（2026-07 已移除「GAS repo 不可動」一條，九州衍生碼清理判斷併入 CLAUDE.md 工程準則）

---

## 🌹 一句話定位

鑑賞＝**約會模式**後日談（NSFW）——**主選單直接進、從英靈殿直接召喚同伴，不需先打贏戰爭**（`actionEnterKanshou`＋`actionKanshouSummonHero`）。**一個更單純的世界**：無戰鬥、無經濟、無房東房客世界觀，一張約會大地圖，她們各自有自己的住處與作息，其餘全靠 AI 即興演出。建在 `nsfwBaseRules`＋`buildDefaultSystemPrompt` 上、與 solo 共用 `actionPlay` 引擎與 `callGeminiAPI`。

### 世界觀鐵則（都是「已砍」的反向定義，別復活）
- **無戰鬥／無經濟**：`Router_Action.gs` dispatcher 明文擋 `KANSHOU_BLOCKED_ACTIONS_`。錢/打工/房租/商店/客房/肉償/欠租/宵禁全部**已刪**，grep 查無定義。AI 需要時自己掰、不寫試算表。
- **無房東房客**：獨立作息制（independent residency），不是跟隊模型。她們有自己的家（`KANSHOU_HERO_HOME_`手寫豪邸優先，查無退回`KANSHOU_GENERIC_HOME_POOL_`隨機分配、見下方§住處分配）、每天自己移動。
- **無奪杯封存**：`claim_grail`/`actionClaimGrail`/排行榜/戰記**已刪**。
- **全程無花錢入口**（跟 solo 一樣）。`COL.PC.MONEY/UPKEEP_WEEK/ROOM` 是恆空死欄。

---

## 📊 資料層

### 分表隔離（與 solo 完全不污染）
- 鑑賞資料寫 **「鑑賞眾生」分頁**（`getKanshouPcSheet_`，lazy 建、schema 複製主「眾生」表頭、`COL.PC` 索引一致），與 solo「眾生」表隔離。
- **KPC_ 前綴路由**：dispatcher（`handleGameAction`）見 `pcId` 以 `KPC_` 開頭 → `sheets.pc` 指向鑑賞眾生。solo 是 `PC_`。
- **引擎硬擋**：`actionPlay` 開頭 `pcId` 非 `KPC_` 直接 return。
- **輸入清洗**（2026-07 稽核補）：主對話 `message` 欄位嵌入提示詞前補 `｜【】` 分隔符清洗，防玩家用這幾個符號偽造 MEMORY 標記或偽造「★【...】」格式的假系統指令段（遊戲本身的真實系統指令就長這樣，不濾會讓兩者無法區分）。
- **歸屬驗證**（2026-07 兩階段補洞後現況）：⚠ 舊版每個 kanshou handler 各自呼叫 `kanshouOwnedRowIdx_`(內部反查帳號表 KPC 欄)才敢信任 pcId；全面稽核先後抓到 `actionPlay`/`actionGetAlbum`/`actionAlbumDelete` 等多處漏掉這道驗證，形同可憑猜中/拿到的 `pcId` 冒名讀寫他人存檔。**這道驗證後來整個上移到 dispatcher**：`handleGameAction`（Router_Action.gs）在派發到任何 handler 之前，統一呼叫 `verifyPcOwnership_(acctName, pcId)`（Account.gs，反查帳號表 `COL.ACC.KPC`/`COL.ACC.PC`），白名單 `OWNERSHIP_CHECK_EXEMPT_` 外的動作一律先過這關。故 `kanshouOwnedRowIdx_` 已刪除，改為純索引查找版 `kanshouPcIdx_(data, pcId)`（Gallery.gs）——所有 kanshou handler 進來前 pcId 已保證屬於呼叫者，不必再反查一次帳號表(那是同一趟請求裡的第二次整表讀，純浪費)。**任何新增的 kanshou action handler，只要會讀寫 pcData，一律用 `kanshouPcIdx_` 換 index 即可（歸屬驗證由 dispatcher 保證，handler 不必自己再查帳號表）。**
- **前綴白名單**：`KPC_`(御主 avatar)／`KHV_`(直接召喚同伴)／`DEAD_`；`KSV_` 是**舊奪杯封存邀請的遺留前綴**——封存管線已砍、不再產生新 `KSV_` 列，僅在 sync／`isKanshou` 判定保留向後相容識別（別當現行機制）。
- 🐛→✅ **2026-09 跨帳號污染：名字一旦離開陣列就不帶 game_id 了**。鑑賞眾生是**全帳號共用一張表**，而每個人都從同一座英靈殿召喚——**撞名是常態，不是巧合**。
  `actionPlay_` 的「第一次同床」蓋章：上游 `allEstablished` 有 `sameGame(r)` 過濾沒錯，但 `intimateNightNames`
  取出來的是**名字字串**，回頭 `pcData.forEach` 掃全表時只比名字。實測（`scratchpad/size/crossgame.js`）：
  甲乙兩帳號各召一個 SABER，甲按睡覺 → **乙那一列也被蓋上 `同床:1`**。補 `sameGame(r)` 修掉。
  ⚠ **這是一種形狀，不是一個 bug**：「先用 game_id 濾出一批列 → 取出名字 → 拿名字回頭掃全表」。
  全樹掃過只有兩處，另一處在 `actionRest` 的死分支裡（已連分支一起砍，見 `SOLO_REFERENCE.md`）。
  **之後要用名字回頭找列，一律先問「這個名字在別人那局會不會也有」。**
- 🐛→✅ **2026-09 長名同伴靠名字查無此人**：`sanitizeUserData_` 對 `targetName` 一律截到 `NAME_MAX=20`，但美遊／小黑／伊莉雅(Caster install) 三位不在 `KANSHOU_CASUAL_NAME_` 裡、列上存的是 23~30 字全名——釘選／刪除共同回憶、關係稱呼、專屬稱呼全部「找不到這位同伴」。根源修法＝**id 優先、名字備援**（`findPcRowIdx_` 本來就吃 `{id, name}`）：前端三個入口帶 `targetId`（關係面板開啟時記 `_krTargetId`；共同回憶按鈕帶 `c.id`），後端三支 handler 傳 `{ id: targetId, name: targetName }`。探針 `names.js`。
- 🐛→✅ **2026-09 世界帳本條目名被清成純中文**：`kanshou_world` 的條目名原本借用 `name` 鍵，被 `sanitizeUserData_` 的 `CHINESE_NAME_FIELDS` 當成 solo 創角姓名——剝掉英數與「·」、截到 10 字；而 AI 寫進同一張帳本的名字卻允許英數＋20 字。改鍵名 `entryName`（含 rg_rename 的 `newName` 一起進 `STRICT_NAME_FIELDS`：剝 HTML／MEMORY 結構字元、上限 20），前後端八個呼叫點同步，`check_contract` 盯兩端。
- **歷史暫存**：solo/鑑賞**共用同一張「歷史暫存」表**，靠 `pcId` 前綴（`PC_` vs `KPC_`）隔離、非物理分表——架構唯一例外，記在案。

### COL.PC 鑑賞實際用到的欄位（定義 `Core_Settings.gs`）
| 欄 | 索引 | 鑑賞用途 |
|---|---|---|
| `MEMORY` | 12 | 所有中文方括號標記共用 cell（見下表），大量讀寫 |
| `LOC` | 6 | **鑑賞「是否同地在場」的唯一判準**（取代 IS_PARTY）。行程骰每回合寫回 |
| `BOND` | 24 | 好感。橋段+3／赴約+5／爽約-5／AI rel_changes 加減（夾天花板） |
| `REL_TAG` | 25 | 五階關係標籤。GAS 自動升降（`kanshouSyncRelTier_`），**AI 無寫入權**、玩家 UI 手動改（`update_rel_tag`，自訂文字需 bond≥80） |
| `REL_MEM` | 28 | 關係專屬記憶／專屬稱呼／態度／稱呼鎖。AI 寫回；專屬稱呼另可由玩家經 `kanshou_set_nickname` 手動鎖定（bond≥80） |
| `MEMOIR` | 27 | **共同回憶**（見專節）。原 `MAJOR_EVENT` 死欄復用 |
| `NAME/SEX/PREF/TRAIT/BACK/FACTION/GAME_ID/ID` | — | 組敘事卡片、sameGame 過濾、前綴判定 |

⚠ **`IS_PARTY`(26) 鑑賞刻意不讀不寫**（改用 LOC 判在場）；solo 仍用，兩軌並存。
⚠ **死欄`MONEY`(33)/`UPKEEP_WEEK`(34)/`ROOM`(35) 恆空**、鑑賞無讀寫端——**不可刪欄**（COL 是位置索引，刪了後面全錯位）。

### MEMORY 標記全表（工廠 `makeIntTag_`/`makeTextTag_` 在 `Core_Settings.gs`；多標記以全形 `｜` 分隔共用一 cell）
| 標記 | 完整格式 | 存哪列 | 讀寫端 |
|---|---|---|---|
| 【約定】 | `【約定】absDay:band:loc` | 該同伴列 | `kanshouGetPromise_`/`SetPromise_`/`ClearPromise_`/`PromisePin_`；赴約結算讀清 |
| 【同居】 | `【同居】1` | 同伴列 | 寫入同居落地；讀 `kanshouIsCohabit_` |
| 【牽手】 | `【牽手】對象名` | **玩家列** | 牽手 set／放手 set('')／在場氛圍讀 |
| 【邂逅】 | `【邂逅】逗號分隔姓名` | 玩家列 | 永久巧遇名單（去重） |
| 【邂逅中】 | `【邂逅中】heroId` | 玩家列 | 本次到訪暫存，換地點清（`clearKanshouActiveEncounter_`）。**2026-07 稽核補**：原本只在移動/結識/`endDay` 清，`advanceHours`/`jumpBand`/`jumpFestival` 這幾條跳時間路徑漏呼叫，路人巧遇旗標可無限期滯留——已在 `advanceHours>0` 分支補齊呼叫 |
| 【住所】 | `【住所】家名` | 玩家列 | `getKanshouHomeName_`（常見預設是`(玩家名)的家`；玩家連名字都沒有才退回通用「我家」）／改名 set |
| 【晨間餘韻】 | `【晨間餘韻】同伴名` | — | 同床隔天引子，讀一次即清 |
| 【初見日】 | `【初見日】absDay`（IntTag 預設0） | 同伴列 | 首次同地寫入，紀念日里程碑比對 |
| 【初次】 | `【初次】事件:absDay,事件:absDay,…`（存儲上限`KANSHOU_FIRSTS_CAP_`=20筆） | 同伴列 | 💞 結構化「第一次」帳（`kanshouGetFirsts_`/`kanshouStampFirst_`，冪等·只記最早那次）。蓋戳點：牽手／同居／同床／約會（赴約成立·準時遲到皆算）／各式橋段（以事件名當 key·僅非拒絕分支）。每回合取最早`KANSHOU_FIRSTS_SHOW_`=5筆進提示詞當既定事實；同月同日撞上則另跳「週年」加強句 |
| 【關係階】 | `【關係階】N`（1=點頭之交…5=戀人·IntTag 預設0） | 同伴列 | 💗 歷來最高關係階（`kanshouRelRank_`）。只升不降，用來偵測「這回合剛跨階」→注入質變提示。**刻意看 BOND 不看 REL_TAG**（玩家自訂稱呼後 REL_TAG 不再等於梯度字面，跨階演出不該因此消失） |
| 【帳號】 | `【帳號】acctName` | — | 人工檢視辨識（非驗證，歸屬走帳號表） |
| 【相處】 | `【相處】N`（IntTag） | 同伴列 | `KANSHOU_MET_COUNT_TAG_`：同地回合累計，`kanshouKnownTier_` 算「她對你的知情度」(初識/混熟/老交情) |
| 【眼中的你】 | `【眼中的你】句／句`（≤3 條·各≤14 字） | 同伴列 | `KANSHOU_NOTED_TAG_`：AI `noticed` 落地（只記會改變之後怎麼對玩家的發現），`kanshouKnownOfYou_` 讀進在場卡 |
| 【時間流速】 | `【時間流速】分`（0/10/20/30） | 玩家列 | `KANSHOU_PACE_TAG_`：`actionKanshouSetPace` 寫、`kanshouPaceOf_` 讀 |
| 【設定已補】 | `【設定已補】1` | 玩家列 | `KANSHOU_BACKFILL_DONE_TAG_`：創角 AI 擴寫只跑一次的閘 |
| 【昨夜道別】 | `【昨夜道別】同伴名` | 玩家列 | `KANSHOU_NIGHT_PART_TAG_`：結束一天時身邊的人，隔天引子用完即清 |
| 【夜未眠】 | `【夜未眠】absDay` | 玩家列 | `KANSHOU_NIGHT_SCENE_TAG_`：兩段式就寢第一段（人釘在房裡、時間照流），第二次按睡覺才真 endDay |
| 【主動日】 | `【主動日】absDay` | 玩家列 | `KANSHOU_INITIATIVE_DAY_TAG_`：她主動來找的當日鎖 |
| 【橋段日】 | `【橋段日】absDay` | 同伴列 | `KANSHOU_SCENE_DAY_TAG_`：夜襲「別有用心」加分的當日鎖 |
| 【夜訪日】 | `【夜訪日】absDay` | 玩家列 | `KANSHOU_KNOCK_DAY_TAG_`：深夜訪客擲骰當日鎖（結束一天可被按很多次） |
| 【夜訪客】 | `【夜訪客】同伴名` | 玩家列 | `KANSHOU_NIGHT_GUEST_TAG_`：這一晚敲門進來的是誰，在場來由「她剛敲了你的門」讀它 |
| 【好感底線】 | `【好感底線】N` | 同伴列 | `KANSHOU_BOND_FLOOR_TAG_`：棘輪——到過的階不會被 AI 扣回去 |
| 【冷卻日】 | `【冷卻日】absDay` | 同伴列 | `KANSHOU_CHILL_DAY_TAG_`：被冒犯後當天的冷淡（`pChillStr`） |
| 【同居解除】 | `【同居解除】1` | 同伴列 | `KANSHOU_COHABIT_END_TAG_`：搬走那回合的一次性事實（`kanshouCohabitEndStr`） |
| 【醒著陪同】 | `【醒著陪同】地點` | 同伴列 | `KANSHOU_AWAKE_HERE_TAG_`：深夜 0~8 在這個地點她是醒著的（夜訪客／陪你熬夜），否則 `pSleepStr` 判熟睡 |
| 【同居問過】 | `【同居問過】absDay` | 同伴列 | `KANSHOU_COHABIT_ASKED_TAG_`：同居泡泡同一天只問一次 |
| 【戀人】 | `【戀人】1` | 同伴列 | `KANSHOU_LOVER_TAG_`：告白牆的唯一鑰匙；`kanshouIsLover_` 讀 |
| 【快速貼圖】 | `【快速貼圖】text1,text2,...`（逗號分隔） | **玩家列** | `kanshouGetQuickPhrases_`/`SetQuickPhrases_`：玩家自訂快速輸入貼圖(2026-07新增)，上限`KANSHOU_QUICK_PHRASE_CAP_=8`句，跟內建4個(害羞/小聲/苦笑/臉紅，2026-07同月再縮減，純前端寫死)分開存 |
| 【換裝】【口吻】【小動作】 | — | 同伴列 | 鑑賞**讀取**（`getOutfit_`/persona），寫入屬 solo/persona 生態、非鑑賞獨有 |

---

## ⚖️ 事實 vs 規則：提示詞的成分比

2026-09 玩家問「是不是我限制太多，AI 反而沒靈性／種子庫要不要減字」。量過之後：

| | 字數 | 資料（事實） | 規則（教它怎麼寫） |
|---|---|---|---|
| user prompt | 1,667 | 47% | 52% |
| system prompt | 1,700 | 0% | 97% |
| **合計** | **3,367** | **781（23%）** | **2,586（77%）** |

**種子庫不是肥的那一塊**：一位同伴在提示詞裡只佔約 110 字（dailyLook 41＋dailyWords 25＋dailyMoe 12＋dailyOutfit 8＋speech 10＋tic 6），
兩人 220 字 ＝ 全部的 6%，而且那 6% 正是唯一讓角色「是這個人而不是任何人」的部分。**要減字不該從這裡減。**

### 判準（之後加規則前先過這三關）

> 1. 這句告訴 AI **「世界上什麼是真的」** → 留
> 2. 告訴 AI **「你應該怎麼寫」** → 砍，除非它擋的是我們真的踩過、寫在 `CODE_NOTES.md` 裡的坑
> 3. 禁令**能被事實取代時，就用事實取代**——尤其別把要禁的句子本身寫進提示詞（模型會鏡射）

按這個篩，system 十條鐵律裡 7 條是事實或機制（承接玩家語氣、不重置歷史、裝扮是既定事實、JSON 格式…），砍了會壞；
純風格規定只有 2 條半，已拿掉：

- 鐵律 2：`女性角色柔美(柔嫩/雪白/婉約·出力柔中帶勁)；男性角色依其體格氣質寫` → **`每3~4句 <br><br> 分段。`**（分段是 `aiHtml_` 的機制，留）
- 鐵律 7：`女女＝純女女之愛·主導跟隨依個性·動作柔美；男女＝依器官自然互動…` → **`肢體互動依雙方【性別】欄自然呈現。`**
- solo `Router_Persona` 性別配對：`【禁】描寫插入式陽具動作(如「進入她」)，改以手指/舌尖/器物…` → **`★【性別】御主與「X」皆為女性。`**

⚠ **「純女女之愛」那條的根因已經消失**：它存在是因為 AI 會用男性視角寫女女，而那是因為**同伴卡上從來沒有性別欄**——
AI 不是被誤導，是根本沒被告知。卡片補上 `【性別:X】` 之後，同一件事已經講了兩次，技法規定是第三次。
（CODE_NOTES 記的原始意圖「直接算好給 AI、不需要它自己推理」本來就是對的，超出的是後來疊上去的技法清單。）
🔬 **這一條沒有實測背書**：`openrouter.ai` 被環境網路政策擋住，無法 A/B 真實模型。依據是「根因已修、內容冗餘」的推論，
不是量出來的等效。
✅ **2026-09 玩家結案：不追了。** 上線後玩家沒有回報筆觸問題，並明示取消追蹤——
**別再把這條當成「待驗證」重提**，也不要主動回滾。真的覺得不對時再說，退回去各是一行。

### 省下來的還給角色資料

同伴的身世一直存在 `COL.PC.BACK`（種子 `persona.dailyBack`，平均 12.8 字）卻**從來沒送進提示詞**——
凜那條「遠坂家現任當家，妹妹是間桐櫻，感情很好」是正典關係，AI 之前完全不知道。卡片補上 `| 經歷:`，
過濾比照 `servantCard_`（等於「職階・名字」或落在 `QUAD_EMPTY_` 的無資訊量 fallback 不送）。

淨效果：總量 3,413 → 3,367 字（**變少**），其中事實 739 → 781 字（**變多**）。

## ⚧ 性別：支援 男女／女女，不支援男男

`actionKanshouSummonHero` 明文擋掉 男玩家×男同伴（「僅支援 男女／女女 配對」），`kanshouInviteStr` 的結識路徑同擋（`_ivMaleMale`）。
種子英靈 25 人裡 **12 男 12 女 1 無**（恩奇都是「無」，但在 `KANSHOU_SUMMON_BLOCKED_IDS_` 裡召不到）。
男性不會被 `actionEnterKanshou` 自動鋪墊進世界，但**玩家可以主動召喚**——所以 **女玩家×男同伴是完全可達的正常玩法**。

### 2026-09 稽核：整個提示詞層預設「御主是男、同伴是女」

實測 女玩家『薰』× 男同伴『庫·丘林』，送出的提示詞裡 **「她」×16、「他」×5，而那 5 個「他」全指向女性玩家**——兩邊同時指錯人，
最誇張的是同一句裡兩個代名詞各指錯一個人：

> ★【視角鎖定】：「我」＝玩家『薰』本人，只演**他**實際輸入的動作與五感……同伴外貌只取材**她**自己那份資料

**而且 solo 有同一個形狀、方向相反**：`Router_Persona`/`Router_Battle` 泛指御主一律寫「他」，但種子御主 15 人裡有 3 位女性（凜／伊莉雅／櫻），
玩家自己的御主性別也是創角時選的。所以這不是鑑賞的局部問題，是**全層的預設值錯誤**。

**根源不是措辭，是資料**：玩家卡有 `【性別:X】`，**同伴卡沒有這一欄**。AI 從來拿不到同伴性別，只能從規則裡那個預設的「她」去猜。

修法（由根到表，全域一次掃完 134 處）：
1. **同伴卡補上 `【性別:X】`** ← 真正的根源修正，AI 終於拿得到這格
2. **`pron_(sex)` 收進 `Core_Settings.gs`**（兩軌共用的單一真實來源，男→他／女→她／其餘→TA）。
   角色的 row 在手上就 `pron_(row[COL.PC.SEX])`，不在提示詞裡寫死
3. `nsfwBaseRules` 把「預設對方是女性」的措辭改成**標明適用對象**（鐵律 2「女性角色柔美…男性角色依其體格氣質」、鐵律 7「配對依雙方【性別】欄」）。
   **不隨在場者變動**，所以 system prompt 仍是穩定的可快取前綴（1739→1791 字，+52，一次性）
4. 泛指的「她／他」全部中性化（對方／那個人／要走的人）：視角鎖定、角色一致性、角色卡抬頭、收尾、獨處時光、在場來由 ×5、現況、
   `KANSHOU_RAPPORT_TONE_` 全 11 條事實句、事件/橋段/撲空字串、三支日常化轉譯提示詞，以及 solo 的 `servantCard_`/`masterCard_`/`enemyMasterCard_`/戰報/盟誼
5. **`check_pronoun.py` 進 check.sh**（第七支）——這才是根源解，不然下次又會長回來

⚠ **在場來由那 5 句必須是中性字面，不能按人塞代名詞**——它們同時是「在場來由人人相同就抽成抬頭」的去重 key（`_presenceSeen_`），
一旦帶性別，混性別在場時去重就失效、每張卡又各自重複一遍。

驗收（兩個方向）：

| 情境 | user「她」 | user「他」 | system |
|---|---|---|---|
| 女玩家 × 男同伴 | 2（玩家本人·正確） | 0 | 0／0 |
| 男玩家 × 女同伴 | 0 | 2（玩家本人·正確） | 0／0 |

> 🧭 **泛指的「她」錯兩次，不只一次**：性別錯，而且**人數也錯**——同時有 2~3 位同伴在場時，單數的「她」本來就不對。
> 中性化是一次修掉兩個問題，所以這不是為了少數配對犧牲主線，主線本來也是錯的。
>
> 🐛 **我自己在這一輪踩的兩個坑**：①一開始是「探針抓到幾個就修幾個」（16→5→3→0），那是疊加不是根源解——
> 玩家要求整體掃描後才發現真實範圍是 134 處、而且 solo 也有。②批次替換時寫了 `sv[COL.PC.SEX]`，但那個作用域裡根本沒有 `sv`，
> **`check.sh` 只驗語法抓不到未定義變數**——批次改完一定要用探針真的把那條路徑跑起來。

## 🧠 AI 記得多久

分兩層，合起來才是「她記得你」：

| 層 | 內容 | 深度 | 重量 |
|---|---|---|---|
| `chatHistory` | 玩家輸入＋AI narration **全文** | `_histWindow_`＝6 則（3 輪）；剛換場景/剛跳時間砍到 2 則 | 約 2,300 字，佔整份 payload 四成 |
| ★【再往前的經過】 | 掉出窗口的較早回合，**只取玩家做過的事** | `KANSHOU_DIGEST_ROUNDS_`＝8 輪、每則 `KANSHOU_DIGEST_CAP_`＝22 字 | 約 145 字 |

`kanshouRecentDigest_(pcId, windowRows)` 從同一張「歷史暫存」表往回多讀，扣掉已經進 chatHistory 的那幾則，
把更早的**玩家側輸入**串成一行。**不收 AI 的 narration**——那壓不了，也不該由 GAS 改寫別人寫的字。
兩者共用同一個 `_histWindow_`，不各算各的（各算各的就會重疊或漏接）。

⚠ 這一層補的是「純聊天的內容 3 輪後徹底蒸發」：MEMOIR／【初次】／紀念日／約定四個長期管道**都要有事件發生才會寫**，
日常閒聊一個都不蓋戳。實測連打 8 回合日常，那四格全空。

## 💬 好感天花板（已整組移除）

~~`kanshouRelChatCeiling_`~~／~~`pAtCeilingStr`~~ 都已不存在。現況 `rel_changes` 落地只做**每回合 ±5 夾值＋總值 [-100,100]**，
**沒有任何依關係階的封頂**（舊版 39/59/79 撞牆、「AI 收得到／玩家收不到」、玩家裁示維持現狀那段紀錄留在 git 歷史）。
80 以上唯一的牆是告白（`KANSHOU_LOVER_TAG_`，見 §告白）。**別再把「聊天天花板」當現況重提。**

## ⏰ 時間系統

- **每動作推進多少由玩家設**：`kanshouHourPerAction_(memory)`＝`kanshouPaceOf_(memory)/60`，選項 `KANSHOU_PACE_OPTIONS_ = [0,10,20,30]` 分鐘（0＝暫停・預設 `KANSHOU_PACE_DEFAULT_`=10），存玩家列 MEMORY【時間流速】，細節見 §⏰ 時間流速。（舊常數 `KANSHOU_HOUR_PER_ACTION_=1/6` 已不存在；2026-07 那次是從半小時改成 10 分鐘「問個菜色都中午了」）
- **五時段** `KANSHOU_TIME_BANDS_`：清晨(5)／午後(11)／黃昏(17)／夜晚(20)／深夜(0)。
- **當日不跨日上限** `KANSHOU_DAY_LAST_HOUR_ = 23`；被動聊天每回合 `+1/6` 小時流動。
- **曆法**：Day1 = 12/20（`KANSHOU_CAL_START_MONTH_/DAY_`），`KANSHOU_FESTIVALS_` 節慶表，前端顯示西曆年月日。
- **主動跳時間**：`kanshouNextStage`(下一時段·深夜則睡過夜)／`kanshouJumpBand`(跳指定時段)／`kanshouJumpFestival`(快轉節慶)／`kanshouEndDay`(結束一天)。跳時間會**全世界重骰去向**（`kanshouRollDailyLocation_`），讓在場人物重新分佈——**牽手中的她豁免重骰**（跟著玩家）。
- **`endDay` 語義＝「睡到即將到來的 6:00」**（2026-07 玩家實測跨2天根修）：`curHour >= 6` 才 `+1 day`；深夜 00:00~05:59 按結束一天＝睡到**當天** 6:00、不再多跳一天（夜→深夜本身已 +1 day，endDay 再 +1 就是雙倍扣）。前端 `_reHourAfter` 樂觀預測同步這套規則（endDay→6／jumpBand→時段起點／jumpFestival→6／advanceHours→mod24）。
- **⏰ 跨時段自然告辭**（2026-07·玩家「NPC 不會自己離開?」）：被動 +10 分鐘若跨越時段界線（`kanshouBandCrossed_`），非牽手/非跟隨(`kanshouPreMoveCompanions_`)/非約定釘住/**非本回合提議對象(`_pendingProposal`·第二輪稽核補：她被骰走＝AI 同回合收到「向她提議」＋「她已告辭」矛盾指令、接受還把牽手落到已離場者)**的在場者會被重骰去向；離場者名單餵 AI（`kanshouNpcLeaveStr_`）演「自然道別後離開」，並帶**豁免句**——告辭這一句不受「在場驗證鐵律」限制（鐵律本文也明示「系統注入段明示豁免者除外」，兩端閉環）。
- 相識紀念日里程碑 `KANSHOU_ANNIV_MILESTONES_ = [7,30,100,365]`。

---

## 🌦️ 環境氛圍（天氣／地點現況／場景種子）— 之前漏記，2026-07全面稽核補上

三個都是「給 AI 一點確定性的氛圍素材，不強制劇情」的輕量花絮機制，各自獨立、互不依賴：

- **天氣**：`KANSHOU_WEATHER_BY_SEASON_`＋`kanshouWeather_()`/`kanshouWeatherEmoji_()`——依當前日期算出的**當日固定值**(同一天問幾次都一樣，跨日才變)，注入每次提示詞的「★【今日天氣】」、時鐘 HUD 圖示、拍照時戳在照片上。
- ~~**地點現況**（`KANSHOU_LOCATION_ACTIVITY_`＋`kanshouLocActivity_`）~~／~~**場景種子**（`KANSHOU_EVENT_SEEDS_`＋`kanshouRollEvent_`）~~ — **2026-09 兩張表連同函式整組移除**（見下方「事件自由」）：寫死的變體池＝把世界關進 22 張表裡，改由帳本記下真的發生過的事、其餘讓 AI 即興。相簿的「活動」欄從此留空（欄位保留不刪，COL 是位置索引）。

---

## 💕 關係五階系統

- **五階門檻** `KANSHOU_REL_TIER_`：戀人≥80／親近的人≥60／熟識的朋友≥40／普通朋友≥20／點頭之交≥-100（起始）。
- **自動分級** `kanshouSyncRelTier_`：僅當現 REL_TAG 仍等於某梯度字面時才覆寫（玩家自訂稱呼後不再自動蓋回）。
- **聊天天花板**：已整組移除（`kanshouRelChatCeiling_` 不存在）。純聊天加好感只受 ±5/回合與 [-100,100] 夾值；GAS 主動約會邀請也已隨八度改版拔除。
- **突破手段**（繞過聊天上限、直接寫 BOND）：
  - **約定赴約 +5**（硬編字面，`kanshouPromiseMetStr` 那段，不吃 chat ceiling）
  - **獨處時光 +3**（`KANSHOU_SCENE_BOND_ = 3`·`kanshouAloneBondStr`）：與她**單獨**在私密場合（`noEncounter` 地點＝家中各處／她的住處／我的房間）＋好感≥`KANSHOU_SCENE_MIN_BOND_`(＝熟識門檻 40)＋當日未給過（沿用 `KANSHOU_SCENE_DAY_TAG_` 日閘門）。2026-07 泡泡拆除後接手原「親密橋段 +3」的破天花板職責——不需按鈕、不靠 AI 判斷，天然一天一次。
    - **🐛→✅ 2026-07 模擬實跑抓到「39 的牆只有一扇門」**：這道門檻本來寫 `KANSHOU_VISIT_BOND_`(40)，跟聊天天花板共用 40 這個數字——但天花板是封在**門檻減一**（`kanshouRelChatCeiling_` 回傳 `t-1`）。於是 bond 39 的人：聊天爬不動（實跑 30 回合原地不動）、獨處差一點用不了、夜襲還要 60，**39→40 這一步只剩「約定赴約」一條路**（含 `kanshouPromiseOffer_` 她主動邀約，仍是同一條）。舊註解宣稱「此門檻不卡任何該通的路」正是漏算了 39→40，而那恰好是門檻自己站的位置。改成直接讀 ~~`kanshouRelChatCeiling_`~~（已移除），兩個數字從此不可能各走各的；日後 `KANSHOU_REL_TIER_` 門檻怎麼調都自動對齊。**寫新的破天花板手段時，門檻一律對齊天花板本身、別再另寫一個看起來一樣的常數。**
- **🗑️ 2026-07 曾經有過第四軌「日記鑑賞」（排程一週→AI 寫成週記），玩家實測後決定砍掉。** 留這行是為了讓下一個失憶的我知道：①**它被砍掉不代表想法不成立**，被否定的是「純排程、沒有即時對話」這個形狀；②整軌是照「可整檔刪除」的規格蓋的，砍完 `git diff` 對照上線前的 commit 是**全空**——`Diary.gs`／`Script_Diary.html`／`DIARY_REFERENCE.md` 三檔刪除，`Router_Action`／`Index.html`／`Script.html` 的掛勾原樣還原，`Gallery.gs` 全程 0 行改動。要再挖出來看實作，`git show 503a37e..fdf97c4`。**這個「先分軌、可整檔刪」的做法本身值得重複**：實驗失敗的成本就只是刪三個檔。
- **🙋 她主動（`KANSHOU_INITIATIVE_DAY_TAG_`·2026-07 玩家「泡泡用的應該也很少了…NPC 是不是就不太主動了？」）**
  - **稽核結論**：她主動的機制只剩「按睡覺時 20% 的深夜訪客」一條——一天 90 個回合裡有 89 個她永遠在等你先開口（`kanshouPromiseOffer_` 早已刪除、情境橋段泡泡整組拔掉、`moveProposal` 其實是**玩家**提議的回覆、`driveOn` 只是寫作風格開關不是事件）。
  - **一律不做泡泡**：GAS 擲骰 →**直接寫成既成事實**→ AI 演，中間沒有任何一句「你要不要？」。舊泡泡被拔的理由全是同一個「條件成立就每回合跳、玩家嫌煩」，裝回去只會再被嫌一次；深夜訪客之所以不惹人厭，就是因為它是這個形狀。
  - **三型**（`_kinds` 等權隨機）：①**她自己找來了** 改她 LOC＋蓋 `AWAKE_HERE`②**她開口約你** 寫入 byHer 約定③**她此刻的心思** 從 `KANSHOU_INIT_WANTS_` 抽一個**處境**（不是台詞）。加新素材＝往那張表加一列。
  - **閘門是「全域每日一次」不是每人每天一次**——10 位同伴搶同一個名額，頻率跟 1 位完全相同（玩家「不然如果 10 個 NPC 我不就天天約會」）。
  - **節流**：`p = min(MAX, BASE + maxBond×PER_BOND) ÷ (1 + 手上未赴的約數)`（玩家「有約的話機率再下降一點」）。且**每回合擲一次**而非每天擲一次——只玩十幾個回合的日子自然更安靜，頻率跟著投入時間走。
  - **只有「她自己找來」看時鐘**（`KANSHOU_INIT_VISIT_FROM_`9 ~ `TO_`21）：另兩型她本來就在你面前，幾點都不奇怪。玩家實測「早上 6 點跑來我家約我?!」就是這道閘門補的；深夜那段本來是深夜訪客的地盤，讓開不重疊。
  - **📐 實跑調校紀錄**：初版 BASE .006/PER_BOND .0002 → 39/40 天都有事，太腳本；降到 .002/.00012 後約 **73% 的日子有事**。初版又給 `want` 加倍權重 → 邀約 20 天才一次（因為邀約還要再過「她名下沒有未赴的約」那道閘，兩層壓抑疊起來），改等權後 **邀約約每 6 天一次**。
  - **byHer 約定＝她單方面說的，沒去不算爽約**：`kanshouGetPromise_`/`kanshouSetPromise_` 第 3 段旗標（`band:loc:1`），`_standUp` 跟「人就在你身邊」共用同一個豁免出口。⚠ 旗標**只在有 band 時才附加**——舊格式是單段 loc，硬加會被解析成 `band='loc'`、`loc='1'`，整筆約定壞掉。其餘時間×地點的結算完全複用既有引擎。
- **🌙 兩段式就寢·【夜未眠】**（`KANSHOU_NIGHT_SCENE_TAG_`·absDay·存玩家列。2026-07 玩家「睡覺按鈕這裡有被夜襲判定+色色…要再切一段深夜的大戰時刻...?」）
  - **舊做法的問題**：「讓她留下」＝ `endDay:true`，於是**同一個回合**要演完深夜相處、收束到就寢、還要把整天結掉推進到隔天 06:00。篇幅上限 500 字（`_kanshouTargetWords_`）塞不下，玩家也完全插不上手——按鈕小字「直接到早上」就是自白。
  - **現在**：第一次按睡覺 → 若身邊有 bond ≥ `KANSHOU_KNOCK_MIN_BOND_` 的人 → **不結束這一天**，時間推到就寢時刻（判準同 endDay 敘事時鐘）、蓋【夜未眠】、送`★【夜已深·門關上了】`。玩家想演幾回合都行（時間照常 10 分鐘流動、人釘在房裡）。第二次按 → 真正 endDay → 隔天第一個動作＝【晨間餘韻】。
  - **門檻用 `KANSHOU_KNOCK_MIN_BOND_`(60) 不是同床的 80**：夜襲招募池本來就是 ≥60，寫 80 的話 70 好感的訪客敲門進來、玩家按「讓她留下」還是會一口氣跳到早上（實測抓到）。兩個數字必須同源。60~79 能演到哪仍由【親密尺度五階】把關，這道門檻只決定「要不要切這一段」。
  - **夜襲命中＝當場進入夜未眠**（2026-07 玩家「夜襲改簡單點？她直接進來，留下就留下可以繼續聊，拒絕就請她自己回家睡；不拒絕、直接對話就是要她留下的意思」）：夜襲本來就發生在你正要睡的時候，那一刻就是深夜，所以擲中當下直接蓋標記＋把時間推到就寢時刻（順帶修掉「早上八點按結束一天卻跳出深夜訪客」這個既有違和）。
    - 泡泡**兩顆都留**（玩家「不用拿掉啦，只是不要跳到早上」＋「要讓玩家直覺一點，有些人就是喜歡按泡泡」）。「🛏 讓她留下」**刻意不帶任何旗標**、就送一句普通訊息——它的職責只是**替玩家打那句話**，泡泡派按鈕、打字派打字，兩邊落到完全相同的結果。⚠ 千萬別讓它帶 `endDay`：夜襲已經進了夜未眠，帶了就會落到真正的結束一天、一口氣跳到早上，跟按鈕上寫的「夜還很長」直接打架。
    - 「🚪 請她回去」仍帶 `dismissGuest + endDay`：送客後直接天亮，就是「拒絕＝請她自己回家睡」。
  - **兩條路共用同一入口**：20% 擲中夜襲後按「讓她留下」送的也是 `endDay:true`，自然走進來，不必為夜襲另寫平行路徑（玩家定案「兩條路一致」——否則變成被夜襲才有完整夜戲、自己的戀人反而沒有）。
  - **三個出口**：①再按一次（真的睡，清標記）②玩家移動離開房間（清標記，且**必須排在讀取狀態之前**，否則走出去那一回合還會送深夜提示詞）③換日自然失效（absDay）。
  - **前端**：`nightScene` 掛在 `buildTagsPayload_`（`window._lastTags`）而非 play 頂層——tags 是狀態通道、會被 `STATE_AFTER_ACTIONS` 重拉，玩家重新整理頁面後 HUD 按鈕不會退回錯的字。HUD 那顆鈕從「🌙 睡覺」變「🌅 睡到天亮」，確認語也跟著換。
- **🕰️ 敘事時鐘 vs 狀態時鐘（`kanshouNarrDay_`／`kanshouNarrHour_`）**：兩者預設相同，**只有「結束一天」會分家**。狀態時鐘必須推進到隔天 6:00（眾人重骰行程、各種日閘門全依賴它），但那一回合要演的是**睡下去的當下**。舊版直接用推進後的時鐘組 🕰️ 那行，於是同一份提示詞同時說「現在 06:00 清晨，★此刻＝清晨·唯一真實，光線/作息一律依此刻重寫」與「夜幕降臨、回到房間安頓下來、今晚可自然發展到同床」，還限定敘事跨度十分鐘——三者互斥，而且「唯一真實」明文叫 AI 覆寫掉夜晚框架，結果玩家按下「睡到天亮」只拿到一段不知所云的清晨空景（2026-07 玩家實測「那我按睡到天亮會有甚麼事情.....」）。修法：組 🕰️ 一律讀敘事時鐘。**節奏因此回到設計意圖**——endDay 那回合＝夜戲（≥80 即同床那一幕），隔天第一個動作＝【晨間餘韻】。
  - 敘事時刻取的是**就寢那一刻**而非按鈕按下那一刻：玩家可能早上八點就按「結束一天」（語意是「今天剩下的就這樣過去，然後睡」），照抄 08:10 會跟玩家意圖那句「夜幕降臨」再打一次架。已在夜／深夜按的照用（那本來就是就寢時刻），其餘一律換成 `KANSHOU_DAY_LAST_HOUR_`(23:00)。
  - **兩條「昨夜」線都依【這回合她在不在場】過濾**（2026-07 玩家「如果我直接移動呢....」）：旗標在回合開頭就讀掉了，那時還不知道玩家這回合要去哪。玩家一起床就走人、她留在房間，舊版照樣送出「昨夜與『凜』共度親密·可自然帶晨間曖昧」——**叫 AI 跟一個不在場的人演晨間戲**，正是本檔【在場驗證·最高優先】自己禁的事。鏡像問題在【昨夜她走了】：原句寫死「今早這個房間只有你自己」，玩家人在商店街就不成立，而她若剛好被行程骰到同一地點更是直接跟【在場人物】打架。修法：`_morningHere_` 取交集、`_partedAway_` 取差集，為空就整條不送。旗標**照樣消耗**——那個早晨已經過去了。
  - ⚙️ 順帶擴充 `check_prompt.py` 的 `var_has_name`：原本只認 backtick 樣板賦值，`const _x = String(yNames).split().filter().join()` 這種由既有名單推導的變數一律誤判成懸空代名詞。加第二條規則（任意賦值，但 RHS 仍須出現名單來源字樣——放寬到「任何賦值都算」會讓這道檢查形同虛設）。已反向驗證：故意插一句真的懸空代名詞仍被擋下。
  - 玩家人在外面時 `curL` 會先被強制拉回 `我的房間`（「玩家永遠有路可退」的安全閥），玩家意圖那句「回到房間安頓下來」正好交代這段移動，故不違反【移動鐵律】。
- **🔒 好感棘輪（2026-07 玩家定案「鎖在已達成的門檻」）**：好感跌不回已經跨過的那道門檻以下。門檻＝`KANSHOU_REL_TIER_` 的 min（>0）＋`KANSHOU_COHABIT_BOND_`，即 **20/40/60/80/90**；高水位存她那列 `【好感底線】`（`KANSHOU_BOND_FLOOR_TAG_`，只升不降，那正是「鎖住」本身）。實作在 `kanshouBondFloorOf_` ＋ `kanshouSyncRelTier_` 開頭——**必須夾在 REL_TAG／同居同步之前**，那兩者都讀 bond，讀到未夾的值就會做出跟棘輪矛盾的降階。5 個寫 BOND 的地方全都緊接著呼叫 `kanshouSyncRelTier_`，故這是單一真實來源。
  - **理由是掉分不對稱**：加分被 `kanshouRelChatCeiling_` 夾住，**扣分完全不夾**，且 AI 每回合可給到 -5（`rel_changes`，`Gallery.gs` 的 `if (change > 0)` 只夾正向）、下限一路到 -100。一次誤判就能抹掉玩家經營兩週的同居，而失去的東西（同居／親密尺度／夜訪資格／拜訪權）全要再跑一次約會或橋段才拿得回來。
  - **數字不鎖**：95 掉到 91 仍記在 BOND 上，只是不退階。
  - ✅ **趨勢已補上（`pChillStr`）**：原本掉分對敘事完全沒有效果：提示詞給 AI 的是純量 `好感:N`，`pTierToneStr` 只看階級、`pAtCeilingStr` 只看卡不卡天花板、`kanshouTierCrossStr` 只在跨階那一回合觸發——**沒有任何欄位表達「好感正在往下走」**。於是「剛爬到 90」與「從 98 摔到 90」在提示詞裡完全相同，都演成熱戀。棘輪讓數字停在門檻，這個盲點因此更明顯（連退階這唯一的間接信號都沒了）。缺的是**趨勢**而不是 **level**，故補 `KANSHOU_CHILL_DAY_TAG_`（**只記「最近一次讓她不高興是哪一天」，不記累計量**——靠日期自然衰減，不必另寫遞減邏輯）。蓋戳點兩處：爽約 `-5`、AI `rel_changes` 掉幅 ≥ `KANSHOU_CHILL_MIN_DROP_`(3)（1~2 分是 AI 日常微調噪音，寫成「不愉快」會讓她每回合都在鬧脾氣）。**判定用 `change` 本身而非 `newFav-oldFav`**——棘輪把值夾在地板時兩者差 0，但「她確實不高興了」仍然發生過。有效期 `KANSHOU_CHILL_DAYS_`(1) ＝當天＋隔天，第三天翻篇。呈現在**她自己的卡片上**（`pChillStr`，接在好感數字後）而非全域旁白——全域那條沒有名字前綴，會犯本 session 反覆出現的代名詞懸空。只給事實，話變少／找碴／笑得淡由 AI 依個性決定。
  - 🏠 **不變式：同居 ⇒ 地板至少 `KANSHOU_COHABIT_BOND_`**（玩家定案「同居成立後不因任何事情解除」）。不是錦上添花——棘輪上線【之前】的存檔沒有【好感底線】，若某人當時 92＋同居中，第一次爽約 -5 之後才第一次跑到 `kanshouSyncRelTier_`，算出的地板是 80（87 已掉出 90 那一格），於是 87≥80 不夾、同居檢查 87<90 照樣把她掃地出門。把「同居中」本身當成一次到過 90 的證據，順便把舊列往上補齊而不是趕人。**全專案只有兩處寫 `KANSHOU_COHABIT_TAG_`**（邀請設 1／跌破設 0），後者已被棘輪擋死，故同居無其他解除路徑。
  - 🎁 **順手補掉自訂稱呼的殘留倒掛**：自訂關係稱呼／專屬稱呼門檻是 `KANSHOU_CUSTOM_TAG_BOND_`(80)，而 `kanshouSyncRelTier_` 對非清單文字**刻意不覆寫**（尊重玩家手動選擇），所以舊行為是「在 80 設好『老婆』→ 好感崩到 45 → 提示詞照樣寫 `TA是你的老婆(好感:45)`」，正是下方記載的「強勢文字覆寫規則式門檻」失敗模式。棘輪讓好感到過 80 就掉不回 80 以下，文字與數字從此自動一致。實測：連扣 8 次 -5 後仍是 `TA是你的老婆(好感:80)`。
- **AI 對關係的影響力只剩「認不認」**：寫在 `intimacy_feedback.npcs[].attitude`，**不能覆寫 REL_TAG**。
- **🔒 2026-07 五度改版·自訂關係稱呼／專屬稱呼門檻**（玩家實測抓到：把 REL_TAG 改成露骨自訂稱呼後，即使好感才 39/59 就能讓 AI 無視親密尺度天花板直接演到底）——根因是 REL_TAG／專屬稱呼的文字會被字面`「TA是你的${tag}」`原樣塞進 AI 提示詞當**既定事實**（`partyDetailsArr`，見下方 §AI 提示詞），跟前面「強迫字眼繞過天花板」是同一種「強勢文字覆寫規則式門檻」的失敗模式。修法：
  - `KANSHOU_CUSTOM_TAG_BOND_ = 80`（Gallery.gs，戀人門檻，跟親密尺度五階「80+無上限」同一個切點——一旦解鎖，尺度本就已經全開，不會有「好感沒到、卻被自訂文字撐開尺度」的倒掛）。
  - **5 階預設標籤永遠可選**，不受此門檻限制（`actionUpdateRelTag` 只擋「新文字不等於任一預設標籤」的自訂路徑）。
  - **專屬稱呼**（原本 100% AI 寫入的 `REL_MEM【專屬稱呼】`）新增玩家可控入口：新 action `kanshou_set_nickname`（`actionSetNickname`，Router_Action.gs）同樣受 bond≥80 門檻保護，成功後在 REL_MEM 寫入 `【稱呼鎖】是` 旗標，讓 AI 的 `mutual_nicknames` 不再自動覆寫（尊重玩家手動選擇，同 `kanshouSyncRelTier_` 對自訂 REL_TAG 的「一旦手動改過就不再被自動覆寫」精神）。
  - 讀值共用 helper `getNickname_(relMem)`（Gallery.gs）：`actionKanshouCompanions`／servant 清單(`buildTagsPayload_`附近)兩處各自要秀給玩家看，都呼叫這支，不重複寫 regex。
  - **UI**：`kanshouOpenRelTag(name, curTag, bond, curNickname)`（Script_Kanshou.html，call site 在 Script.html 卡片鈕與 Script_Kanshou.html 同伴清單）——bond<80 時整個自訂區塊換成鎖定說明文字，不給輸入框；bond≥80 才顯示「自訂關係稱呼」＋「專屬稱呼」兩組輸入框，並附一句引導文案：**這格是填空的名詞，不要打完整句子**（打完整句子會跟系統模板`「TA是你的___」`文法打結、AI 容易理解錯方向）。
- **親密尺度分五階**（提示詞側·硬約束肢體親密程度）：<20完全碰不到／20-39婉拒／40-59輕度接觸／60-79親吻擁抱／80+無上限。門檻 20/40/60/80 與 `kanshouAsleepOutcomeStr_`(深夜訪客分寸)共用同一組數字·單一來源。
  - **🚨 2026-07 真實 bug 修復（玩家實測抓到）**：低好感(剛認識·<20)場景輸入明講「強姦她」等強迫字眼，AI 仍讓行為得逞、寫成一段完整的非合意侵犯過程(她持續哭喊/掙扎/表達厭惡憎恨、甚至咬破嘴唇流血)——天花板規則完全沒擋住。根因：`nsfwBaseRules`(紅線①·不可改)第4條「玩家自己的動作/台詞【如實發生】」跟親密尺度天花板兩條指示對輕量模型來說互相打架，模型在強勢命令句面前選擇了「如實發生」，把「這個舉動的嘗試」誤讀成「這個舉動一定成功」。已在**天花板規則本體**(不受紅線保護，可改)追加兩條：①**明講強迫/暴力字眼一樣受天花板約束**——未達門檻就是「攻勢落空」，並明文「這條凌駕【依玩家輸入確實推演】——如實發生只代表嘗試、不代表成功」正面解決跟紅線第4條的優先權衝突；②**禁對同伴造成真正傷害**（無論好感高低）——流血/骨折/撕裂傷這類真實身體傷害一律禁止，親密場景激烈歸激烈，不能寫成傷害她的身體。**⚠ 玩家再修（「呼救/逃離這感覺不用吧，狠狠教訓玩家就好！！」——玩家設計理念「裡面的女的都不好惹才對」）**：拿掉「掙脫/呼救/逃離」這幾個偏向受害者視角的選項，改成單一方向「她依個性狠狠反擊/教訓回去(吃虧受傷的是玩家、不是她)」——這個方向沒有安全疑慮(挨打的是玩家不是同伴)，且更貼合「同伴不是好惹的」的角色形象，比消極閃避更帶感。
  - **🐛→✅ 2026-07 玩家再抓到一個漏洞**：低好感(-10·點頭之交、無任何ignoreBond道具)場景，玩家用命令口吻「命令凜跪下」，她內心明明寫滿抗拒(顫抖/咬唇/屈辱/痛恨)，身體卻還是照做了跪下/靠近的動作——天花板規則又被繞過一次。根因：擋「玩家真做的肢體舉動」那條說明句把生效範圍限定在「玩家輸入本身確有描述肢體接觸/親密試探」，一道純口頭的命令並不是玩家自己伸手碰她，技術上不算「肢體接觸」，於是這個命令沒被天花板攔下、AI 只能靠自己拿捏該不該服從，小模型在強勢命令句面前選擇了順從。已擴大生效範圍：**命令/要求她做出肢體服從動作(如「命令她跪下」「命令她過來」「命令她脫」)一樣算數、跟直接伸手同等級受五階天花板約束**——未達門檻她一樣依個性當場拒絕/反抗這道命令(不服從/嗆回去/翻臉/視情況還手)，不會因為只是「動口沒動手」就被迫順從。

---

## 🫧 泡泡總表（2026-07 人類邏輯稽核·「不點會怎樣」是設計的一部分）

| 泡泡 | 同意 | 不同意 | **不點會怎樣** |
|---|---|---|---|
| `moveProposal` 前往 | 同意 | 拒絕 | 不移動。它是**已裁定完的結果**（她答應同去），不點＝就是沒走，狀態零損失 |
| `nightGuest` 深夜訪客 | 🛏 讓她留下 | 🚪 請她回去 | 她留下過夜。她**已經在房裡**（LOC 已落盤），打字繼續聊完全正確 |
| `cohabitOffer` 同居 | 邀她同住 | 現在還不用（純關 DOM·不送請求） | 今天不再問、**明天再問**（`KANSHOU_COHABIT_ASKED_TAG_` 存 absDay） |
| `promiseWait` 等約定 | 等到約定前10分 | 無 | 自己慢慢等，約定 tag 早已落盤 |
| `encounterOffer` 結識 | 結識她 | 無 | 緣分散了——這正是設計語意 |
| `photoResult` | （純回饋） | — | — |

**判準**：泡泡是**待決狀態的唯一出口** → 忽略就會壞（必須改成先落盤再給善後選項）；泡泡只是**已落盤狀態的捷徑** → 忽略無害。現況全部屬於後者。

## ⏳ 時間推進與約定的交互（`kcPromiseWarn_`·三入口共用）

爽約是玩家唯一會**真的虧到**的機制性懲罰（-5 好感 ＋ 寫進共同回憶）。⚠ 2026-07 稽核抓到：舊版**只有「結束一天」**會警示，而破壞力最大的「跳到節慶」（可能一口氣跨好幾個月、手上所有約定必然全滅）**零提示**——保護程度跟危險程度剛好顛倒。已抽成共用 helper：

| 入口 | kind | 觸發條件 |
|---|---|---|
| 結束一天／深夜按下一階段 | `day` | 今天有沒赴的約 **且** 現在 ≥6 點（凌晨睡下醒來仍是同一天，不會爽約） |
| 跳時段／下一階段 | `band` | 目標時刻會**越過準時窗**（窗後 2 小時等同爽約）。沒燒到就**不打斷**——高頻按鈕，無謂確認框比不警告更惹人厭 |
| 跳到節慶 | `far` | 手上**任何**約定都會被跨過去 → 全滅警告 |

`KC_TIME_BANDS_` 因此補上 `startHour`（鏡射後端 `KANSHOU_TIME_BANDS_`，唯一真實來源仍在 Gallery.gs）。

## 💐 約會的根絕方案：**她自己會走**（2026-07 玩家「沒有根絕方式嗎」）

**病根**：「跟玩家同地點的人永遠不會被重骰」是為了修「按跳時段人就憑空消失」而加的規則——但它讓玩家可以牽著她走一整天、直接錯過她自己的約，最後系統還判她被放鴿子。**豁免爽約只是止血，沒有治本。**

**根治**：`KANSHOU_APPT_LEAVE_EARLY_ = 0.5`（提前 30 分＝約三個動作）。每回合檢查——約定日 × 進入該動身的窗口 × 她此刻跟你在一起 × 你不在約定地點 → **她自己動身前往約定地點**：

- LOC 改成約定地點、清 `AWAKE_HERE`、**放開牽手**（人要先走了）
- **從 `kanshouWithMeAtStart_` 快照移除**——她走了就不再算「一直陪著你」，否則之後真沒去，爽約豁免會誤放行
- 給 AI 事實：`★【她先過去了】…說了聲要先過去「X」等你，就從這裡動身離開了`＋「她已經不在這裡，此後不可再讓她開口」

比「讓她消失」更貼近人的行為，也給玩家明確信號而不是人憑空不見。**此後沒赴約＝她真的一個人在那裡等**，爽約回歸它原本的意思。

**連帶對齊**：準時窗下緣從 `ah-10分` 改成 `ah-KANSHOU_APPT_LEAVE_EARLY_`（＝她抵達的時刻）。否則 13:30 她已經站在那、兩人面對面，系統卻到 13:50 才認、還說「她還沒到」。等待框文案同步改成「⏳ 在這等到她來（13:30）」。

| 時刻（約 14:00） | 判定 |
|---|---|
| ~13:24 | 等待框（她還沒動身） |
| 13:30 ~ 14:30 | ★赴約 **+5** |
| ~16:00（約定＋2h） | 遲到 **+3** |
| 16:00~ / 跨日 | 爽約 **−5**（`_standUp`） |

## 🎊 節慶＝今天是什麼日子（只給事實）

節慶走**獨立通道** `kanshouFestivalStr`：當天送 `★【今天是「X」】：…要不要過、怎麼過，由你們自己決定——不必報幕`；
按「跳到節慶」抵達前一天則送 `★【節慶前夕】`。**沒有完成判定、沒有戳記、沒有習俗表**——
2026-09 砍掉 `KANSHOU_FESTIVAL_EVENTS_`／`KANSHOU_FESTIVAL_DONE_TAG_`（三態：未完成提醒→完成蓋戳→餘韻），
理由：那張表規定每個節慶「老規矩是做什麼」還追蹤有沒有達成，等於把節日變成待辦清單。日子是世界事實（留），怎麼過是你們的事（砍）。
連帶：篇幅「大事」旗標串裡原本的 `/就是此刻/.test(kanshouFestivalStr)` 已無字可對（死旗標），2026-09 稽核一併拿掉——**節慶不拉篇幅**。

⚠ **算在 `partyMembers` 之後**（不是上面的 ambient 區）：完成判定要用**移動後**的 `curL` 與在場名單，不然「這回合走進神社」不會算數。

### 「不限時段」連帶修掉的三個副作用

拿掉 `bands` 閘門本身是玩家要的，但它一併造成／暴露了三個問題，都在同一批解決：

1. **節慶日整天壓掉所有地點 ambient**——節慶在優先鏈裡最高，不限時段之後就整天佔著，膝枕／共浴／下廚／同居日常全都跳不出來。→ 抽成獨立通道，兩者並存。
2. **節慶被注入兩次**——一次走 ambient、一次走提示詞裡另一段 `★【節慶】`。→ 舊的那段移除，只剩新通道。
3. **同一句整天每回合重印**——這正是玩家「做過就完成、不要再出現」直覺要解的問題。→ 完成後只剩一句短餘韻。

節慶文字本身也已改寫成**任何時刻都成立**的說法（「不提就不用禁」）：初詣～~她正準備去神社參拜~~→街上到處是要去參拜的人潮與攤販；中秋～~中秋夜月色正好~~→空氣裡都是月餅與團圓的味道；跨年～~跨年夜遠處已經有人倒數~~→一年的最後一天，街上到處是準備跨年的人。`bands` 欄位保留但**不再被讀取**（留著當文件，日後想恢復時段限定不必重寫結構）。
## 🧠 「她就在你身邊」不算爽約（2026-07 人類邏輯稽核）

**舊行為**：約定日你沒去約定地點，卻一整天跟她在一起（牽手／同去／同床），結束一天後日期一過 → 判爽約 -5，還把「我爽約了，讓她空等了一場」寫進共同回憶。**她整晚睡在你旁邊，系統卻記下你放她鴿子。**

**現行**：`_standUp()` 開頭先判她此刻是否與玩家同地——是的話只默默取消約定、**不扣好感、不寫回憶**，並給 AI 一句中性事實（「那個約下次再說吧」的默契）。⚠ 刻意**不給 +5**：約沒真的赴，不該有赴約獎勵，所以「乖乖去赴約」的誘因仍在。

## 🔍 行為級稽核（2026-07 玩家「我怎覺得整個約會系統到處是漏洞…之前整體修都沒察覺」）

**為什麼前幾輪沒抓到**：先前的稽核查的是**代碼一致性**（懸空引用／死碼／契約不符／宣告順序），那類問題 grep 就找得到。約會這幾個是**行為語意**——必須把自己當玩家真的走一遍流程才會撞到。兩種是不同的檢查。

| # | 問題 | 判定 |
|---|---|---|
| **D-1** | **約定地點可以＝你此刻站的地方** | 🚨 真漏洞 |
| — | 住處鎖／時段鎖對「已有約定」的豁免 | ✅ 兩處都有 |

### D-1：站著不動的 +5 農場

兄弟函式 `proposeMove` 一直有 `_pvLoc !== curL` 這條，只有 `promiseMeet` 漏了。後果：

```
13:00 你和凜都在【社區公園】→ 約「今天午後14:00·社區公園」→ 原地打三回合字
13:30 結算：她在約定地點(本來就沒動)、你也在 → 準時赴約 +5
```

而赴約的 **+5 是【不吃聊天天花板】的破關獎勵**——等於站著不動就能無限推高好感。人類邏輯上「我們約在我們現在站的這個地方見面」本身也說不通。

**修**：後端 `_pmSameSpot_` 擋下並給專屬敘述（「才發現你們此刻就站在那裡，你話說到一半自己笑出來」）；前端地點清單同步排除。為此在 `buildTagsPayload_` 補了 **`myLoc`**——鑑賞前端過去**完全沒有玩家所在地的可靠來源**（只有 `locationCounts` 這種彙總數字），放進既有 payload＝零額外 round-trip。

## 🗓️ 約定 2.0

- **存儲**：`【約定】absDay:band:loc`（存**該同伴列**），同時只存一筆（新約蓋舊約）。舊格式 `【約定】day:loc`（無時段）**向後相容＝整天有效**。
- **時段** `KANSHOU_APPT_BANDS_`：午後(14:00)／黃昏(18:00)／夜晚(20:00)；`kanshouApptHour_(band)` 回具體小時（null＝舊格式整天有效）。
- **赴約結算**（`actionPlay` 內，**必須在 `partyRows` 組裝之前**——聊天/拍照/原地消磨三路才吃得到剛移過來的從者）：
  - **赴約**（`_pr.day === curDay` 且同地）→ 清約＋`BOND+5`（夾100）＋餵「依約相會」提示＋回 **`promiseSettle{type:'promise_met'}`** 前端亮「依約相會💕」通知條（2026-07 稽核補：原本赴約成功玩家零回饋）。
  - **爽約**（`_pr.day < curDay`）→ 清約＋`BOND-5`（夾0）＋寫一筆 memoir「我爽約了…讓她空等一場」＋回 **`promiseSettle{type:'promise_missed'}`** 通知條；在場才餵「爽約之後」提示（讓她流露被放鴿子的在意）。前端 `kanshouEndDay` 睡前若有今日之約會先跳警告。
  - 累加進 `kanshouPromiseMetStr` 餵 AI。⚠ **結算回饋走獨立通道 `promiseSettle`、不借用 `proposalResult` 單槽**（第二輪稽核三路同時撞到：post-AI 提議結果無條件覆寫單槽，同回合「爽約結算＋新提議成功」時結算通知被吞）——前端收到 `promiseSettle` 或 `proposalResult.type==='promise'` 任一都重抓 `kanshou_companions` 刷新 `_kcCur`（否則舊快取殘留已結算的約→假爽約警告）。
  - **⚠ 未解鎖私宅不可當約定地**（2026-07 第二輪稽核·必爽約陷阱）：鎖住的 visit 住處物理上進不去（地圖灰鎖＋後端擋移動）→ 約在那裡＝到期必 -5。前端 `kanshouPromiseMeet` 地點面板不列、後端 `_pmLocOk`，皆走 `kanshouResidenceUnlocked_`（八度改版移除GAS主動邀約`kanshouPickDate_`後，約會地點選擇現只剩玩家自選路徑`promiseMeet`一條，這條驗證是它唯一的把關）。
  - **⚠ 「必爽約陷阱」還有一個成立後才會出現的變種**（2026-07 全面稽核）：約定成立當下有解鎖檢查沒錯，但**赴約前**若好感因其他事件跌破熟識(40)，屋主私宅會重新上鎖——玩家想赴約走過去卻被登門攔截擋在門外，隔天還被系統判「爽約」倒扣好感，兩個各自正確的機制互相矛盾、且玩家全程無法得知這個已成立的約定即將必然失敗。已補 `kanshouLocHasPendingPromise_`：已成立且未過期(day≥curDay)的約定，若目的地正是該私宅，移動時豁免解鎖檢查——赴約優先於門檻。
- **前端**：`kanshouPromiseMeet(name, npcId)`（2026-07 id 化重構補 `npcId` 第二參數）發起（選地點+時段，走 `kanshouPickLocation_` 共用地點面板，**`#kloc-overlay`**——2026-07 全面重構收尾：原沿用`#kp-overlay`跟小道具/催眠面板共用，選地點跟道具完全無關，已換獨立id解耦）；`kanshouWaitForPromise(targetHour)` 撲空等待框（跳到約定前 30 分＝`_ah - KANSHOU_APPT_LEAVE_EARLY_`，她抵達的時刻）；地圖 `promiseByLoc` 徽章顯示哪個地點有約。撲空類敘事（相約/同去/同居/牽手/結識五種）統一查 `KANSHOU_MISS_COPY_` 表、經共用 `kanshouMissStr_(type,name)` 組裝，不再五處各自手刻字串。
  - **婉拒類通知條只寫「成沒成」**（2026-07-28 玩家：「提議移動失敗的台詞超級無敵僵硬」）：舊版寫的是「『她』這次不想去「X」——心情或時機不對，待會再邀、或自己先過去也行」——但 `★【提議·同去·GAS已裁定】` 早就叫 AI【依她的個性演出婉拒的反應】了，系統再補一句通用理由等於把剛演出來的個性壓平成同一套罐頭（跟 `KANSHOU_RAPPORT_TONE_` 那次「有模板誰來演都一樣」同一個形狀），後面還接一句教玩家怎麼做。現在婉拒條只留【敘事看不出來的機制後果】：同去→`🚶 沒有同行——你要自己去「X」的話，照常點地圖。`／同居→`🏠 沒有搬來——同住這一步需要非常深的信賴，好感還不到。`／相約→`📅 這次沒有約成。`／牽手→`🤝 沒有牽到手。`。**成功那幾條不動**——它們帶的是敘事給不了的資訊（「點下方的『同意』才動身」、赴約的好感升降規則、同居後的作息），那不是贅字。告白被拒也不動（帶好感回落與 N 天冷卻兩個真機制）。
  - **對象解析改 id 優先**（2026-07 id 化重構，跟 solo 側同一輪）：後端 `_pmIdx` 改呼叫共用 `findPcRowIdx_(pcData, _myGid_, {id, name, faction:"從者", loc:curL, excludeIdx:pcIndex, nameCandidates:kanshouNameCandidates_})`——卡片渲染 onclick 已多帶 `s.id`，有 id 就精準命中同地點/在世/從者陣營的那一列，查無 id（或 id 已過期，例如她已離開此地/已改換陣營）才退回原本的 `kanshouNameCandidates_` 別名模糊比對。`kanshouHoldHand`/`kanshouInviteCohabit` 兩支同一輪同步補了 id（見下方牽手/同居章節），三處共用同一支 resolver、同一份在場/在世過濾邏輯，不再各自手刻名字比對。
- **移動接人**：時間快轉在「給 AI 資料之前」先把該去的人拉到約定地點（順序鐵則同上）。
- **她也能主動邀約**（`kanshouPromiseOffer_`，2026-07 三度改版·玩家「AI亂寫萌點…也想要當好感卡39之類的時候GAS主動發出邀約」，順勢把這塊也從AI手上拔掉）：曾改成GAS依好感數值直接判定觸發（每回合掃在場同伴，~~`kanshouRelChatCeiling_`~~（已移除） 剛好卡在天花板(bond===39/59/79)且無尚未赴的約 → 就跳邀請泡泡），地點由`kanshouPickDate_`依好感梯度挑。
  **🐛→✅ 2026-07 三度改版·邀約方向被AI敘事倒著演**（玩家「看起來是她邀請我，點下去卻演成我邀請她」——AI narration 寫成「聽到我邀約的瞬間」，把她主動、他點頭的方向倒過來演）：根因是 `kanshouAcceptPromise` 送給 AI 的合成訊息「（點頭答應了『大河』…）」主詞不夠明確，小模型容易誤讀成「我點頭[要約]大河」而非「大河約我、我點頭答應」。當時改成明講「是她主動開口」的句式消歧義。
  **🐛→✅ 2026-07 四度改版·乾脆整個框成玩家自己的念頭**（玩家釐清設計意圖：「當回合AI敘述完全沒鋪墊邀約，突然跳出『她想約你』很怪；應該是玩家自己意識到好感度到頂了、要用約會突破」）：上一版的「她主動開口」修法只是消歧義的過渡，這次直接定案——泡泡文案／按鈕／送給AI的合成訊息全部改成「玩家自己起心動念、開口邀她」的口吻，跟`kanshouInviteCohabit`(玩家自己隨時可發起的同居邀請)同一種口吻，不再是「她開口、你同意」。
  **🐛→✅ 2026-07 八度改版·移除GAS主動邀約（玩家「約會泡泡也好煩人」）**：這個泡泡在卡關期間是**每回合**都跳(不是一天一次)，比同居泡泡還更頻繁，玩家實測後嫌煩，已整組移除——`kanshouPromiseOffer_`/`kanshouPickDate_`/`promiseProposal`泡泡/`kanshouAcceptPromise`/`kanshouDeclinePromise`/`promiseAccept`全數刪除。約會現在只剩`promiseMeet`（玩家自己主動相約）這一條路，赴約成功一樣拿+5好感、一樣能突破聊天上限（見上方赴約結算），沒有損失任何機制，只是系統不再主動跳出來問。**⚠ 2026-07 全面重構收尾：`KANSHOU_LOCATIONS_`上的`minBond`欄位已確認零讀取點、整批物理刪除**（非保留不用，是真的不存在了；`dateOnly`欄位仍在用，見住處章節）。

---

## 📖 共同回憶（memoir）

- **存 `COL.PC.MEMOIR`(27)**，每位同伴一份。
- ⚙ **2026-09 起 `processMemoir_` 只是一層包裝**：append/去重/上限的本體抽成頂層 `kanshouAppendUnique_(old, line, {sep, cap, maxLen, pin})`，與「她眼中的你」共用同一份（去重含 bigram 相似度比對，複製一份出去必然走樣）。
- **AI 每回合吐 `memory` 欄**（里程碑一句、≤30字、**2026-09 起同 narration 用第二人稱「你」稱玩家**），`processMemoir_` append：**cap 10**，去重（比對忽略★前綴），**★釘選永不驅逐但釘選本身另有上限8**（`actionKanshouMemoirOp` 已釘滿8條會拒絕新釘選，留2格給未來新回憶自然淘汰）。
- **玩家 UI 管理**：`actionKanshouMemoirOp`（pin/unpin/del，帳號綁定，後端只清 `｜【】[]★` 幾個分隔符、要求傳入文字與儲存原文逐字相符；2026-07 全面重構收尾：找目標同伴列改委派共用 `findPcRowIdx_`，取代原本手刻迴圈，行為不變）。前端 `kanshouOpenMemoir`/`kanshouMemoirOp`（`_kmBusy` 讀條保護擋連點）；同伴卡片 **💞回憶** 按鈕。⚠ **2026-07 稽核修**：前端組 onclick 參數時舊版用 `.replace(/'/g,'')` 把回憶原文的撇號整個剝掉才送出，跟後端「逐字相符」的比對規則衝突——含撇號的回憶（AI 自由生成的文字不保證不含）因此永遠釘選/刪除失敗；改成正確的 JS 字串跳脫（`\'`）而非刪字。同批也補上操作進行中點背景關閉會被 `finally` 重新打開視窗的遺漏 `_kmBusy` 判斷。
- 餵 AI 時★前綴會剝除。

---

## 💞 關係的長期記憶（2026-07 新增·「玩越久越有份量」的三塊）

補的是同一個結構性的洞：**這套原本沒有長期記憶**——因果幾乎全靠 `memoir`（AI 自由書寫·cap 10·會被新回憶擠掉），玩到後期開頭那段必然消失，於是「我們第一次牽手是哪天」這種只要講錯就直接戳破沉浸感的事實，反而最先被遺忘。三塊都走「GAS 記事實、AI 只演出」。

### ① 【初次】結構化第一次帳
- 存該同伴列 MEMORY（見上方標記表）。`kanshouStampFirst_` 冪等、只記最早那次；`kanshouGetFirsts_` 讀回。
- **蓋戳點**（全部是 GAS 已在判定的節點，不靠 AI）：`同居`（cohabitInvite 成立）／`牽手`（post-AI 接受分支·記在**她**那列，故 `_pendingProposal` 多帶 `herIdx`）／`同床`（endDay 的 `intimateNightNames`·記「睡下去」那天 `_nightDay` 而非醒來那天）／`約會`（`_settle` 赴約成立·準時遲到皆算）／**各式橋段**（以事件名當 key·僅非拒絕分支——被轟出浴室不算「第一次共浴」）。
- **餵 AI**：每回合取最早 `KANSHOU_FIRSTS_SHOW_`(5) 筆當既定事實（存得下 20 筆不代表每回合都該送），明令不可編造清單外的第一次。
- **🎂 週年**：曆法固定 365 天/年，故「同月同日」必然是整年之差——今天撞上就另跳一句加強提示。這是把第一次記成結構化事實的主要回報：她能精準說「一年前的今天…」而不是含糊感嘆。

### ② 關係階質變演出
- solo 早有 `BOND_MILESTONES_`(30/60/90) 的里程碑演出，鑑賞的五階 `REL_TAG` 卻是**自動升、完全無聲**——40→60、60→80 這兩個全模式最大的情感節點，跨過去只是聊天天花板悄悄放寬，什麼都沒發生。
- 補 `【關係階】`(只升不降) ＋ `kanshouRelRank_`，在既有的 `partyRows` 迴圈裡偵測跨階，注入**純事實**「『某某』從「A階」跨進「B階」」＋一句 show-don't-tell 護欄（禁報幕/禁寫數字）。⚠ 2026-07 玩家「我要的是真實資料 > AI 發揮」：原本的 `KANSHOU_REL_TIER_CROSS_` 是 GAS 預先寫好的四句成品文案（如『你們越過了那條線——從此是戀人了』）＋一段導演指示（「她看你的眼神、說話的分寸…都該比先前更進一層」），等於 GAS 代筆。已刪表改用 `kanshouRelTierLabel_` 從 `KANSHOU_REL_TIER_` 反查階名（順帶解決同一份文字存兩處），提示詞 150→55 字。
- **首次見到她時靜靜記下當前階、不報**——剛認識的人不該演出「我們變成朋友了」，那是初始值不是質變。
- 明令**不可報幕**（不出現階級名詞/數字），要演成關係自然到達的那一刻。

### ③ 同居日常橋段（2026-09 已砍）
- 曾有 `KANSHOU_COHABIT_EVENTS_` 五則預寫 ambient（同居晨光／午後／黃昏／夜話／深夜）補時段空檔；隨預寫池整批移除。
- 現況：同居只剩事實（在場卡「她現在與你同住一處」＋`kanshouCohabitStr` 落地當回合），一起生活的日常由 AI 即興。

## 🖥️ 同伴卡片按鈕（2026-07 整合·8→6）

玩家回饋「同伴的卡片欄位太多了，介面需要整合減少但是功能要保留；關係/回憶/同居可以整合在一起？這是最少按到的」。

**現況 2-2 四顆**（2026-09 拿掉 `🎀 小道具｜🌀 催眠指令` 那排後）：`📜 詳細狀態｜💞 關係` ／ `📅 相約｜🤝 牽手(已牽→✋ 放手)`

- **🏠 同居改成「同一天只問一次」的泡泡**（玩家「同居做成泡泡問一次、完全隱藏才是正解」）：好感**首次達 `KANSHOU_COHABIT_BOND_`(90)** 且她在場、尚未同住、也還沒問過 → 跳一次邀請泡泡（`cohabitOffer`）並蓋 `【同居問過】`（存 absDay，同日不再問、換日再問；接受後靠 `!kanshouIsCohabit_` 自動停）。⚠ 刻意**不**綁在「跨進戀人」那一階——戀人是 80、同居門檻是 90，在 80 問會被後端以「關係還沒深到能同住」回絕，變成問了也沒用的假泡泡。跟八度改版拔掉的舊版泡泡差別在**一天一次 vs 每回合都跳**。
- **💞 關係 ＝ 中樞面板** `kanshouOpenBondHub(name, tag, bond, nickname, id, cohabit)`（Script_Kanshou.html）：把三個低頻入口收在一起，**三個既有面板完全不動、原樣複用**（`kanshouOpenRelTag`／`kanshouOpenMemoir`／`kanshouInviteCohabit`）。
- 刻意做成「**有摘要的分派面板**」而非把三頁硬合成一頁——三者互動型態差很多（選標籤／管清單／送邀請），硬合只會變成更長的捲動頁；分派面板既省卡片空間，又能一眼看到三者**目前狀態**（現在的關係稱呼＋專屬稱呼、是否已同居、自訂稱呼門檻還差多少）。
- 為此 `buildTagsPayload_`（Router_Action.gs）的 servants 新增 `cohabit` 欄位供面板顯示狀態；solo 恆 false。
- 中樞裡的同居列**平時完全不顯示**：已同住→只顯示狀態（非入口）；未達 90→整列隱藏；只有「已達門檻但還沒同住」這個唯一還有意義的窗口才留一條靜默備援——否則玩家在泡泡按了「現在還不用」之後當天就沒有入口（同日不再跳）。

## ✂️ 提示詞瘦身·鑑賞輪（2026-07）

玩家問「鑑賞提示詞是否有太多贅詞」＋「這模組塞太多給他他也會忘記，精簡的時候表現更好」。實測後的結論：

| 區塊 | 量 | 判斷 |
|---|---|---|
| 🔴 `nsfwBaseRules` | **266 字 / 6 條** | **已是全場最精簡**（`·` 串接、零解釋句）——玩家授權可動，實測後判定**不需動**，砍它風險高回報極小。已逐字驗證未改。 |
| `specificRules` 慾海律令 | 314 字 / 7 條 | 其餘 6 條合計 179、**律令7 一條就 131**（含純解釋句「那是你腦補的修正…」）→ 已壓到 79。 |
| USER prompt 常駐 ★ 指令 | 1702 → **1577 字** | 肥肉所在，但多數在前五輪已壓過。 |

**實際省下 177 字，其中 134 字來自兩處**：①今天剛加的「在場來由」(161→79，我自己寫得囉唆) ②慾海律令7(131→79)。其餘 19 段每段只擠得出 0~5 字——**代表舊有指令確實早就壓乾了**，不是還有大片贅詞沒砍。

**刻意不動的兩條**：`【親密尺度五階】`、`【天花板也管命令/強迫/暴力】`。這兩條的歷史紀錄顯示是靠「講得更明確」才修好玩家實測抓到的繞過漏洞（見上方親密尺度章節），壓縮它們風險不對稱。

沿用第五輪判準：**拿掉後 AI 會少知道一件事 → 保留、只換短詞；只是解釋「為什麼」給維護者看 → 整句砍。** 全程零規則流失。

### 🚫→🎯 禁令改成範圍（2026-07·玩家「一提禁止他又只記得關鍵字，沒把禁止記住」）

**問題**：否定句在小模型裡編碼很弱，字串卻編碼很強。`禁「那一刻/那一夜/自此/從此」` 等於把那四個現成的收尾詞遞到它眼前——它記住字、丟掉禁。**列舉反例＝發誘餌。**

**判準（三分法）**：

| 類型 | 例 | 處置 |
|---|---|---|
| 禁 ＋ 可抄字串 | `禁「鏡中我的表情」`／`禁沿用舊時段(「這麼晚了」)` | **改寫**成正向界定範圍 |
| 禁 ＋ 抽象行為 | `禁憑空開口`／`禁大段內心戲` | 留（沒東西可抄） |
| 禁 ＋ 它本來就不會犯 | `不輸出戰鬥/生命值`（schema 根本沒這欄） | **刪** |

**改寫公式：「禁 X」→「只能是 Y」**——界定範圍後 X 自動落在範圍外，而且沒有誘餌。

| 舊（禁令＋誘餌） | 新（正向界定） |
|---|---|
| 禁「那一刻/那一夜/自此/從此」等翻頁式收尾 | 最後一句必須落在正在發生的動作或剛說出口的話上，時間刻度不超出這一分鐘 |
| 禁沿用舊時段(「這麼晚了」)／禁寫「過了好幾個鐘頭/一段時間」 | 所有光線/氣溫/作息的感受一律依此刻重寫；本回合敘事跨度上限【十分鐘】 |
| 禁提聖杯戰爭/從者/御主/令咒/寶具/英靈/召喚 | 在場每個人就是這座城裡的普通市民，來歷只能取材自下方她自己的在場資料 |
| 禁讓玩家自動「準備好禮物」「掏錢包」「變道具」 | 玩家手上只有他自己說出口的東西，其餘一切物品與金錢在這個世界不存在 |
| 禁「鏡中我的表情」這類外部視角 | 『我』的描寫只能來自他自己的感官——他看不見自己的神情 |
| 禁問「你怎麼會在這裡」 | 她把你在場視為理所當然 |
| 禁介面詞(泡泡/按鈕/地圖)入敘事 | **不提地圖就不必禁地圖**——移動鐵律改寫成「一律由系統裁定」，介面詞整條消失 |

**刪掉的純贅詞**：`不輸出戰鬥/生命值`——鑑賞 JSON schema 根本沒有生命值欄位，而且同一件事在世界觀那行已用「血量」禁過一次，等於每回合把「血量」送到它眼前兩遍。

⚠️ **刻意保留列舉的例外**：`【天花板也管命令/強迫/暴力】` 裡的「命令她跪下」「強姦」「壓制」——那不是「禁止 AI 這樣寫」，而是**辨識玩家輸入的判準**（玩家真打這些字時要擋下）。功能是輸入分類，不是輸出禁令，字串必須在。

### 🎯 更有效的一招：收緊條件式區塊的觸發

壓字只省 177；**真正的浪費是「名義上條件式、實際上每回合都成立」的區塊**——它們整段（含 200+ 字的指令本體）在絕大多數用不到的回合照送。已收緊兩處：

| 區塊 | 原觸發 | 現觸發 | 一次省 |
|---|---|---|---|
| `kanshouWorldRosterStr`（世界概況·你認識這些人） | 只要有人不在場（≒永遠） | 玩家這句話**真的可能問到不在場的人**：句中出現名單上任一人的名字（含大小寫變體）或有詢問關鍵詞（認識/聽過/見過/在哪…） | **~355 字** |
| `kanshouFirstsStr`（你們之間的第一次） | 只要她有任一筆【初次】（玩幾天後≒永遠） | ①今天是某個第一次的週年 ②本回合剛發生一件第一次 ③玩家這句話在回顧（第一次/當初/那時/記得/多久…） | **~87＋每人 60 字** |

**典型回合省 300~400 字，遠超過整輪壓字的 177。** 兩者都是 GAS 端的關鍵詞/狀態判斷，不靠 AI 判斷，符合「GAS 掌事實」。

**判準**：這個區塊是「AI 每回合演出都要用的規則」還是「查得到就好的參考資料」？後者一律加觸發條件。其餘條件式（質變/週年/情境事實/獨處/牽手中/巧遇/氛圍種子）本來就稀有，維持原樣。

### 🧬 在場者命格詳情：儲存不動、只砍「送出去的」

玩家：「性格四格／特徵四格分這麼細，AI 也沒辦法演出來，是不是可以再刪減一些？」

**結論：分格本身不佔字，佔字的是內容；而 4 格是儲存 schema**（逆天改命編輯 UI／工房／AI 生成／solo 共用同一組 `COL.PC.PREF`/`TRAIT`），動它是全面重構，風險與收益不成比例。**故儲存維持 4 格，只精簡送給 AI 的呈現**：

| 欄 | 原本送出 | 現在送出 |
|---|---|---|
| 特徵 | `[外貌本相][氣質舉止][台詞自稱(僅其本人引號內用，非旁白視角)][卸下心防的私密一面]` | `[外貌氣質]`（前兩格本來就是同一幅畫面，合併）＋`[台詞自稱]`；**`[私下一面]` 只在真的獨處時才送**　⚠ **2026-09 起 `[台詞自稱]` 已整格不存在**（自稱併進【口吻】、特徵收成 3 格，見文末 §「自稱退休」） |
| 性格 | `[表象][內裡][喜歡][討厭]` | `[表象][內裡]`（反差是核心、必須分開）＋`[喜惡]`（聊到才用的話題燃料，併一格） |

**一位在場者的【特徵】欄：99 → 53 字**（非獨處回合）。三人同場＝一回合省 138 字。

**再一輪（玩家「只要給超級少資料就可以了，因為之前玩起來沒有特別的差異」）**——選擇 A：砍掉的欄位，鑑賞側的編輯 UI 也一併處理，不留「填了沒效果」的假欄位。

| 欄 | 鑑賞現況 | 對應的 UI 處理 |
|---|---|---|
| 性格 `[喜歡][討厭]` | **不再送** —「聊到才用」的話題燃料 | 鑑賞的逆天改命**只顯示前兩格**；後兩格改 hidden input 原值帶回（**資料不清空**，solo 仍吃完整四格） |
| 同伴的 `身世` | ~~不再送~~ → **2026-09 改回送**（在場卡 `\| 經歷:`，過濾比照 `servantCard_`，見 §省下來的還給角色資料） | 舊的「鑑賞敘事不會用到」加註是假話（2026-09 稽核抓到），已改成標籤「固定事實·AI 讀得到但不會改寫」 |
| 玩家自己的 `經歷` | **仍有送**（見【玩家命格】那行） | 不變 |

性格＋身世：**55 → 21 字/人**。

⚠️ **創角流程完全不必改**：這些欄位不是鑑賞專用——solo 的 `servantCard_`／`masterCard_` 仍吃完整四格、前端「📜詳細狀態」仍顯示、逆天改命仍在編輯同一份。變的只有「鑑賞這一軌要不要塞進提示詞」。

另外把「台詞自稱僅其本人引號內用、非旁白視角」這條**全域規則**從欄位標題移到常駐規則區只講一次——它原本被當成每個人的欄位標題，三人同場就重印三遍（同 `performanceNote_` 那次的「跑時重複」修法）。
⚠ **2026-09 這條規則本身已不存在**：旁白改第二人稱「你」之後，旁白與角色台詞不再搶同一個「我」字，這條註記沒有存在的理由了。

## ⚠️ 種子改版必升 `CODEX_PERSONA_VER`（2026-07 複查抓到·差點整批白做）

`seedFateCodex_` 的升級閘是 **`props.codex_persona_ver !== CODEX_PERSONA_VER`**。線上 Script Property 已經是舊版號時，改再多種子 `upgradeCodexPersonas_` 都不會跑——**只有全新試算表吃得到**，既有存檔完全 no-op。

**改 `daily*`／`persona` 一定要順手把 `CODEX_PERSONA_VER` 升一版**（`gas/Seed_Codex.gs`）。忘了升版不會有任何錯誤訊息，只會靜靜地什麼都沒發生。

同批補上的第二個缺口：`resyncSummonedServants_` 的「鑑賞眾生」分支原本只刷 **BACK／TRAIT／MEMORY【口吻】** 三樣，漏掉 **PREF（`dailyWords`）與 INTENT（`dailyMoe`）**——但這兩欄一樣會被寫進在場卡（`formatPref` 的 [表象][內裡]、萌點欄）。結果是種子性格/萌點改版後，**已召喚的同伴永遠停在舊文字**，只有之後新召喚的才吃得到。已補齊。

`actionDevResyncCodex`（DEV 按鈕）可無視版本閘強制重刷，但那是救援手段，不是正常路徑。

## 🎭 橋段（scripted events）

> ⚠️ **2026-07 九度改版拆掉 offer/accept 泡泡、2026-09 再把整個預寫池砍光**（見 §事件自由）：
> `KANSHOU_SCENE_EVENTS_`／`KANSHOU_FESTIVAL_EVENTS_`／`KANSHOU_LOCATION_EVENTS_`／`KANSHOU_COHABIT_EVENTS_`／
> `kanshouSceneKey_`／`kanshouPlayerHomeLocs_`／`_sceneIsCohabit_` 全部不存在，`kanshouSceneAmbientStr` 是刻意留空的字串
> （回傳形狀不動）。地點×時段×同居發生什麼，全由 AI 依 ★ 事實即興。下面留的是還活著的機制。

- **🛏️ 夜襲／賴床叫醒：整組移除**（玩家「最需要保留的是夜襲，但其實也不需要泡泡詢問，只要確定 npc 會睡覺就可以了」）。她睡著這件事**本來就由 `pSleepStr` 每回合當既定事實餵給 AI**、判準完全相同（`KANSHOU_ASLEEP_HOUR_END_=8`／`KANSHOU_NIGHT_RAID_HOUR_END_=5`），再包一層按鈕只是把自然的處境變成一張要點的卡。連帶移除：`kanshouRollSceneBranch_`、`roomEventOffer`、`kanshouAcceptRoomEvent`。
- **好感天花板破口**：原本靠橋段 `BOND+3`，橋段拆除後改綁 `kanshouAloneBondStr`（真的獨處的私密場合×當日一次，沿用 `KANSHOU_SCENE_DAY_TAG_` 同一個日閘門）。
- `kanshouAsleepOutcomeStr_(bond)` **仍在**，但唯一呼叫點只剩**深夜敲門·別有用心**：60以下＝她又驚又惱把人趕走／60~79＝半推半就但卡在親吻擁抱／80+＝無上限。切點沿用親密尺度五階的 60/80，單一來源。
- **🚪 深夜訪客（2026-07 十度改版·由「待決泡泡」改成「先落盤再善後」）**
  - **判準**（玩家「如果玩家沒有按泡泡而是打對話呢？」問出來的通則）：泡泡若是**待決狀態的唯一出口**，玩家改用打字就會壞；泡泡若只是**已落盤狀態的捷徑**，忽略它完全無害。深夜敲門原本屬於前者。
  - **舊版的病**：`endDay` 命中擲骰後純早退（`return {text, knockEvent}`，**零落盤**）。玩家改打字 → ①「結束一天」的意圖靜靜蒸發（日期沒推進、只 +10 分）②訪客還在她自己家、沒進 `partyRows` ③但「敲門聲」已寫進歷史，AI 同時收到「有人敲門」和「她不在場、不准開口」兩條矛盾指令。
  - **新版**：擲骰命中就**直接把她的 LOC 設成玩家所在地**（沿用既有接人流程，含「別有用心」判定），本回合不推進日期，回傳 `nightGuest` 讓前端給兩顆**善後**鍵：`🛏 一起睡`（`kanshouSleepWithGuest`：只送一句普通訊息、**刻意不帶任何旗標**——她已落盤在房裡，帶 endDay 反而會跳到早上）／`🚪 請她回去`（`kanshouSendGuestHome`：`{endDay:true, skipKnockCheck:true}`，結束一天本來就會讓所有人各自回家）。**玩家無視這兩顆直接打字也完全正確**：她真的在房裡，AI 照常演。
  - **擲骰位置**：必須排在 `actionPlay_` 最前面（`sameGame` 之後）——它會取消本回合的 `endDay`，而 `_reHourAfter`（情境時段）與 `kanshouTimeJumped_`（在場來由）都讀 `endDay`，晚一步算就會拿到「已經睡到清晨6點」的錯值。`kanshouTimeJumped_` 的宣告因此也一併下移到擲骰之後。
  - **當日鎖** `KANSHOU_KNOCK_DAY_TAG_`（存**玩家**列·absDay）：落盤化後「結束一天」可能被按很多次（她進來→打字聊天→再按一次），沒有這個鎖會反覆擲骰、一晚來三個人。
  - **在場來由**：夜訪者在 `pPresenceStr` 有自己的最高優先分支「她剛剛敲了你的門、這一刻才進來」。舊版靠另一個 ★ 區塊（`kanshouKnockGuestStr`）講，跟同一欄的「你們從剛才就一直在這裡」直接打架——已刪，同一件事只留一個出處。
  - **安全**：`knockAccept` 從 client 可傳參數收斂成內部變數 `_knockGuestReq_`。好感門檻只在 `knockPool` 篩過，接人流程本身不檢查——留成 client 欄位等於開一條「任意把同伴傳送到身邊」的後門。
  - **多人在場（玩家「如果我已經有牽手一個高好感的，那個人的反應呢？」）**
    | 誰 | 狀態 |
    |---|---|
    | 牽手中的那位 | 在 `partyRows`、`kanshouIsAwakeWithMe_` 認她醒著。GAS 只給事實（你正要歇下／X 找上門／此刻這裡還有她），吃味·尷尬·大方全由 AI 依她性格＋好感演 |
    | 夜訪客本人 | LOC 已落盤到玩家所在地，並蓋 `KANSHOU_AWAKE_HERE_TAG_`——否則深夜 0~8 點在「我的房間」會被 `pSleepStr` 判成熟睡，跟「她剛敲門」直接矛盾 |
    | 同房其他人 | 照 `pSleepStr` 判定（跟夜襲同一組 0~8 點判準）：21 點按結束一天→沒人睡、都在準備歇下；2 點按→是真的在睡 |
  - **三種收場**
    | 選擇 | 行為 |
    |---|---|
    | 🛏 讓她留下 | **不帶旗標**（不結束一天）：夜襲命中當下已蓋【夜未眠】，接下來照常打字演；之後再按結束一天，在場好感≥80 的**每一位**都留在玩家房間過夜，蓋【同床】初次帳 |
    | 🚪 請她回去 | `dismissGuest:true` 先把她送回自己家、清 AWAKE_HERE，**才**跑 `endDay` → 只剩原本陪著的人過夜 |
    | 直接打字 | 她真的在場、AI 照常演、日期不推進。送客鍵不再出現→之後按結束一天她會留下過夜 |
  - ⚠ **`dismissGuest` 必須排在 `endDay` 之前**：`intimateNightNames` 的條件是「好感≥80 且此刻與玩家同地」，她剛被落盤到 `curL`、好感又通常夠高，晚一步送就會被留下過夜——「請她回去」等於毫無作用。姓名只從玩家列的 `KANSHOU_NIGHT_GUEST_TAG_` 讀，**不吃 client 傳的名字**。
  - **選項只給一次**（玩家定案「就只要問一次就好」）：`nightGuest` 只在她進門那一回合回傳。代價是明白的——玩家改用打字繼續陪她就沒有送客鍵了，之後按結束一天她會留下過夜。那本來就是「你選擇繼續陪她」的自然結果，不算 bug。⚠ **別為了補這個缺口把泡泡改成常駐**：玩家對常駐泡泡的容忍度是零（同居泡泡、約會泡泡都因此被拔過）。
  - 機率 `KANSHOU_KNOCK_CHANCE_ = 0.2`、候選需同居或好感≥`KANSHOU_KNOCK_MIN_BOND_ = 60`；`KANSHOU_KNOCK_RAID_CHANCE_ = 0.5` 決定這次來訪是否「別有用心」（共用 `kanshouAsleepOutcomeStr_` 分寸判準＋`KANSHOU_SCENE_DAY_TAG_` 當日鎖＋`KANSHOU_SCENE_BOND_` 加分）。
- **🎭 橋段提醒徽章（前端·2026-07·玩家「不知道去哪、幾點」）**：地圖每個地點按鈕旁標該地橋段＋時段（客廳💤午後、浴室🛁夜/深夜、屋頂花園🌌夜/深夜、廚房🍳黃昏、隱藏溫泉♨️、和室🌙0~8點、她們各自私宅🌙0~8點）；**命中就高亮**。⚠ 九度改版後徽章語意改了：住處/和室那組 🌙 標的是**「她熟睡中」這個狀態**（不再是「夜襲/賴床」按鈕，那組橋段已移除），tip 也改成「場景會自然帶入這個情境，要不要理會全由你決定」。造訪她的家（`KANSHOU_HERO_HOME_`手寫豪邸，或`KANSHOU_GENERIC_HOME_POOL_`泛用住處，見§住處分配·好感40解鎖登門）0~8點登門她必定在睡；同居(90)只是「她搬來睡和室」的另一條路。徽章對齊後端所有住處值，鎖住的私宅走🔒分支不顯示徽章。前端鏡像 `KC_LOCATION_EVENTS_`＋`kcSceneBadge_`（住處熟睡類用 `hourEnd` 直接比對 `kcClock.hour`，其餘橋段走 `bands` 比對時段key；唯一真實來源仍是後端，改後端觸發表記得同步鏡像）。
  - **🐛→✅ 2026-07 玩家實測抓到「深夜/清晨去她房間找她，有提示AI要讓她是睡眠狀態嗎？」**：查證後確實沒有，AI只能自己從時段猜，容易演成她還醒著閒聊。已在 `partyDetailsArr`（每回合重算·非持久狀態）補 `pSleepStr`：她此刻 LOC＝自己的家（或同居者在`和室`、或在玩家自己的房間）且時刻落在 0~8 點窗口內時，注入「現況:她此刻在自己家、多半已熟睡／多半還在賴床（除非橋段已明確叫醒她，否則維持這個狀態演出）」。⚠ **九度改版後這句成了睡眠事實的唯一來源**——夜襲/賴床泡泡整組移除，正是因為 `pSleepStr` 判準完全相同、已經把事實餵到位，再包一層按鈕只是把自然的處境變成一張要點的卡。
  - **🐛→✅ 2026-07 玩家連兩次實測抓到「放開手馬上跳賴床泡泡」「離開又走回來也跳賴床泡泡」**：`kanshouIsAwakeWithMe_`(排除熟睡誤判用)原本只認「這回合牽手中」或「這回合剛跟玩家一起移動抵達」(`kanshouArrivingNames_`)，兩者都是**當回合暫存**，一放手或下一回合就失效——即使她全程明明醒著陪玩家互動，仍會被誤判成剛好躺在自己家/和室裡熟睡。改成新增持久 MEMORY tag `KANSHOU_AWAKE_HERE_TAG_`(存該同伴列·地點值)：`kanshouIsAwakeWithMe_(idx)` 判定「當下醒著」時就把她目前 LOC 寫進這個 tag，之後只要她的 LOC 沒變就持續視為醒著(自我修復：地點一變──她離開/被重骰走──tag 自動被視為不匹配，不必手動清)。函式簽名同步改成吃 `pcData` 索引(原本吃姓名字串)，兩處呼叫點(room-event候選過濾、`pSleepStr`)都改傳索引。

---

## 📷 拍照／相簿

**📱 2026-09 拍照四項優化（玩家「拍照方面有可以優化的地方嗎」→「做」）**：
① **🤳 自拍**：拍照選單多一排「跟「X」自拍／大家一起自拍／自拍一張」，`send()` 帶 `selfie:true`；
   後端 `names` 把玩家自己排第一（相簿篩選才找得到「有你的照片」），提示詞叫對方依好感決定靠不靠過來；一個人時是獨處自拍、不是風景。
   緞帶用第一位同伴的髮色（`names[1]`）。⚠ 提示詞不可以寫「她要不要靠過來」——`check_pronoun` 當場抓到，改「對方」。
② **合照拿掉「前 3 位」暗規則**：按鈕寫「大家的合照」，第四位卻被 `slice(0,3)` 靜靜排除。現在在場的人全部入鏡——
   「在場」本來就受 `KANSHOU_PARTY_DETAIL_CAP_`（提示詞預算）約束，那是 AI 這回合看得到的人，不是拍照另砍。
③ **拍照不推進時間**：真的用手機拍是一秒鐘的事，玩家常「拍一張、再拍一張」，每張走半小時下午就沒了。`takePhoto` 回合豁免被動流時。
④ **對話裡的即時卡不放「撕掉」**：`kcAlbumCardHtml_(p, {noDelete:true})`——手滑就沒了，而且刪了對話裡那張還在、變假卡。相簿面板照舊。
探針 `photo.js` 擴到 18 條。

**📱 2026-09 拍完當下就看（玩家「拍照改成手機拍 當下能看」）**：原本拍完只回一行字「已存進相簿」，照片要點開 📚 才看得到。現在 `play` 回應的 `photoResult.photo` 直接帶卡片物件（`kanshouPhotoObj_(row)`，跟 `get_album` 同一支、同一個形狀），前端用相簿同一支 `kcAlbumCardHtml_` 畫在那回合的對話裡。探針 `photo.js`（8 條）＋ `check_ui` 釘卡片畫得出來。

**⚠ 2026-07 拍照改「手機」（玩家「拍照要改成手機、不用等」）**：原本是寶麗來設定（每日底片限量`KANSHOU_FILM_PER_DAY_=3`＋隔天沖洗才看得到），玩家覺得手機沒有底片這種東西、拍完也該立刻能看——兩個限制都拔掉了：
- **沒有每日張數上限**：只驗**相簿總容量** `KANSHOU_ALBUM_CAP_ = 100`（滿了要刪舊照才能再拍）。`kanshouFilmUsed_`/`kanshouFilmStamp_`/【底片】MEMORY tag 整組刪除。
- **拍完立刻能看**：`actionGetAlbum` 不再回傳 `developed` 欄位，相簿卡片一律顯示完整內容，不再有「🎞️沖洗中……明天就能看了」的半成品卡；`showPhoto` 也不再檢查「還沒洗好」。
- **色色時也不用隱晦**：`photo_caption` 的生成指示補一句「若拍到的是親密畫面也直接寫實描述，不用刻意隱晦帶過」。
- 措辭全面從「相機」改「手機」（`kanshouPhotoStr`/`finalUserMsg`/UI 按鈕 title 等）。

- 拍照落地在 **AI 成功後**（失敗不寫入，反正沒有底片可浪費）：AI 多吐 `photo_caption`，寫 `kanshouAlbumSheet_`。髮色從被拍者 TRAIT 現場解析（`KANSHOU_HAIR_COLORS_`）。**2026-07 稽核補**：`photo_caption` 寫入前補控制/零寬/雙向字元清洗＋公式引導字元阻擋（比照 `sanitizeUserData_` 同等保護），防 AI 輸出被利用來污染相簿列。
- 前端：`kanshouTakePhoto`／`kanshouShowPhoto`(拿照片給在場者看)／`openKanshouAlbum`（`get_album`）／`kanshouDeletePhoto`（`album_delete`）。

---

## 🚶 作息／同居／拜訪

- **獨立作息**：每人有自己的家 `KANSHOU_HERO_HOME_`（`region:'visit'`）；`kanshouRollDailyLocation_` 每逢時間推進重骰全世界去向；**LOC 判在場**。
- **🏠 住處分配（2026-07 七度改版，玩家「新增的英靈會有住處嗎？種子庫是不是要整理、直接隨機就好」）**：手寫專屬豪邸(`KANSHOU_HERO_HOME_`)只有7位種子英靈有——其餘所有英靈(其他種子＋玩家原創/AI生成)過去完全沒有可造訪的家，`拜訪住處`(及當時還在的夜襲/賴床橋段)對她們全數失效，只能退回不可造訪的通用值「自己的住處」。已改成**方便·合理·隨機**三原則的自動分配：
  - `KANSHOU_GENERIC_HOME_POOL_`（8間泛用住處：河畔小公寓/巷弄老屋/高塔套房/郊區透天/老街閣樓/街角公寓/靜巷租屋/河堤畔宅）——純泛用命名、不影射任何角色背景，跟手寫豪邸一樣登記進 `KANSHOU_LOCATIONS_`(`region:'visit'`)，共用同一套移動驗證/拜訪門檻，不必另開機制。
  - `heroToKanshouRow_`(英靈召喚/入駐/結識的唯一建列函式)在建列當下檢查：查無 `KANSHOU_HERO_HOME_` 專屬住處，就從泛用池隨機抽一間，寫進她自己列的 `【住處】` 記憶標記——**一次分配、終身持有**(不重骰、不會搬家，不像約會地點那樣每天可能換)。不做「避免跟其他同伴撞同一間」的去重邏輯(接受偶爾撞名的極小機率，narrative上也可解讀成她們剛好是室友，跟`小黑`/`伊莉雅絲菲爾`共用「愛因茲貝倫城」的既有設計精神一致)。
  - `kanshouGetHeroHome_(heroId, memory)`：住處統一讀取入口，手寫專屬豪邸優先，查無才讀 `【住處】` 標記，兩者皆無才退回不可造訪的「自己的住處」(理論上七度改版後不該再發生，只保留給改版前已存在、尚未補分配的舊存檔)。`setKanshouHeroHome_(memory, homeName)` 寫入，比照 `setOutfit_` 同款「清除舊值再整段append」寫法。
  - **🐛→✅ 順手抓到的連帶bug**：`kanshouRollDailyLocation_`(深夜/清晨的homeBias)、`kanshouResidenceUnlocked_`(拜訪門檻判定)、`buildTagsPayload_`(前端`unlockedResidences`鎖圖示清單)，原本全部只認 `KANSHOU_HERO_HOME_`/`Object.values(KANSHOU_HERO_HOME_)` 這張手寫表，即使加了泛用住處池，這幾處還是認不出「這是某人的家」——已全數改走 `kanshouGetHeroHome_` 或直接篩 `KANSHOU_LOCATIONS_` 的 `region==='visit'`(單一真實來源)，之後再擴充住處池也不必記得同步這幾處。
  - 前端 `KC_LOCATIONS_`／`KC_LOCATION_EVENTS_`(🌙她熟睡中徽章)鏡射同步補上 8 間泛用住處。
- **同伴詳情上限** `KANSHOU_PARTY_DETAIL_CAP_ = 5`（同地最多給5張詳細卡，敘事上限非隊伍容量），依 BOND 排序。
- **🌍 世界概況(輕量版)**（2026-07 玩家「NPC不知道彼此存在，很怪」）：不在場的同伴也給 AI 一份極簡名單——只有**名字＋大分區**(`KANSHOU_REGIONS_`的房間/家的共用空間/深山町/冬木市中心/山林/拜訪住處，不給精確地點/在幹嘛)，依 BOND 取前 `KANSHOU_WORLD_ROSTER_CAP_ = 8` 位，避免同伴一多每回合無限膨脹。★提示詞明講**僅供閒聊背景話題、絕不可讓對方憑空出現/開口/被指名互動**——不影響【在場驗證鐵律】，指名互動/追蹤好感仍只認同地點的 `partyRows`。純粹解決「明明認識彼此、對話裡卻連提都不能提」的違和感，不是給 NPC 即時動向。
- **同居**：好感≥`KANSHOU_COHABIT_BOND_ = 90` 可邀（`kanshouInviteCohabit(name, npcId)`，2026-07 id 化重構補 `npcId` 第二參數，對象解析同上改走共用 `findPcRowIdx_`；玩家自己隨時主動發起，需她在場被問），就寢在 `KANSHOU_COHABIT_ROOM_ = '和室'`；`【同居】1` 標記。⚠ **2026-07 稽核修·同居會隨好感跌破90自動解除**：舊版`【同居】`只有兩處會寫成1、全檔案沒有任何地方清回0——好感若同居後因爽約/冒犯一路跌到接近「點頭之交」，標記仍在，AI仍照樣把她骰進和室、仍吃同居日常橋段，敘事跟數值直接矛盾。已在`kanshouSyncRelTier_`(跟REL_TAG梯度同步同一個函式、呼叫時機也一致)裡補上：BOND低於`KANSHOU_COHABIT_BOND_`就清掉`【同居】`。
  - **🐛→✅ 2026-07 八度改版·移除GAS主動邀同居（玩家「取消同居詢問的泡泡吧，太煩人了」）**：三度改版曾一度改成GAS每天主動掃好感≥90的在場同伴問「要不要邀她同居」(`kanshouCohabitOffer_`/`KANSHOU_COHABIT_ASK_TAG_`/`cohabitProposal`泡泡/`kanshouAcceptCohabit`/`cohabitAccept`)，玩家實測後嫌每天被系統追問太煩，已整組移除——同居現在只剩`kanshouInviteCohabit`這一條路，玩家想同居時自己隨時主動邀請即可，GAS不再每天倒數計時式地跳泡泡問。
- 🎛️ **GAS 主動提議通則**（現只剩 moveProposal/proposeMove 這條——約會/同居的 GAS 主動邀約機制皆已於八度改版移除，見上）：都是「意圖非結果」，落地只在玩家按泡泡同意後才發生，AI 只在那一刻演她開口/你點頭的反應。**⚠ 2026-07 拔掉 `move_proposal`**（玩家實測「AI一直提議移動、頭痛」）：換地點不再是 AI 能決定的事——AI 完全沒有這個欄位了，只剩兩條合法路徑：玩家自己用地圖走，或玩家在地圖對同伴提議「一起去」（`proposeMove` → GAS 依好感直接裁定接不接受，AI 只演反應），accept 後才回填 `moveProposal` 觸發同一顆「前往」同意泡泡。
- **💌 約會地點**：`KANSHOU_LOCATIONS_` 每筆的地點驗證只看 `dateOnly`/`noEncounter`/`bands` 三欄。**⚠ 已刪除的 `minBond` 欄位**（原給已移除的`kanshouPickDate_`選點用的好感梯度分級，2026-07 三度改版新增、八度改版隨`kanshouPickDate_`一起變成零讀取點、2026-07 全面重構收尾時確認零讀取後整批物理刪除——`KANSHOU_LOCATIONS_`現行資料結構完全不含這個鍵，不是「保留但沒人讀」）。`dateOnly:true`（情侶溫泉套房／深夜賓館）＝這類明顯是約會限定的私密地點不進 `kanshouRollDailyLocation_` 的日常閒晃保底池，這個用途仍在使用中；`noEncounter:true` 一併擋掉陌生人巧遇。
  **🐛→✅ 2026-07 補漏·前端地圖清單漏同步**（玩家「怎麼沒看到新地點」）：`Script_Kanshou.html` 的 `KC_LOCATIONS_` 是純顯示用 mirror(唯一真實來源仍是後端 `KANSHOU_LOCATIONS_`)，這 6 個新地點加進後端後，前端只補了「隱藏溫泉」(它本來就存在、這次只是補門檻)，其餘 5 個全新地點(摩天輪/水族館/深夜賓館/夜景展望台/情侶溫泉套房)漏加——地圖上完全看不到，玩家自然點不到、也走不過去。已補齊 5 筆進 `KC_LOCATIONS_`(fuyuki/dojo 對應分區)。**改地點務必雙邊同步**，這是本檔已經提醒過的坑，這次還是漏踩了。
- **🕐 時段開放地圖（`bands`，2026-07 六度改版新增，玩家「清晨走進深夜賓館，櫃檯空無一人像恐怖片開場」）**：`KANSHOU_LOCATIONS_` 每筆可選填 `bands`（`timeBand_` 5段：清晨/午後/黃昏/夜/深夜 的子集），省略＝不受限、全天候開放（向後相容，其餘地點不受影響）。目前設限：書店二樓(清晨/午後/黃昏)、水族館(清晨/午後)、摩天輪(清晨/午後/黃昏/夜，只排除深夜)、深夜賓館／夜景展望台／情侶溫泉套房(黃昏/夜/深夜)——依地點名字/現實常識訂門檻，其餘地點一律不設限，想擴大範圍只需往表加 `bands` 一列，不必動判定邏輯。
  - **擋在哪**：`actionPlay_` 的 `moveTarget` 判定（跟私人住處未解鎖同一套「擋在移動前、當作沒真的進去」寫法）——時段不合就不讓真的走進去，narrate 成「撲了個空、轉身作罷」，不報錯不解釋機制。有該地點的 pending 約定時豁免（赴約優先於門檻，同 `kanshouLocHasPendingPromise_` 對私人住處的既有豁免邏輯）。
  - **必爽約陷阱同步修掉**：玩家 `promiseMeet`（相約，現唯一約會發起路徑）已改成只挑/只准跟該地點 `bands` 相容的約會時段（`KANSHOU_APPT_BANDS_` 午後/黃昏/夜）——避免「約好了、赴約時卻被地點未開放擋在門外」的必爽約陷阱（同一類 bug 之前在拜訪私宅那邊修過一次，這次換 `bands` 踩到同個坑，修法完全比照）。（`kanshouPickDate_`——原本的GAS自動選點/選時段機制——已於八度改版隨主動邀約一併整支刪除，此規則現只需套用在 `promiseMeet` 一處。）
  - **前端 UI**：玩家提議「上鎖比較快」——地圖按鈕比照未解鎖私宅的既有灰鎖樣式（🔒＋不可點＋alert 說明幾點開），不做完全隱藏。`KC_LOCATIONS_` 鏡射同一份 `bands`（改後端記得同步這裡）；`kanshouPromiseMeet` 選時段的面板也只列出跟選定地點相容的時段。
  - **已知小落差（非 bug，範疇取捨）**：非 `dateOnly` 地點的日常閒晃保底池（`kanshouRollDailyLocation_`）目前不比對 `bands`——極少數情況同伴可能被骰去玩家當下走不進去的地點，純屬「她剛好在，你正好碰不上」的日常感，不影響任何機制正確性，暫不處理。
- **拜訪私宅**：好感≥`KANSHOU_VISIT_BOND_ = 40`（熟識朋友切點）才解鎖登門（`kanshouResidenceUnlocked_`）。
- **巧遇**：`kanshouToggleEncounter_` 開關；保底池 `kanshouEncounterPool_()`（2026-09 改資料驅動：`SEED_SERVANTS` 裡非男性 ∖ `KANSHOU_ENCOUNTER_EXCLUDE_IDS_` ∖ `KANSHOU_SUMMON_BLOCKED_IDS_`，新增種子自動進池）；結識 `kanshouAcceptInvite`（`inviteResident`）。
- **召喚全開（2026-09）**：`KANSHOU_SUMMON_BLOCKED_IDS_` 清空，斯卡哈-Assassin／伊莉雅-Caster／恩奇都-Lancer 都召喚得到、也進了巧遇池與老地方（夜景展望台／水族館／書店二樓）。擋撞名的改由 `kanshouSummonClash_` 負責：**同一位英靈只能有一種姿態在場**（靠列上的【英靈源】`KANSHOU_SRC_TAG_` 比對來源種子 id，再比真名，舊列退回跨名比對）。伊莉雅-Caster 的日常稱呼是「伊莉雅（魔法少女）」——括號會被 `kanshouNameCandidates_` 拆出「伊莉雅」，剛好與伊莉雅絲菲爾-Master 互斥。
- **牽手**：`kanshouHoldHand(name, npcId)`/`kanshouReleaseHand`（2026-07 id 化重構補 `npcId` 第二參數，單獨約會氛圍，`【牽手】` 存玩家列·值＝她的短名）。對象解析同 §約定 2.0 說明，改走共用 `findPcRowIdx_`（id 優先，查無才退回 `kanshouNameCandidates_`）。⚠ **2026-07 玩家「牽手太用力·每次都提·地理錯亂」重寫 `kanshouHoldingStr`**：舊版每回合強推「交握的溫度／並肩距離／別人也看得見」＝AI 每回合死抓著手講；且沒斷言「她此刻與你同處」＝AI 腦補成「她在○○等你、你跑進來」（明明牽著手寸步不離）。新版＝**背景資訊·別過度著墨**（偶爾輕帶一筆、重心放當下互動）＋明確斷言「她就在你身邊、和你同處一地、絕非在別處等你」。**生命週期不變式**（2026-07 玩家實測補齊）：① 跳時間重骰**豁免**牽手對象（不會憑空消失）；② 每回合算 `kanshouHeldName_` 時驗「她真的在場」——不同地點自動放手清標記（根治「隔空牽手/重逢自動牽手」）；③ `endDay` 睡覺一律放手；④ 對象名一律走 `kanshouNameCandidates_` 比對（別名/大小寫都認得）。
- **🗑 小道具／催眠指令（2026-07 建·2026-09 整套移除）**：玩家「太複雜太沒用了」，後端 4 支 handler（`actionKanshouSetProp`／
  `AddCustomProp`／`CastHypnosis`／`DeleteCustomProp`）、7 顆常數、7 支 MEMORY helper、提示詞注入（`pPropStr`／`_ignoreBondLines`／
  `_kanshouHypnosisActive_` 三處豁免）、同伴卡兩顆按鈕、故事欄快調抽屜、`Style.html` 的 `#pane-chat{position:relative}` 全部刪除。
  舊存檔 MEMORY 裡殘留的 `【小道具】`／`【自訂道具】` 是**惰性資料**——MEMORY 一律經 tag helper 讀取、從不整串餵 AI 或下傳前端，
  沒有任何路徑會再讀到它們，故不寫遷移去改玩家資料。

- `ensureOverlay_(id, opts)`／`_showOverlayLoading_(overlayId, ensureOpts, boxOpts)`（Script_Kanshou.html）— 前端共用「取得或建立全螢幕遮罩容器」／「顯示讀條」殼，取代11個彈窗函式（`kanshouOpenMemoir`/`kanshouOpenProps`/`kanshouOpenHypnosis`/`kanshouPickBand_`/`kanshouTakePhoto`/`kanshouOpenRelTag`/`openKanshouFestivals`/`kanshouPickLocation_`等）各自手寫的重複DOM建立樣板。

## 🤖 AI 管線

### `actionPlay` 執行階段順序（**順序鐵則·勿亂動**）
0. **`actionPlay` 現在是一層薄包裝**（2026-07 稽核補）：真正的引擎邏輯搬進 `actionPlay_`（下面1~12步都在這支裡）。外層 `actionPlay` 只做「同 pcId 併發軟鎖」——見下方教訓區「play 故意豁免全域鎖」。
1. 入口守門（非 `KPC_` return）＋`driveOn`/`encounterOn` 旗標＋標籤化
2. 讀表（整表只讀一次）→ 定位 pc、curL/curDay/curHour
3. UI 按鈕意圖落地（相約提議/同居/牽手/結識入駐/橋段 offer+accept）
4. 深夜訪客擲骰（**已移到最前面**，見 §深夜訪客——它會取消 endDay，晚算會讓時段/在場來由拿到錯值）
5. **跳時間/advanceHours**（重骰全世界去向、換幕鐵律）
6. **約定赴約結算（`_settle`）** ← ⚠ **必須在第7步之前**
6.5 **「她主動」三型擲骰（visit/invite/want）** ← ⚠ **必須在第7步之前**（2026-07 矛盾掃描抓到）：`visit` 型會把她的 LOC 搬到你這裡，排在 partyRows 之後的話她沒有在場人物卡，AI 同時收到「她剛剛出現在這裡」與「只有【在場人物】可以開口」兩條打架的指令。這段需要的「誰此刻在你面前」是自己從 pcData 的 LOC 算的（`_hereRows`），不依賴 partyRows 這個變數。
7. **partyRows/partyMembers 組裝**（同地在場名單、詳情卡 `partyDetailsArr`）
8. 拍照（驗相簿容量 → 掛 `kanshouPhotoPending_`）
9. 提示詞組裝（USER prompt，見下）
10. `aiConfig` → 歷史餵入 → `callGeminiAPI`
11. `sanitizeAiData_` → 各欄位落地（拍照/npc_exit/rel_changes/intimacy_feedback）→ 髒列窄讀重定位寫回 → `saveGameHistoryBatch`
12. 回傳前端 key：`text/statusString/people/options/tags/moveProposal/encounterOffer/photoResult/promiseWait/proposalResult/promiseSettle/kanshouClock/clock`

### 系統提示詞 `buildDefaultSystemPrompt()`（**鑑賞專屬**，solo 走 `miniSystem`）
`nsfwBaseRules`（⚠ **Gallery 函式內自己那份·非 Engine_Combat.gs 紅線·可改**）＋`★【輸出範本】`(finalJson)。`specificRules` 現為**空字串**（舊「慾海律令」5+1 條已整併進 nsfwBaseRules，見下方「specificRules 改成空字串」；return 形狀不動）。
⚠ **2026-07 玩家「在場人物放最後面·歷史只是歷史」USER prompt 結構調整**：`PROMPT_PARTY_SYSTEM`(【目前在場人物命格詳情】·partyDetailsArr組裝)原本緊接在最開頭【敘事法旨】之後，改搬到整段USER prompt的**最尾端**(緊接在「現在演化玩家動作」之前)，並在前面補一句「★【歷史僅供參考·專注本回合】：對話歷史只是脈絡背景…別被歷史裡已經過期的設定/情緒/場景牽著走」——用「放在提示詞最後面」利用模型對鄰近生成點內容的權重(recency)，讓最關鍵的「現在到底誰在場/該依據什麼」在AI真正下筆前最後被讀到。中段既有的「★【在場驗證·最高優先】」行為規則沒動，這次調整的是**資料**(誰在場的實際名單)擺放位置，規則跟資料現在分離在提示詞前後兩端，彼此不衝突。（原本資料後面還補了一句「★【現場僅此名單】」，2026-07 玩家「提示詞會不會太多、有沒有重複」稽核時發現這句夾在【在場驗證·最高優先】canonical版跟真正收尾前的「★【動筆前最後確認】」之間，三次講同一件事、位置上兩邊都不是純屬贅字，已刪除，不影響任何護欄完整性。）
⚠ **2026-07 玩家「提示詞太大量」全面壓縮**（核心訴求「AI 只依資料扮演」）：System＋USER＋driveStr＋schema 逐欄全部壓成最短句，冗字/重複/客套/最高級通膨砍到見骨——**但機械護欄（在場驗證/親密尺度五階/移動鐵律/冠名格式/角色一致性/世界觀）一條不刪、只縮字**。實際送 AI 文字砍約 35~45%。加規則前先想「這是機械護欄還是冗字」，冗字不進提示詞。
⚠ **2026-07 玩家「我一直想要做減法」三次瘦身**（USER prompt **3069 → 2658 字，−13%**，規則一條沒少）。
先寫回歸網再動刀：`scratchpad/sim/dedup.js` 釘住 **24 條規則「必須還在講的那件事」**（用關鍵詞交集判定、容得下改寫）
＋**重複偵測**（同一件事出現次數的上限）。合併七處：
1. **好感天花板講了三次、而且互相矛盾** ← 最嚴重的一處。`driveStr`【主動掌握】①用的是「<40／高好感／80+」，
   跟【親密尺度五階】的 20/40/60/80 **對不上**；④⑤⑥ 又各複述一次「受天花板約束／未達依個性」。
   改成 driveStr 只講【怎麼寫】、門檻一律指回五階（那才是唯一真實來源）。順手修掉「冷傲冷傲地」錯字。
2. **【視角鎖定】→ 併進【視角鎖定·不替玩家腦補】**（同一份提示詞裡「我＝玩家」講了三次）。
3. **【歷史僅供參考】→ 併進【在場驗證】**。
4. **【移動鐵律】→ 併進【地點釘死】**，刻意併到**尾端**那條（緊貼「現在演化玩家動作」，recency 位置比中段強）。
   再壓成一句，並把「路程略過不演」改成**只在 `moveTarget` 為真的回合才送**——沒移動的回合根本沒有路程可演。
5. **【在場驗證】砍半**（玩家：「說太多了·只要專注在現在給AI的資料就好」）。⚠ 順手修掉一個**真矛盾**：
   舊版在這裡開了「例外①玩家引入第三人②系統豁免段」，但尾端的【動筆前最後確認】是**無例外的絕對句**
   （「其他名字即使歷史提過也不准出現」）——兩條打架，而位置靠後又點名在場者的那條一直在贏。例外既然沒生效就別再寫。
6. **「沒給的就沒有」散在三處 → 一條【沒寫的就不存在】**（世界觀的「來歷只能取材自資料」＋【不憑空生東西】＋路人條款）。
7. **【角色一致性】刪掉「禁止兩個人用同一套反應」**（玩家：「說了只有反效果」）——正面句「一律依她自己的個性演」
   已經把話講完，再補一句否定只是把「同一套反應」送進模型的注意力裡。
**第二波（同日·玩家「肯定還有可以刪減的地方」→ 2658 → 2484 字，累計 −19%）**：
8. **【在場驗證】vs【動筆前最後確認】真的是重複**（玩家：「這不是重複嗎...?」——我先前答「刻意的 recency 錨點」
   只對一半）。實際不重複的只有三樣：①路人條款 ②缺席者要交代去向 ③**尾端那份實際名單**。
   「只有在場人物能開口」這句確實講了兩次，而尾端那條**帶名字、位置又更好**，是嚴格更強的版本
   ——所以 canonical 移到尾端，前面那條只留它獨有的兩件事，改名【路人與缺席者】。
9. **【視角鎖定·不替玩家腦補】161→~120**：刪掉三個同義反覆的括號補述（「不誤寫成他人心境」被「我＝玩家」涵蓋、
   「禁挪用玩家特徵」被「只取材她自己那份資料」涵蓋、「旁白不得套用」被「只用在引號內台詞」涵蓋）。
10. **【命令/強迫】146→~120**、**時鐘 123→~95**（三句「以此刻為準」收成一句）、
    **【稱呼】77→~55**（「僅narration/台詞·JSON欄仍填真名」是 schema 自己的事；「非清單所有人都要出聲」語意不清，改寫成「名單上的人這一回合都要有戲」）。
11. **【角色一致性】163→~100**（玩家直接給了改法）：刪掉「(高傲咬牙不示弱/虔敬掙扎/活潑藏羞)」
    ——那正是同一批剛砍掉的**模板**，三個例子會把所有角色往同一個方向拉。
    ⚠ 但兩處**刻意留下**：①「生理反應可以有…不退化成發情機器」是 NSFW 的**雙向**平衡，只留「人格不崩」
    會被讀成「保持矜持」；②「既定事實、**不是演法**」的對比是上一批的核心，拿掉對比只剩半句。

12. **📍「現在演化玩家動作」移到整份提示詞的最後一行**（玩家：「這個組裝在最後面!」）。
    它原本被【稱呼】【收尾】【動筆前最後確認】三塊壓在後面——但**約束是背景、動作才是題目**，
    題目該貼著生成點。新順序：…資料 →【地點釘死】→【稱呼】→【收尾】→【動筆前最後確認】→ **玩家動作**。
    ⚠ 這也讓「在場名單」緊貼在動作前一行，recency 錨點的效果反而更好（先框住人，再給題目）。

13. **【主動掌握】改寫（玩家給的版本）146→127，同時是一次去重＋一次行為改動**：
    - 去重：「同伴主動推進至實際發生、拒絕空轉」跟尾端 🚨【收尾·主動掌握】的「大幅推進到位，該發生就發生，
      別在曖昧邊緣空轉」是**同一句話**，尾端那條位置更好 → 這裡不再重複。「禁霸道模板」也刪（正面句
      「依她自己的個性主動」已涵蓋）。
    - ⚠ **行為改動（不是壓縮）**：色度由「**跟隨**玩家」改成「**可以走在玩家前面**」，並加上 Dirty Talk／直白不迴避。
      仍受【親密尺度五階】天花板夾住（那是唯一的准不准線），且只在 `driveOn=true`（玩家自己按下主動掌握）
      才組進提示詞——平時矜持模式拿不到這一段。
    - 「不對玩家造成真實傷害」**刻意保留**：【命令/強迫】那條保護的是 NPC，這條是全提示詞裡唯一保護玩家角色的。
    🐛 這次順手抓到 `dedup.js` 一個**假通過**：「既定事實交棒句」原本只查「依她自己的個性」，
    而【主動掌握】剛好也有這幾個字 → 它一度是靠**錯的區塊**在通過。改成連「不是演法」這個對比一起查
    （那句只存在於 canonical 出處【角色一致性】）。**回歸網自己也會有覆蓋漏洞，這是第二次踩到同一種形狀。**

**第三波（同日·2481 → 2299 字，累計 3069 → 2299，−25%）**：
14. **【後日談模式·最高優先覆寫】72→37**：後半段「各自熟到什麼程度依相處基調演」已被【角色一致性】
    的交棒句涵蓋（那才是 canonical），刪。**前半段刻意留下**：模型對 Fate 這幾個名字有很強的先驗
    （她們在原作裡是打過聖杯戰爭的舊識），【沒寫的就不存在】這種泛用句壓不過**專有名詞的先驗**，
    得指名否認才擋得住。
15. **NSFW 記憶區 `nsfwMemories` 砍掉三種浪費**（玩家：「【在場人物】這是不是多餘?」）：
    - **裝扮講了兩次**：`[X 裝扮]` 一份、【角色背景資料】的「裝扮:」又一份，同一個值。刪前者。
      「五官/髮色/體態不隨換裝改變」原本跟著每個人重複 N+1 次 → 掛在名單抬頭講**一次**。
      玩家自己的裝扮同理（【玩家資料】那行本來就有；舊註解「不能只在【玩家命格】看得到」是排版
      還很長的時代留下的補償，現在那行就在提示詞第二行）。
    - **`[快照]：[羈絆]…` 整條刪**：REL_MEM 現在只裝【專屬稱呼】，而那個已經由 `pMemStr` 帶進
      【角色背景資料】。⚠ 更關鍵的是種子預設值「初次相遇，緣分才剛開始」會跟好感 70 的相處基調
      「她喜歡你卻說不出口」**直接打架**——這不只是冗字，是會讓 AI 演錯的**假事實**。
    - **`[X 肉體]:{"狀態":"如常"}` 預設值不送**（`_isPlainBody_`）：零資訊量卻每人一行。
16. **正名**（玩家：「改成角色背景資料?」）：【目前在場人物命格詳情】→**【角色背景資料】**、
    【玩家命格】→**【玩家資料】**。「命格」是九州武俠留下來的詞，跟「和平現代城鎮」的世界觀對不上。

17. **【命令/強迫】129→90**（玩家：「目前分段有做好·這段也是多餘的」）：五階本體已經逐階寫明
    「未達門檻會怎樣」，這裡只留兩件**推不出來**的事——①命令/強迫**不是繞過門檻的捷徑**
    ②禁演成真實傷害。⚠ 第②件刻意留：那是全提示詞裡唯一保護 **NPC 身體**的一句
    （【主動掌握】那句保護的是**玩家**，兩者不重複）。
18. **【在場來由】整條刪**（玩家：「不用再說明了吧?」）：每個人自己那一欄現在是**自我說明**的
    （「【你們從剛才就一直在這裡】相處著——她早已在場，這一刻是延續，不是重新登場」），
    這個 ★ 只是在說「請照那一欄演」＝用一句話介紹一句話。
    🧭 **新判準：欄位本身就講清楚的事，不必再開一條規則去介紹它。**
    （MIN_STAR_BLOCKS 124→123；改 ★ 標題時同步改了 `check_prompt.py` 的 ALLOW key，那個坑今天踩過一次。）
**累計：3069 → 2244 字（−27%）。**

**第五波·清醒度（玩家：「我不是要省字數·我是要 AI 更清醒！不做無聊的禁止——他本來就不太會 OXOX，一說他就有可能會 OXOX」）2244 → 2015 字**
判準從「短」換成 **①只講事實 ②negative 只在模型真的會犯時才留、能用正面句就用正面句 ③同一件事只講一次**。
- 🐛 **抓到真矛盾**：時鐘寫「本回合只寫這**十分鐘**」、收尾卻寫「時間刻度不超出這**一分鐘**」——差十倍，
  兩條都是硬指令。統一成十分鐘。
- 🐛 **五階裡藏著演法模板**：`<20` 那格寫「(閃避/擋手/喝止/還手依個性)」——四個具體動作等於所有角色
  用同一套反應，正是同一天砍掉的東西。全部改成只講「到哪裡為止」，怎麼擋交給個性。
- **刪掉「他本來就不會做」的禁令**（講了反而把概念放進去）：【主動掌握】的「不對玩家造成真實傷害」
  （她主動示好時傷害玩家不是模型的失敗模式；真實傷害統一由【強迫】那條講一次、涵蓋雙方）。
- **負面改正面**：「不退化成發情機器」→「她說話做事仍然照她的性格」；「不自創第三種叫法」→「就這兩種叫法」；
  「禁大段內心戲/禁替他決定」→ 刪（「只演他實際輸入的動作與五感」已經涵蓋）；「別裝作還在」→「用一句話交代去向」。
- **刪掉「教她怎麼用」的話，只留事實**（玩家：「★【今日天氣】：雪後轉晴 這樣就好」）：
  天氣後面的「自然滲入場景不必每句提」、時鐘的「歷史停在哪個時段都不算數」、世界觀的「調性不限悠閒」、
  篇幅的「不必精確別落差太大／低好感別寫大段內心戲」（字數本來就依好感自動調 250/400/500）。
- **刪掉第三次講的東西**：【敘事法旨】（「我＝玩家」第三次）、五階標題的「(人格不崩)」、
  【演出而非說明】的「數值只輸出 rel_changes」（schema 自己每回合都在講）。
- ⚠ **世界觀那句刻意留七個字**（玩家版是「和平現代城鎮，大家都是生活在這冬木市」）：
  改寫後仍保留「沒有魔術與從者」——因為這句剛好**指名了冬木市**，那是聖杯戰爭的舞台，
  光講地名會**加強** Fate 先驗而不是壓下去。這是「世界事實」不是「禁令」。
- 🐛 我改寫時引進兩個**沒有指涉對象的「她」**（那兩塊講的是任一在場者），`check_prompt.py` 當場抓到並擋住 push。
  **這正是那支掃描器存在的理由。**

**第六波（同日·2015 → 1860 字，累計 3069 → 1860，−39%）**——玩家一句話講完了整個方向：**「寫再多它會歪」**。
- **💕【後日談】整條刪**（玩家：「這個不能砍嗎」——可以，而且是**今天自己的改動讓它變多餘的**）：
  ①同一份提示詞現在寫著「和平的現代冬木市…**沒有魔術與從者**」＝聖杯戰爭沒發生過，「沒有戰前舊識」是它的推論；
  ②**相處基調**（今天新增）逐人講明「你們到底多熟」（好感 5＝「你對她而言是陌生人」），比一句全體通用的
  否定精準得多——那一句本來就是基調表還不存在時的 1D 替代品；③空場那一支也重複（`PROMPT_PARTY_SYSTEM`
  已寫「這個地點沒有其他人」，【動筆前最後確認】再講一次）。
- **★★【強迫也照上面那張表判】整條刪**（玩家：「理論上好感可以控制了」）：五階現在寫的是
  「**超過的那一步不會發生**」，那是對【任何嘗試】講的，命令/強迫本來就包含在內——**指名它反而
  把那些字送進模型的注意力**。
  ⚠ **有意識的取捨**：唯一沒被涵蓋的是「不寫成真實傷害(流血/骨折/撕裂傷)」，全提示詞現在沒有別處在講。
  日後若敘事真的出現受傷描寫，**補回來的是那一句、不是整段**。

**第七波·系統提示詞（每回合不變的那半份）2379 → 2097 字**（玩家：「那每次給系統不變動的…那邊是不是也很多可以砍?」）
🚨 **先確認可動範圍**：系統提示詞＝`nsfwBaseRules`(623 字·**紅線①·一個字都不可改**)＋【慾海律令】(481·可改)
＋★【輸出範本】schema(1275·可改·最大塊)。本波只動後兩者，`git diff` 確認紅線常數本體 0 改動。
- **律令7 砍掉跟 schema 重複的前半**（兩處都在講「有變化才填、否則留空」），只留它獨有的
  「玩家指定的裝扮＝既定事實」。
- **律令6（options 出題規則）搬進 schema 的 options 欄位本身**——規則貼著欄位，不必兩處對照。
- **三個 `_note` key 全部拿掉**：它們長得就像欄位，模型很可能照抄回來——那是**輸出 token**（比輸入貴），
  而我們一個字都不讀。內容併進欄位自己的說明字串。
- **`fav_change` 拿掉「單回合上限+5」**：`actionPlay_` 早就 `Math.max(-5, Math.min(5, …))` 硬夾。
  照「GAS 掌數值、AI 只說書」——告訴它一條違反也沒用的規則，只是多給它一個可以鑽的縫。
- 🐛 **我自己做壞又當場修掉的兩個**：①刪掉律令 6 之後**編號跳號**(1,2,3,4,5,7)，小模型會以為漏了一條；
  ②options 的說明我一度塞成陣列的**第 5 個元素**——`sanitizeAiData_` 是 `slice(0,6)`，五條會照單全收，
  **前端真的會長出第 5 顆按鈕**。說明併回第 1 條字串裡，陣列維持剛好 4 個。
- ⚠️ **`attitude` 是「沒用卻不能刪」的唯一一處**：今天已證明它是自我強化迴圈、拿掉了落地與餵回，
  grep 全檔沒有任何地方讀它——但**紅線 `nsfwBaseRules` 第 5 條指名了這個欄位**，刪欄位會讓紅線文字
  指向不存在的東西。欄位與律令 5 只能留著空轉（約 60 字）。**要處理只有一條路：玩家明確授權動紅線那一句。**

## 🚨 2026-07：紅線① 由玩家明確授權開啟過一次（`nsfwBaseRules` 整合）
**這是全專案唯一一次動紅線常數本體，逐項授權如下，沒有第二次的預設許可。**
1. **「女子體態柔美(…)」只拿掉「體態」兩個字，其餘原封不動** ← ⚠ **這一條是我當天最大的教訓，請讀完再動它。**
   我先判斷它是「統一體態模板」要收窄，玩家說明了它的真正用途（「不要讓AI敘述女性時候有甚麼薄繭…
   出力也不能用甚麼鋼鐵夾住這種奇怪的形容才會增加這一句的」）——**它是對的補丁**：這些角色在原作是戰士，
   模型會自然寫出薄繭/傷疤/鋼鐵般的力道。然後我改了四版，**每一版都引進一個新的失敗**：
   ①給詞彙範例「力道用絞緊/包裹/吸附」→ 玩家實測「我每次都被毒蛇絞緊…他會換方式搞我」（給詞＝指定方向）
   ②加「溫熱」→ 把**體溫**也綁死（玩家：「有些人是冰冷的阿」）
   ③寫「無繭無疤」→ 否定句被當成要提的事（玩家：「他就會一直強調 無繭無疤」）
   ④只留「膚質細緻柔嫩」→ 玩家：「先用原本的 體態可以拿掉」。
   **最終落點＝原句減兩個字。** 🧭 判準：**改一個活著的句子之前，先問它為什麼活著**；
   玩家實際玩過、活下來的措辭，比我推理出來的「更乾淨的版本」可靠。
2. **`nsfwBaseRules` 與【慾海律令】整合成一份 10 條**（玩家：「妳看看能不能整合吧」）。兩份實測 5 處重複：
   律令1(inner_monologue)、律令4(mutual_nicknames·schema 欄位自己講就好)、律令5(attitude)、
   律令6(裝扮)、鐵律5/6 與 schema 的 options。`specificRules` 改成空字串（不動 return 形狀）。
3. **`attitude` 欄位整個移除**：全檔已無任何程式讀它，它活著的唯一理由就是紅線第 5 條指名了它。
   紅線那句一走，欄位跟著走——AI 每回合填一格然後被丟掉，那是白付的**輸出** token。
   （三處解釋「為什麼刻意留著」的註解已同步改掉，不然會騙下一個失憶的我。）
**系統提示詞 2379 → 1899 字。**
✅ **CLAUDE.md 的紅線① 已由玩家決定取消**（「nsfwBaseRules 紅線設定直接取消吧」）：改成「不再是禁區，
但它仍是演化核心——改之前先用模擬器量現況、改完必跑 `dedup.js` 與全套探針」。
**編號刻意保留為 ①**：②③④⑤ 在全專案的註解與文件裡被大量引用，重編會讓那些引用全部指錯。

🚩 **給下一個失憶的我：為什麼前幾次瘦身都只砍得動一點點**（玩家問「為什麼之前找妳削減 妳都砍一點點而已...」）
- **根因是上面那句「機械護欄一條不刪、只縮字」**——它聽起來很負責，實際上是**自己給自己設的天花板**。
  句子能擠的水有限，擠完就沒了。諷刺的是那句點名保護的六條護欄裡，**移動鐵律**與**在場驗證**
  今天都被合併掉了，規則一個字都沒少：它們從來不是六條護欄，是**四條護欄講了六次**。
- **我把「這條規則存在」當前提，只問「怎麼寫得更短」。** 該問的是「**這條需要存在嗎／別處是不是已經在講**」。
  兩個問題的收穫差一個量級。
- **每條規則背後都有一道看得到的疤**（註解寫著它修好哪個 bug），刪掉像把 bug 放回去，於是一律選「留著縮短」。
  那是拿「看得見的成本（被抓到回歸）」換「看不見的成本（提示詞被稀釋）」——而後者才是玩家一直在抱怨的。
- **沒有量測就不敢動。** 今天第一件事是寫 `scratchpad/sim/dedup.js`（24 條規則的「那件事」＋重複偵測）。
  **一旦能證明沒東西掉，刪除就變便宜了**；在那之前每一刀都是憑感覺，所以只敢下小刀。
- ✅ **下次的開工順序**：① 先寫回歸網（釘事實、不釘措辭）② 逐塊問「這條需要存在嗎」③ 才輪到縮字。
  順序反過來就會再交出「一點點」。

🧭 判準：**負面指令能用正面句涵蓋就刪**；**只在需要的回合才送的句子不要每回合送**；**同一條規則只能有一個 canonical 出處**
（recency 錨點是例外，但要在註解裡寫明是刻意的）。
⚠ 刪 ★ 區塊要同步把 `check_wiring.py` 的 `MIN_STAR_BLOCKS` 調下來（128→125），這是刻意設計成「必須有意識地動一次數字」。
⚠ 改 ★ 標題時記得看 `check_prompt.py` 的 `ALLOW` ——它的 key 是**標題文字**，改了標題豁免就失效（這次踩到一次）。

⚠ **2026-07 玩家「提示詞會不會太多、有沒有重複」二次瘦身**：抓到 3 處意外重複（非刻意的 recency 設計）：①schema 欄位`appearance_extras`描述與`specificRules`第7條把「禁寫成場景/姿勢(如「在水下」「泡在浴池」)」這句完整範例講了兩次——第7條改成指回schema說明，不再重複舉例；②【此地唯一真實】跟【地點釘死·此刻只有這裡】幾乎同義(都在講「歷史提過的其他地點是過去式」)，且不是recency設計、單純位置相鄰——刪除前者，後者已涵蓋全部內容；③【現場僅此名單】見上一則。三處都只刪重複陳述，親密尺度五階/移動鐵律/冠名格式/角色一致性/世界觀等機械護欄本體字句一個未動。
⚠ **2026-07-28 玩家實測·收尾第二輪：所有按鈕補上【結果·GAS 已裁定】**（玩家：「交換聯絡 不是100%成功嗎 幹嘛還要這樣演 到底GAS給AI甚麼資料」「玩家不能是最後說話的 因為這樣玩家很難接!! 請依照這個方向 去檢查所有按鈕 並補上」）

上一輪把 🚨【收尾】的錨點翻到對方身上了，但玩家又撞到同一件事：按「結識」（100% 成立），
AI 演成「我鼓起勇氣開口問……忐忑地觀察她的反應」然後停筆，RIDER 一句話都沒回。

**★ 區塊其實寫對了**（`★【正式結識】：你與『X』交換了聯絡方式，這段緣分正式接上了…`），
壞在**提示詞的最後一行**：`現在演化玩家動作：『【玩家意圖】：鼓起勇氣向『X』提出想繼續深交…』`
——那是離生成點最近的一句話，而它只寫「他開口提了什麼」，等於叫 AI 演一個**還沒有答案的請求**。
★事實寫在二十行以前，**recency 贏了**。這跟 check_wiring ⑥ 對 solo 釘的鐵則是同一條
（「意圖→GAS 結果→AI 只演，缺一樣 AI 就會自己補那一半」），只是鑑賞這邊兩半被拆在提示詞的頭尾。

修法：新增 `_settledVerdict`（跟 `_pendingProposal` 同處宣告），各分支填一句人話，
prompt 組裝前收成 `_settledTail_` 接在 `finalUserMsg` 後面：

> 【結果·GAS 已裁定，不可改寫】『櫻』答應了這個約定——本回合演到這件事發生為止。

**「拋一個玩家接得住的話題」搬進 🚨【收尾】了**（玩家：「這可以用在敘述裡吧? 不然有時候常常沒話題接」）——那不是按鈕專屬需求，**每一回合**都該留鉤子，所以放在每回合都吃的收尾鐵律才是單一真實來源；收束句只留它獨有的那件事（「這件事已經有答案了，演到它發生為止」），不再重複講收尾。
現行收尾全文：`把最後一句留給被搭話的那個人——用對方的答話或神情收尾，並讓她拋出一個玩家接得住的話題(一個問句、一句邀約、一件她此刻在意的事，都行)，停在等玩家回應的那一刻。玩家只說他自己輸入的那一句，結尾不替他再開口、再發問。沒有別人在場時才收在「我」的動作上。`
裁定句一律由名字開頭（玩家原話：「『RIDER』同意交換聯絡方式」），這樣收尾鐵律裡那個「她」才有貼身的指涉對象。

相約／同去／牽手三型的成敗都在 `_pendingProposal.accepted`，**一處集中轉成人話**（新增第四型會自動吃到）；
同居／告白／結識在各自分支填。拍照／鬆手／開門這類「本來就沒有對方答不答應」的按鈕**刻意留空**，交給收尾鐵律。

順手抓到一個真 bug：promise 型的 `_pendingProposal` 只有 `idx` 沒有 `name`，直接讀 `.name` 會印出空的『』
——比照 post-AI 落地那段的既有寫法，拿不到就回列上讀。

**脈絡量過了**（玩家問「按鈕有吃到全部的脈絡嗎? 歷史資料那些??」·探針 `scratchpad/sim/ctx.js`）：按鈕回合與純聊天回合走的是**同一條 `actionPlay_`**，拿到的脈絡完全一樣——歷史 6 筆(3 輪)、同一份 system(約 1810 字)、角色卡／五階／時鐘天氣地點／收尾鐵律一塊不缺；按鈕只是多帶一段 ★事實與收束句（user prompt 1498→約 2100 字）。唯一會變少的是**換幕縮窗**（移動／跳時段／結束一天只餵 2 筆＝1 輪），那是刻意的，防止被舊場景帶著跑。

回歸探針 `scratchpad/sim/tail.js`（13 個情境·跑真的 `actionPlay_`·比對提示詞最後一行）：
相約答應／同去答應婉拒／牽手答應婉拒／告白被拒成立已是戀人／同居婉拒再問／**結識100%成立**／拍照·鬆手不硬塞裁定。

⚠ **2026-07-28 玩家實測抓到的兩個 bug（同一批修）**：

**① 敘事的 `<br><br>` 變成畫面上看得見的四個字。** 2026-07-24 補 self-XSS 那次（`fa56630`）把 AI 敘事本文整段 `escapeHtml`，但提示詞（`nsfwBaseRules` 第 2 條與 solo `miniSystem`）明寫「換行一律用 `<br><br>` 分段」——escape 之後分段標籤就成了字面文字，**兩軌同時中招整整四天**。根源解：`Script.html` 新增共用的 `aiHtml_(text)`（escape 全部 → 只把 `<br>` 放回來 → 再轉真換行），`narrate()`／鑑賞 `send()`／老虎道場三處統一走它。**放行清單就只有 `<br>`**，`<br onload=…>` 這種帶屬性的假標籤不放行。**這條已改成機器擋**：`check_render.js`（`check.sh` 第四支掃描）把 `aiHtml_` 真的跑起來對答案——排版（`<br>` 要活）與安全（其餘標籤要死）兩個方向同時釘死，另查「印【說書人】的地方有沒有繞過 `aiHtml_`」與「提示詞還要不要 `<br>` 分段」。實測把當初那個 bug 重新種回去，三條同時紅燈。

**② 敘事收在玩家自己的台詞上，玩家沒東西可接。** 舊的 🚨【收尾】寫的是「停在『我』當下進行式…最後一句落在正在發生的動作、**或剛說出口的話**上」——那句話字面上就在叫 AI 收在玩家的台詞。實測結果：AI 替玩家多問了一句「你今天……怎麼會在這裡？」然後停筆，沒有任何人回答，玩家只能對著自己被代寫的問題發呆（玩家原話：「玩家不能最後說話 超級難繼續對話」）。改成把結尾錨點翻到對方身上：

> 🚨【收尾】：把最後一句留給被搭話的那個人——用對方的答話或神情收尾，停在等玩家接話的那一刻。玩家只說他自己輸入的那一句，結尾不替他再開口、再發問。沒有別人在場時才收在「我」的動作上。

`dedup.js` 的釘子同步從「當下進行式」換成「被搭話的那個人／不替他再開口」——舊釘子釘的正是這個 bug 本身。

⚠ **2026-07 玩家「認真看看有沒有可以合併的」三次瘦身**：逐段細讀整個 system+USER prompt 找「同一件事講兩次」，這輪抓到 1 處可合併：★【不替玩家腦補·結尾停外部】(USER prompt中段)跟🚨【收尾】(USER prompt真正結尾、緊接動筆前最後確認)都在講「narration 停在當下未完成的動作、把下一步留給玩家、不要用總結式收尾」——差別只在措辭，不是刻意的早晚雙重reinforcement(不像在場驗證/移動鐵律那兩對有明確記錄的 recency 設計理由)。已把中段那句唯一與收尾重複的子句(「禁把段落收在玩家的期待/渴望上·結尾停外部當下…把下一步想怎樣留給玩家」)拿掉、改指回【收尾】，中段只留它獨有的兩條規則(不寫自己的臉/同伴外貌不可挪用玩家命格特徵)；把「禁把段落收在玩家的期待/渴望上」這句唯一的新資訊補進真正收尾那句的括號裡，不遺漏任何約束。**同時稽核並確認排除以下兩對不合併**（都是有意圖的「canonical版早段+recency版尾段」設計，不是意外重複，貿然合併會抵銷 recency 增強效果，且【地點釘死】/【移動鐵律】那對正是這幾個月「同伴消失又亂入」bug系列反覆修的核心防線）：①【在場驗證·最高優先】(早·完整版含例外)＋【動筆前最後確認】(真正尾端·recency)；②★★【移動鐵律】(早·機制面「AI不能自己決定換地方」)＋★【地點釘死】(近尾端·敘事面「歷史地點是過去式」)。
⚠ **2026-07 玩家追問「移動鐵律？？」逼出的第四輪瘦身——同一條規則「自己」內部的重複**：上面兩輪都在找「跨區塊」的重複，玩家這次點名要求再檢查【移動鐵律】本身，結果發現這條規則**自己內部**把「你現在在curL」講了兩次：開頭「你和玩家【此刻在「curL」】」跟結尾「沒有系統明講換地方，就是還在curL」是同一件事講兩次，不是跟【地點釘死】的跨區塊重複（那兩對早已確認保留）。同一個毛病也長在【地點釘死】自己身上：開頭「你們現在人在curL，就是這裡，不是別的地方」跟後面「同伴此刻的言行、場景描寫只能圍繞curL，不可以講得好像人還在別處」也是同一件事講兩次。兩條各拿掉了純屬贅述的開頭句，只留一次陳述＋各自獨有的機制/敘事細節，內容一個字沒少。**教訓**：稽核「有沒有重複」不能只查「不同規則之間」，同一條規則自己起手式重申標題含意、結尾又重申一次，也是同一種浪費，下次抓重複要連單一規則內部也一起看。
⚠ **2026-07 玩家「我覺得可以再精簡」第五輪——不找重複、改找純措辭壓縮**：前四輪都在抓「同一件事講兩次」，這輪換一種性質：找**沒有重複、但單條規則本身寫得比必要囉唆**的地方，壓縮措辭但不動任何判準/例子(牢記二輪改版的教訓——拿掉具體例子小模型會抓不住重點，這輪只砍轉折詞/重複框架用語，例子/門檻數字/行為選項一個沒刪)。改了3處：①`specificRules`第6條(options建議越界)砍掉「只能是…真能做到的動作」「那是玩家當下不可能知道、也跟眼前這回合無關的設定資料」這類重複框架語，221字→180字；②★【天花板管命令】跟★★【強迫/暴力】兩條相鄰且高度相關(都在講「未達門檻的舉動不會得逞」)，合併成一條★★【強迫/暴力/傷害類…】，順手把命令規則裡「命令不因動口沒動手就豁免」跟「不會單純因為玩家用命令的口吻講就順從照做」這句自我重複的第二句也砍掉，577字→521字。兩處合計省約100字，親密尺度五階的門檻數字/反擊行為/例子(命令跪下/命令過來/命令脫/幫她解決OO困擾/提點家電困擾)全部保留，只是句式更緊。
⚠ **2026-07 玩家「再再再精簡！！！！！！！」第六輪——確認已到安全底線**：再逐段複查一次找純措辭空間，只再找到1處可再砍(【親密尺度五階】標題括號裡「見上方佩戴道具說明」這句純交叉引用、後面已經接著講清楚例外內容，屬多餘的meta備註，直接刪)，其餘如`driveStr`/`dialogueFormatRule_`/appearance_extras schema 描述都已經過前面幾輪或更早的「Gemini改寫再壓縮」處理過，內含的具體例子清單(喘息/輕吟/悶哼等口腔聲響、絲綢襯衫/牛仔褲/浴巾等穿著範例)都是先前實測證實小模型需要的錨點，繼續砍字只會讓提示詞變輕但判斷力變差——**已達成本輪能做的安全精簡上限**，再要更短就得拿掉具體例子，那是拿「省字數」換「AI理解力下降」，划不來，故在此停手，除非之後又抓到新的實際行為異常、需要用具體例子去對治。
⚠ **2026-07 玩家「保留範例、其他都可以砍！砍到現在的10~15%！我去測試，真的不行再還原」第七輪——大膽壓縮實驗**：玩家明確授權更激進的實驗性壓縮，並自備還原計畫，因此這輪把整段 system+USER prompt(不含`nsfwBaseRules`本體——紅線保護，一字未動)重寫成電報式短句：`dialogueFormatRule_`(4條格式規則)、`specificRules`第6條、schema欄位描述(inner_monologue/appearance_extras/intimacy_feedback._note/memory/master_note)、`driveStr`(6條主動掌握規則)、以及 USER prompt 固定規則鏈(視角鎖定/角色一致性/在場驗證/焦點禮讓/世界觀/親密尺度五階標題/天花板管命令與強迫暴力合併/篇幅指定/演出而非說明/移動鐵律/不憑空生東西/不替玩家腦補/歷史僅供參考/地點釘死/收尾/動筆前最後確認)全部大幅壓縮連接詞與說明句，只留判準/門檻數字/具體例子(命令跪下/過來/脫、強姦/壓制/無視掙扎、絲綢襯衫/浴巾/在水下/泡在浴池、鏡子顯現我的表情、那一刻/那一夜/自此/從此、泡泡/按鈕/地圖等全部保留)。**⚠ 過程中一度誤觸紅線**：第一次改寫時手滑把`nsfwBaseRules`常數本體(那6條鐵律)也一起砍了，被`git diff | grep nsfwBaseRules`當場抓到，立刻逐字還原回原文後才繼續——之後全程繞開這個常數。**實測結果誠實回報**：這輪(不含schema部分因較早幾輪已動過)實際只壓到約 81.5%(省 18.5%)，遠遠達不到玩家要求的「10~15%」(=省85~90%)——因為「保留所有具體例子/門檻數字」這個前提本身就決定了壓縮的物理上限，例子/數字本身占了原文很大比例，能砍的只剩連接詞跟說明句，那些早在前幾輪已經砍得差不多了。若要真的砍到10~15%，勢必得犧牲部分例子或把判準本身簡化，那就違背「保留範例」的前提了。**還原點**：這輪改動前的乾淨版本是 commit `94c3858`，玩家實測若發現narration品質下降(如忘記在場驗證/親密尺度天花板失守/角色一致性跑掉)，可以 `git revert` 這輪commit或直接checkout回`94c3858`重新部署。
⚠ **2026-07 後續二輪·玩家拿 Gemini 改寫再壓縮回精簡風格**：`nsfwBaseRules`(規則1)/`driveStr`/`🛑角色一致性`/`dialogueFormatRule_`①②③ 這幾段文字重新順過措辭，同步拿掉幾個「具體例子/原因說明」（如規則1的「？？不可演成從容挑釁」範例、driveStr的「該親就親/該進一步/該索求」具體推進動詞、角色一致性的「冷酷」型例子、對話格式②冠名規則的「為何要冠名(櫻認錯自己台詞)」解釋、③的錯例/正例對照）——**這些多半是「示範用具體例子」而非機械護欄本體，砍了會讓提示詞更省字，但也可能讓模型在對應的邊角情況下少一點依循，若之後實測某個對應行為(如認錯自己台詞/停在曖昧不推進)又回歸，優先考慮把對應的具體例子/原因說明補回去，而不是整句重寫**。**曾考慮把 driveStr(僅driveOn=true才出現的USER prompt動態片段)直接合併進恆常存在的System prompt(nsfwBaseRules/慾海律令)裡——玩家決定不做**：driveStr關掉時是0字(GAS動態組裝不塞這段)，合併進恆常規則反而讓「開關關掉」的多數回合每回合都要多付這段字數，且拿掉了「矜持/主動掌握」兩種節奏可切換的實際功能，省字帳算下來不划算。
⚠ **2026-09 玩家「我想要拉高 AI 回覆的字數」第十輪——先找天花板再調**：

**天花板不在提示詞，在 `max_tokens`。** 它管的是【整份 JSON】不是只有 narration，而 narration 以外的
欄位開銷很可觀：

```
JSON 固定開銷（三人在場）：典型 757 字 ／ 欄位全滿 1,057 字
舊 max_tokens 1500 → narration 撐到約 670 字就會被截斷
截斷 ＝ JSON.parse 失敗 ＝ 重試 ＝ 玩家看到「🌫️ 因果紊亂」
```

也就是舊的 500 字目標**已經貼著上限在跑**，再往上加提示詞也沒用——會直接壞掉。

三件事必須一起改（只改一件都無效）：
| # | 改動 |
|---|---|
| ① 目標字數 | `250/400/500` → `380~500 / 550~700 / 700~900`，並改成**資料驅動查表** `KANSHOU_WORDS_` |
| ② 指示寫法 | 「約 X 字」→「寫 A~B 字，**不可少於下限**」。小模型看到「約」一律往下取（舊教訓已記在本檔）。再補一句「寫不滿就往互動裡加：動作細節、觸感／氣味／聲音、多給一次真實反應。別靠拉長環境描寫充數」——給它**填充的方向**，否則它會用環境描寫灌水 |
| ③ `max_tokens` | `1500` → `2400`（時間轉場那支 600 → 700）。餘裕實算：narration 900 字＋欄位全滿 ≈ 2,054 tokens，即使 AI 超寫到 1,000 字也還在 2,400 內 |

`check_wiring.py` 的 `TIER_TABLES` 新增一組盯著 `KANSHOU_WORDS_` 每階都要有相異的 `range`
（已用故意弄壞一階驗證抓得到）。

⚠ 排序不受影響：改完重量可快取前綴仍是 **92%**（第九輪的成果沒被打壞）。

⚠ **2026-09 玩家「輸入那麼多、AI 只回一點點，是不是虧大了」第九輪——排序，不是再砍**：先量投入產出比。

```
穩態每回合：system 1,655 ＋ user 1,812 ＋ history 1,536 ＝ 約 5,000 字輸入
            AI 產出 662 字（narration 500 ＋ 內心 50 ＋ 4 選項 ＋ 經歷）
            比例 7.6 : 1
```

**結論：不是「給太多」，是「同樣的字每回合重付一次」。** 相鄰兩回合逐行比對，
**93% 的內容一字不差**（system 53 行只有 5 行變、user 26 行只有 2 行變）。

⚠ **這一輪是在「假設 prompt cache 存在」的前提下做的**——玩家指示「先用預設有快取做修正」。
實際有沒有快取**尚未證實**：payload 裡沒有任何快取參數、回應也沒讀 `usage`，而此開發環境的網路
政策擋掉 `openrouter.ai` 無法查證。**若最後證實沒有快取，本輪三項改動對成本沒有幫助**（但也沒有
壞處——排序本身不影響字數），屆時要省只能真的送更少字（history 6→4 筆、JSON 範本再壓）。

**排序原則（新增·寫進 `actionPlay_` 的 prompt 上方註解）**：cache 是**逐 token 比對前綴**，
一個會變的東西插在中間，它後面全部作廢。所以【穩定的放前面、每回合會變的放後面】。
唯二例外是 🚨【收尾】與 ★【在場名單】——它們雖然穩定，但 recency 對它們特別重要
（實測過「事實寫在 20 行以前就會被 AI 當成沒發生」），故仍壓在最尾端，不為快取讓路。

三項改動與量測（探針 `scratchpad/size/prefix.js`，逐字元算可快取前綴）：
| | 可快取前綴 |
|---|---|
| 改前 | 47~59%（天氣/時間卡在第 5 行，把後面全部作廢） |
| ① user prompt 重排（世界觀/規則/視角 → 玩家資料 → 角色卡 → 尺度/篇幅/地點 → 情境 → 天氣時間 → 收尾/名單 → 玩家動作） | 48% ↔ 92% 交替 |
| ② `master_note` 節流改在落地端做，schema 一律保留該欄 | 62% |
| ③ 【玩家資料】的「(可透過 master_note.經歷 滾動增補)」改成固定文字 | **每回合穩定 92%** |

②的取捨：原本非側寫回合把 `master_note` 從 schema 刪掉，省 77 字輸入，卻讓 system prompt 每 3 回合
變一次形狀、1,655 字前綴整個作廢。改成 schema 固定、只在落地端用 `_doSideWrite` 決定要不要寫回表
——代價是那兩回合 AI 多吐約 40 字（丟棄不用）。

⚠ **2026-09 玩家「我給太多、它注意力跑掉」第八輪——先量再砍**：這輪不憑感覺挑段落，先把真正送進 `callGeminiAPI` 的 system／user／history **整串攔下來秤**（探針 harness 直接驅動真實 `handleGameAction`，不是估算）。量出來的事實推翻了原本的假設：

```
鑑賞 play（3 人在場、不含歷史）：4,143 字
  試算表來的資料（3 張角色卡 895 ＋ 玩家資料 98）＝ 993 字，只佔 24%
  其餘 3,150 字全是我們自己寫的規則與 JSON 範本
```

**所以問題從來不是「寫太多進試算表」**——角色卡一張 286 字已經很精實。真正在稀釋注意力的是**指令數量**：一次呼叫 24 個 ★🚨🛑 記號＋10 條編號鐵律，而且同一條規則被重複講：「不替玩家做決定」6 次、「篇幅字數」5 次、「用真名別用暱稱」5 次、「在場只有這些人」5 次。

本輪動作與結果（`4,143 → 3,539`，**−15%**；指令記號 24 → 18）：
1. **JSON 範本瘦身**（1,154 → 約 700）：每欄只留 schema 級約束（字數/真名/留空沿用），演法一律退回鐵律講一次。
2. **重複指令合併**：路人許可＋缺席者交代合成一條；焦點禮讓＋演出而非說明＋沒寫的就不存在合成一條；★【稱呼】＋★【動筆前最後確認】合成結尾的★【在場名單】。繁體要求在 system 首行與 `callGeminiAPI` 的【語言鐵律】已各講一次，user 端第三次刪除。
3. **親密尺度改資料驅動**：門檻併進既有 `KANSHOU_REL_TIER_` 的 `ceiling` 欄（同一組 20/40/60/80 原本存兩處），`kanshouIntimacyLines_` 只吐在場者實際落在的那幾階——三人全在 60~79 就只送一行，其餘四行是雜訊；沒人在場整塊不送。
4. **在場來由去重**：三張卡逐字重複同一句「你們從剛才就一直在這裡」，相同就抽成抬頭講一次。
5. **玩家自己那張卡改走 `formatPref`/`formatTrait`**：原本是裸資料直接倒，提示詞裡真的印著 `性格:沉穩寡言·認真、、、` 和 `特徵:…、、我、無`。
6. **`QUAD_EMPTY_` 補齊佔位字**（見 `FUNCTION_MANUAL.md`）：一張敵從者卡曾同時夾帶「珍視之物」「厭惡之事」「Archer 職階英靈」三格純噪音；從者卡的「自稱」也講了兩次。

**教訓（下次想精簡時先讀這條）**：憑感覺找肥肉會找錯地方。**先把真的送出去的字攔下來秤、再數同一條規則被講了幾次**——體重計跟重複計數器合起來才指得出該砍哪裡。探針做法見 `scratchpad/size/`（`sheet.js` 假試算表＋`probe.js` 攔 `callGeminiAPI`），容器重開會消失，重建成本約十分鐘。

⚠ **2026-07「色色部分搬去給點火」實驗（進行中·結果待玩家實測回報）**：慾海律令原 0(色度跟隨)＋4(情慾場生理特寫)兩條**整段搬進 `driveStr`**（新編號⑤⑥，只有 `driveOn=true` 才組進提示詞），慾海律令本體剩 5 條(重新編號1~5)。**這兩條原本是「怎麼寫得好」的常駐風格指導、不是「准不准寫」的開關**——准不准寫全程由【親密尺度五階】的好感天花板決定、跟 driveOn 無關，天花板不變。搬走後的實際影響：矜持模式(driveOn=false)不再拿到這兩條的具體露骨寫作指引，即使好感已達戀人階(80+，天花板本身仍允許無上限)，措辭可能反而變保守含糊；主動掌握模式因為同時吃到 driveStr 的「推進到真的發生」指令＋這兩條的露骨寫作指引，兩者疊加可能更猛。**若實測發現矜持模式下的高好感場景意外變乾癟/含糊，這就是根因，把這兩條原樣搬回 specificRules 即可還原。**
- **finalJson 欄位**（2026-09 現況）：`inner_monologue`(純思考不顯示)／`narration`(字數照【篇幅】區塊·**第二人稱「你」＝玩家**)／`npc_exit`(自然告辭離場真名陣列)／`options`(固定4·可由 optionsOn 整欄刪)／`intimacy_feedback`{player:{physical_state,appearance_extras}, npcs[]:{name,physical_state,appearance_extras,mutual_nicknames,memory,**noticed**(≤14字·她眼中的你)}}／`world_note`[]{kind,…}(世界帳本)／`rel_changes`[]{target真名,fav_change整數·日常+1~2、明顯進展+3~5、冒犯給負；GAS 端硬夾 ±5}。~~attitude~~／~~master_note~~／~~單回合上限+5 文字~~ 都已拿掉。
  **⚠ 2026-07 四度改版·`dynamic_skills`(雙修技巧)整個拔掉**（玩家「雙修技巧還有在用？UI拿掉玩家也看不到了」）：稽核發現這欄早就沒有任何玩家UI顯示，也沒有任何規則告訴AI該怎麼運用讀回的技巧清單(純粹讀進去擺著)，形同每回合白吃AI注意力換不到實質效果——連帶`kanshouSkillTagStr_`/`processSkills`/`setSkillTag_`三個輔助函式與`【身體記憶】`/`【快照】[技巧]`兩段USER prompt注入全數刪除，`[雙修技巧]`MEMORY標記不再讀寫(舊存檔殘留值不影響任何邏輯，純孤兒資料)。順手修正一個連帶發現的off-by-one：`mutual_nicknames`/`attitude`schema文字原本寫「見律令5」「見律令6」，實際慾海律令只有5條，拔掉dynamic_skills後律令4=mutual_nicknames、律令5=attitude，schema引用已同步改對。
  **⚠ 2026-07 三度改版·`promise_proposal`／`cohabit_proposal`／`proposal_accept` 三欄全部拔掉**（玩家「proposal_accept可以拿掉…promise_proposal也可以拿掉，讓GAS好感超過90…詢問玩家她是否可以與玩家同居…想要當好感卡39之類的時候GAS主動發出邀約」）：AI 不再有任何欄位能自己決定「要不要開口約/邀同居」，這兩件事改由 GAS 依好感數值直接判定觸發（見上方「約會地點好感分級」與「她也能主動邀約/邀同居」段落），`proposal_accept` 本就早已停用、一併真正刪除不再保留相容佔位。
  **⚠ 2026-07 拔掉 `move_proposal`＋`location` 欄（玩家實測「AI一直提議移動、頭痛」）**：舊版讓 AI 自己決定要不要提議換地方、換去哪，結果反覆出現「同地點原地邀約」「跟歷史地點串戲」等 bug，且體感一直被打斷。現在 AI **完全沒有任何欄位能提議或宣告換地點**，換地方只剩兩條 GAS 決定的路：①玩家自己用地圖走(`moveTarget`)；②玩家在地圖對在場同伴提議「一起去」（`proposeMove` 機制標記，見下），GAS 依好感直接裁定接不接受，AI 只演她答應/婉拒的反應。因為①②都不讀 AI 輸出，也就不需要「攔截 AI 硬吐 location」這層保險了，直接整段刪除。
  **🐛→✅ 2026-07「options 建議越界」修法**（玩家「4個選項…有時候會出現移動、或是呼喚/尋找人物，但這些都做不到、點了AI直接當機」）：`options`(命運的抉擇)schema/rule 原本只約束格式(4條·每條≤20字·分類標籤)，完全沒有內容約束——AI 會生出「去咖啡廳走走」「找Saber聊聊」這類選項，但選項一旦被點擊會**原樣當成玩家輸入送回**(`Script_Kanshou.html` `send()` 的 `currentOptions[optIndex]`)，而移動/呼喚不在場者本來就不是 AI 演得出來的事(受【移動鐵律】/【在場驗證】約束)，AI 被迫在明知不可行的前提下硬掰，容易生出前後矛盾/離題的敘事，玩家觀感上讀成「當機」。**修法**：`specificRules`(慾海律令，可改)補第6條——「options 只能是玩家對『此刻在場人物』在『當下這個場景』真能做到的動作，禁止建議移動地點、呼喚/尋找不在場角色」，從產生選項的源頭擋掉，不必等到玩家點下去才在敘事端硬圓。
  **🐛→✅ 同日再抓一種「options 越界」**（玩家實測：場景明明是凜在便利商店對著一款冷飲的成分皺眉，選項卻冒出「3. 注意到她對家電的困擾，提點兩句」——查 `Seed_Codex.gs` 凜的 `dailyMoe`「操作家電永遠一竅不通，出糗次數多到數不清」，證實 AI 是把她的**萌點設定表**字面掏出來塞進選項，跟本回合場景(冷飲)完全無關，玩家角色也不可能知道這件事）：跟上面「建議做不到的動作」是不同失敗模式——那次是動作辦不到，這次是**選項另外調用了角色設定表**，等於繞過 narration 直接把不該說出口的萌點字面端上桌，同時違反 show-don't-tell(紅線②)與「只能演本回合」。同一條第6條規則追加後半句：「只能基於『這回合 narration 實際寫出的內容』出題……禁止另外調用她的萌點/個性/背景設定表字面塞進選項……那是玩家當下不可能知道、也跟眼前這回合無關的設定資料」。
  **🚶👋 玩家提議同去（唯一存活的換地點提議路徑）**：地圖 👋 鈕帶 `proposeMove` 機制標記 → pre-AI 記 `_pendingProposal{type:'move'}`＋依好感用 `kanshouProposalAccepts_` 直接裁定成不成，注入★【提議·同去·GAS已裁定】鐵律讓 AI 只演反應 → 接受＝post-AI 回填 `moveProposal` 出既有「前往」泡泡＋`proposalResult` 通知條，玩家按同意才真的移動（moveWithCompanion 帶同地眾人）。多人在場＝一起邀（以第一位個性判定）。**裁定基準 2026-07-28 由「最生疏那位」改成「在場平均」**——提議對象是【同地全部人】，取最低等於把否決權交給剛好路過的陌生人：跟好感 80 的她在咖啡廳，來一個好感 5 的路人，成功率直接從 97% 掉回 58%（玩家：「提議移動的成功機率是多少 太低了吧」）。平均值仍保有「人越多越難成行」的意思，但一個生面孔不再一票否決。
  **移動規則二條版**（2026-07 三度改版拔掉 `promise_proposal` 後，★【地點清單】整條刪除——那條規則存在的唯一理由就是替 AI 的 `promise_proposal.loc` 圈合法地名，AI 現在連這個欄位都沒有了，自然也不再需要看完整地點清單）：★★【移動鐵律——換場景完全不是你能決定的事】(合併版·含地圖移動例外與換幕一致)／★【地點釘死·此刻只有這裡】(想去別地方頂多嘴上聊聊、不會真的發生，註明自然告辭豁免)。
- **對話格式** `dialogueFormatRule_()`：**鑑賞專用**，唯一呼叫點＝ Gallery 版 `nsfwBaseRules` 第3條（⚠ 非 Engine_Combat.gs 紅線那支·紅線不呼叫它·改它安全）。規則三條：①口/喉發出的聲音(話語＋喘息/輕吟/吸吮)一律進單層「」，禁（輕哼）括號描述　②每句角色台詞在「」前冠說話者名字（硬格式·2026-07 玩家「櫻不知道是自己說過的話」→ 台詞浮在敘述裡沒名牌·輕量模型跨回合認錯人），例 `櫻「…」`；玩家台詞免冠名，且**語氣照原句寫、禁改寫成「於是我問了一句」這類轉述句**（2026-07 玩家實測：打「Saber啊你們不知道？」，AI 寫成「於是我又追問了一句」——台詞被轉述掉、沒有真的說出口。文意上併進 `nsfwBaseRules` 第1條更順，但那是常數本體，改在這裡效果相同）　③動作/撞擊/水聲走敘事、單層「」禁巢狀。
  ⚠ **2026-09 更正：solo 的 `miniSystem` 並【不】呼叫這支**——全樹只有一個呼叫點，文件四處長期寫成「兩軌共用／全遊戲單一真實來源」都是錯的。而且**不該改成共用**：這支的具體例子是 NSFW 的（喘息／吸吮／舔啜／啪啪／交合處水聲），灌進鎖 SFW 的 solo 軌是污染。solo 第2條是刻意的精簡 SFW 版（冠名＋單層「」＋動作走敘事）；少掉的「玩家台詞照原句寫」在 solo 也用不到——**solo 是純按鍵，玩家根本不打字**。兩條規則刻意分家，不是漏同步。

### 🤝 相處基調（好感 × 相處次數的 2D 表）— 2026-07 新增，取代「態度」與 pTierToneStr

**為什麼要第二條軸**（玩家：「好感又太絲滑…想保留很熟但不親密的感覺」）：好感一個數字被迫兼職
「她多喜歡你」與「你們多熟」，於是 **好感59×相處20次**（新鮮期的試探與心動）跟 **好感59×相處300次**
（自在到不必說完整句子、卻也就停在這裡）演出來一模一樣。兩者正交，缺一軸。

- **`【相處】N`**（`KANSHOU_MET_COUNT_TAG_`，存她自己列 MEMORY）：每個「真的在對話」的回合對每位在場者 +1。
  跟【初見日】同一個迴圈蓋戳（每回合只跑一次，天然冪等，不必另記日戳）。
  **⚠ `kanshouTimeJumped_` 時不加**——跳時段/結束一天她只是「時間流轉後恰好在這裡」，不是你陪了她；
  也順手擋掉「連按跳時段刷熟悉度」（跟獨處+3 當初被抓到的刷分同一類）。
- **`KANSHOU_FAMILIAR_TIERS_`**：老交情 150+ ／ 混熟 30~149 ／ 初識 <30（一個遊戲日專心陪一人約 20~40 回合）。
- **`KANSHOU_RAPPORT_BOND_TIERS_`**：很喜歡 80+ ／ 在意 50~79 ／ 朋友 20~49 ／ 陌生 0~19。
  **跟 `KANSHOU_REL_TIER_` 的五階是兩回事**：那個是「稱謂」（玩家看的、可自訂），這個是「該用什麼調子演」。
- **`KANSHOU_RAPPORT_TONE_[好感段][熟悉段]`** → 一句**既定事實**，開頭一律是「事實：」。
  **刻意留白**：15 格只填 11 格，查無＝不輸出這個欄位、讓 AI 自由發揮。填滿只會稀釋真正有戲的那幾格。
  最有戲的一格是 **陌生×老交情**：「很熟，但她對你【沒有戀愛的意思】」——這個狀態在改之前**機制上不可能存在**。
  第五排 **`'交往中'`** 不看好感、看 `kanshouIsLover_`（告白成立）——上面四排全是「還沒在一起」的溫度，
  尤其「她在等你先開口」那句，交往後再演就變成她失憶。查表入口 `kanshouRapportTone_(bond, metCount, isLover)`。
- 掛在 `partyDetailsArr` **每人自己那一行**（沿用 `pTierToneStr` 這個變數名）。

⚠ **這張表只寫事實、不寫演技**（玩家兩次糾正，都是本專案反覆踩的形狀）：
1. **不當謎語人**：初版寫「熟到什麼都能聊，唯獨這件事聊不了」→ 玩家：「**???? 啥事情 姊姊你不要當謎語人
   直接告訴那笨AI**」。未指涉的代詞小模型解不開，它就自己編一個。全部改成寫白（「唯獨【我喜歡你】這句說不出口」）。
2. **不替角色決定微動作**：二版寫「愣一下／打呵欠／慌一下然後裝沒事」→ 玩家：「**這樣有模板 誰來演都一樣
   那我多個角色不就沒有意義了?**」「**不要抹殺人格!! 只要告訴她絕對得既定事實!**」。那些表情是【她的個性】
   該決定的，是 AI 唯一該自由發揮的東西。GAS 只回答「此刻什麼是真的、什麼不會發生」（數值決定、跟誰演無關）。
   交棒句寫在 `PROMPT_REL` 的【角色一致性】★ 區塊**一次**（「標『事實：』那句是既定事實不是演法，
   怎麼表現一律依她自己的個性，禁止兩個人用同一套反應」），不在每一格裡重複。
- 🤖 兩種形狀都已寫成機器檢查：`check_wiring.py` 的 ⑤（`FACT_TABLES`／`VAGUE`／`ACTING` 三張清單）。
  **`check_prompt.py` 只掃 ★ 行、看不到資料表**——這正是它們當初漏網的原因。

**同批改掉的四件事：**
1. **🗑 態度（REL_MEM 的 `[態度]`）整個拿掉**。它宣稱「當下的臨場態度」，實作卻是**永久狀態**：AI 依差分模式
   留白→GAS 沿用舊值→提示詞又把舊值餵回去讓 AI 照著演→沒有轉變可報→再留白。**自我強化迴圈**。
   實測：第 1 回合寫進「警戒又帶點好奇」，29 回合後、好感 60→95，一個字都沒變，卡片同時寫著「戀人·好感95」
   和「態度：警戒」。而且它是**形容詞標籤**（違反 CLAUDE.md「給事實不給形容詞標籤」）。
   防忽冷忽熱的原職責由**歷史視窗**接手（AI 看得到自己前 3 輪的完整敘事，資訊量遠大於 15 字標籤且會自然推移）。
   ⚠ **schema 的 `attitude` 欄位刻意保留**——紅線 `nsfwBaseRules` 第 5 條引用了它、不可改；只是不再落地、
   也不再餵回，迴圈就斷了。慾海律令第 5 條（非紅線）同步改成「系統不保存此欄」。
2. **起始好感 10 → 0**。舊的 10 沒有任何理由（純寫死），而它只離「脫離陌生」的 20 差 10 分＝聊天 5~10 回合就走完。
3. **聊天封頂補上 90 那道牆**。量到的洞：牆只有 40/60/80，**過了 80 封頂直接是 100**——最濃的戀人階（80~100，
   21 分寬）反而是唯一能純靠閒聊走完的親密階段，而同居門檻 90 就卡在中間、毫無保護。改成直接把
   `KANSHOU_COHABIT_BOND_` 也納入牆的清單（讀常數、不寫死數字）。
4. **🐛 後日談模式的 `every` 全域覆寫**。舊版用 `partyRows.every(bond<20)` 決定「初次相遇」還是「已經熟了」，
   於是**在場只要有一位好感夠高的舊識，全體都吃到「別退回才剛認識的生澀」**——連今天才召喚的新人也一起變熱情。
   這是「群體動作只點名一人」的鏡像版：**一人破格、全體破格**；而且 19→20 是語意完全相反的斷崖。
   改成只留全體共通的事實（無戰前舊識），「各自多熟」交給逐人的相處基調——**逐人算天生沒有 every 的問題**，
   相鄰格的措辭也由我們自己寫、可以漸進而非斷崖。
   實測同一場景：好感70的凜拿到「目光會不自覺跟著你走」、好感5的櫻拿到「基本上是個陌生人」。

### 📝 她眼中的你（知情度 ＋ 她偷偷做的筆記）— 2026-09 新增

**玩家原話**：「跟外面 ai 不同，這裡的 ai 明確知道所有設定，所以**第一次遇到玩家就把玩家看透了**，
經歷也是亂掰；現在有開店功能，但我創角色就幫我說有開店，我其實還沒開店」／
「應該要**用關係去掌握角色對玩家角色的熟悉度**，外面的聊天 ai 也會這樣子**偷偷做筆記**紀錄玩家的特點吧？」

**根源**：玩家卡（原【玩家資料】）一直被**每個在場角色無條件全知**——包括 `性格:[內裡]`、`萌點(僅供內化)`、
`經歷`。那份資料其實有**兩種用途被混在同一行**：①寫玩家自己的內心與感受（★【你也是這座城裡的一個人】要用）
→ **該全知**；②在場角色對玩家的認識 → **不該**。
`grep "還不認識\|不會一眼看穿\|知情" gas/Gallery.gs` 改之前是 **0 筆**。

**⚠ 第一版寫壞了，玩家當場擋下來**：我留著洩漏、另外寫一個 225 字的★★區塊（含初識/混熟/老交情
三段定義）叫 AI「你讀得到但請假裝不知道」。玩家：「**我是要妳幫我讓 ai 更自然，不是一直追加設定！
妳必須要去看增加這個是否對 ai 有用處，如果沒有就不要增加雜音**」「**核心越簡單越好，所有的設定都只留
必要的提示**」。**初識／混熟／老交情 是模型天生就懂的詞，不需要我寫三段定義去教它**——留機制、砍說明。

**現況＝兩層，各由不同的東西保證：**

| 層 | 誰算 | 提示詞成本 |
|---|---|---|
| **她對你多熟** | **GAS**：`KANSHOU_MET_COUNT_TAG_` 查 `KANSHOU_FAMILIAR_TIERS_` | 卡上 `你在她眼中:初識` 約 8 字 |
| **她記下你哪些特點** | AI 吐 `noticed` → `kanshouAppendUnique_` | schema 一行 ＋ 卡上最多 3 條 |

擋住「一眼看穿」的是**第一層**，GAS 自己算得出來，**AI 吐不吐都擋得住**。

- **界線只有一句，掛在玩家卡自己那一行**（不另開★區塊）：
  `【玩家資料·旁白用】(只給旁白寫「你」的內心用·在場的人沒讀過這張卡)`。
- **`kanshouKnownTier_` / `kanshouKnownOfYou_`**：共用 `KANSHOU_FAMILIAR_TIERS_` 這條軸，**不另立門檻**。
  卡上印 `你在○眼中:<熟悉段>`，有筆記才接 `·<筆記>`；**沒筆記就只印熟悉段，不多說一句廢話**。
- **`【眼中的你】`**（`KANSHOU_NOTED_TAG_`，存**她自己那列**的 MEMORY）：分隔符 **`／`**——
  **不可用 `｜`/`【】`**，`makeTextTag_` 會把結構字元從值裡剝掉。cap 3、單條 14 字。
  **存在她列上（不是玩家列）是關鍵**：每個人各記各的，同一個玩家在不同人眼中才會不一樣。
- **schema `noticed`**：「≤14字·只記【會改變之後怎麼對玩家】的發現·**喜好習慣瑣事與卡上已有的一律不記**·
  多數回合填「無」」。⚠ 第一版寫成「真的看出來的一件事」，結果記的全是「咖啡只喝黑的」這種
  **卡上性格四格早就有的東西**（玩家：「不是記那些沒用或是重複的東西，像是玩家的喜好之類的」）。
- **AI 生成的 `background` 不准先幫玩家寫好「現在擁有什麼」**（`KANSHOU_MASTER_GEN_SYS`）：
  只寫【來到冬木**以前**】的來歷；開店／店面／在哪上班／與誰同住／交往對象／養了什麼／已經有哪些朋友
  **全禁**——那些由系統逐項記錄，創角就寫好＝玩家一開局讀到一段沒發生過的人生。

**探針** `scratchpad/size/known.js`（30 條斷言）＋**退化測試**：把 `noticed` 寫入改 `if (false && …)`
→ **7 條 ❌**，還原 → 0。
⚠ **測資必須彼此夠不像**：bigram 去重是真的會擋（這正是它的工作），第一版用「第N件同樣的話」
被自己的防線吃掉、害我誤判上限壞了——**探針說「沒作用」時先確認是不是探針自己的問題**（本專案第五次）。

**目前刻意不做**：面板不顯示這些筆記（玩家要的是「**偷偷**做筆記」）。

### 🧹 2026-09 提示詞去雜音（玩家「核心越簡單越好」）

上一節那 225 字被玩家擋下來，順勢把整份鑑賞提示詞重量一次：**指示段落 32 → 27 段、
全域最高子句重疊 0.26 → 0.19、總字數 3738 → 3681**（知情度＋筆記兩個新功能含在裡面，**淨減 57 字**）。
每一刀都先回答同一個問題：**這句改變 AI 寫出來的東西嗎？**

| 刀 | 砍掉什麼 | 為什麼它是雜音 |
|---|---|---|
| ① | ★★【對方對你的認識】整塊 225 字 | 見上節：教模型它本來就懂的詞 |
| ② | 鐵律 8 後半「關係標籤由玩家定，不可改動」 | **死規則**——`rel_changes.tag` 2026-07 就從 schema 移除了，AI 根本沒有管道寫它，這句是在對著不存在的欄位下禁令 |
| ③ | 鐵律 5 整條（85 字） | 與 schema 的 `inner_monologue` 欄位說明講同一件事（**全域最高重疊 0.26 就是這一對**）。併進欄位說明裡（那裡更具體，且就在欄位旁邊），鐵律編號 5~11 → 5~10 |
| ④ | 鐵律 4 尾巴「肢體親密照【親密尺度五階】」 | ★【親密尺度·**最高優先**】自己就是最高優先，不需要鐵律再指一次路 |
| ⑤ | ★【視角鎖定】「旁白一律用第二人稱」 | SYSTEM 第一行已經寫著「第二人稱「你」＝玩家·禁上帝視角」 |
| ⑥ | world_note 的兩段（207→177） | 兩段互相解釋同一件事，合成一段 |
| ⑦ | ★【你也是這座城裡的一個人】的「動作與台詞仍只有玩家能決定(鐵律1)」 | 對鐵律 1 的**回頭指路**，而鐵律 1 就在同一份提示詞的最上面 |
| ⑧ | ★【篇幅】的「加什麼」清單 | 「動作細節、觸感/氣味/聲音、感受與身體反應」與上一條的感受清單逐項重複 |

⚠ **砍過頭一次、被探針抓回來**：⑦ 同時把「卻感覺得到臉發燙、喉嚨發緊」也刪了，`mecard.js` 立刻叫。
那 12 字是**這條規則唯一能抓的具體錨**（玩家當初要的就是「用這個角色的角度去感受」），已還原。
**規則可以砍，讓規則可執行的那個例子不能砍。**

### 💗 告白＝關係階的質變事件（2026-07 新增·`confess`）

**問題**（玩家：「感覺好感又太絲滑… 沒有一個交往的確定過程… 人人都可以自然變成戀人 感覺很怪」）：
舊做法是好感爬到 80 就自動長出「戀人」標籤，全程沒有任何一刻是「你們決定在一起」。

**做法**：**戀人不是爬出來的、是問出來的。**
- **一道牆，不是一堆門檻**：`kanshouSyncRelTier_` 最前面把未告白者的好感夾在 `KANSHOU_REL_TIER_[0].min - 1`＝**79**。
  那支是所有好感變動的**唯一漏斗**（聊天/赴約/獨處/橋段/AI `rel_changes` 改完都補呼叫一次），夾一次＝全路徑都夾到。
  於是 80 以上的每件事（戀人標籤／自訂稱呼 80／同居 90／親密尺度最高階）**自動一起關在告白之後**，不必逐項再開門檻。
- **成敗 GAS 擲、AI 只演**：~~`kanshouConfessAccepts_`~~（2026-09 已移除，告白改必定成功）＝`(好感-60) × KANSHOU_CONFESS_SLOPE_(0.028)
  × KANSHOU_CONFESS_FAMILIAR_MULT_[熟悉段]`（初識 .35／混熟 1／老交情 1.2），夾在 2%~95%。
  好感 79 的老交情約 .64；同樣好感但才見過幾次面只有 .19。**刻意不保證必成**——必成的告白等於舊版自動變戀人。
- **三種結果**（全走 ★ 事實、AI 不得改寫）：`已經在一起了`／`告白·被拒`(好感未達 `KANSHOU_CONFESS_BOND_`=60·不動數值)／
  `告白·成立`。撲空另有 `KANSHOU_MISS_COPY_.confess`。
- **沒有代價、沒有冷卻**（2026-09 起）：`KANSHOU_CONFESS_DAY_TAG_`／`KANSHOU_CONFESS_COOLDOWN_`／`KANSHOU_CONFESS_SLOPE_` 都已不存在；
  好感夠就必成，不夠就是被拒，前端在門檻下直接鎖住入口，所以「連按到過」不會發生。
- **成立**：蓋 `KANSHOU_LOVER_TAG_`＝1（牆的唯一鑰匙）→ 好感推到 80 → 跑一次同步漏斗（標籤自動變「戀人」）
  → `kanshouStampFirst_('告白')` 記進「第一次」帳。
- **入口**：同伴卡「💞 關係」中樞面板第三列。三態各自明講（未達 60 鎖／可告白／交往中），
  不留「按了才被拒」的洞。門檻靠 `KC_CONFESS_BOND_` 鏡射，**逐人狀態** `lover` 由 `buildTagsPayload_` 下傳（`confessWait` 已隨冷卻移除）。
- 🩹 **舊存檔補齊**（`kanshouSyncRelTier_` 開頭）：牆上線前就跨過 80 的人是照當時規則掙到的，不能被降回 79
  順便掃掉同居。三個證據任一成立就補蓋戀人：**已同居／REL_TAG 已是「戀人」／`【好感底線】`棘輪 ≥80**。
  ⚠ **刻意不用「此刻 bond≥80」當證據**——AI 這回合 +5 把 78 推到 83 時長得跟舊存檔一模一樣，會自己把牆拆了。
  （模擬器 `confess.js` ⑧ 就是釘這一條。）

🧭 **留下的判準**：好感/門檻/標籤/基調是**四件事**，別讓一個數字兼職。門檻（機制）想多細就多細、AI 不必知道；
標籤（稱謂）維持粗糙；**精細度放在基調表**——它是唯一 AI 真的要照著演的東西，而且一次只會看到屬於它那一格的一句。

### 🧠 記憶全景（AI 每回合看得到什麼·寫回什麼·多久一次）— 2026-07 整理·2026-09 稽核修正過期處
**AI 每回合看得到（組進 prompt）**：
- **近期對話**：`getGameHistoryBatchRaw(pcId, 6)` 滑動窗（6筆＝3輪，更早的靠下面的持久欄接力）。
- **玩家**：性格(PREF)／特徵(TRAIT)／裝扮／**經歷(BACK·固定事實·AI 只讀不寫·≤80字)**／位置（地點活動表已隨預寫池砍除）／肉體(PHYSICAL)。⚠ **玩家萌點(INTENT)絕不餵**（紅線②）——2026-07 二度改版後 AI 連「盲寫」都不准了，性格/萌點創角時 `actionBackfillKanshouAi` 寫一次定案，遊戲中只有玩家自己改命能動，AI 完全不碰。**2026-07 四度改版拔掉雙修技巧(身體記憶)**：沒UI也沒使用規則的孤兒欄位，見上方finalJson欄位說明。
- **每位在場 NPC**（`partyDetailsArr` 一行一人）：身世(BACK)／裝扮／性格／特徵／日常風味／**萌點(有餵·標「僅供內化」，與玩家不同)**／當前活動／**同居狀態**(`kanshouIsCohabit_`判定·2026-07 稽核補：已同居者額外標註「她現在與你同住一處」，讓AI語氣能自然帶同居的日常親近感、不是每次都當作客處理)／共同回憶(MEMOIR)／與玩家的約定／關係 tag＋好感＋相處記憶＋階調＋「你在她眼中」(知情度＋noticed)；NSFW 區另帶 肉體＋專屬稱呼(REL_MEM)。（聊天天花板、REL_MEM 態度欄、當前活動欄都已移除）

**AI 寫回（GAS 落地）**：
- **每回合**：`physical_state`/`appearance_extras`(原outfit_change)→PHYSICAL·【換裝】；`mutual_nicknames`→REL_MEM；`memory` 里程碑→MEMOIR(cap10·★釘選不驅逐)；`noticed`→MEMORY【眼中的你】；`world_note`→世界帳本；`rel_changes`→BOND；`npc_exit`→LOC。（`attitude`、proposals 泡泡都已不存在）
- ~~每 3 回合 `master_note` 經歷滾動~~：2026-09 整組移除（經歷改回固定事實，見 §經歷改回固定）。

**🩺 AI 負擔瘦身（2026-07 玩家診斷「滾動式+衣服外觀神情太要他老命」·小模型注意力有限，能省則省）**：
- **狀態差分**：`physical_state`/`appearance_extras`(原outfit_change)/`attitude` 沒實質變化留空＝系統沿用舊值（GAS 空值本就跳過寫入；attitude 配套修掉「空值洗白態度」舊 bug——空→從 oldRMem 撈回舊態度；**「無/同上/不變/沿用/維持原樣/如前」等敷衍值也視同空**，否則差分模式下 AI 真的會把「同上」二字寫進態度欄）。有變化（脫/穿/沐浴/情事/神情轉變）必須更新，NSFW 場景照記。
  - **🐛→✅ 2026-07 玩家實測「用👕換裝手動設定的裝扮，下一回合就默默變回別的」**：schema `_note` 裡「沒實質變化就留空」只是 JSON 欄位描述文字，對這個模型的約束力不夠強——AI 會自己判斷「這身打扮好像跟這幕場景/氣氛不搭」就主動「合理化」改寫成別的描述，即使敘事裡根本沒有任何穿脫/更衣動作發生，等於自行推翻玩家手動鎖定的裝扮。已在**慾海律令**(`specificRules`，非紅線)新增第7條，把「appearance_extras只在劇情真有穿脫/更衣/入浴動作時才填新值、不准因『不搭』自行合理化改寫」提升成跟第4/5條(mutual_nicknames/attitude)同等級的明文規則(schema _note是弱信號、慾海律令明文規則對這個模型才是強信號，同一套教訓)。
  - **🐛→✅ 同批再抓一個變種**（玩家回報「在溫泉泡湯→移動到商店街，服裝狀態卻顯示『在水下』」）：AI 把**所在環境/姿勢描述**(「在水下」)誤寫進這個**只該記衣物本身**的欄位，離開泡湯地點後這句話語意不通、也永久卡住(因為上面第7條規則已讓AI不再主動改寫，這格就會一直卡著錯的舊值不放)。schema描述與慾海律令第7條都補上明文：**這格只能寫「身上穿/沒穿什麼」，禁止寫成所在環境或姿勢**(如「在水下」「泡在浴池」)——那是敘事場景，該讓narration自然演出，不該塞進持久裝扮欄。**已卡住的存檔需要玩家自己用👕換裝手動蓋掉**(輸入正確裝扮或留空清除)，這次修正只防未來不再發生，不會回溯修好已寫壞的舊值。
- **options 連動開關**：前端帶 `optionsOn`（玩家關【命運的抉擇】＝false）→ `buildDefaultSystemPrompt` 唯一參數 `includeOptions=false` 把 options 欄整個 delete——沒人看的東西不叫 AI 生。連 Gallery 版 `nsfwBaseRules` 第2條的「options固定4個」字樣也隨開關拿掉（schema 刪了、規則文字還催繳＝AI 精神分裂）。
- **🎬 換幕縮窗**：移動/跳時段/跳節慶/推進時間/結束一天的回合，歷史窗 6筆→2筆（1輪）——舊場景對話物理上不進 AI 眼睛，根治「換地點/時段被舊場景帶著跑」（「此地是唯一真實」「此刻時段是唯一真實」兩條鐵律是文字輔助線，縮窗才是確定性主力）。
- 原則：**AI 只管演戲，記帳全給 GAS**——別再往每回合 schema 加欄位，要加先想「能不能差分/節流/事件驅動」。

**⚠「今日情景」查證結論（2026-07·勿重複造輪）**：曾考慮加「今日情景」滾動摘要接住 3 輪窗外的當日細節——查證後**不做**：**經歷(BACK) 的滾動摘要實質已涵蓋今日進展**（實測會寫入「正在逛街、計畫一同前往咖啡廳」等當日動態），另設欄位＝跟經歷重複。若長場景實測出現「忘記前段」，優先調經歷的提示詞（讓它多保留今日細節）而非加新欄。舊 `log_summary` 是因果表的主/被動方向記錄、非情景摘要，已隨因果表一起砍除。

### 模型配置（`Core_Settings.gs`）— 🚀2026-09 玩家定案「只要這兩顆」
```
AI_MODEL       = google/gemini-3.5-flash-lite  (屬性 MODEL)           ← 兩軌共用主力
FALLBACK_MODEL = x-ai/grok-4.20                (屬性 FALLBACK_MODEL)  ← 被擋才自動換的後援
```
- **後援是全域行為、不是呼叫端特例**：`callGeminiAPI` 裡 `config.fallbackModel || FALLBACK_MODEL`，任何路徑被擋死都有救。`actionPlay` 的 `aiConfig` 只寫 `model: AI_MODEL`＋`retries = 1`（Gemini 試一次、真被擋才交棒，不浪費柔化重試）。
- ⚠ `SOLO_MODEL`／`UNLOCKED_MODEL` 已整組刪除，同名指令碼屬性從此無效；換模型只改 `MODEL`／`FALLBACK_MODEL` 兩個屬性（或 `Core_Settings.gs` 的預設字面）。
- **`driveOn`(點火/主動掌握) 只控敘事推進幅度的 `driveStr`、不再切模型**。⚠ **2026-07 玩家「一直步步逼近都不做」重寫**：`driveStr`＋敘事終極警告的點火分支原本框架是「把玩家逼向毫無招架餘地／堵退路／想跑也跑不掉」＋「不必每回合寫到終點」——這等於授權 AI 永遠停在「快要、就差一步」空轉。改成**「主動且明確地推進到真的發生」**取向（該親就親、該進一步就進一步、嚴禁在曖昧邊緣反覆空轉），保留全部好感天花板/角色一致性/不真正傷害護欄，只把「逼近但不做」的空轉框架換掉。點火≠壓迫鋪陳，點火＝她主導、實際往前推到位。
- 採樣：`temperature 1.08, top_p 0.97, top_k 60, repetition_penalty 1.12, presence/frequency_penalty 0.25, max_tokens 2400（時間轉場且無人在場 700）`。⚠ 後四顆旋鈕 gemini-lite 被 OpenRouter 靜默忽略（原壓重複用）——若 Gemini 跳針/套路化，需另想防重複提示詞手段。
- 歷史：`getGameHistoryBatchRaw(pcId, 6)`（6筆＝3輪）。

### `callGeminiAPI` 失敗行為（`Engine_Combat.gs`·兩軌共用·只讀不改）
被 NSFW 擋（`PROHIBITED_CONTENT/SAFETY`/空 content）→ 同模型內首次觸發加 `softenSuffix` 降階柔化重試 → 整組失敗且有 `fallbackModel` 才換模型再跑一輪 → 最終被擋回「🌸結界觸發」、一般錯誤回「🌫️因果紊亂」。

---

## 🖥️ 前端地圖（`gas/Script_Kanshou.html` 為主）

- **唯一引擎入口 `send(customMsg, isSilent, opts)`＝`action:'play'`**（2026-07 重構：原 22+ 位置參數收進單一 opts 物件，payload 不變零速度影響）。移動/相約/拍照/牽手/同居/敲門/橋段**沒有各自的 action**，全靠 opts 夾旗標：`moveTarget`/`moveWithCompanion`/`promiseMeet`/`takePhoto`+`photoIntent`/`showPhoto`/`handHold`/`cohabitInvite`/`cohabitInviteId`/`handHoldId`/`skipKnockCheck`/`dismissGuest`/`lookAround`/`inviteResident`/`endDay`/`advanceHours`/`jumpBand`/`jumpFestival`/`loaderCaptions`。
- **回饋條 `proposalResult`** 涵蓋 相約/牽手/同去/同居 四型＋**撲空含「她似乎在○○」位置提示**；**`promiseSettle`（獨立通道）** 涵蓋 赴約成功/爽約過期 結算通知（與提議結果並發時各自顯示·見教訓區「單一回饋槽」）；相簿滿的 `photoResult` 附直達鈕（📚開相簿）——「撲空/婉拒/卡住」一律要有下一步，別讓玩家對著空氣猜。
- **改命同伴卡**（2026-07 第二輪稽核修）：`update_fate` 名字比對原硬性要求 `IS_PARTY==='同行'`，但鑑賞列從不寫該欄→同伴卡改命鈕恆「查無此人」；現比照 `update_rel_tag` 給 `k_` 世界豁免（同世界名字直配），改同伴的 個性/特徵/身世 是合法自訂。**萌點例外(2026-07 再修)**：同伴/NPC的萌點改成「真正內化」——`intent-box`(Index.html)在非自己卡片整格連改命鈕都隱藏，`actionUpdateFate` 也擋掉 `fateType==='intent'` 且目標非自己的請求，玩家從此看不到也改不了同伴萌點，只留給AI演出參考。（2026-07 二度改版：玩家自己卡的性格鎖快取 `_kcPrefLocks` 已隨性格鎖系統整組刪除）
- **獨立 action**：`kanshou_companions`／`get_heroes`／`kanshou_summon_hero`／`get_album`／`album_delete`／`update_rel_tag`／`kanshou_set_nickname`（2026-07 五度改版新增·專屬稱呼手動鎖定，bond≥80）／`kanshou_memoir_op`／`kanshou_set_home_name`／`kanshou_set_name`／`kanshou_set_sex`／`enter_kanshou`／`backfill_kanshou_ai`。
- **函式分組**：地圖移動(`kcMapListHtml_`/`kanshouMoveTo`/`kanshouProposeMove`/`kanshouLookAround`)、同伴面板(`openCompanions`/`renderKcHeroList_`/`kanshouOpenBondHub`→`kanshouOpenRelTag`)、召喚(`kanshouSummonHero`)、回憶(`kanshouOpenMemoir`/`kanshouMemoirOp`)、約定(`kanshouPromiseMeet`/`kanshouWaitForPromise`)、拍照相簿(`kanshouTakePhoto`/`openKanshouAlbum`)、時鐘(`kanshouEndDay`/`kanshouNextStage`/`kanshouJumpBand`/`kanshouJumpFestival`)。
- **泡泡 UI**（`send()` 內依回傳欄位組）：移動同意(`moveProposal`)、深夜訪客善後(`nightGuest`)、同居邀請(`cohabitOffer`)、巧遇結識(`encounterOffer`)、等待約定(`promiseWait`)、拍照結果(`photoResult`)、地圖人數徽章(`_lastTags.locationCounts`)。
  - ⚠ **每加一顆泡泡都要先回答「玩家不點它、直接打字會怎樣？」**：泡泡是待決狀態的唯一出口→忽略就會壞（必須改成先落盤再給善後選項）；泡泡只是已落盤狀態的捷徑→忽略無害。現況全部屬於後者。
  - `cohabitOffer` 的 `KANSHOU_COHABIT_ASKED_TAG_` 存 **absDay 不是布林**：布林版玩家一旦改用打字，這個「一生一次」的邀請就永遠消失；完全不記又會退回被嫌煩的「每回合都跳」。同一位、同一天最多問一次，接受後靠 `!kanshouIsCohabit_` 自動停。
- **前端鏡像常數**（後端為真實來源）：`KC_REGIONS_`/`KC_FESTIVALS_`/`KC_TIME_BANDS_`/`KC_LOCATIONS_`/`KC_APPT_BANDS_`。時鐘全域 `kcClock`（`Script.html`）。
- **`Script.html`/`Index.html` 的鑑賞殘留**：`applyModeUI()` 總開關（依 isKanshou 切 topbar/輸入框/drive開關/photo-btn/快速輸入貼圖列/相簿抽屜/節慶抽屜）；同伴卡鑑賞按鈕列(📜詳細狀態／💞關係中樞／📅相約／🤝牽手↔✋放手，見 §同伴卡片按鈕)；`enterKanshou()` 入口。⚠ `Index.html` 的 `#victory-memoir` div 是**戰爭軌殘留**：奪杯回憶錄機制已砍，該 div 現只被清空/隱藏、不再填充（非鑑賞，別誤接鑑賞邏輯）。
- **🎀 快速輸入貼圖**（2026-07 新增，玩家「打符號會不會被砍掉？可以加類似罐頭訊息的貼圖嗎，點下去幫玩家輸入好(不送出)就是塞進對話框」；同月再追加「內建8句想改玩家自訂」＋「內建句數想減少」）：查證後**玩家輸入從前端到後端全程沒有任何地方會過濾/剝除文字**(`send()` 直接讀 `input.value`；後端 `const userMsg = userData.message || ""` 原樣轉送)，玩家誤以為被砍掉的是「AI輸出narration不寫括號」——那是 `dialogueFormatRule_` 故意禁止 AI 自己用（輕哼）（嬌喘）這類括號描述聲音，跟玩家打字輸入無關，兩者是不同機制。現況：`Index.html` 輸入列上方只留空容器 `#kc-quick-phrases`(鑑賞限定，`applyModeUI()` 切 `display:flex`/`none`)，內容改由 `Script_Kanshou.html` 的 `renderKcQuickPhrases_()` 動態渲染——`KC_QUICK_PHRASES_BUILTIN_` 4顆固定內建短句(害羞/小聲/苦笑/臉紅，2026-07再縮減，純前端寫死不可刪)＋玩家自訂 `_kcQuickPhrases`(來自後端`kanshouGetQuickPhrases_`，見上方 MEMORY 表格)＋一顆「⚙️自訂」管理鈕。`kcInsertPhrase(text)` 把文字插入 `#u-in` 游標處並聚焦，**不呼叫 send()**、純粹幫忙打字，玩家仍要自己按傳送。要改內建句子改 `KC_QUICK_PHRASES_BUILTIN_` 陣列即可；玩家自訂走 `kanshou_add_quick_phrase`/`kanshou_delete_quick_phrase` action（上限見 `KANSHOU_QUICK_PHRASE_CAP_`），不涉及任何提示詞邏輯。

---

## ⚠️ 教訓區（防回歸警語·只留警告不留故事）

- **COL 是位置索引**：`MEMOIR`(27)/`MONEY`(33)/`UPKEEP_WEEK`(34)/`ROOM`(35) 就算恆空也**不刪欄**——刪掉後面全部欄位錯位、砸爛既有試算表每列對齊。要棄用就標「死欄」，不刪。
- **`grep -c nsfwBaseRules` 假陽性**：顯示非0常是 `return nsfwBaseRules + ...` 那行被帶進 diff context，`+`/`-` 兩側都沒該字樣＝本體未動。仍須自己判斷是否碰到紅線①保護的機制範疇。
- **`physical_state` slice(0,20)** 是刻意的容錯緩衝，**別對齊文件改回15**（會半句截斷「臉頰帶著因尷尬而生的」）。
- **敘事終極警告**：被玩家搭話的 NPC **必須先給出她此刻的回應**才停筆，別只寫完玩家動作就收尾（「她不理我」的根因）。
- **好感突破改「約會路徑」**：送禮突破已廢；橋段/約定提示詞尾會 append「好感已由系統上調·敘事勿再另計」防 AI 重複計。
- **查洩漏查兩份鏡像**：同伴側 vs 玩家自己側、AI生成內容 vs AI失敗的降級保底值，兩層都要查。
- **平行世界原則**：`persona.back/speech/tic` 的**戰時值別照搬進鑑賞**（後日談無聖杯戰爭）。
- **onclick 字串內的呼叫不算死碼**；`removeAllTriggers()` 零呼叫是 GAS 工具正常型態、非死碼。
- **HTML 刪除先手算 div 開合平衡**再刪，避免刪頭忘刪尾崩整頁。
- **泡泡(必點UI)別跟可關閉的東西同住一個容器**：「AI選項開關」曾整個 `options-container` display:none，所有泡泡(前往/邀約/同居/敲門/橋段/結識)陪葬——關掉選項的玩家**從沒見過任何泡泡**。現制：【命運的抉擇】包在 `#ai-options-grid` 小盒、開關只藏它；容器本身恆 flex(載入時強制恢復)。新泡泡一律放容器直下、別放進 grid。
- **「先擋、再刪字」的順序會讓防線失效**：擋公式引導字元(`^[=+\-@]`)的那道必須是**最後一道** replace——它前面每一道清洗都還會再刪字元，刪掉開頭那個字之後，原本被擋在第二位的 `=` 就重新變成開頭。實測 `photo_caption` 打 `"=SUM(1+1)` 落地就是一格活的公式。`sanitizeUserData_` 與相簿 caption 兩處都犯過，已一起修。
- **同一格欄位有兩條寫入路徑時，消毒要抽成一支**：REL_MEM 的 `[專屬稱呼]` 有「玩家手動」與「AI `mutual_nicknames`」兩條路，舊版只有手動那條消毒——AI 回 `"小可愛| [稱呼鎖]是"` 就能偽造出稱呼鎖。現在兩條都只走 `sanitizeNickname_`。
- **前端渲染的順序＝資料就位的順序**：`updateClock` 讀 `kcClock`(判深夜) 與 `window._lastTags.nightScene`(判夜未眠) 決定那顆最常按的鈕寫哪個字；`send()` 與 `applyClientState` 兩處都曾把它排在這兩份資料**之前**，按鈕永遠慢一回合——按下🌙睡覺、夜未眠成立了鈕上卻沒變，玩家再按一次就真的睡掉。**任何讀 tags 的渲染，tags 必須先就位。**
- **永遠不會成功的按鈕就不要長出來**：私人房間(`isRoom`)在後端是 region `'room'`、相約/邀同去一律不受理，前端卻照樣給 👋。比照地圖上鎖住的地點：不給、或給灰底🔒＋說明，別讓玩家按了才被拒。
- **前端手抄的後端表會走鐘**：`KC_REL_TIERS_`/`KC_LOCATIONS_`/`KC_FESTIVALS_`/三個門檻…全是手動同步。表一走鐘 UI 就會說謊（例：關係面板讓好感30點『戀人』）。scratchpad 有 `mirror.js` 逐項比對，改任一邊記得跑。
- **群體動作要點名全部人**：👋 邀同去按下「同意」時 `moveWithCompanion` 帶走**同地全部人**，舊版提示詞只點名第一位——其餘的人是無聲跟來的。點名誰就走誰（裁定取好感最低的那位）。
- **別依賴小模型「自發」填選填欄位**：Gemini-lite 從不自發填自由選填欄位(舊 move_proposal 就是活教訓，2026-07 乾脆整欄砍掉)——玩家發起的機制動作一律走「明確 payload → pre-AI 記提議 → 確定性管線」(👋proposeMove/相約/牽手同款)，別再指望 AI 自己決定「要不要」。
- **🙂 `physical_state`（狀態欄）2026-07-28 改標點收尾**：schema 要 ≤15 字、後端截 20 當緩衝，但 AI 把整句敘事寫進來時照樣硬剪出斷尾（玩家實測「狀態：原本專注看書的動作停下，抬頭望向風音，眼」——正好 20 字）。改成**在預算內的最後一個標點處收尾**（沒有標點才退回硬剪），並把欄位定義從「顏面神情」改寫成「只寫臉上看得到的(眼神/臉色/表情)，不寫動作與劇情」。探針 `scratchpad/sim/pstate.js`。
- **🫶 玩家提議（相約/牽手/同去）她答不答應＝GAS 依好感擲、AI 只演**（2026-07 由 AI 判定改 GAS 判定）：pre-AI 用 ~~`kanshouProposalAccepts_`~~（2026-09 已移除，三個提議一律成立） 依好感擲定 `_pendingProposal.accepted`（曲線：move base .55/slope .006、promise .30/.007、hold .10/.010，夾[.03,.97]——牽手最看好感、同去最隨和；2026-07-28 move base .45→.55，實測值見 `scratchpad/sim/odds.js`：同去好感 0/40/80 → 55%/79%/97%），★提議鐵律直接告訴 AI「她【答應/婉拒】了，只演她的反應、不可改寫決定」；post-AI 落地讀 `_pendingProposal.accepted`。**2026-07 三度改版**：`proposal_accept` schema 欄已整個刪除(不再保留相容佔位)。她主動提議約會(`kanshouPromiseOffer_`)＋同居(`kanshouCohabitOffer_`≥90)也已改成同一批 GAS 直接判定觸發，不再等 AI 自己開口。**紅線①核對過：nsfwBaseRules／慾海律令／driveStr／五階演化 0 改動。**
- **通用錯誤文案是查案毒藥**：send()/saveFate 的 catch 已帶出 e.message(【原因】行)；後端 success:false 的 message 會演進故事流。別再寫吞掉真因的 alert。
- **獨立事件別共用單一回饋槽**：赴約/爽約「結算」與相約/牽手/同去「提議結果」是兩條獨立事件流，舊版共用 `proposalResult` 單槽＝並發時後寫的吞掉先寫的(爽約通知無聲消失2.0)。現制：結算走 `promiseSettle`、提議走 `proposalResult`，前端各自出通知條。新回饋事件進來時先問「跟既有槽是同一條事件流嗎」，不是就開新欄位。
- **後端早退回應別夾空集合**：敲門早退曾夾 `people: []` 把前端 `localNPCs` 快取洗空(泡泡期間拍照面板變無人)。早退＝沒人移動＝不帶 people；前端也只在 `Array.isArray(data.people)` 才更新快取(雙保險)。
- **「下方/上方」方位詞會過期**：prompt 注入段引用其他規則時**用【規則名】不用方位**——注入點會搬家，方位詞跟著說謊(告辭/巧遇豁免句曾指「下方」而鐵律實在上方)。
- **小模型會沿用剛出現的視覺詞、用到失去邏輯**（2026-07 玩家實測抓到）：某回合合理寫了「手機螢幕的光亮映照在她臉龐」(拍照後·螢幕當光源照在對方臉上，合理)，接下來幾回合卻沿用「螢幕」這個詞寫成「螢幕上顯現出我的驚呼/不解的表情」——變成用「螢幕」描寫**玩家自己**的表情，但第一人稱『我』根本看不到自己的臉，這個畫面邏輯上不成立。已在★【不替玩家腦補】結尾補一句：「『我』看不到自己的臉，禁止寫成『螢幕/鏡子顯現出我的表情』這類從外部看見自己臉的描述——我的反應只能透過動作/感受/台詞呈現」。這類「沿用剛用過的顯眼詞彙、用到脫離語境」的失誤模式值得留意，不只螢幕這一個詞可能發生。
  - **⚠ 同類 bug 第二例（2026-07 玩家實測「紫水晶眼眸是我的ㄟ，AI又拿去用」）**：玩家御主卡「氣質」特徵是「紫水晶眼眸」，跟美狄亞聊天時 AI 卻把「紫水晶般的眼眸」直接套在美狄亞身上——查證美狄亞自己的種子資料只有「紫色長髮」，從沒設定過紫水晶眼睛。跟螢幕那個 bug 同一種病根：【玩家命格】跟【在場人物】兩段的「特徵」在提示詞裡位置相近，小模型把剛讀到的玩家外貌詞彙錯誤沿用到同伴身上。同一句★【不替玩家腦補】結尾再加一句處理：「同伴的外貌只能取材自她自己的資料，禁止把玩家自己命格的外貌/氣質特徵挪用套在同伴身上」。
- **假容量上限別憑空捏造**：2026-07 稽核抓到 `Script.html` 同伴卡片列表曾用寫死的「3」補畫「（空位）」佔位框，暗示「此地最多3位同伴」——但 §91 駐留制改版後同地點召喚無數量上限(`buildTagsPayload_` 只按 LOC 篩選)，這個「3」是介面上憑空捏造、跟後端規則脫鉤的假限制，已整段移除。加任何「剩餘X個」「上限N」這類顯示前，先確認後端真的有對應的數量約束，沒有就別顯示。
- **審查攔截保底文字別讓它悄悄變成歷史**：`callGeminiAPI`(Engine_Combat.gs) 全部重試/審查攔截皆失敗時，回傳的是一組跟真正生成成功長相一模一樣的「保底文字」JSON——2026-07 抓到 `actionPlay`(Gallery.gs)/`narrateWithState_`(Router_Narrative.gs)舊版都會把這句「什麼都沒發生」的保底措辭原封不動存進 `saveGameHistoryBatch`，下次呼叫又把它當成上一輪的既定事實餵回AI，可能接續出跟實際劇情矛盾的敘事。已加 `_genFailed` 旗標讓兩處呼叫端辨識、失敗時完全跳過寫歷史。**任何新的敘事呼叫端要接 `callGeminiAPI` 並自己存歷史，記得先檢查這個旗標。**
- **提示詞講的規則，代碼要真的照做**（2026-07 全面稽核抓到）：`rel_changes`/`intimacy_feedback.npcs[]` 寫入好感/外顯前，提示詞明講「只有【目前在場人物】才准變動」，但兩處寫入邏輯從沒真的檢查 `pcData[idx][COL.PC.LOC]` 是否等於 `curL`——AI 若因對話歷史殘留或幻覺提到不在場的人名，好感值一樣被悄悄寫入。同批也發現 `rel_changes` 的「單回合漲跌上限±5」只寫在提示詞裡，代碼只有 `sanitizeAiData_` 的 `[-100,100]` 粗夾，從沒真的把±5夾進去。兩處都已補上對應的程式碼檢查/夾值。**提示詞裡承諾的每一條護欄，都要回頭確認代碼是不是真的照做了，不能只靠告訴AI「請遵守」。**
- **回饋通道只開一槽，同回合兩筆就吞一筆**（2026-07 稽核抓到）：`kanshouPromiseSettle_` 沿用了 `proposalResult` 那次改版學到的「獨立通道」教訓，卻自己還是單槽——玩家若同時有兩位同伴的約定在同一回合結算(如A赴約成功+B同時爽約)，`forEach` 跑兩輪，後跑的無條件覆寫前一筆，前一筆的通知條就消失(底層BOND/MEMORY寫入正常，只有UI通知被吞)。改成陣列，前端逐筆渲染。**同一件事「可能同時發生不只一次」時，回饋通道要用陣列，不要嫌麻煩用單一物件卡死自己。**
- **「防偽造」helper 存在，不代表每個呼叫點都真的用了它**（2026-07 全面稽核·本輪最嚴重發現）：`kanshouOwnedRowIdx_` 明明就是為了防 `pcId` 被猜中/偽造而寫的，但 `actionPlay`／`actionGetAlbum`／`actionAlbumDelete` 三個高頻/高權限 handler 一路用裸 `pcData.findIndex` 繞過它，等於整套防禦形同虛設——而且是系統負擔最重、寫入面最廣的那個函式漏掉。**新增任何會讀寫 pcData 或私有資料的 handler，一律要主動去比對現有的歸屬驗證 helper 是否真的被呼叫，不能假設「這套機制存在＝全部路徑都受保護」。**
- **`play` 故意豁免全域鎖，但「豁免鎖」不等於「不用防併發」**（2026-07 全面稽核·兩組獨立agent各自收斂到同一根因）：`actionPlay`(現為`actionPlay_`)的寫回機制是「整表快照→本回合全部改動只在記憶體→結尾整列覆寫」，若同一 pcId 的兩次呼叫執行窗口重疊(同帳號兩分頁/兩裝置同時操作、或聊天等AI回應時另開改命視窗存檔)，後flush的請求會用自己那份舊快照整列覆寫掉先flush者的所有改動。已用 `CacheService` 做「同一pcId」的軟性互斥(外層薄包裝 `actionPlay`，見上方執行階段順序第0步)：偵測到同pcId仍有一次在跑就直接拒絕待玩家稍候，不佔全域鎖、不影響其他玩家。**⚠ 已解除的相關限制（2026-07 拍照改手機後自然消失）**：舊版拍照的「寫相簿(立即/不可逆的`appendRow`)」與「扣底片(記憶體→回合尾端才flush)」曾是分離提交，中途有未接住的例外時可能出現免費照片+其他當回合狀態改動一併消失的風險；拔掉底片機制後這條路徑只剩單一的`appendRow`寫入，這個併發疑慮已經不存在，不需要再另外根治。
- **字串前綴比對記得連 `indexOf` 的意義都要核對，不要只挑「有沒有寫」**（2026-07 稽核抓到）：`Router_Action.gs` 的 `_state` 隨動作回應夾帶機制原本只判斷 `String(pcId||"").indexOf("PC_")===0`——但 `"KPC_xxx".indexOf("PC_")` 結果是 `1` 不是 `0`，鑑賞被整個排除在外，即使 `update_fate`/`update_rel_tag` 兩個handler明明就是特地做給鑑賞共用、也確實有交棒`STATE_PRE_DATA_`，改命/改稱呼存檔後鑑賞玩家還是得白跑一趟整表`sync`。已補上 `isKanshouCtx` 條件放行(這兩個action是`isKanshouCtx`為真時唯一能走到這個判斷點的，其餘solo專屬action都在更早被`KANSHOU_BLOCKED_ACTIONS_`擋掉，改動安全)。
- **給小模型的篇幅指示，沒數字的那端會被無視**（2026-07 玩家實測「好感高了字數還是100~200」）：`★【篇幅隨關係濃淡】`原本只有低好感端給了具體數字(200~300字)，高好感端只寫「越深越濃才放長寫細」這種沒有錨點的模糊話——`AI_MODEL`(gemini-3.5-flash-lite)這類小模型對沒有具體數字的指示執行力很弱，結果好感再高篇幅也沒跟著拉長。已補上熟識(350~450字)/親近以上(450~600字)的具體區間。**任何要求「隨程度增減」的提示詞，每一端都要給具體數字，不能一端有數字一端純形容詞。**
  **⚠ 2026-07 再修（玩家「字數一下很長一下很短」）**：即使兩端都給了數字區間，還是留了兩個變因給 AI 自己判斷：①要把好感數字bucket進哪個區間本身對小模型就不穩、②多人在場、各自好感不同時不知道該以誰為準——兩者疊加就是忽長忽短的根因。改成 GAS 直接算好一個**具體目標字數**（`_kanshouTargetWords_`，取在場好感最高者、沒人在場用最低檔：<40→250字／40~59→400字／60+→500字）直接指定，規則名也從`★【篇幅隨關係濃淡】`改成`★【篇幅指定】`（`driveStr`/schema 的`narration`欄描述同步改引用新名字）。AI 不必再自己做「好感→區間」的判斷，只需要照給定的數字寫，同名多人不同好感也有唯一答案。
- **持久欄位的「欄名」跟「範例」都會窄化 AI 的理解，兩處都要顧**（2026-07 玩家實測「幫她戴貓耳朵，過幾輪就忘記」）：`appearance_extras`(原名`outfit_change`)本身是每回合都重新餵回的持久欄位(存`【換裝】`)，理論上不該被對話歷史的6則滑動視窗限制住——但舊欄名字面就是「換裝」、範例又只給「絲綢襯衫」這種正經換裝，AI 容易把玩家臨時加的配飾/道具(貓耳朵之類)當成那句台詞的趣味描述、覺得不算「持久狀態」而不寫進來；沒寫進持久欄，這個設定就只活在對話歷史裡，滑出視窗後就真的看不到了。已改名`appearance_extras`(外觀附加物，不再暗示只認衣服)＋範例加「貓耳頭飾」，兩處一起下手。**排查「AI 忘記某個設定」時，先確認有沒有對應的持久欄位在接住這類事實——通常有，缺的只是欄名/範例沒涵蓋到這種情境、AI 沒把它跟該欄位對上號，不是機制本身漏接。**
  - **🔄 2026-07 曾把配飾讓給小道具機制**（玩家「小道具已經有專門機制了，外觀服裝也幫我專注在外觀服裝吧」）：當時範例拿掉「貓耳頭飾」、只專注服裝本身，避免兩套機制搶著記同一件事。**⚠ 2026-09 小道具整套移除後已回滾**：欄位改回「穿著與配飾」、範例補回「貓耳髮箍」——那個承接配飾的專門機制沒了，這一欄要是還把配飾排除在外，配飾就會變成沒有任何機制記得住的東西（正是這條教訓當初要根治的問題）。
- **前端 maxlength/長度檢查不能取代 backend 上限**（2026-07 玩家「全面防呆、限制輸入的字號前後端都要」全面複查抓到）：一次性稽核揪出好幾個「只有前端擋、backend 從沒設上限」的欄位——`actionEnterKanshou`首次建檔的 pcName(只靠前端`maxlength=16`)/appearance・persona(只靠前端`maxlength=60`)、`actionBackfillKanshouAi`餵進 AI 提示詞的同三欄。也抓到反向缺口：`changeKanshouName()`前端連檢查都沒做(對照組`kanshouRenameHome()`有做)，白白多一趟round-trip才被backend擋下。**兩條都要顧，且順序是backend優先**：backend 的上限才是真正擋得住的那一道(前端能被繞過)，前端檢查只是省一趟round-trip、體驗更好，兩者不能只做一邊。另抓到`setKanshouHomeName_`只裁長度、沒清`｜`等標籤分隔字元(小道具的`name`欄之前也犯過同款錯)——凡是要塞進 MEMORY 單值`【tag】`格式的自由輸入，一律要過`kanshouSanitizeTagValue_`，不能只信賴呼叫端自己記得清。**新增任何吃自由輸入的欄位，checklist：①backend 長度上限(不是只靠前端) ②若寫入的是 MEMORY tag，字元淨化不能省 ③前端也做同款檢查省一趟round-trip，但不能只做前端。**
- **「強度三階」不該是同一根拉桿把所有軸一起拉**（2026-07 催眠三階二度重寫）：第一版三階套同一句「更強烈」→小模型分不出來；第二版改成弧線式具體指令、但把「意識/行為/常識」三條軸綁在一起越拉越滿，最高階變成「判若兩人」——結果**最高階反而是最壞的一階**：人格被關掉（＝紅線②的反面），而且對方沒反應就沒有互動性，NPC 從對手降級成道具。定案版把三階改成**作用點不同**（意識永遠清醒／只有「能不能拒絕」隨階數升／最高階動的是「這算不算異常」的常識判斷，人格不動）。**任何做成「輕/中/重」的機制，先問清楚每一階實際上在動哪一條軸；如果最高階的做法是「把某個既有系統整個關掉」，那多半是設計錯了，不是強度大。**
- **改一階的措辭，要同步掃全部替它開的例外**（同上，2026-07）：催眠三階當初為了讓「判若兩人」不跟其他規則打架，在 `driveStr`①、`PROMPT_REL`【角色一致性】、【親密尺度五階】三處各開了一道 `_kanshouHypnosisActive_` 例外。三階重寫後，【角色一致性】那道「★催眠暗示生效中例外，可判若兩人」直接變成新設計的反面，不改就是兩處自相矛盾。**`grep _kanshouHypnosisActive_`（或任何為某機制開的旗標）是改這類機制的必跑第一步——例外散在別的區塊裡，只改主體會漏。**
- **不變式掃描器自己也會有覆蓋漏洞，而且是靜默的**（2026-07 小道具★行）：`check_prompt.py` 的 ★ 區塊標題上限寫死 `{2,20}`，任何標題較長的區塊直接不進 `blocks`，兩道不變式對它形同不存在——放寬到 40 之後掃描數 80→83，等於先前有 3 個區塊從來沒被檢查過、卻一直顯示「✅ 全部通過」。**加了新的提示詞區塊之後，順手確認掃描數有跟著增加**；數字沒動就代表它沒被看見。
  **⚠ 同一天內就再犯一次**：放寬到 40 之後，合併多件道具的那條 ★ 標題又超過 40、再次靜靜掉出掃描（82→本該 84）。根源是「上限」這件事本身沒道理——`[^】]` 跨不過收尾的 `】`，不會失控。已改成 120 純防呆，並在 `check_wiring.py` 加了 `MIN_STAR_BLOCKS` 下限：覆蓋數只要無聲變少就擋下來。放寬後現形的兩個區塊裡，立刻有一個被抓到代名詞無指涉（深夜訪客·別有用心，已補上訪客名字）——**它從寫出來那天就沒被檢查過**。
- **重複踩過的漏洞形狀，能寫成檢查就別只寫進筆記**（2026-07 玩家「感覺好多小漏洞」）：筆記會過期、人會失憶，檢查不會。目前 `check.sh` 除語法外跑三支：`check_prompt.py`（★ 區塊不變式）、`check_mirror.js`（前後端常數鏡射，**自動發現**所有 `KC_*`，對不上又沒登記就擋）、`check_wiring.py`（門檻有無前端出口／查表覆蓋與各階相異／★ 覆蓋數下限／死路由）。三支的共同設計原則：**不消滅差異，而是逼每個差異都有人簽名**——「忘了接」跟「不用接」從外面看長得一樣，只有白名單＋理由能區分。所以新增常數/門檻/★ 區塊時你會被強迫做一次有意識的選擇。

---

## 📎 主要常數速查（都在 `gas/Gallery.gs`）
`KANSHOU_REL_TIER_`(五階) · `KANSHOU_STYLE_MODULES_`(13 段說書人風格·玩家可改)/`KANSHOU_STYLE_CATS_`(4 個面板分頁) · `KANSHOU_SCENE_BOND_`(3) · `KANSHOU_APPT_BANDS_`(午後14/黃昏18/夜20) · `KANSHOU_PACE_OPTIONS_`(0/10/20/30 分鐘·玩家自選流速) · `KANSHOU_TIME_BANDS_`(5時段) · `KANSHOU_LOCATIONS_`(合法地點白名單·AI location/move 驗證) · `KANSHOU_HERO_HOME_`(手寫專屬豪邸)/`KANSHOU_GENERIC_HOME_POOL_`(泛用住處池·8間·查無專屬豪邸者隨機分配) · ~~`KANSHOU_SCENE_EVENTS_`~~(橋段庫·2026-09 已整組砍除) · `KANSHOU_ALBUM_CAP_`(100)（`KANSHOU_FILM_PER_DAY_`已隨底片制拍照改手機一起刪除，見上方拍照/相簿章節） · `KANSHOU_KNOCK_CHANCE_`(0.2)/`KANSHOU_KNOCK_MIN_BOND_`(60) · `KANSHOU_COHABIT_BOND_`(90)/`KANSHOU_VISIT_BOND_`(40) · `KANSHOU_PARTY_DETAIL_CAP_`(5) · `KANSHOU_STARTER_IDS_`(開局起手池) · `KANSHOU_SUMMON_BLOCKED_IDS_`(暫不開放召喚)。


---

## §「旁白改第二人稱『你』」＋「自稱退休」（2026-09）

**玩家原話**：「我覺得不能第一人稱 ai根本分不清楚 要扮演誰？？？」／「是不是 取消自稱這個設定....感覺太細 很亂」

### 旁白人稱翻面

`nsfwBaseRules` 開頭與 ★【視角鎖定】原本都寫「第一人稱「我」」，但**場上每個角色引號內的台詞自稱也是「我」**
——同一個字在同一份提示詞裡指兩個不同的人。歷來的補法全是加註記去繞（同伴卡 `[台詞自稱]` 加括號限定範圍、
`inner_monologue` 特地要求第三人稱、常駐規則區再重申一次「旁白的我永遠是玩家」），沒有一個是解掉根因。
旁白改「你」後衝突不存在，那些註記一併拆掉。

鑑賞這一軌動到 8 處：`nsfwBaseRules` 開頭、schema 的 `narration`／`memory`、`photo_caption` 規則、
★【視角鎖定】、🚨【收尾】的「收在「我」的動作上」、★【再往前的經過】、以及爽約寫進 MEMOIR 的那句系統文案。

⚠ **紅線① 程序**：改前量了現況（system 1700 字），改後跑了全套鑑賞探針
（`k` / `k_male` / `k_sex2`（女玩家×男同伴、男玩家×女同伴兩組配對都確認沒被改壞）/ `k_verify` / `k_cd` / `k_flow` / `k_hist`）
＋等效重複子句檢查（原 `dedup.js` 已不在 repo，改用臨時腳本：74 條子句、完全重複 0、近似重複全是同場多人的同款狀態句）。
system 1700 → 1724 字。

⚠ **近期摘要餵回去的是玩家自己打的字**（天然「我替她倒了杯茶」），已在該行補一句括號說明，
免得模型把它當旁白範例鏡射。

### 自稱退休（特徵 4 格 → 3 格）

鑑賞這邊的自稱**本來就重複兩次**：`heroToKanshouRow_` 把 `dailyLook` 第3段抽進 MEMORY 的【口吻】，
同一段又原封不動當成特徵第3格 `[台詞自稱]`（`formatTrait`）。`pFlavorStr` 甚至有一段
`_traitSpeech` 判斷「兩者相同就不印口吻」——那個 workaround 現在整段拆掉了。

現況：
- **特徵 3 格** = 外貌本相／氣質舉止／卸下心防的私密一面。⚠ 2026-09 起**第三格不再送進提示詞**（見下方「私密一面停送」）；`TRAIT_SLOTS_` 仍是 3，資料照存。`formatTrait` 只送 `[外貌氣質]`，第3格照舊只在獨處時走 `traitPrivateOf_` 併進萌點。
- **口吻**永遠印（來源：MEMORY【口吻】→`dailySpeechByName_` 退回英靈殿 `DAILY_LOOK` 第3段）。
- **`dailyLook` 仍是 4 段**（外貌／氣質／**日常口吻**／私密面）——第3段是口吻的唯一來源，`parts.length>=4` 三處硬依賴不動。
  種子庫 17 個零資訊量的「自稱我・」前綴刪掉，6 個有特色的（俺／拙者／吾／余／我們／本小姐）保留在口吻裡。
- `translateLookToDaily_`（工房/自訂角色轉日常版）第3段的指示從固定格式「自稱「X」，…」改成
  「日常口氣（自稱不是尋常的「我」才寫進這句）」。
- 舊局相容：`traitParts_`（GAS）／`traitSegs_`（`Script.html`）讀到 4 段就地剝掉 index 2，**不改存進表裡的值**。


---

## §「全面出戲稽核」（2026-09）

玩家：「全體 慢慢檢查一遍 看哪裡怪怪的，不可以出戲！重點的檢查鑑賞模式」

做法：寫 `kscan.js` 把 10 種情境（房間／三人客廳／外出／問起不在場的人／告白／牽手／拍照／深夜／
拜訪別人家／同居邀約）真正送出去的 system+user 全部收下來，再拿一份違和詞表掃全文。
掃出來的東西**沒有一個是靠讀原始碼會發現的**——都得把提示詞真的組出來才看得見。

### A. 世界觀自打嘴巴

第一行寫「★世界觀＝…**沒有魔術與從者**」，往下三行就冒出聖杯戰爭詞彙：

| 洩漏點 | 原本送出去的字 | 現在 |
|---|---|---|
| `kanshouLocContextForAI_` ×3 分支 | 「**御主**自己的家…的私人房間」 | 「你自己的家…」 |
| schema `name` 欄 | 「不填暱稱/**職階**/台詞/地名」 | 「不填暱稱/稱號/台詞/地名」 |
| 鑑賞御主創角提示詞 | 「與身邊**英靈**共度…的「**御主**」本人形象」 | 「與身邊夥伴共度…的主角本人形象」 |
| `kanshouRoomDisplayName_` fallback | `"御主" + '的房間'` | `"自己" + '的房間'` |

### B. 「GAS」四個字母直接寫在提示詞標題裡

`★【提議·相約·GAS已裁定】`／`同去`／`牽手`／`【結果·GAS 已裁定】`。
既是三個拉丁字母（跟「全程繁體中文」的語言鐵律互斥），又是我們後端的名字——抄進敘事就是最徹底的出戲。
同一句後半本來就寫「系統已依好感…裁定」，統一成「系統」。solo 的示好提示詞也有一處，一併改掉。

### C. 「TA」

`關係:TA是你的○○` 每位在場者印一次，巧遇區塊再印四次。同樣是拉丁字母＋非台灣用語。
**資料裡本來就有【性別】欄**——改成 `pron_(sex)` 直接印「她/他」，既不是外來寫法、也更精確。

### D. 地名「我的房間」是第一人稱

資料鍵叫「我的房間」，而旁白剛改成第二人稱——等於一邊說「旁白不可用『我』」、一邊遞給它一個叫
「我的房間」的地方。新增 `kanshouLocNameForAI_()`：**只改餵 AI 的字面**（→「你的房間」），
存表／比對／前端一律仍用原鍵（改鍵會動到存檔、住處骰點、前端分頁）。
四個把 `curL` 寫進提示詞的點都走它。

### E. 時鐘顯示「1年12月20日」

`kanshouAbsDayToDate_` 的 `year = 絕對天數/365 + 1`，從來沒有起算年份——
開局玩家的時鐘與送給 AI 的日期都在講一個不存在的年份。新增 `KANSHOU_CAL_START_YEAR_ = 2005`。
⚠ 週年是用 absDay 差算的，不吃 `.year`，所以改基準年只動顯示字串。

### F. 鑑賞漏了兩道 solo 早就有的濾網

1. **`stripLeakedScaffold_`**：AI 偶爾把 `★【…】` 指令或 `〈演出依據〉` 卡原樣抄進敘事，
   solo 走 `narrateWithState_` 會濾掉，鑑賞這條路徑從來沒濾過——玩家直接看到提示詞。
   補在 `sanitizeAiData_`（單一咽喉）。⚠ 得先把真實換行轉成 `<br>` 再濾：濾網是掃到下一個「<」為止，
   沒有 `<br>` 的話會把整段吃光。
2. 順手修了濾網本身的舊坑：剝掉指令會留下它前後各一組 `<br>`，疊成四連斷行＝玩家看到一塊莫名空白。
   現在收斂回 `<br><br>`，並改成只壓縮水平空白（原本 `\s{2,}` 會把真實換行也吃掉）。兩軌同時受惠。

### G. 選項的鷹架原樣變成按鈕

schema 教 AI 寫 `"1. [主動]…"`，而 `data.options` 是**原樣**長成按鈕文字的——
玩家看到的是「1. [主動]伸手替她理瀏海」，按下去那整串又原樣回灌成【玩家意圖】、再進近期摘要。
分類本身有用（逼出四種不同走向），但那是給 AI 的。`sanitizeAiData_` 落地前剝掉編號與標籤，
半形全形、任一順序都剝（迴圈剝到不再變動）。

### H. 缺一條全域「不報幕」鐵律

提示詞裡滿是 `好感:75`、`【親密尺度五階】`、`系統`、`回合`、`narration`，
而慾海律令十條**沒有一條**說「這些不准寫進敘事」——只有「關係質變」「週年」兩個區塊各自補了一句
「不可報幕」。同一個漏洞形狀補兩次，就該收成一條鐵律。新增律令 10（原 10 順延為 11）。

### I. 兩條規則打架

- 【只演給的資料】：「玩家專一對著一個人時，其他在場者維持**背景輕描**」
- 【在場名單】：「名單上每個人這回合**都要有戲**」

改成「都要真實存在（沒被搭話的人有個動作或反應即可，**不必平分戲份**）」。

### J. 跑時重複（第三次遇到同一形狀）

每張在場卡的 `現況:…賴床…` 後面都掛著同一句 30 字的演法規定，三人同場印三遍。
**事實留在卡上、規則抽成抬頭講一次**——跟當年 `在場來由` 與 `performanceNote_` 同一個修法。

### K. 其餘措辭

`名號:` → `名字:`（九州武俠殘留，現代冬木日常線不合）／
`現在演化玩家動作：` → `接著往下演，玩家這一步是：`（「演化」是機器語感，而且這是整份提示詞的最後一句、recency 最強）／
`inner_monologue` 的「對方」在三人同場時指誰不明 → 點名成「被搭話的那個人」／
`更早的回合裡` → `稍早`（新律令 10 剛把「回合」列為不准寫進敘事的字，不該在前一行就遞給它）。

### ⚠ 紅線① 程序

`nsfwBaseRules` 動了兩處（開頭人稱那次之外，這次加律令 10、律令 8 措辭）。
改前量：system 1725 字。改後：1822 字（增量全在快取前綴裡，等於不花錢）。
全套鑑賞探針（k／k_flow／k_hist／k_male／k_sex2／k_verify／k_cd／kscan 10 情境）＋
等效重複子句檢查（76 條子句、完全重複 0）。
**「原本正常的配對沒被改壞」**：k_sex2 兩組配對（女玩家×男同伴、男玩家×女同伴）輸出逐句比對過。


---

## §「篇幅改成事件驅動」＋「近期摘要砍包裝」（2026-09）

玩家：「1 沒有更好的方式嗎.....ai自己決定字數？」「3 我要簡單 有力 不要太多多餘的！！盡可能砍吧！」

### 篇幅：AI 自己決定？——這條路已經走過了

`KANSHOU_WORDS_` 當年就是因為「約 X 字」不管用才改成【下限~上限】的：
**小模型對「約」一律往下取**（實測過，見上方篇幅段）。完全放手讓 AI 自己拿捏，
只會是同一個結果的極端版——它永遠寫最短的那個量。**所以不是 AI 決定，是換一個更準的裁定依據。**

舊版的毛病不是「有裁定」，是**裁定依據只有好感**：
場上只要有人好感 ≥60，連一句「早安」都得寫到 700~900 字——那不是細膩，是逼 AI 灌水。

新版：**好感給底盤、這一回合真的發生了大事才拉到上限**。

| 好感 | 平常 | 有事發生 |
|---|---|---|
| ≥60 | 480~620 | 700~900 |
| ≥40 | 400~520 | 600~750 |
| 其餘 | 300~400 | 450~580 |

「大事」不靠猜也不必多花一次呼叫——**那些區塊本回合有沒有組出字串，GAS 自己最清楚**：
`kanshouConfessStr`（告白）／`kanshouTierCrossStr`（關係質變）／`kanshouFirstsAnnivStr`（週年）／
`kanshouNightSceneStr`（夜已深）／`kanshouKnockRaidStr`（深夜訪客）／`kanshouCohabitStr`／
`kanshouCohabitEndStr`／`kanshouPromiseMetStr`（赴約）／`driveOn`（主動掌握）。（節慶刻意不在串裡：它是日子不是戲，舊的「就是此刻」regex 是死旗標、已拿掉。）
**加新橋段時往那串旗標加一個即可**，不動查表邏輯。

⚠ 上限 900 沒動 → `max_tokens` 的截斷風險不變。

### 近期摘要：框比內容還大

原本 145 字的框只裝得下 100 字內容，而且**每句開頭的「我」還得另外寫一句話去解釋**
（旁白已改第二人稱，摘要卻是玩家自己打的字、天然自稱「我」）。

改法是**把禁令換成事實**——句首的「我」直接剝掉，變成純動作，那句解釋就沒有存在的理由了：

```
舊 ★【再往前的經過】：更早的回合裡，你依序做過這些事——我替她倒了杯熱茶→…→我承認自己不太會做菜。
   （這幾句是玩家當時自己打的字、所以自稱「我」；旁白一律仍寫「你」。）這些都已經發生過了，
   需要時自然呼應、別當沒發生過，也不要重演一次。

新 ★【稍早做過的事】：替她倒了杯熱茶→問她昨晚睡得好不好→…→承認自己不太會做菜
   ——都已發生過，需要時自然呼應，別重演。
```

包裝 145 → 28 字，內容一字未減。順手剝掉句尾標點（原本會出現「她在哪？。」這種雙標點）。

### 沒做的一項

鑑賞介面上還有三十幾處「御主／從者／召喚英靈」，跟「這裡從來沒發生過聖杯戰爭」的世界觀不合。
**玩家定案：「應該沒差」——不動。** 送進 AI 的提示詞已經清乾淨，UI 維持原樣。


---

# 🌍 §「世界帳本」——鑑賞從選單變成沙盒（2026-09）

玩家：「我鑑賞覺得不夠自由不夠好玩／我的想法是我應該是自由的！但ai有試算表可以記錄我的歷程！
應該要這樣才對...不是像現在全都有設定過」

## 問題出在一句話

```
★【只演給的資料】：系統給的資料就是這個世界的全部，沒寫到的人/物/過往都不存在
```

量出來的事實：鑑賞的「世界」是 **22 張寫死的表**（28 個地點、固定節慶表、固定巧遇池、固定住處表，
連「她此刻心裡想要什麼」都是 6 選 1）。而 **AI 能寫回試算表的東西，全部是「已經在表上那些人」的屬性**
（好感／外顯／暱稱／共同回憶／玩家經歷）——它不能新增一個地方、一個人、一件設定。
所以它只能在玩家畫好的盒子裡排列組合。玩家感覺到的「全都有設定過」，就是這件事。

## 反轉

> 試算表從【AI 讀的選單】變成【AI 寫的帳本】。

那句禁令當初有它的理由（AI 憑空捏造角色和地點、玩家當成設定、它下回合就忘了）。
但正確的修法不是禁止發明，**是讓它發明完要負責記下來**。
發明會出問題，只是因為沒落盤；落了盤，發明就不是雜訊，是在蓋世界。

## 資料層：分頁「鑑賞世界」

`遊戲ID | 類別 | 名稱 | 內容 | 性別 | 建立日 | 最後提及日 | 提及次數 | 釘選`

類別＝`地點`／`人物`／`設定`。**刻意用一張表加一個類別欄，不開三張表**——
一條寫入路徑、一套淘汰政策、一份快取；以後要加類別是加一個值，不是加一張表。
（性別只有「人物」用得到，但升格成正式同伴時它是必要的，所以存在表上而不是事後猜。）

| helper | 做什麼 |
|---|---|
| `kanshouWorldRead_(gid)` | 讀這一局的帳本，走 CacheService（每回合都要讀） |
| `kanshouWorldWrite_(gid, entries, day)` | 唯一寫入點：清洗 → 去重 → 更新/新增 → 淘汰 → 整批寫回 → **快取換成新內容**（不作廢，否則同一趟執行裡後面那支 read 又要整表讀一次） |
| `kanshouWorldEvictees_(d, gid, added, day)` | 純函式，只回答「該砍哪幾列」：超過各類上限就砍「最久沒被提到、提及次數也最少」的；★釘選永不驅逐 |
| `kanshouWorldRow_(a, rowNum)` | 一列 → 一個條目；讀與回填快取共用（欄位長相的單一真實來源） |
| `kanshouWorldFeed_(rows, loc, names, msg, day)` | **不是全餵**：算相關性分數排序取前 6 |
| `kanshouWorldSame_(a, b)` | bigram 近義比對，**只用在近期迴聲**（見下方⚠） |

**相關性分數**：釘選 +100／此刻地點提到它 +40／在場者名字出現在內容裡 +30／
玩家這句話提到它 +50／最近 3 天剛提過 +20／提及次數（上限 +5）。

⚠ **`kanshouWorldSame_` 只比對最近兩天寫的，不掃全表。** 第一版掃全表，實測把
「她喜歡在便利商店買關東煮」和「…買茶葉蛋」merge 成同一條——句型相近但語意不同的事實太常見，
拿去掃全表會把世界愈合併愈空。門檻也從 0.6 提到 0.7。
**mechanical 去重抓不到真正的語意複述**（實測「她討厭下雨」的兩種說法 bigram 只重疊 0.36），
那一層靠提示詞（「已經在名單上的不必重寫」）＋淘汰處理，不硬調門檻換誤殺。

## AI 的新出口

```json
"world_note": [{ "kind": "地點|人物|設定", "name": "...", "text": "≤40字·之後要當真的事實",
                 "sex": "僅 kind=人物 時填 男/女/異" }]
```

一回合最多 2 筆。`sanitizeAiData_` 擋結構（合法類別、筆數），
逐欄的字元清洗與長度在 `kanshouWorldWrite_` 做（唯一寫入點）。

籠子那句改成：
> ★【可以發明，但發明完要記下來】：這是一座活的城，資料沒寫到的店家、路人、往事、習慣，
> 你可以當場創造——但凡是【之後要當真】的東西，都必須同時寫進 world_note，否則下回合它就不存在了。

## 四條放開的線

### 🗺️ 地點：世界會長大
- `kanshouLocationsFor_(gid)` ＝ **內建地圖 ∪ 這一局自己走出來的地方**；`kanshouFindLoc_` 是唯一查法。
  ⚠ 原本 16 處直接 `.find(KANSHOU_LOCATIONS_)` 已全數改走它——漏一處，自己走出來的地方就會查無、被當成非法目的地。
- 前端「🧭 去別的地方…」自由輸入 → `newPlace` → 後端查無就**當場把它加進這一局的世界**、走過去，
  並給 AI 一句「你第一次來，由你決定它長什麼樣，並寫進 world_note」。
- 地圖多一區 `mine`＝「走出來的地方」。**靜態鏡射長不出這些**，所以後端 `buildTagsPayload_` 下傳 `myPlaces`
  逐項清單（同 `unlockedResidences` 的做法：後端算好結果，不下傳門檻）。

### 🧑 人：不限那 25 人
- 帳本的「人物」＝這座城的常民（名字＋樣貌＋性別），**可以出現、可以開口，只是不追蹤好感**。
- 【在場名單】鐵律改寫清楚：它管的是**正式同伴**（好感/關係只跟這些人算），不管常民。
- **升格**：`inviteResident` 查無巧遇英靈時改查帳本 → `kanshouFolkToRow_` 建列。
  刻意不叫 AI 補一整份設定（那又變回「全都有設定過」），比照玩家自己的御主走「留白＋滾動成長」。
  ⚠ ID 沿用 `KHV_` 前綴：三處白名單都認它，另開新前綴要同步三個地方。
- AI 這回合造了新的人 → 回傳 `folkOffer`，前端跳「要不要往來下去」泡泡（跟巧遇那顆共用樣式）。

### 🎭 事件：砍掉預寫橋段池
整批移除 **7 張表**（22 → 16 張）：

| 砍掉的 | 它原本在做什麼 |
|---|---|
| `KANSHOU_SCENE_EVENTS_` | 情境橋段的 ambient 句庫 |
| `KANSHOU_LOCATION_EVENTS_` | 地點×時段 → 寫死的情境 |
| `KANSHOU_COHABIT_EVENTS_` | 同居日常 時段→事件 |
| `KANSHOU_EVENT_SEEDS_` | 三類各七句、20% 機率抽一句當「氛圍靈感」 |
| `KANSHOU_INIT_WANTS_` | 「她此刻想要什麼」6 選 1 |
| `KANSHOU_LOCATION_ACTIVITY_` | 14 個地點各兩句「她正在做什麼」 |
| `KANSHOU_FESTIVAL_EVENTS_` | 每個節慶「老規矩是做什麼」＋達成追蹤 |

依既有判準（①事實留 ②寫法砍）：**節慶的「日子」留著**（`KANSHOU_FESTIVALS_` 是世界事實，
「跳到節慶」按鈕也吃它），砍掉的是「這一天該做什麼、做了沒」——那是把節日變成待辦清單。

連帶清掉的孤兒：`kanshouRollEvent_`、`kanshouLocActivity_`、`KANSHOU_FESTIVAL_DONE_TAG_`、
`kanshouPlayerHomeLocs_`、前端 `KC_LOCATION_EVENTS_` 的五條橋段徽章
（那張表兼做「🌙她熟睡中」提示，那部分是 `pSleepStr` 的顯示層、留著，改名 `KC_SLEEP_HINTS_` 並登記為純前端）。

## 🌍 面板：☰ →「這個世界」

玩家：「世界帳本我能看到嗎？有地方看嗎？還是後台紀錄」——問得好，**原本只有後台**：
地點那一類會出現在地圖的「走出來的地方」，人物與設定在遊戲裡完全看不到。
而且帳本有 `釘選` 欄、淘汰時會保護它，**卻沒有任何入口能設定**——做了一半。

`actionKanshouWorld`（action `kanshou_world`，op = `list`／`pin`／`unpin`／`del`）
把「看得到」與「管得動」一起補上，分工比照 `actionKanshouMemoirOp`：

- 三類分組列出（地點／人物／設定），各自標 `現有數 / 上限`
- 📌 **釘住 → 永遠不被淘汰**（這是 `KANSHOU_WORLD_CAP_` 那套政策唯一的人為出口）
- 🗑 **刪掉 AI 亂記的**——刪了 AI 就真的不記得了
- 排序**跟提示詞不同**：面板是「釘選優先、其次最後被提到由新到舊」（給人看的）；
  提示詞是相關性分數（給 AI 的）。兩者刻意不共用。

空的時候不顯示空表格，而是講清楚它會怎麼長出來——新開的世界一定是空的，
直接給一張空表會讓玩家以為壞掉。

## ⚠ 已知代價

**帳本會無限長大**——這是這整套機制唯一的真風險（提示詞爆炸、快取失效）。
所以從第一天就有淘汰（各類上限 地點40／人物24／設定30）＋餵回上限（一回合最多 6 條）。
實測：41 條灌到上限後淘汰回 31（30 未釘選＋1 釘選），餵回去 125~180 字。
**加新的「類別」時記得同步 `KANSHOU_WORLD_CAP_`。**

隨機 NPC 日常落點（`kanshouRollDailyLocation_` 等）仍只用內建地點池，
沒有把「玩家走出來的地方」納入——那些是公共城市地標，隨機把人丟進玩家自己發現的地方
會有一堆邊界狀況。要不要開放是之後的事。

## 驗證

`world.js`（發明→落盤→餵回→去重）、`world2.js`（淘汰/釘選/相關性單元測）、
`place.js`（自訂地名→世界多一格→走得回去→前端拿得到）、`folk.js`（造人→記住→升格→不重複跳框）。
紅線①：system 1822 → 1977 字（增量全在快取前綴）；全套鑑賞探針；
等效重複子句 85 條、完全重複 0；k_sex2 兩組配對逐句比對沒改壞。


---

## 🎨 說書人風格交給玩家（2026-09）

玩家：「其實我應該直接在這裡打造一個類似酒館就好？！」→「好 稽核跑完就往這個方向做 符合自由 玩家自己決定增減！」

**邊界（量出來的）**：鑑賞一回合 prompt 約 3600 字＝事實·GAS 裁定 ~1280（不交）／技術契約 ~1200（不交）／資料 ~283／**風格 ~965 字·13 段（交給玩家）**。
交的只有「怎麼寫」的口味；「發生了什麼」（好感、在場、地點、時間）與「怎麼回」（JSON、分段、在場驗證）一律不交。

**模組表** `KANSHOU_STYLE_MODULES_`（Gallery.gs·資料驅動，加一段＝往表加一列）：

| key | 名稱 | slot | 預設＝原本寫死的那句（**只在 .gs 裡，不下傳前端**；玩家看到的是 `hint`） |
|---|---|---|---|
| voice | 筆觸 | sys | 「後日談敘事核心·輕小說筆觸…」（nsfwBaseRules 開頭） |
| agency | 玩家主權 | sys | 鐵律 1 動作與台詞只有玩家能決定 |
| enact | 演玩家這一步 | sys | 鐵律 2（2026-09 新增）：narration 從玩家這一步演起、撐成完整的一拍，但只擴寫他真的寫的那一步 |
| dialogue | 對話格式 | sys | `dialogueFormatRule_()`（預設是函式，所以走 `kanshouStyleDefault_`） |
| drive | 推演 | sys | 鐵律 4 |
| continuity | 情緒連貫 | sys | 鐵律 5 |
| moe | 萌點用法 | sys | 鐵律 7 |
| immersion | 不出戲 | sys | 鐵律 9 |
| world | 世界觀 | user | ★世界觀＝和平的現代冬木市… |
| pov | 視角 | user | ★【視角鎖定】（`{玩家}` 佔位） |
| feel | 你的感受 | user | ★【你也是這座城裡的一個人】（`{玩家}`／`{代名詞}`） |
| length | 篇幅 | user | ★【篇幅】（`{篇幅}`＝這回合查表算出的區間） |
| ending | 收尾 | user | 🚨【收尾】（`{主動掌握}`／`{推進}` 隨 driveOn） |

留死不交：鐵律 2 `<br><br>`、6 性別欄、8 裝扮既定、10 JSON、整個 finalJson、親密尺度、地點釘死、這個世界有誰、world_note 規則、此刻、在場來由、角色一致性。

- **🖋️ 玩家打的字要被演出來**（2026-09 玩家「我想要使用者輸入的文字也會被擴寫到劇情」）：兩件事一起修才成立。
  ① **標籤說實話**：玩家自己打的字原本跟按鍵一樣被冠上 `【玩家意圖】：`——那是 GAS 寫的意圖摘要專用的標籤，
  貼在玩家原話上等於告訴模型「這只是他想做什麼」，於是 AI 把它當前提跳過、直接寫對方的反應。
  改成 `【玩家原話】：`（按鍵路徑仍是 `【玩家意圖】`，因為那真的是 GAS 的摘要）。
  ② **`enact` 鐵律**：narration 從玩家這一步演起，撐成完整的一拍（動作、距離、觸感、開口的語氣與視線），
  但只擴寫他真的寫的那一步——不補沒做的動作、沒說的話。跟 `agency`（不替玩家決定）是同一件事的正反兩面，故緊鄰排在鐵律 2。
  ⚠ 台詞「照原句抄、禁轉述」**不在這條裡重複講**——`dialogue`（對話格式②）已經寫了，同一件事只留一個出處。
  ⚠ 這條是 **sys** 模組，拿不到 `{代名詞}`（那組變數只餵 user 段），所以句子必須中性——`check_pronoun` 第一版就抓到寫死的「他」。
  ③ **輸入框的提示字**：原本是「輸入行動...」——等於告訴玩家這裡只打動作，跟「你打的台詞會照原句演出來」正面打架（UI 說謊的同一個形狀）。改成「說點什麼，或做點什麼…」。
  ⚠ **刻意不做「玩家要學的語法」**（`*動作*`／`"台詞"`）：沒打標記的人會默默拿到比較差的結果、又沒有任何訊號，那是沉默的陷阱；中文本來就有現成的「」可用。也刻意**不讓 GAS 去猜**哪段是說、哪段是做——句子一長就錯，錯了沒人發現。界線本身已經分在兩條規則裡（`dialogue` ②照原句抄／`enact` 擴寫成畫面），不再寫第三句重複講。

- **資料層**：新分頁 `鑑賞風格` `[遊戲ID, 模組, 文字, 開關]`（`KS_`）。缺列＝預設；文字空＝預設；開關 `0`＝整段不送。表上只放真的改過的列（存空文字＋開啟＝刪列）。快取 `KS_<gid>` 120 秒，比照世界帳本 `KW_`；歸零重來一併清掉。
- **讀取**：`kanshouStyleRead_(gid)` → `{key:{text,on}}`；組 prompt 唯一讀口 `kanshouStyle_(styles, key, vars)`：玩家版 → 關閉＝'' → 預設，並代入 `{玩家}{代名詞}{篇幅}{主動掌握}{推進}`。
- **接線**：`buildDefaultSystemPrompt(includeOptions, styles)` 的 `nsfwBaseRules` 改成**陣列組裝＋動態編號**（關掉一段其餘自動補號）；`actionPlay` 的 USER prompt 五段改讀區域閉包 `_sty_`（＝`kanshouStyle_(_styles_, key, _styleVars_)`）。**預設一格不改時，SYSTEM＋USER 逐字等於改版前**（`scratchpad/style/baseline_prompt.txt` diff 為空）。
- **動作**：`kanshou_get_style`（回 `modules[]{key,name,slot,cat,hint,text,on,custom}`＋`cats[]{key,name}`＋`max`——**`def` 刻意不下傳**，見下）／`kanshou_set_style`（`{key,styleText,on}`／`{key,reset}`／`{resetAll}`）。文字上限 `KANSHOU_STYLE_TEXT_MAX_`=300；**不剝 ｜【】**（這不是 MEMORY，它自己一張表；`sanitizeUserData_` 只擋控制字元與公式前導）。
- **UI（2026-09 改三態）**：☰ →「⚙ 說書人設定」（`openKanshouStyle`·Script_Kanshou.html）：每段 名稱／狀態（預設·自訂·已關閉）／**一句 `hint`「這段管什麼」**／三顆鈕 **預設｜自訂｜關閉**；選「自訂」才長出文字框（中性 placeholder ＋ 字數計 ＋ 儲存），底部全部還原。
  - **🙈 預設句本體不顯示給玩家**（玩家：「把我的設定目前就是預設值 但是不要顯示內容給玩家看到」）：預設值就是調好的提示詞，貼在畫面上玩家會照著看、照著改，等於把敘事的骨架攤在桌上出戲。所以 `actionKanshouGetStyle` **改回 `hint` 不回 `def`**——連網路封包裡都沒有那串字；`KANSHOU_STYLE_MODULES_` 每列多一欄 `hint`（一句話講這段管什麼，不透露句子怎麼寫）。
  - **三態＝原本兩個控制項的合併**：舊版是「文字框（灰字＝預設）＋啟用勾選」，玩家要從灰字/黑字/勾沒勾去推自己現在是哪一種；灰字一拿掉那個介面就無法讀了。現在狀態是明講的一顆亮著的鈕：`ksMode(m)` 是唯一判讀口（關閉 → 自訂 → 預設）。
  - **按「自訂」不打後端**：只是本地 `_ksEdit_[key]=true` 把輸入框長出來，按【儲存】才送（守住每按鍵最少 round-trip）。留白存檔＝刪列＝回到預設，跟後端原本的規則同一條，沒有新特例。
  - **🗂 四個分頁**（玩家：「那13段 這樣一長排 很難設定 排版分類一下」）：`KANSHOU_STYLE_CATS_`＝`✍️ 文筆`(voice/dialogue/length)｜`🎭 你`(agency/enact/pov/feel)｜`💞 對方`(drive/continuity/moe)｜`🌍 世界`(world/immersion/ending)。分類依【這段在管誰】，**跟 `slot` 是兩回事**（slot 是進哪一段提示詞）——舊版用 slot 當分組是拿技術欄位當 UI，玩家讀不出意思。
    分頁表跟 `cat` 欄一起下傳，前端不寫死；加一類＝往 `KANSHOU_STYLE_CATS_` 加一列。
    ⚠ 分頁會**把狀態藏起來**，所以每個分頁右上角有角標＝那一類有幾段不是預設；`style.js` 釘住「每段都歸在存在的分頁」「沒有空分頁」。
  - 🔒 機器擋：`check_ui.js` 的 `ksRender_ 三態＋分頁` 會餵一個**帶 `def` 的誘餌模組**進去渲染，只要那串字出現在畫面 HTML 就當場叫（退化測試確認過會叫，改版後再測一次），並驗分頁列畫得出來、切頁真的換內容、別頁的東西不會漏到這一頁；`style.js` 另有「get_style 不下傳預設句本體」「每個模組都有 hint」兩條。
- **紅線①流程**：改前 `_audit.js` 存基線 → 改後 diff 零差異 → `style.js` 32 條（預設零差異／改一段只動那段／關一段整段消失且鐵律重新編號／單格與全部還原／兩帳號互不污染／注入 `=SUM ｜【同居】1 <b>` 不切壞 MEMORY／300 字上限／不存在的模組被拒／歸零清風格）→ `known.js`／`k_sex2.js`／`k_cd.js`／`audit_all.js` 零退化。
- ⚠ **佔位符刻意叫 `{代名詞}` 不叫 `{他她}`**：`check_pronoun.py` 會把字面「他／她」當寫死代名詞抓出來，而它抓得對——預設句裡不該出現任何一個。
- ⚠ **預設值留在 `.gs`、不搬進試算表**：十三支掃描器只看 `.gs`，搬走等於讓 `check_prompt`／`check_pronoun` 對這 12 段失明。

## 🔦 聚光燈：點名誰，誰才拿完整的卡（2026-09）

玩家：「砍掉重複！看能不能縮減，因為給太多資料 AI 反而演不出來。」

先量：同地 3 位同伴時，在場人物卡合計 706 字，其中**每張 115 字是原廠不會變的、93 字才會動**（原廠佔 55%）。
而提示詞本來就寫著「玩家專一對著一個人時其他人背景輕描」——那句話一直沒有被做出來，
所有人每回合都拿全套。

作法：`_spotlight_` 從**玩家自己這一步的文字**裡找出被點名的同伴（走既有的 `kanshouNameCandidates_` 別名橋，
短名也算），被點名的拿完整卡，同場其他人只拿「此刻的情境」那幾欄
（名字／性別／在場來由／裝扮／**口吻**／現況／同居／約定／你在他眼中／關係好感），
砍掉 性格／特徵／招牌小動作／經歷／萌點／共同回憶——**下一回合被點名時就全部回來**。
⚠ **口吻刻意留著**：背景的人也可能被要求給一句反應，那一句還是要像那個人。
⚠ **沒點名任何人就全部給完整卡**（維持原行為）：猜錯的代價是那個人當場失格，不值得賭。
量測：三人同場、點名一人 → 706 → 464 字（**−34%**）。探針 `kspot.js`。

### 私密一面整組退休（2026-09）

玩家：「私密一面是不是不太需要……太難表演了。」量完之後發現他說的是對的，但理由更精確：
**它跟【萌點】是同一種東西**。25 位裡有 **23 位**的私密一面寫的是**條件觸發句**
（越想要的東西越裝作不在意／被善意觸動會別開眼掩飾／看到當季的花草就走不動），
而萌點本身也大多是條件句。一張卡同時掛兩個「等情境對了才能演」的東西，
模型只能硬塞一個進去，寫出來就尷尬——這不是哪一句不好，是**同類的東西擠在一起**。

⚠ 這種重複 2-gram 量不出來（`check_back_not_personality` 對它們是 <10%）：
它是**功能上的同類**，不是字面重複。量測工具看不到的東西，還是得有人讀過內容才發現。

作法：兩軌都停送第三格——鑑賞的萌點只送 `dailyMoe`（35 → 23 字/張，原廠欄 115 → 103 字/張），
solo 的 `TRAIT_LABELS_` 砍成兩格。
**接著玩家看了內容本身**：「越想要的東西越裝作不在意…這些是什麼，有必要嗎」「感覺她也不是這種個性的人」
「懷疑是當初為了設定而設定」。查證下來三件事都成立：
- 我們自己的 `CODE_NOTES` 就寫著它的出身是「DAILY_LOOK 改為四段…**對齊 PERSONA.traits/PREF 格式**」
  ——是為了對齊格式生出來的欄位，不是角色需要它。
- 25 句裡有 **6 句**用同一個模板「越X越（裝作／用）Y」（越想要的東西越裝作不在意／越在乎越用抱怨包裝／
  越感興趣越裝作不屑／越被依賴越裝作事不關己／越想靠近越先開口捉弄／越在意越用小惡作劇試探）
  ——那是填格子的手感，不是寫角色的手感。
- 生成端的提示詞**必須明文叫 AI「這一格禁止跟萌點重複」**才寫得出來；要靠一條禁令才能區分的兩欄，本來就是同一欄。

所以整組退休，不只是不送：
- 種子 25 筆的 `dailyLook` **四段→三段**（外貌本相／氣質舉止／日常口氣），省 281 字元
- `TRAIT_SLOTS_` **3→2**、`looksToTraitParts_` 不再補第三格、各處 fallback 一併收成兩格
- 生成端 `translateLookToDaily_` 改產三短句，兩支 translate* 各少一個參數
  （`dailyMoeHint`／`lookPrivateHint` 都只是用來跟私密一面去重的）
- `traitParts_` 保留「舊局讀到三、四格就地剝掉」的相容，`CODEX_PERSONA_VER` → v76 重刷

### 氣質舉止改寫成畫面（2026-09）

玩家問「檢查種子庫，我感覺可能有不少是為了設定而硬要寫的資訊」。用三把尺量過
（字面撞詞／句型骨架共用／**純形容詞比例**），只有一欄明確中：**`dailyLook` 第2段「氣質舉止」
88% 是純形容詞**（誰都適用、沒有任何看得到的動作），而且生成端**必須明文禁止它跟口吻重複**
（`【不可與下方口氣段用相同字眼重複描述】`）——跟私密一面同一個形狀。

實際讀內容，規律很清楚：**寫成畫面的那幾筆有用，寫成形容詞的就跟口吻是同一句話。**
```
⚠ 重複   美杜莎 溫婉恬靜 ／ 寡言低沉卻藏溫柔      ✅ 有畫面  庫丘林 爽朗常在街頭閒晃
         迪盧木多 溫雅內斂 ／ 謙恭有禮情感壓在心底           吉爾伽美什 走在街上像在視察領地
         咒腕哈桑 肅穆低調 ／ 話極少必要時才開口             遠坂凜 下巴總是微抬半分
```
玩家選擇「改寫成畫面」而不是砍掉。21 筆改寫（走路幾乎沒有聲音／進門總先替人扶著門／
總站在光照不到的那一側／袖子總是捲到手肘…），**純形容詞比例 88% → 12%**，
且改寫後跟口吻／萌點／性格的撞詞率沒有一筆 ≥25%。
⚠ 這一欄**只有鑑賞在用**：`dailyLook` 是日常版；solo 的氣質來自 `persona.look` 的末段
（王者威儀／厭世冷峻那種），是另一份、本來就有味道，一個字沒動。
⚠ 寫的時候守三條：不可跟【口吻】(管說話)重複、不可跟【萌點】(招牌反差)重複、限 `TRAIT_SEG_HINT_` 字內。

### 順手砍掉的重複
- **「經歷」是性格的改寫**：25 筆裡有 5 筆跟 `dailyWords` 的 2-gram 重疊 ≥40%
  （庫丘林「隨性愛湊熱鬧，重情義」vs 性格「隨性自來熟、重情義」＝同一句講兩次），整組刪掉。
  `check_seed.py` 新增 `check_back_not_personality` 把這條變成機器擋（經歷放事實，形容留給性格那格）。
- **泛用墊底句移除**：`heroToKanshouRow_` 原本在沒有 `dailyBack` 時塞「生活在這座城鎮裡的普通身影，與你尚無深交」。
  對 25 位有名有姓的英靈那句零資訊量，還擋掉 AI 自己補一段合理來歷的空間；現在留空＝那一欄整個不印。
- ⚠ 查過但**沒有**重複的：`dailyMoe`（萌點）vs `dailyLook` 第 4 段（私密一面）——25 筆全部 <10% 重疊，
  是兩件不同的事，不要因為那一欄最長（35 字/張）就砍。

## 🩸 肉體狀態不跨夜（2026-09）

玩家要求查「有沒有新的變動資料跟原廠設定衝突」。量出來的第一個是**變動資料自己卡住不退**：
`PHYSICAL`（肉體）由 AI 的 `intimacy_feedback[].physical_state` 寫入，但
**AI 沒吐那個欄位的回合不會覆寫它**（`if (nCleanState)` 才寫）——於是「腿還在發軟」會一路跟著那個人
走過好幾天、好幾個地點、甚至跨過關係質變，跟後來的一切互相打架。

修法比照既有的「🤝 睡覺自然放手：牽手不跨夜」：【一天結束】時 `kanshouRestBody_` 把玩家與所有正式同伴
的肉體狀態設回 `{"狀態":"如常"}`（`_isPlainBody_` 認得這個值，回到如常就不再送進提示詞）。
**同一天內刻意不清**——那一欄寫的就是此刻的身體，當天還在是對的。
⚠ 探針 `kbody.js` 釘住三件事：寫得進去／同一天內留著／跨夜歸位。
⚠ 寫探針時踩到**兩段式就寢**：身邊有羈絆 ≥60 的人時，第一次按睡覺只會進「夜未眠」，
第二次才真的結束一天——只按一次會讓人以為修沒生效。

## §「後日談歸零重來」（2026-09）

玩家：「我用風音這個帳號進去 可以有個按鈕把鑑賞資料歸0重新開始」

**兩個入口，一支核心**（`kanshouResetCore_(kpcId, acct, reloadAfter)`）：

| 入口 | 何時顯示 | 清完 |
|---|---|---|
| 🏠 **主選單**（登入後、🌹進入鑑賞 底下） | `account_login` 回傳的 `kpcId` 非空才長出來——沒東西可清就不長那顆鈕 | 收起按鈕、旗標打掉，**不重載**（`pc` 根本還沒載入） |
| ⚙️ 遊戲內系統設定 | 只在 `pc.mode === 'kanshou'` 時顯示 | **必須整頁重載**——`pc` 指著已經被刪掉的那一列 |

`reloadAfter` 是兩者唯一的差別。solo 有自己的「⚜️奪得聖杯」在收尾，不在同一個地方混兩套入口。

| | |
|---|---|
| **會清掉** | 鑑賞眾生（玩家自己那列＋所有同伴）／他們的對話歷史／相簿／世界帳本／帳號表的鑑賞連結 |
| **不會動** | 英靈殿（含工房鑄的原創英靈——兩軌共用的資產）／solo 那一局的任何東西／**別的帳號的鑑賞** |

清完解除 `COL.ACC.KPC` 連結，下次 `actionEnterKanshou` 查無連結就從頭建一個全新世界。

**順序刻意是「先清資料、最後才解連結」**：中間任何一步炸掉，連結還在，玩家至少回得去原本的世界。

🔒 **授權比照 `actionEndRun` 那次稽核的修法**：`pcId` 是可預測的時間戳，【不可】裸 find——
一律驗「這個帳號登記的鑑賞角色是不是就是它」(`getAccountKanshouPcId_`)，
否則任何人都能猜 id 清掉別人的存檔。前端再加一道「照抄帳號名」的打字確認（不可逆的東西不給按一下就走）。

`kanshouPurgeByGame_(sh, gidCol, gid, idCol)` 是共用的「依 game_id 刪整批列」helper
（由下往上刪避免索引位移），三張表共用一支。

⏳ **不可逆的操作要看得到它在跑**：玩家實測「輸入名字後就消失了，不知道有沒有在執行」。
輸入框一關、設定視窗還開著，後面整段清除完全沒有畫面回饋。現在先收掉設定視窗、壓一張
**不可點背景關閉**的讀條（跑到一半被關掉，玩家會以為沒事而重按一次），跑完換成結果、再重載。

⚡ **但等待畫面只是症狀，慢才是病**：原本逐列 `deleteRow`，在 GAS 裡那是**一列一次 API 呼叫**。
改成「留下來的整批寫回、尾巴一次砍掉」（同 `actionPurgeOrphans` 的寫法）。
實測 400 筆歷史＋5 位同伴的整局清除：**`deleteRow` 0 次、總共只打 4 次試算表**（原本要 405 次）。
`purgeHistoryForPcIds_` 也一起批次化了——solo 的 `end_run` 走同一支，兩軌同時受惠。

**探針 `reset.js`** 開兩個帳號各一局，驗四件事：別的帳號清不動／沒帶帳號名擋下／
本人清得乾淨（眾生 5→0、歷史/相簿/世界帳本歸零、連結解除）／**另一個帳號與英靈殿一根寒毛沒動**。


---

## §「經歷改回固定」（2026-09）

玩家：「我的經歷怪怪的.....好像是滾動式的／改回固定吧....？」

### 病在「反覆重新摘要」

`master_note.經歷` 每 3 回合（`KANSHOU_SIDEWRITE_EVERY_`）叫 AI
「承接舊經歷·只增補本回合有意義的新事·**滾動摘要≤50字**」——
**壓過的東西再壓一次**。開局寫的身世撐不過幾輪就被最近幾回合洗掉，
內容往「剛剛發生的事」漂、細節一路掉光。這是 lossy re-summarization 的典型衰變，
不是提示詞措辭能修的。

而且「一路上發生了什麼」現在是**世界帳本**的工作，它是**追加＋淘汰**、不是反覆重寫——
留著這條等於用一個更差的機制做同一件事。

### 現況

**經歷＝固定事實**：創角時 `actionBackfillKanshouAi` 生成一次，之後只有玩家能透過
逆天改命改（限 80 字）。AI 全程只讀不寫。

這是 2026-07「性格四格/萌點不再交給 AI」那次的最後一塊——
**鑑賞的角色欄位現在全部只有兩條寫入路徑：創角時生成一次、玩家改命。AI 一欄都碰不到。**
（AI 能寫的只剩「發生了什麼」那一類：好感、外顯、暱稱、共同回憶、世界帳本。）

### 連帶移除

`master_note` schema／`_doSideWrite`／`KANSHOU_SIDEWRITE_EVERY_`／
`kanshouGetSideWriteCount_`／`kanshouSetSideWriteCount_`／MEMORY 的【側寫計數】標記／
`buildDefaultSystemPrompt` 的 `includeMasterNote` 參數（簽名變成 `buildDefaultSystemPrompt(includeOptions)`）。
舊存檔殘留的【側寫計數】是純孤兒資料，不影響任何邏輯。

**副作用（好的）**：system prompt 不再每 3 回合變一次形狀，可快取前綴恆定。

探針 `exp.js`：連打 10 回合、AI 每回合硬吐 schema 外的 `master_note` → 經歷一字未動；
玩家逆天改命仍改得動。

---

## 🔍 2026-09 整體稽核（玩家：「檢查整體吧！！！！詳細 慢慢看 不要有任何遺漏」）

抓到四件事，都已修。四件裡有三件是**綠燈、零錯誤訊息、玩家不會收到任何提示**的那種壞法。

### ① 拍照整條路靜默壞掉（最嚴重）

事件自由那輪砍掉預寫橋段池時，`kanshouLocActivity_`／`kanshouReFest_` 連同表一起刪了，
但**相簿落地那段還在叫它們**——而那整段包在 `try/catch` 裡。結果：
**拍照永遠失敗、相簿永遠存不進去**，玩家只會覺得「怎麼都沒存到」，後台沒有任何錯誤。

修法：節慶旗標改用 `curDateObj_` 現場算（`KANSHOU_FESTIVALS_` 還在）；活動欄留空
（**欄位保留不刪**——COL 是位置索引，刪欄會位移全表）。

根源修法是新的掃描器 **`check_undef.py`**：`node --check` 只看語法，看不到「叫一個不存在的東西」，
而 GAS 這種 try/catch 包起來的落地路徑正是最容易這樣壞的地方。探針 `land.js` 盯著落地結果。

### ② `world_note` 沒擋公式引導字元

`world_note` 是 **AI 產的、不經過 `sanitizeUserData_`**，而帳本的名稱/內容會直接寫進儲存格——
開頭是 `=`／`+`／`-`／`@` 會被 Google Sheet 當公式執行（`=IMPORTXML(...)` 這類）。
相簿的 `photo_caption` 當年就為了同一件事補過，這裡漏了。已在 `kanshouWorldWrite_` 的 `_f` 補上。
探針 `sec2.js`。

### ③ 每按鍵整表讀 6 → 9 次（效能退化）

世界帳本第一版三支各自讀表：餵回讀一次、寫入讀一次、淘汰再讀一次。
CLAUDE.md 寫著「別把多餘 round-trip 或重複整表讀回加回來」——修法見
`CODE_NOTES.md` 的 `kanshouWorldWrite_` 條。現在 **7 次**（多出的 1 次是帳本寫入，無法省）。
探針 `perf2.js` 直接數 `getDataRange`/`setValues`。

### ④ 文件裡有 17 條幽靈條目

`kanshouRollEvent_`／`kanshouLocActivity_`／`stanceLine_`／`summonTab`／`checkName`… 
功能砍掉了、索引沒跟著砍。CLAUDE.md 說 `FUNCTION_MANUAL.md` 零容錯，正是因為
「照著文件去 grep、查無此函式」比沒有這份文件還誤導人。全數修正，並加掃描器 **`check_docs.py`**
（文件裡用 `` `名字(` `` 點名的函式，代碼裡必須真的還在）。

### 順手修

`actionPrepMeal`（solo）沒從者時仍講「御主與從者」——沒附卡卻點名從者＝邀 AI 憑空生一個
（同 `check_cards.py` 在擋的形狀）。改成條件式兩分支。


---

## ⏳ 等待畫面（2026-09，玩家第二次提同一件事）

玩家：「如果需要玩家等待的地方 都加上一個等待畫面 **不要只是背景執行**！」
（第一次是歸零重來那顆：「輸入名字後 就消失了 不知道有沒有在執行」。）

**鑑賞這邊補上等待畫面的入口**：同伴面板（👥，**兩趟 round-trip**，原本按下去到面板彈出中間完全沒反應）、
快速貼圖新增/刪除、家改名、御主改名、切換性別、關係稱呼、專屬稱呼、撕照片。全部走共用的
`withProcessing_('…中…', () => gasRun(...))`。

**刻意不擋畫面的**：`backfillKanshouAi`（進場後補完御主設定）。它非阻塞是對的——玩家已經可以開始玩，
擋他 20 秒是把好事做成壞事；但「不擋」不等於「不講」，改用 `bgHint_()` 的小提示條。

**維持原樣的**：對話主線 `send()` 本來就有 `loaderCaptions` 行內讀條；
約定變動後的背景刷新（已抽成具名的 `kcRefreshAfterPromise_`）玩家沒在等它。

機器擋：`check_wait.py`——每個 `gasRun()` 呼叫點往上找不到等待指示就叫，
樂觀更新/背景同步要逐支登記並寫理由（**登記過的才算例外，沒登記的就是漏了**）。
另有 runtime 探針 `scratchpad/size/wait.js`：把三個 `Script*.html` 串進同一個作用域配最小 DOM，
真的把 `withProcessing_`／`bgHint_` 跑起來對答案（巢狀不可提早收、例外一定收、小提示計數歸零才消失）——
CI 不驗 .html JS，只有這裡驗得到。


---

## 🎭 你也是這座城裡的一個人（2026-09·紅線① 改動）

玩家：「鑑賞我想要的是 AI 扮演玩家的角色，用這個角色的角度去感受這個世界。」

### 改了什麼

**`nsfwBaseRules`（紅線①·演化核心）兩條**
- 鐵律 1：「不擴寫**不代玩家加戲**」→「**玩家的動作與台詞只有玩家能決定**：不替他新增動作、
  不替他開口、不改寫成轉述（玩家角色的**感受不在此限**）」。原句把「感受」跟「加戲」綁在一起，
  一刀切掉了整個內心。
- 鐵律 5：`inner_monologue` 從「只判**對方**的真實反應」→「判對方，**也判玩家這個人此刻真正的感受**
  （依其性格/經歷，不是表現出來的那個）」。感受要有依據，否則每回合即興、跨回合會飄。

**★【視角鎖定】拆成兩塊**
- 視角鎖定只留「第二人稱、旁白不可用『我』」。
- 拿掉「**只演你實際輸入的動作與五感——你看不見自己的神情**」（這就是把玩家關成攝影機的那句）。
- 新增 **★【你也是這座城裡的一個人】**：用這個人的角度感受世界（觸感/冷熱/氣味/聲音、**真正**的情緒、
  性格帶來的反應底色、沉默也要有理由）；但**做了什麼說了什麼只有玩家決定**；
  **寫感覺得到的，不寫看不到的外觀**（臉看不見，臉發燙感覺得到）。
- 代名詞走 `_mePron_ = pron_(pc[COL.PC.SEX])`——御主性別是資料，而且隨時可切換。

**玩家角色卡補厚（4 格 → 6~7 格）**
補印 `萌點(僅供內化)`、`口吻`、`招牌小動作`，全部**複用同伴那一行的同一組 helper**
（`getPersonaSpeech_`／`getPersonaTic_`／~~`traitPrivateOf_`~~·已移除／`COL.PC.INTENT`）。
⚠ 萌點**本來就有存**（`actionBackfillKanshouAi` 一直在生成），只是從來沒印出來。
口吻/小動作是新生成的：schema 加 `speech`／`tic` 兩欄，落地走召喚同伴那支 `stampPersonaFlavor_`。

**篇幅**：寫不滿時「你自己此刻的感受與身體反應」也是正當的填法（本來只列在場者的動作與感官）。

### 順手修掉的兩個舊漏洞

- **`driveStr` 寫死「她」**（`🔥【主動掌握】①她依自己的個性主動出擊`）——同伴性別是資料，
  種子 25 人裡 12 位男性，男同伴的主動橋段會被指成女性。改「對方」。
  這條**藏在多行樣板字串裡，升級前的 `check_pronoun.py` 看不到**（見下）。
- **`actionBackfillKanshouAi` 會洗掉玩過的角色**——原本無條件覆寫，改成「第一次蓋【設定已補】章、
  之後只填還空著的格子」。舊角色（玩家自己就是）靠這個才拿得到新的口吻/小動作欄位。

### 紅線① 程序（全跑完）

| 步驟 | 結果 |
|---|---|
| 改前量現況 | system 1900 字／user 1732 字 |
| 改後 | system 2000 字／user 2420 字（合計 4038→4420，**+382 字/回合**） |
| 全套探針 | 50 支跑完，無結構性退化（`dump2.js` 改前改後都壞，是本來就失效的暫存探針） |
| 等效重複子句檢查 | 12 條規則兩兩比對，最高重疊 0.15（門檻 0.50）✅ |
| 配對沒被改壞（`k_sex2`） | 女玩家×男同伴 她×10／他×3；男玩家×女同伴 她×1／他×12 ✅ 兩邊代名詞各自跟著自己的資料走 |
| 新探針 `mecard.js` | 20 條全過（落地／不覆寫／卡片厚度／感受解禁但動作仍鎖） |

### ⚠ `check_pronoun.py` 本來一直在說謊

舊版是**逐行**用正規表達式找字串字面，所以**只看得到單行字串**——而這個專案幾乎所有提示詞本體
都住在跨數十行的樣板字串裡，那裡面寫死「她/他」它一個都抓不到。
實測：把「★【退化測試】她一定會這樣做。」塞進 `nsfwBaseRules` 附近，它照樣回報 ✅。

已改成走一次真正的 tokenizer（逐字元分辨 程式碼／註解／三種字串／正規表達式字面），
跨不跨行都看得到，並且**報命中那一行**而不是字串開頭那一行。
⚠ 正規表達式字面必須單獨處理——repo 裡真的有 `/[<>&"'` + "`" + `｜【】…]/` 這種內含引號與反引號的樣式，
不認得它的 tokenizer 會從那裡開始把整份檔案的引號配對全部算錯。
升級後**當場抓到兩個藏了很久的真漏洞**（`driveStr` 的「她」、以及我自己剛寫進去的寫死代名詞）。


---

## 🪶 大道至簡（2026-09·紅線① 改動）

玩家：「看看鑑賞有什麼是多餘的、或太細緻對 AI 來說是累贅的，可以刪除或整合成一塊！
還有角色關係跟世界帳本的效果要分清楚，世界帳本應該是要紀錄更大事件的？剛剛連咖啡廳都紀錄。
另外世界帳本的新增角色功能真的能做到嗎？如果不行就不要有。」

### ① 新增角色：能做到，整條路是通的（探針 `folk2.js` 端到端驗過）

AI 當場造人 → 寫進帳本 → `folkOffer` 泡泡 → 下回合 AI 還記得他 → 玩家點「往來下去」
→ `kanshouFolkToRow_` 建 `KHV_` 列 → 正式在場卡、好感/關係追蹤得到。**不砍。**

**🐛→✅ 但抓到一個真 bug**：升格成正式同伴之後，帳本裡那條常民【沒有清掉】——
同一個人變成**兩份真相**，同時出現在【在場人物】卡與【這個世界已經確立的事】名單裡，
而帳本那句是升格當下的舊描述、之後永遠不會更新。
已加共用的 `kanshouWorldDrop_(gid, kind, name)`，升格時清掉；面板的「刪掉」也改走同一支。

### ② 地點歸地圖：不是兩個系統，是同一筆資料被展示兩次

玩家：「我本來就有地圖阿....是否地圖系統大改進」——**這句話是對的，但不用大改。**
量出來：地圖有 6 個分區，最後一區「走出來的地方」的資料來源**就是世界帳本的「地點」類**
（`kanshouLocationsFor_` ＝ 內建 29 個地點 ∪ 帳本地點）。咖啡廳被記下來時，
它其實**已經是地圖上的一格**了；問題是它同時又出現在世界帳本面板裡，長得像一條回憶。

修法：**展示收成一處**。帳本面板不再逐條列地點，改成一行指路
（「你走出來的地方有 N 個——在地圖的『走出來的地方』那一區」）。
資料層完全不動（地點仍存在帳本表，地圖照樣長）。

### ③ 帳本 vs 角色關係：分工從來沒寫給 AI 看過

這才是「連咖啡廳都紀錄」的真正成因。`world_note` 與 `memory`(共同回憶) 兩個欄位並存，
**但提示詞裡沒有任何一句話說它們誰管什麼**，AI 只好自己猜，於是把「她說過她討厭下雨」
（某個人的事）寫進了世界帳本。已在提示詞明講：

> world_note 記【這座城有什麼】；某個人的喜好習慣、你們之間發生的事記進那個人的
> memory【你們之間發生過什麼】——兩邊不要互相寫。

並把門檻拉高成「只記之後真的還會用到的」。
**「大事件」本來就有地方記**——每個人各自的共同回憶(MEMOIR)，那是 `memory` 欄的工作。

### ④ 提示詞瘦身：4420 → 4066 字（−354，−8%）

玩家選「連細節規則一起砍」。量出來最大的一團是**「誰算在場」散在 8 段、855 字**，
其中三段在講同一件事：

| 原本 | 字 |
|---|---|
| ★【路人與缺席者】 | 96 |
| ★【可以發明，但發明完要記下來】 | 161 |
| ★【在場名單】 | 151 |

合併成 ★【這個世界有誰】（正式同伴/常民/路人 三層一次講完）＋
★【要它之後還在就寫進 world_note】。順手把兩句寄生句歸位——
「萌點只演不直述」回鐵律 8、「專一時其他人輕描」進在場那段。

其餘：我上一輪造成的重複（動作台詞講兩次、收尾撞一次）、換裝講兩次、
親密散在 5 段、對話格式 ④ 併進 ③、在場來由與現況併一條、時間與天氣併一行、
options 四條各一行併成一行。

⚠ **不動**：角色卡（那是資料不是指示）、親密五階、篇幅、對話格式①②
（①②是踩過坑才加的：AI 曾把喘息寫進敘事、台詞認錯人）。

改後 `nsfwBaseRules` 12 條 539 字，等效重複子句最高 0.15（門檻 0.50）。

### 紅線① 程序

| 步驟 | 結果 |
|---|---|
| 改前量現況 | 合計 4420 字 |
| 改後 | 合計 4066 字（−354） |
| 等效重複子句 | 最高 0.15 ✅ |
| 配對沒被改壞 | 女玩家×男同伴 她×5/他×3；男玩家×女同伴 她×1/他×7 ✅ |
| 全套探針 | 無結構性退化（`solo_all` 的 ❌ 數本來就在 7~13 間跳，是從者死活的骰運） |
| 新探針 | `folk2.js`（升格全鏈）、`census.js`（逐段字數）、`overlap.js`（段落重疊） |

⚠ **探針的坑（第三次踩）**：提示詞裡的★規則現在會**提到**區塊名（「下方【在場人物】的卡」），
所以探針抓卡片一律要錨行首 `/^【在場人物】/m`，裸 match 會抓到那條規則、看起來像卡片消失了。
`legacy2.js`／`mecard.js`／`legacy3.js` 都已改。


---

## 🗾 大區自由（2026-09）

玩家：「最好連大區都能創建，這樣想去哪就去哪…我的家玩家可以自由生成家中地點！
大區也給玩家自訂，他們就可以做成旅遊、地點是各個自訂國家…然後就可以類似有我的店！
（純開店互動不用真的金錢數值，但還要有額外的店名跟營業內容）」＋「我需要可以自由增減喔！！」
＋「基礎地點可以減少一些，留點有特色的就好，其他讓玩家自己決定」。

### 為什麼三個需求是同一個機制

「自訂大區」「家中地點」「我的店」講的都是**一個地方身上的兩件事**：
**它在哪一區**、**它是不是你的**。所以資料層只加兩欄，不是三套系統。

**關鍵發現：自訂大區天生就能用。** 所有 region 的行為判斷都是
「`region !== 'room'`／`region !== 'visit'`」這種【否定】形式（不巧遇、要好感才能登門…），
所以一個陌生的區 id 自動落在「一般公共區」那一邊——**一行行為邏輯都不用改**。

### 資料層

`鑑賞世界` 表加兩欄（**接在最後**，COL 是位置索引絕不插中間）：

| 欄 | 給誰用 | 內容 |
|---|---|---|
| `KW_.REGION`(9) | 地點 | 屬於哪一區（空＝走出來的地方）。大區自己那一列放的是**它自己的穩定 id** |
| `KW_.OWN`(10) | 地點 | 空＝不是你的；有值＝營業內容（「小吃」「按摩」…） |

新 kind `KANSHOU_REGION_KIND_ = '大區'`（上限 `KANSHOU_REGION_CAP_ = 12`）。
它刻意**不在** `KANSHOU_WORLD_KINDS_` 裡，因為那張表管兩件事：①AI 能寫哪些 kind
②哪些 kind 會被淘汰——大區兩者皆非（AI 不能自己生一個國家；大區被淘汰會讓底下地點變孤兒）。
⚠ 所以 `kanshouWorldWrite_` 的 kind 檢查要額外放行大區，這裡踩過一次。

**區 id 為什麼是生成的、不是從名字拼的**：第一版用 `'rg_' + 區名`，
一改名底下所有地點就指向一個不存在的區、全變孤兒。改成建立時生成 `rg_<timestamp36>`
存在該列的 REGION 欄，改名只動 NAME。

### 自由增減（全套 CRUD，住在既有的 `actionKanshouWorld` 裡，不另開路由）

| op | 做什麼 |
|---|---|
| `rg_add` | 開一個大區（查重名、擋上限） |
| `rg_rename` | 改名（底下地點不受影響——id 穩定） |
| `rg_del` | 收掉；**先把底下地點的 REGION 清空**放回「走出來的地方」，不留孤兒 |
| `loc_region` | 把地點搬到別的區（空＝拿出來） |
| `loc_own` | 開店／改營業內容／收店（留空＝收店，地方留著） |
| `del`（既有） | 把地點整個刪掉 |

共用 helper：`kanshouWorldSet_`（改某列某欄）／`kanshouWorldDrop_`（刪一列）。
`kanshouWorldPayload_` 讓 `list` 與每個 op 回**同一種形狀**（條目＋大區＋上限），前端只認一種。

### 🔒 AI 不能宣告「這家店是你的」

`world_note` 原本是整包物件穿過去的——AI 只要塞 `{own:"按摩"}` 就能把任何地方變成你的店，
或把地點塞進別人的大區。已在 `sanitizeAiData_` 改成**只重建白名單四欄**
（kind/name/text/sex）。region/own 只有玩家自己的動作寫得到。探針 `region.js` ⑤ 釘死這條。

### 內建地點 29 → 23

先查綁定關係再砍——**有些地點是角色資料綁著的，砍了那角色就沒去處**：

| 綁死 | 誰綁的 |
|---|---|
| `我的房間` | 睡眠判定 |
| `和室` | `KANSHOU_COHABIT_ROOM_` 同居房 |
| 河邊小徑/商店街/古老神社/社區公園/咖啡廳/便利商店/書店二樓/廢棄神社 | `KANSHOU_LOCATION_TAGS_` 角色出沒點 |
| 隱蔽的工房/島嶼道場/埃德費爾特宅邸/愛因茲貝倫城/遠坂邸/藤村家＋8 間泛用住處 | `KANSHOU_HERO_HOME_`／`KANSHOU_GENERIC_HOME_POOL_` |

砍掉的 6 個都**沒有任何綁定、也沒有特色**：廚房、庭院、屋頂花園、老道場、山間小徑、隱藏溫泉
（溫泉留「情侶溫泉套房」當代表）。前端鏡射 `KC_LOCATIONS_` 同步砍（`check_mirror.js` 會盯）。

### 前端

地圖分區列改成**資料驅動**：內建鏡射 ∪ 後端下傳的 `myRegions`，末尾一顆「➕ 新大區」。
站在自己開的區上會多出「✏️改名／🗑收掉這區」。每個**你開出來的**地方多一顆「⋯」
（開店/改營業內容/收店、搬區、刪掉）；是你的店就顯示 🏪 與「你的小吃」字樣。
新地點流程會問「開在哪一區」「是不是你要開的店」。共用小選單 `kcChoose_`。

探針：`region.js`（20 條：開區/穩定id/開店/家中地點/收店/改名不成孤兒/收區回收地點/刪地點/AI 越權）。


---

## 📍 老地方：牢籠 → 偏好（2026-09）

玩家：「砍了就沒去處?! 是不是不要綁地點 有點沒必要?」——直覺對，但實情比「沒必要」更嚴重。

### 量出來的兩個問題

`kanshouRollDailyLocation_` 原本是 `pool = haunts.length ? haunts : 全部地點`
——**有綁定就只從綁定裡挑**。逐角色拆開：

| | 原本這輩子只會出現在 |
|---|---|
| 斯卡哈・美遊・大河・美狄亞・小黑・伊莉雅・阿爾托莉雅・凜・黑櫻 | **各自一個地點**（9 人） |
| 美杜莎 | 兩個地點 |

**9/10 的角色被釘死在單一地點。** 而且第二個問題更直接：保底池讀的是 `KANSHOU_LOCATIONS_`
（只有內建地點），所以**沒有任何同伴會出現在玩家自己開的地方**——跟「地點自由／大區自由」
整條線正面打架：你開了一座泰國海灘，沒有人會去。

### 修法：池子永遠是整個世界，老地方只是加權

```
pool = 內建 ∪ 玩家自己開的地方（排除 room/visit/dateOnly）
老地方多放 KANSHOU_HAUNT_WEIGHT_(=6) 份進池子
```

實測 2000 次：

| 角色 | 老地方 | 去過幾種地方 | 玩家新開的地方 |
|---|---|---|---|
| 阿爾托莉雅 | 咖啡廳 31%（原 100%） | 16 種 | 8.9% |
| 遠坂凜 | 便利商店 32%（原 100%） | 16 種 | 9.2% |
| 美杜莎 | 兩處合計 50% | 16 種 | 6.5% |

「想找凜就去便利商店碰運氣」這個手感留著了，但她不再是那裡的家具。
**`KANSHOU_HAUNT_WEIGHT_` 是唯一的旋鈕——設 0 就是完全隨機、誰都沒有固定去處。**

### 順帶解掉「砍了就沒去處」

新的池子邏輯裡，`if (all.indexOf(h) < 0) return;`——**那個地點被砍掉就當沒這條偏好**，
保底是整個世界。所以現在砍任何地點都不會讓誰沒地方去，
`KANSHOU_LOCATION_TAGS_` 從「綁定」降級成「純裝飾性的偏好表」。

⚠ 簽名變動：`kanshouRollDailyLocation_(heroName, hour, cohabit, memory, gameId)`
——沒有 `gameId` 就看不見玩家自己開的地方。5 個呼叫點全部補上。
效能不變（每按鍵整表讀仍是 7 次，`kanshouWorldRead_` 走快取）。

---

## 🎲 GAS 擲骰的地方剩下哪些（2026-09 盤點＋清理）

玩家：「有哪些是用 GAS 去算機率的？你覺得必要嗎 還是都給 AI 發揮較好??」→「改成必定成功」

### 分界線

**要寫進試算表、之後要當真的 → GAS**（AI 沒辦法「決定」誰在哪、關係有沒有成立）。
**怎麼發生、什麼反應、什麼表情 → AI**。

### 已經整組退休的擲骰

| 原本 | 為什麼拿掉 |
|---|---|
| `kanshouConfessAccepts_`（告白成敗·好感×相處次數） | 玩家主動說出口的意圖，被看不見也影響不了的骰子否決 |
| `kanshouProposalAccepts_`（相約/同去/牽手·依好感） | 同上；而且好感高時本來就幾乎必成，骰子多半只在低好感時擋路 |
| `kanshouRelChatCeiling_`（聊天加好感封頂） | 節奏本來就被每回合上限管著，這是第二層，還會叫 AI 踩煞車 |

連帶刪掉的：3 天告白冷卻、被拒扣 3 點、【告白日】標記、`confessWait` 下傳欄位、
三個提議的「婉拒」分支與 `_ppNo` 字串、提示詞裡的「系統已裁定…成敗由系統定」措辭
（改成直接陳述事實：★【約定成立】／★【同行成立】／★【牽起來了】／【這件事已經發生，不可改寫】）。

⚠ 前端兩句確認框也跟著改——它們原本寫「她也可能婉拒／被拒後要隔幾天」，
現在是謊話（同居是**門檻**不是擲骰、告白必定成功）。

### 保留的擲骰（都是「必須寫進試算表」那一類）

| 還在擲 | 為什麼必須是 GAS |
|---|---|
| 每個同伴今天在哪（`kanshouRollDailyLocation_`） | 要寫 LOC 欄。AI 搬不動人 |
| 出門走走 70% 遇到誰（`kanshouRollEncounter_`） | 同上 |
| 深夜訪客 / 夜襲 / 她主動邀約 | 「她主動來找你」——AI 敘述得了但搬不動她的位置；這正是「世界是活的」的來源 |
| 泛用住處分配、ID 尾碼 | 建檔一次性，不是玩法 |

### 門檻（不是擲骰，是資格）——全部保留

告白 ≥60 開得了口／同居 ≥90／登門拜訪 ≥40／親密橋段 ≥40（`KANSHOU_SCENE_MIN_BOND_`）／
告白牆（好感 79 封頂直到真的告白）。這些是**有沒有資格**，不是**運氣**，玩家看得見也控制得了。


---

## ⏰ 時間流速（2026-09·玩家定案）

玩家：「對話 10 分鐘太慢，30 分鐘比較符合但又太快，對話沒幾次時間就溜走了…
如果是色色又太快，沒辦法平衡」→「時間那增加流速選項：每次對話 10、20、30 分鐘、暫停、
下一時段、自由設定日期時間」。

### 根源

**一個回合不是一段固定的時間。** 「早安。」是三秒、一起吃頓飯是四十分鐘、一場情事是兩小時——
固定任何數字，對其中兩種永遠是錯的。原本 `KANSHOU_HOUR_PER_ACTION_` 寫死 10 分，
換算下來一個遊戲日＝ **90~102 個回合**（改 30 分則剩 30~34 個）。

**把旋鈕交給玩家**：只有他知道這一幕是三秒還是一下午。

### 現況

| 東西 | 說明 |
|---|---|
| `KANSHOU_PACE_OPTIONS_` = [0,10,20,30] | 每回合前進幾分鐘；**0＝暫停** |
| `KANSHOU_PACE_TAG_`（MEMORY【時間流速】） | 存玩家列，**設一次就記住** |
| `kanshouPaceOf_(memory)` / `kanshouHourPerAction_(memory)` | 唯一讀取入口；查無/不合法一律回預設 10 |
| `actionKanshouSetPace`（`kanshou_set_pace`） | 設定流速 |
| `kanshouHoursUntilDateTime_(curDay, curHour, y, m, d, hh)` | 指定日期時刻→差幾小時。**只能往前，往回一律回 0** |
| `userData.setDateTime` | 走既有 `advanceHours` 管線（跟跳時段/跳節慶同一條路） |

**為什麼只能往前**：約定存絕對日、好感棘輪、相簿日期、節慶完成標記、初見日/紀念日
全部是單向時間戳，往回調會讓「已經發生的事」錯亂。要重來有歸零那顆。

**暫停時世界是真的凍住的**——她們不換地方、不會想睡、約定不會到。所以時鐘會顯示 **⏸**，
否則「怎麼都沒人來赴約」會被當成 bug。明確的快轉（跳時段/指定日期/睡覺）**不受暫停影響**。

### UI

時鐘那一行的時間標籤**本身變成入口**（`openKanshouTime`）：流速四顆鈕 ＋
⏰跳下一個時段 ＋ 📅直接指定日期與時刻 ＋ 🎊跳到某個節慶。
前端鏡射 `KC_PACE_OPTIONS_`（`check_mirror.js` 盯）。

### 刻意沒做

**移動另外算耗時**。有了流速旋鈕之後移動就只是另一個回合；再加第二個時間來源
會毀掉這個模型唯一的優點——玩家完全掌控。（這是我自己上一輪提的，量完之後收回。）

探針 `pace.js`：11 條（預設 10／切 30／暫停 15 回合不動／流速記得住／暫停時快轉仍有效／
指定日期時刻／往回被擋／不合法流速被拒）。


---

## 🔍 2026-09 鑑賞全面稽核

玩家：「全面檢查鑑賞！！」——這一輪動了很多（帳本、角色內心、瘦身、大區/店、好感瓶頸、
擲骰、時間流速），所以重點放在**跨功能交互作用**與**大量移除留下的殘骸**。

### 結果：沒有找到功能性缺陷

| 查什麼 | 怎麼查 | 結果 |
|---|---|---|
| 十一支既有掃描器 | `bash check.sh` | ✅ 全綠 |
| 孤兒宣告（移除留下的） | 150 個 `KANSHOU_*`／`kanshou*` 宣告逐一數使用次數 | ✅ 0 個孤兒 |
| MEMORY 標記寫了沒人讀／讀了沒人寫 | 37 個標記的 get/set 計數 | ✅ 4 個「讀了沒人寫」都是 solo 創角用字面寫的，不是 bug |
| 跨功能交互 | 新探針 `cross.js` 14 條 | ✅ 全過 |
| 提示詞殘句 | 新探針 `stale.js`（10 個已移除的東西 ＋ 5 個該在的） | ✅ 全乾淨 |
| 前端 runtime | 新探針 `ui.js` 25 條 | ✅ 全過 |
| 文件宣稱的數字 | 12 個常數 ＋ 內建地點數 ＋ 角色出沒點數 對照代碼 | ✅ 全一致 |
| 全套探針 | 75 支 | ✅ 零 runtime error |

### 交互作用實測（`cross.js`）

暫停 × 約定（時間凍住約定不會消失、AI 仍看得到）／暫停 × 睡眠現況／
自訂大區 × 同伴行程（**同伴真的會走進你開的店**，實測 4.5%）／
刪掉腳下的地點（不炸，AI 仍知道你在哪）／收掉大區時人在那區裡（地點回到「走出來的地方」）／
告白必成 × 同居／帳本常民升格 × 行程。

⚠ 一個記錄下來的行為（不是 bug）：**刪掉腳下的地點後你還站在那裡**，
但地圖上沒有它了——移動一次就自然解決。

### 瘦身的成果（量出來的）

| | 之前 | 現在 |
|---|---|---|
| 提示詞總量 | 3781 字 | **3403 字** |
| 「誰算在場」散落 | 8 段 855 字 | **3 段 254 字** |
| 「萌點別直述」 | 2 段 202 字 | **1 段 41 字** |
| 指示段落兩兩重疊 | 最高 0.31 | **沒有任何一對 ≥0.14** |

### 這次稽核最大的結構性發現

**`.html` 的 runtime 完全沒有自動防線**——CI 不驗 .html JS，`check.sh` 的語法檢查也只看得到
「這段 JS 合不合法」，而 `wait.js`／`ui.js` 這些 VM 探針住在 scratchpad，只有想到才會跑。
這正是這專案被燒過的形狀（2026-07 敘事排版壞了四天：綠燈、部署成功、零錯誤訊息）。

已補上第十二支掃描器 **`check_ui.js`** 並掛進 `check.sh`：把三個 `Script*.html` 串進 VM、
配最小 DOM，真的把 19 個玩家入口與 4 個面板叫起來。兩種注入退化都確認會叫
（入口被改名 → 「叫不到 openKanshouTime()」；面板裡叫不存在的東西 → 「拋例外」）。


---

## 🔎 玩家看到的 vs 後台（2026-09 對帳）

玩家：「檢查玩家看到的資訊和後台是否一致」。UI 說謊有三種形狀，逐一查：

### ① 下傳契約（前端讀的欄位後端有沒有送）

`buildTagsPayload_` 24 個欄位逐一比對前端讀取點 → **每個前端讀的欄位後端都有送**。
反向抓到一個白送的：`servantSlots: servants.length` —— 沒人讀，而且是 `servants` 的重複
（同一個數存兩處，違反單一真實來源）。已移除。

### ② UI 文案宣稱的規則 vs 後端實際的規則 🐛→✅

**抓到兩處說謊**（都是這一輪改動的殘留）：

| 位置 | UI 說 | 後端其實 |
|---|---|---|
| 告白結果 `_pr.blocked` 分支 | 「前幾天才被拒絕過，還要 N 天才說得出口」 | 冷卻整組退休，**這個分支永遠不會被回傳** |
| 告白被拒訊息 | 「沒有答應——**好感略降**，要隔 N 天你才說得出第二次」 | 不扣好感、沒有冷卻；唯一的失敗原因是好感未達 60 |

改成一句真話：「話說不出口——你們之間還遠不到那個程度(好感要到 60)。這次沒有任何變動。」

（先前那一輪還修過兩句：同居確認框的「她也可能婉拒」實際是**門檻**不是擲骰、
告白確認框的「被拒後要隔幾天」。同一個形狀，這次補完。）

### ③ 寫死的數字前後端各寫一份 🐛→✅

**共同回憶的兩個上限**原本是**寫死在三處的魔術數字**：後端總量 `processMemoir_(..., 10)`、
後端釘選 `>= 8`、前端說明文字「最多10條…上限8」。改一個地方另外兩個不會跟著動，
而 `check_mirror.js` 只盯得到 `KC_*` 常數，**看不到寫死的數字**。

已抽成 `KANSHOU_MEMOIR_CAP_`(10) / `KANSHOU_MEMOIR_PIN_CAP_`(8) 並鏡射成
`KC_MEMOIR_CAP_` / `KC_MEMOIR_PIN_CAP_`，前端文字改成讀常數。
鏡射從 14 組變 16 組，退化測試確認會叫（後端改 10→12 → 「KC_MEMOIR_CAP_ ↔ KANSHOU_MEMOIR_CAP_ 走鐘了」）。
⚠ 釘選上限刻意比總量少 2：釘滿會讓新回憶永遠擠不進來。

### ④ 值一致性（試算表 ↔ 下傳 ↔ 畫面）

新探針 `truth.js` 11 條：好感／關係階／時鐘時刻／日期／時間流速／走出來的地方清單／
帳本條目數／帳本上限／大區上限，全部三處一致；並驗「後端拒絕時畫面不會留下假象」。

⚠ 記一筆給下一個人：**時鐘不在 `get_tags` 裡**——它隨每回合 `play` 回應下傳
（`data.kanshouClock` → 前端 `kcClock`）。在 tags 裡找時鐘會找不到，那不是 bug。

## 🧹 提示詞垃圾字（2026-09 全面稽核）

**形狀**：送進 AI 的那份文字裡混進 `undefined`／`NaN`／`[object Object]`／空標籤／佔位字。
玩家看不到它，AI 卻會當真——solo 那邊就抓到「這一局輸在：**undefined** 日時限耗盡」
（`DOJO_CAUSE_` 在檔案載入當下求值了別的檔的常數，已改成機器擋 `check_loadorder.py`）
與「25 位從者的卡上都寫著**討厭的事物：之事**」（佔位字被剝疊字剝成殘骸、逃過濾網）。

**怎麼查**：靜態 grep 看不到這種，樣板字串要**真的組起來**才知道長什麼樣。
鑑賞這側的探針是 `scratchpad/size/k_garbage.js`（9 種情境：開局第一句／三人在場高好感／同居中／
有約定／深夜／走去新地方／空白訊息／超長訊息／危險字元），逐回合掃真的送出去的 user＋system。
solo 那側掛在 `solo_all.js` 尾巴（24 顆按鍵跑完一起掃）。現況兩邊皆乾淨。

⚠ **「：」後面接換行是正常的**——提示詞常寫「鐵律：↵1. …」。第一版規則寫成「冒號後面沒東西就叫」，
九條情境全紅，那是掃描器自己的問題。只抓「：｜」與整段結尾。愛叫的掃描器會被直接無視，比沒有更糟。


## 🔰 第一次進來看到的那段（新手引導）

鑑賞**沒有** ❓教學 入口——那顆在 `top-navbar`，`applyModeUI` 在鑑賞把整條藏了，而且內容全是聖杯戰爭的事。
所以「這裡怎麼玩」只有一個地方講得到：`enterKanshou()` 裡「歷史是空的」那個分支（`Script_Kanshou.html`）。

八段：怎麼玩／👥 邀請／🗺️ 地圖／⏰ 時鐘／👤 標籤（📜💞📅🤝）／＋ 更多（📚🌍⚙）／🔥 主動掌握／好感怎麼長。

⚠ **它點名的每一顆按鈕都必須真的存在**——寫一顆不存在的鈕比不寫更誤導人。
2026-09 起這條由 `check_ui.js` 機器擋：抓出那段裡的控制項**圖示**，逐一確認介面別處真的有對應的
`onclick`／抽屜項／`title`。⚠ 比對的是【圖示】不是整串字：圖示穩定，後面幾個字常為了語氣微調
（教學寫「📚 相簿收著你拍的照片」、按鈕上是「📚 相簿」）；第一版比整串字，四條全誤報。
純段落圖示（✍️）登記在 `TUT_NOT_A_BUTTON`。退化確認：把 🔥 改成不存在的 🎪，它確實會叫。
